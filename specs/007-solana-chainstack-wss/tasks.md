# Tasks: Migrate Solana streaming off Helius onto Chainstack WSS

**Input**: spec.md in this directory. No fixed end date -- closes when no further bug/improvement is found.

- [x] T001 [P1] DONE 25/08 -- confirmed live: Chainstack Solana WSS reachable by deriving it from `ARIA_SOLANA_RPC_HTTP_POLLING` (same host/token, `https://`->`wss://`), no new env var/credential needed. `getVersion` round-trip succeeded.
- [x] T002 [P1] DONE 25/08 -- confirmed via Chainstack's own support docs: WebSocket billing is 1 RU per pushed event, identical shape to `eth_subscribe` on Base/Robinhood -- migrating provider doesn't change per-message cost, only which budget absorbs it.
- [ ] T003 [P1] IN PROGRESS -- `PumpSwapWebSocketFeed` instrumented (`notification_count`/`pool_count`, 2 new tests), deployed 25/08 23:48 UTC, but **still unmeasured**: `pools_tracked=0` throughout the whole observation window (no PumpSwap position happened to be open). Revisit when a real PumpSwap position opens and produces actual data -- never assume it's safe just because it's architecturally similar to bonding_ws, given how wrong that assumption turned out for bonding_ws itself (see T004).
- [x] T004 [P1] DONE 26/08 00:58 UTC -- `PumpFunBondingWebSocketFeed` instrumented (3 new tests) and MEASURED: pool count climbed steadily for over an hour post-restart (13->18->24->...->58->63, never plateaued), real instantaneous rate between the last two 300s-spaced reads (58->63 pools, 23160->25037 notifications) is **~6.26 notifications/s -> ~540 576/day, with only 63 pools tracked and still growing**. That alone is ~3x the ENTIRE 175k/day Solana cap, let alone the ~120-125k/day margin actually left after `pumpfun_curve_tracker.py`'s own ~50-55k/day polling. **Decision: NOT migrating -- stays on Helius/the free-tier fallback.** This spec's own "targeted subscription = automatically safe" assumption was wrong for this specific feed: a bonding-curve account changes on every single buy/sell, and this dome tracks a lot of curves concurrently (up to ~235 Solana positions seen at once per prior session notes) -- volume, not subscription shape, is what determines safety. Counters kept as a standing observability tool, not removed.
- [ ] T005 [P1] Conditional on T003 -- migrate `pumpswap_ws.py` to the Chainstack-derived WSS URL ONLY if a real measured headroom is confirmed once positions actually exist. `pumpfun_bonding_ws.py` stays on Helius per T004's decision -- not part of this task anymore.
- [x] T006 [P2] DONE 26/08 -- both program-wide (`pumpfun_trade_stream.py`, ~5.7M/day projected) AND targeted-but-high-volume (`pumpfun_bonding_ws.py`, ~540k/day measured) feeds now have a clear, measured reason to stay on Helius, cross-referenced in this spec (T004 above) rather than left as a silent oversight. `pumpfun_trade_stream.py`'s own comment already explained its case; `pumpfun_bonding_ws.py`'s new counters ARE the explanation for its case going forward.
- [ ] T007 [P2] Full test suite + deploy already done for the instrumentation itself (commit `7970f0d6`). Remaining: only if/when T003 data ever justifies building T005's actual migration.
- [x] T008 [P1] DONE 26/08 -- operator hardened the directive: "supprime Helius de notre base d'utilisation... transfere ce qui est faisable et utile seulement". `pumpswap_ws.py`'s `RPC_WS_DEFAULT` now derives Chainstack from `ARIA_SOLANA_RPC_HTTP`/`_HTTP_POLLING` (`https://`->`wss://`) instead of reading `ARIA_SOLANA_RPC_WS` (Helius) -- migrates `PumpSwapWebSocketFeed` (T005, low measured volume) automatically. `shadow_persistent.py`'s `HELIUS_SOLANA_WSS_URL`/`HELIUS_SOLANA_HTTP_URL` mandatory-fallback removed entirely, replaced by a Chainstack-only startup check.
- [x] T009 [P1] DONE 26/08 -- discovered `_TRADE_STREAM = PumpFunTradeStream()` runs `targeted=False` (program-wide) by default in prod, NOT the targeted/capped mode T006's "own comment already explained" assumed -- `MAX_WATCHED_MINTS=12` is a no-op in this mode. `stream_ws_endpoints()` now takes a `targeted` flag: the metered fallback (Chainstack) is only ever offered in targeted mode -- program-wide mode keeps ONLY the free tier (`solanavibestation.com`), by design, rather than silently failing over into a fallback that would 33x the whole Chainstack Solana cap. A program-wide free-tier outage is now a contained failure (this feed alone), never a dome-wide one.
- [x] T010 [P1] DONE 26/08 -- `pumpfun_bonding_ws.py` (T004's ~540k/day measured feed) is the ONE deliberate remaining Helius dependency, made explicit via a new `RPC_WS_HIGH_VOLUME_DEFAULT`/`require_solana_rpc_ws_high_volume()` pair in `pumpswap_ws.py` (still reads `ARIA_SOLANA_RPC_WS`) -- never silently left on the removed default. `wss://public.rpc.solanavibestation.com` (the free tier) was tested live the same day for `accountSubscribe` and returned HTTP 429 (already saturated by `pumpfun_trade_stream.py`'s own load) -- no usable free alternative today. **Open**: either shrink this feed's concurrent pool count to fit Chainstack's real headroom, or find/build a provider with more capacity, before this last Helius dependency can be closed.
- [ ] T011 [P2] Out of scope for this spec, flagged not forgotten: `jupiter_swap_signer.py` also references a paid Helius endpoint, but for signing/sending REAL swap transactions (real-capital path) -- never touched without a separate, explicit operator validation on that specific file. Distinct risk category from the discovery/pricing feeds this spec covers.

## Closure

Mark Status "Closed" in spec.md once every task above is done or explicitly deprioritized with a reason, HANDOFF entry in the same commit as the closing change.
