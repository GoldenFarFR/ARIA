# Tasks: Shadow pockets tuning -- Robinhood Chain

**Input**: spec.md in this directory. No fixed end date -- this file closes when no further bug/improvement is found (same doctrine as `001-audit-code-sans`), numbering continues indefinitely (T001, T002, ...).

- [x] T001 [P1] CONFIRMED REAL BUG (25/08) -- verified against GeckoTerminal's real OHLCV history (`mode="scalping_5m"`, the exact mode this pocket itself uses) for 9 of the top-20 trades in `robinhood_pump_shadow_log_archive_nofloor_age25_20260823`: 2/9 (22%) have a stored `peak_price` far above the real historical high of their own pool -- id=448 (the #1 ranked trade, reported x9.09) stored `peak_price=8.946e-07` vs a real max high of `1.04433305507929e-09` (856x inflated -- this trade's REAL peak never exceeded its own entry price); id=383 (#7 ranked, reported x3.03) stored `peak_price=1.071e-07` vs real max `5.19e-09` (20.6x inflated). The other 7 checked (id=542/585/384/553/487, ratio 0.15-0.99) are consistent with the real market, no anomaly. `ws_feed` is confirmed never instantiated in prod (only in tests) so the live price cascade actually used is DexScreener-first, GeckoTerminal-fallback -- the exact source of the bad reading at trade time could not be reproduced retroactively (DexScreener has no historical archive for this pool, `shadow_candle_archive` has almost no rows for this module). Impact on the headline figure: the outlier test (avg without top5) already excludes id=448 (the worst offender) but NOT id=383 (ranked #7) -- so even the "outlier-resistant" +27.78% figure reported earlier still carries at least one confirmed artifact. Root cause (a bad DexScreener/GeckoTerminal read on an illiquid pool, or a decimals/token-confusion bug) not yet isolated -- no historical log of the exact price read at trade time exists to confirm which.
- [ ] T002 [P1] Explain the 119/217 rows in that same table missing `realistic_final_multiplier` -- benign schema-migration gap or a real hidden bias.
- [x] T003 [P1] DONE 25/08 -- a candidate peak more than `_PEAK_JUMP_SUSPECT_RATIO=10x` the last CONFIRMED peak in a single cycle now routes through `paper_trader._advance_high_water`'s confirmation gate (reused verbatim, same mechanism already protecting the real $1M portfolio from this exact bug class) instead of ratcheting instantly -- a normal jump stays instant (unchanged behavior, all 102 pre-existing tests pass unmodified). 4 new dedicated tests cover: suspect jump doesn't ratchet instantly, confirms once held past the confirmation window, abandoned if price falls back, normal jump stays instant. Root cause of the 25/08 artifacts (T001) still not isolated (no historical log of the exact bad read exists) -- this is defense-in-depth, not a root-cause fix.
- [ ] T004 [P2] PAUSED 25/08 -- 13/98 closures cross-checked against GeckoTerminal so far (11 of the top-20 by multiplier: 383/384/553/585/448/542/487/560/566/572/589), 2 confirmed artifacts (id=383, id=448, both already covered by T001). Excluding just those 2: **91 closures, +23.89% without top5** -- close to the +25% bar but NOT to be treated as final: 9 more of the top-20 (450/456/405/452/426/597/421/540) remain unverified. **Paused, not abandoned**: the operator explicitly corrected this whole verification approach ("je t'ai dit de pas utiliser gecko, ca fonctionne mal") -- GeckoTerminal cross-checking must not resume as-is. Next session: find an RPC-native or DexScreener-chart alternative for the remaining 9, or accept the gap as permanently unverifiable and say so honestly rather than re-hammering GeckoTerminal.
- [x] T005 [P1] DONE 25/08 -- live forward-test of a more aggressive scale-out schedule, per the operator's explicit degen-trading framing ("arrete de penser comme une IA... pense comme un degen" / "simule en direct sur des nouveaux lancements, tu as l'infra pour le faire"). v1 sells 25%-of-remaining every +25% rung; a new module `robinhood_pump_v2_shadow.py` sells 50%-of-remaining every +15% rung instead -- same entry filters, same trailing-stop/max-hold/liquidity-collapse/peak-jump-guardrail safety net, reused (imported) from v1, never restated. Built as a FULL SEPARATE module (own table `robinhood_pump_v2_shadow_log`, own ledger), not a parameter added to v1's own functions -- v1's headline PnL is still under active dispute (T001/T004), and CLAUDE.md's own doctrine forbids stacking a second change onto a mechanism whose effect is still being measured. Same precedent already established for exactly this situation: `solana_support_bounce_v2_shadow.py`. Deliberately spot-price-only (no OHLCV window refinement, no `shadow_candle_archive` wiring) -- smaller GeckoTerminal footprint, consistent with the operator's correction above, proportionate since the variable under test is the schedule, not exit-price precision. 17 new tests, all pass. Wired into `shadow_persistent.py` (reuses the SAME `pools` discovery fetch as v1, no second network call) and restarted live via `systemctl restart aria-shadow-persistent.service` -- confirmed healthy (table created with all migrated columns, no traceback, process stable) but zero real signals logged yet in the first ~2min (rare, strict filters, real-market-dependent -- expected). No retroactive backtest possible for either variant (no price-trajectory archive for this pocket, see T001) -- this is why it runs live rather than replayed. Revisit once it accumulates enough closures for its own `summary()` figure to mean anything (n>=100 per Doctrine d'Ingestion).
