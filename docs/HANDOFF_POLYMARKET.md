# HANDOFF — Polymarket (ARIA parie en paper trading sur des marchés de prédiction)

> **Repo PUBLIC — jamais d'IP/secret/token/clé/adresse email personnelle en clair ici.** Noms de variables OK (ex. `GOPLUS_APP_KEY`), leurs valeurs jamais.

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.

[CODE] Sujet    : Item #147 -- calibrage reel (Brier score) des paris resolus
Date : 2026.07.28 / Probleme : `MIN_WIN_PROBABILITY=0.85` (skills/polymarket_thesis.py) est une affirmation ("quand ARIA dit 85%, elle a vraiment raison ~85% du temps") jamais verifiee empiriquement contre les paris reellement resolus -- trouve via un workflow de recherche+audit du 27/07.
Solution : nouvelle `compute_calibration_buckets()` dans `polymarket_paper_trader.py` -- methode classique de courbe de calibrage (reliability diagram) : regroupe `get_closed_positions()` par DECILE de `win_probability_at_entry`, compare la moyenne predite du decile au taux de reussite REEL de ce decile, calcule le score de Brier (erreur quadratique moyenne prediction/issue 0-1, 0=parfait, 0.25=hasard pur "toujours parier 50%") par decile ET sur l'ensemble des paris resolus. Une position sans `win_probability_at_entry` (ligne ancienne pre-migration) est exclue proprement, jamais une fausse valeur 0.0 qui fausserait le score. `format_calibration_report()` (rendu texte separe du calcul, meme decoupage que le reste du module) surfacee dans `format_portfolio_report()` -- donc a la fois `/polymarket` et toute future surface -- seulement si au moins un pari est resolu (sinon "indisponible" honnete, jamais un chiffre a 0 pari). Comme `MIN_WIN_PROBABILITY=0.85` borne TOUS les paris a >=85% par construction, seuls les deciles 8 (80-90%) et 9 (90-100%) seront jamais peuples en pratique -- comportement attendu, pas un bug, la methode reste generique. `polymarket_paper_trader.py` -- 8 nouveaux tests dedies (`test_polymarket_paper_trader.py`, 39 total dans le fichier), suite complete + `test_coherence.py` a confirmer avant commit.

------------------------------------------------------------

[CODE] Sujet    : Item #148 -- seuils differencies par horizon (7h != 29 jours)
Date : 2026.07.28 / Probleme : `list_liquid_events()` traitait un marche a 7h et un marche a 29 jours avec exactement le meme moteur de jugement -- trouve via un workflow de recherche+audit du 27/07, qui montre que l'edge reel se trouve aux horizons LONGS (macro/politique, ou la recherche d'ARIA peut devancer le marche) et non aux horizons courts/live (deja satures d'info, quasi coin-flip -- un ecart apparent y est plus probablement du bruit d'estimation qu'un vrai avantage).
Solution : `days_left` (deja calcule dans `list_liquid_events` pour le filtre de fenetre temporelle, jusque-la jete) porte desormais sur `PolymarketCandidateMarket`. Nouvelle `_horizon_thresholds()` dans `polymarket_thesis.py` : sous `SHORT_HORIZON_DAYS=3.0` jours, la barre se resserre (`MAX_VOTE_SPREAD_SHORT_HORIZON=0.10` vs 0.15, `MIN_EDGE_PROBABILITY_SHORT_HORIZON=0.20` vs 0.12) -- gradue, jamais une exclusion brute (un vrai edge sur un marche court reste possible, plus rare, il demande juste une preuve plus forte). Horizon inconnu (`days_left=None`, end_date manquant/illisible) -> repli sur les seuils de base, fail-open sur donnee manquante. `MIN_WIN_PROBABILITY` (plancher operateur explicite 0.85) non touche -- hors scope de cet item. `services/polymarket.py`/`skills/polymarket_thesis.py` -- 6 nouveaux tests dedies (`test_polymarket_thesis.py` 37 total, `test_polymarket_client.py` +1), suite complete + `test_coherence.py` a confirmer avant commit.

------------------------------------------------------------

[CODE] Sujet    : Item #146 -- casser la monoculture des 3 votes + retarder l'affichage du prix
Date : 2026.07.28 / Probleme : le "systeme de probabilite de qualite" du 26/07 (VOTE_COUNT=3 votes LLM independants qui doivent CONVERGER) reposait sur un mecanisme casse en pratique -- les 3 appels envoyaient EXACTEMENT le meme prompt (question + PRIX DE MARCHE affiche en premier + contexte) a temperature=0.3. Un re-echantillonnage a basse temperature d'un prompt identique converge presque par construction (spread <= MAX_VOTE_SPREAD quasi garanti), ce n'est pas un vrai signal de qualite -- juste du bruit d'echantillonnage deguise en consensus. Second probleme lie : montrer le prix de marche AVANT le vote risque d'ancrer chaque estimation sur le chiffre meme qu'elle est censee tester (le prompt systeme disait deja "ne recopie jamais le prix" mais l'ancrage LLM resiste souvent a une simple instruction).
Solution : (1) `market.yes_price` ne rentre plus JAMAIS dans le prompt envoye aux votes -- utilise seulement APRES, en code pur, pour calculer `edge = aria_probability - market.yes_price` (deja la structure existante, rien de casse en aval). (2) Les 3 votes argumentent desormais depuis un angle analytique reellement distinct (`_VOTE_LENSES` : taux de base historique / catalyseurs recents / avocat du diable) au lieu de re-tirer le meme prompt 3 fois -- meme doctrine "perspective-diverse verify" deja documentee ailleurs dans ce projet (convergence Sybil wallet-scoring, judge panel). `VOTE_COUNT` derive desormais de `len(_VOTE_LENSES)` (jamais un chiffre en dur qui pourrait diverger). `_single_probability_vote` accepte un `system_prompt` explicite (defaut = `_SYSTEM_PROMPT` nu pour les appelants directs/tests). `skills/polymarket_thesis.py` -- 3 nouveaux tests dedies (`test_polymarket_thesis.py`, 28 -> 31 tests, tous verts), suite complete + `test_coherence.py` a confirmer avant commit.

------------------------------------------------------------

[CODE] Sujet    : Item #151 -- log des jugements + rotation anti-monoculture de sujets
Date : 2026.07.28 / Probleme : operateur constate qu'ARIA n'a ouvert AUCUN pari depuis l'activation (2 jours) et demande si c'est louche. Verifie en direct (cycle force manuellement, bootstrap.configure + appel reel) : le mecanisme fonctionne (heartbeat_state.json confirme un vrai passage recent), le fix starvation (#133) marche (2 skips gratuits Fed n'ont pas empeche le 3e candidat d'etre reellement juge par le LLM). Mais ce 3e candidat a recu aria_probability=0.84, rejete a 0.01 point du seuil MIN_WIN_PROBABILITY=0.85 -- pas un bug, un seuil de conviction volontairement strict qui fait son travail. Deuxieme constat, plus important, trouve en creusant : `list_liquid_events()` trie TOUJOURS par volume decroissant sans aucune memoire de progression -- les memes 2-3 marches a plus fort volume (Fed) sont re-juges a CHAQUE cycle depuis l'activation, les ~170 autres candidats liquides du pool (176 au total, verifie en direct) n'ont jamais ete atteints. Operateur : "aria doit parier sur tous les sujets possibles sur polymarket pas seulement quelques une." Second gap trouve au meme moment : aucune trace persistee des jugements REJETES (seul un pari reellement ouvert laissait une trace) -- impossible de repondre "pourquoi n'a-t-elle pas parie" sans forcer un cycle manuel comme ici.
Solution : nouvelle table `polymarket_judgment_log` (event_slug PK, upsert -- dernier verdict seulement, jamais un historique complet puisque le prix reel bouge en continu) peuplee pour CHAQUE jugement reel (BET ou SKIP, toute raison), servant un double objectif : (1) observabilite honnete -- repondre a "pourquoi pas de pari" sans cycle manuel force ; (2) memoire anti-monoculture -- `recently_judged(event_slug, cooldown_hours=24)` filtre gratuitement (avant tout appel reseau/LLM, meme doctrine que les FREE_SKIP_REASONS deja en place, jamais compte contre CANDIDATES_PER_CYCLE) un marche deja juge dans les dernieres 24h, laissant la place aux marches d'autres sujets/categories au fil des cycles. Cooldown en duree fixe (pas lie a la cadence heartbeat, temporairement acceleree a 60min puis restauree a 12h par #133) -- couvre le pool de ~176 candidats en quelques jours a la cadence actuelle, jamais une exclusion permanente (un marche reste rejugeable des que le cooldown expire). `polymarket_paper_trader.py` -- 7 nouveaux tests dedies (`test_polymarket_paper_trader.py`), suite complete + `test_coherence.py` a confirmer avant commit.

------------------------------------------------------------

[CODE] Sujet    : Item #108 -- diligence + briques 1+2 (client de données étendu + moteur de jugement "edge")
Date : 2026.07.26 / Probleme : demande opérateur explicite ("je veut que aria utilise polymarket") clarifiée en "qu'elle prenne des paris" -- Polymarket n'était jusque-là branché qu'en LECTURE PASSIVE pour le contexte macro LLM (`fetch_top_event_by_tag`, un seul événement sur `fed-rates`, jamais un signal de décision). Diligence réelle avant tout code (deux plateformes distinctes -- Polymarket Global wallet-based sans KYC mais géobloquée US, vs Polymarket US QCX LLC régulée CFTC avec KYC complet -- capital réel écarté pour l'instant, périmètre validé opérateur : paper trading d'abord, même doctrine que le reste du projet).
Solution : `services/polymarket.py` gagne `list_liquid_events` (listing réel filtré volume/liquidité/échéance, ~140 marchés candidats trouvés en conditions réelles) et `get_order_book` (carnet d'ordres CLOB réel, `clob.polymarket.com`, throttle SÉPARÉ de Gamma -- domaine/infra distincts, jamais un dédoublement du même fournisseur). Nouveau `skills/polymarket_thesis.py` : ARIA estime SA PROPRE probabilité (recherche Tavily réelle + jugement LLM), jamais un simple suivi du prix de marché. `services/polymarket.py`/`skills/polymarket_thesis.py` (nouveau) -- 39 tests dédiés (`test_polymarket_client.py`+14, `test_polymarket_thesis.py`, nouveau), suite complète + `test_coherence.py` à confirmer avant commit.

------------------------------------------------------------

[CODE] Sujet    : "Système de probabilité de qualité" -- seuil 85% + convergence multi-votes
Date : 2026.07.26 / Probleme : trois précisions opérateur successives dans le même échange -- (1) "aria doit miser la ou c'est recherche lui permettent d'avoir un taux de reussite importante" (un simple écart chiffré vs le prix de marché ne suffit pas, peut être du bruit de calibration LLM) ; (2) "ses recherches avant de parier doivent lui permettre de dire oui je suis sur a 85% que le parie mise va reussir" (seuil quantitatif explicite, pas un label qualitatif type "confiance: fort/modéré/faible" -- trop dépendant du vocabulaire d'un seul appel LLM) ; (3) "tu dois lui creer un systeme de probabilite de qualite" (un seul appel LLM qui déclare sa propre confiance n'est pas un système de qualité vérifiable).
Solution : `MIN_WIN_PROBABILITY = 0.85` -- ARIA ne parie que si SA probabilité estimée du côté qu'elle prend réellement (pas juste le "Oui" brut : parier NON gagne avec 1-probabilité) atteint ce plancher. Qualité mesurée plutôt que déclarée : `VOTE_COUNT = 3` estimations LLM INDÉPENDANTES (même question/contexte, aucune ne voit la réponse d'une autre -- évite l'ancrage) doivent CONVERGER (`MAX_VOTE_SPREAD = 0.15`, majorité réelle ≥2/3 valides exigée) avant que la probabilité moyennée soit utilisée -- même doctrine "adversarial verify"/panel de votes déjà documentée ailleurs (convergence Sybil wallet-scoring, judge panel), appliquée ici à un seul jugement LLM plutôt qu'à un workflow multi-agents délégué. `MIN_EDGE_PROBABILITY = 0.12` conservé comme garde secondaire (empêche un pari sur un marché déjà proche du prix estimé, techniquement au-dessus de 85% mais sans vraie valeur). Testé en conditions réelles (recherche Tavily + 3 appels LLM réels sur plusieurs marchés Polymarket réels) : sélectivité confirmée -- 2 BET sur 6 marchés testés, spreads de convergence faibles (0.01-0.1) sur tous les cas. `skills/polymarket_thesis.py` -- 18 tests dédiés (`test_polymarket_thesis.py`).

------------------------------------------------------------

[CODE] Sujet    : Portefeuille papier dédié + résolution des paris + cycle heartbeat (briques 3-5)
Date : 2026.07.26 / Probleme : suite directe de "continue a tout construire" (opérateur) -- les briques 1+2 (client + jugement) décidaient déjà BET/SKIP mais rien ne persistait/exécutait la décision en paper trading.
Solution : nouveau `polymarket_paper_trader.py` -- poche dédiée `STARTING_CAPITAL_USD=100_000$` (séparée du 1M$ momentum, actif structurellement différent : résolution binaire à date fixe, pas de stop suiveur ATR). Sizing Kelly fractionnaire (`KELLY_FRACTION=0.25`, quart de Kelly -- jamais Kelly plein, trop volatile même avec une probabilité correcte) sur le prix RÉEL du carnet d'ordres (best_ask du côté parié, jamais le prix Gamma affiché seul -- même doctrine que `simulated_fill_price` du pipeline momentum), plafond dur `MAX_BET_PCT=5%` quel que soit Kelly. `services/polymarket.py` gagne `get_market_resolution` (`/events/slug/{slug}`, matche le market par `clobTokenIds` jamais par texte de question) pour détecter la résolution réelle et clôturer au vrai payout (1$/0$ par part). Nouveau cycle heartbeat `polymarket_paper_cycle`, gate dédié `ARIA_POLYMARKET_PAPER_ENABLED` (OFF par défaut), notifications Telegram (pari/résolution) réutilisant `_notify_telegram_trading`. Testé en conditions réelles de bout en bout : pari ouvert sur un marché réel déjà résolu (Coupe du Monde, Espagne gagnante), résolution détectée et PnL calculé correctement (+5000$ sur une mise de 5000$ à 0.5). Bug de test trouvé et corrigé au passage (pas un bug de prod) : un test avait copié un `token_id` tronqué à l'affichage au lieu de la vraie valeur complète. `polymarket_paper_trader.py` (nouveau)/`heartbeat.py`/`services/polymarket.py` -- 22 tests dédiés (`test_polymarket_paper_trader.py`, nouveau) + 9 tests client (`test_polymarket_client.py`).

------------------------------------------------------------

[CODE] Sujet    : Deux trous réels trouvés en auto-relecture critique (opérateur : "on n'a oublier dautre implementation")
Date : 2026.07.26 / Probleme : (1) coût Tavily jamais mis en balance avec le budget mensuel PARTAGÉ -- vérifié en direct (`tavily_budget.monthly_status()`, 900 crédits/mois, 110 déjà consommés ce mois, 790 restants) : le calibrage initial (`CANDIDATES_PER_CYCLE=8` x cycle 240min = 6/jour) aurait coûté jusqu'à 48 crédits/jour = ~1440/mois RIEN QUE pour ce chantier -- plus que le budget TOTAL partagé avec X/Website/Docs substance, conviction research, et les questions opérateur/visiteur (`web_verify`). (2) Aucun point d'accès opérateur -- une fois le gate activé, aucun moyen de consulter l'état du portefeuille sauf en faisant défiler les alertes Telegram.
Solution : (1) `CANDIDATES_PER_CYCLE` réduit 8→3, cycle heartbeat élargi 240→720min (12h) -- calibré ENSEMBLE pour ~6 crédits/jour max (~180/mois, ~20% du budget partagé, part volontairement non-dominante face aux consommateurs déjà établis). (2) Nouvelle commande Telegram `/polymarket` (admin-only, lecture seule, même patron que `/performance`) -- `polymarket_paper_trader.format_portfolio_report()` affiche équité/cash/positions ouvertes/dernières résolutions. `polymarket_paper_trader.py`/`heartbeat.py`/`gateway/telegram_bot.py` -- 5 tests dédiés supplémentaires (`test_telegram_polymarket_command.py`, nouveau, + 2 dans `test_polymarket_paper_trader.py`).

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Limites connues, documentées honnêtement (pas corrigées -- scope creep évité)
Date : 2026.07.26 / Probleme : —
Solution : (a) aucun coupe-circuit de drawdown équivalent à `risk_guard.py` (momentum) sur cette poche -- acceptable pour l'instant car 100% papier et gate OFF, mais à construire avant toute extension de portée. (b) aucun cache/cooldown sur les candidats déjà jugés SKIP récemment -- si les mêmes marchés dominent le tri par volume d'un cycle à l'autre, le budget alloué (3/cycle) peut se concentrer sur les mêmes candidats plutôt que d'explorer plus large ; pas un bug de sécurité, juste une perte de diversité diagnostique. (c) aucune diversification par catégorie/thème (contrairement au plafond 40%/chaîne du momentum) -- un portefeuille pourrait théoriquement se concentrer sur une seule thématique (ex. marchés "novelty" à faible substance). Les trois sont des candidats naturels pour un futur ajustement une fois des résultats réels observés (même doctrine "mesurer avant de resserrer" que le reste du chantier), jamais construits par excès de prudence sans données.

------------------------------------------------------------

[CODE] Sujet    : Corrélation trading/prédiction -- signal de vélocité de probabilité
Date : 2026.07.26 / Probleme : deux remarques opérateur successives -- "beaucoup de parie sont des aide pour les evenement a predire le marché donc il soit exister une correlation entre le trading et la prediction aria doit apprendre a utiliser les deux a bon essiant" puis un exemple concret : "imaginon que un parie soit a 20% de chance que btc soit a 80k dici 3 mois et que dans 1 semaine la chance passe a 70 c'est que quelque chose se passe et peut etre en profiter". Un mouvement rapide de probabilité de marché est lui-même une information -- mais suivre aveuglément ce mouvement serait juste copier la foule, l'inverse de la doctrine "jugement propre" du moteur déjà construit.
Solution : `services/polymarket.py` gagne `get_price_history` (endpoint CLOB `/prices-history`, vérifié en direct, historique réel de probabilité) et `compute_probability_velocity` (delta entre le premier et le dernier point de la fenêtre). Câblé dans `skills/polymarket_thesis.py::estimate_market_probability` comme CONTEXTE informationnel injecté dans le prompt LLM quand le mouvement dépasse `VELOCITY_HIGHLIGHT_THRESHOLD=0.15` (15 points sur ~7 jours) -- jamais un déclencheur ni un gate autonome, même doctrine que `radar_x.py` ("le signal réveille l'attention, le jugement réel arbitre"). Nouveau champ `PolymarketJudgment.probability_velocity_7d`, toujours peuplé (même en SKIP) pour observabilité future. Piste explicitement notée pour un chantier séparé (pas construit ici, périmètre trop large pour cette session) : faire remonter ce même signal vers le pipeline momentum/`market_sentiment.py` pour les marchés Polymarket macro pertinents (Fed/BTC/ETH) -- actuellement scopé au seul jugement Polymarket lui-même. `services/polymarket.py`/`skills/polymarket_thesis.py` -- 9 tests dédiés (client + jugement).

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Portée réelle à ce stade (briques 1-5 sur 5, construites)
Date : 2026.07.26 / Probleme : —
Solution : les 5 briques prévues sont construites et testées -- (1) client de données étendu, (2) moteur de jugement edge/qualité + vélocité, (3) portefeuille papier dédié, (4) résolution des paris, (5) cycle heartbeat + gate. `ARIA_POLYMARKET_PAPER_ENABLED` reste OFF par défaut -- rien n'est actif en prod tant que l'opérateur ne l'active pas explicitement. Aucune capacité d'exécution réelle (CLOB signé EIP-712, wallet Polygon/USDC) construite ni prévue à ce stade -- resterait un chantier de diligence légale/custody séparé si un jour le paper trading valide l'approche. Pont bidirectionnel trading↔prédiction (au-delà du signal de vélocité déjà câblé côté jugement Polymarket) reste une extension future, pas construite.

------------------------------------------------------------

[CODE] Subject  : Two of the three documented "known limits" closed -- dedicated drawdown circuit breaker + judgment cache (Items #109/#115)
Date : 2026.07.26 / Problem : the entry above ("Limites connues") explicitly flagged (a) no drawdown circuit breaker equivalent to risk_guard.py on this pocket, and (b) no cache/cooldown on markets already judged SKIP recently -- a real manual test the same day showed the same ~6 evergreen questions (Fed decisions, novelty bets) re-researched (Tavily + 3 LLM votes) 4 times in 35 minutes, confirmed by the full-pipeline audit launched earlier that day.
Solution : (a) new `polymarket_risk_guard.py`, same doctrine as `risk_guard.py`'s portfolio circuit breaker (#186) but a STRUCTURALLY SEPARATE module/state file/equity high-water-mark (own column on `polymarket_paper_state`, never shared with the momentum portfolio's) -- hard tier (drawdown >= 20% or 5 consecutive losses) blocks any NEW bet until manual `resume_new_bets`, soft tier (>= 10%) halves new bet sizes via `compute_bet_size`'s new `alloc_multiplier` param. Wired into `run_polymarket_paper_cycle` (checked once per cycle, before judging new candidates -- `check_resolutions` still always runs regardless) and surfaced in `/polymarket`'s `format_portfolio_report` (an armed circuit breaker was otherwise invisible to the operator). (b) process-local TTL cache (`_JUDGMENT_CACHE_TTL_SECONDS = 6h`, keyed by `yes_token_id`) in front of `estimate_market_probability`, same pattern as momentum_entry.py's own `_cached_get_token_holders` -- shorter than the 12h cycle cadence on purpose (a genuinely fast-moving market still gets re-judged well within a day per `probability_velocity_7d`'s own logic), long enough to kill same-session/same-cycle repeats. Real bug caught and fixed while writing the new circuit-breaker tests: 2 tests initially patched the `polymarket_client` SINGLETON INSTANCE instead of the `PolymarketClient` CLASS -- monkeypatch's teardown left a residual instance attribute masking the class for every later test in the session, exactly the trap already documented elsewhere in this codebase (test_momentum_entry.py's `_stub_polymarket_unavailable` comment) -- caught by running the full Polymarket test suite together, not each file in isolation. `polymarket_risk_guard.py` (new)/`polymarket_paper_trader.py`/`skills/polymarket_thesis.py` -- 15 new dedicated tests (`test_polymarket_risk_guard.py`, new) + 4 new cache tests (`test_polymarket_thesis.py`) + 1 new autouse fixture to stop the shared judgment cache from leaking state across tests reusing the same `yes_token_id`. Full suite green (7980 passed, 17 skipped), `test_coherence.py` green. Third known limit (no category/theme diversification) stays open, unchanged, deliberately deferred (same "measure before tightening" doctrine).

------------------------------------------------------------

[CODE] Subject  : Real starvation bug found live -- a free skip (extreme price) was consuming the CANDIDATES_PER_CYCLE budget, silently blocking ARIA from ever evaluating a real candidate (Item #133 investigation)
Date : 2026.07.27 / Problem : while checking whether the burn-in cadence (60min/1, since the gate's first activation 26/07 20:35) could be reverted to nominal (Item #133), live inspection found 0 positions AND 0 Tavily calls from `polymarket_thesis` since activation (~25h), despite `heartbeat_state.json` confirming the cycle ran on schedule with no exception logged. Root cause confirmed live (called `list_liquid_events()` 3 times in a row): the SAME market ("Will the Fed decrease interest rates by 50+ bps...", `yes_price=0.0015`) is ALWAYS first in the volume-sorted candidate list -- and `run_polymarket_paper_cycle`'s loop incremented `evaluated` (against `CANDIDATES_PER_CYCLE`) BEFORE calling `estimate_market_probability`, so this always-first, always-extreme-price market permanently consumed the entire per-cycle budget before any real candidate (e.g. a genuinely 50/50 market later in the list) could ever be reached -- confirmed by manually running the full cycle live: 0 opened, 0 Tavily calls, exactly reproducing the ~25h of silence.
Solution : new `FREE_SKIP_REASONS` frozenset (`skills/polymarket_thesis.py`) distinguishing skip reasons decided BEFORE any research/LLM call (`market_price_unavailable`/`market_price_already_extreme`, both synchronous checks on data already in hand) from those that only happen AFTER research + `VOTE_COUNT` LLM votes already ran (`insufficient_votes`/`no_consensus`/`win_probability_too_low`/`no_edge`, which DO still consume the budget -- they already cost real money/tokens). `run_polymarket_paper_cycle` now judges every non-already-open candidate first, then only increments `evaluated` if `judgment.skip_reason not in FREE_SKIP_REASONS` -- a free skip no longer blocks the loop from reaching the next candidate. `skills/polymarket_thesis.py`/`polymarket_paper_trader.py` -- 2 new dedicated regression tests (`test_polymarket_paper_trader.py`: `test_cycle_free_skip_never_starves_the_next_candidate` reproduces the exact live bug shape and confirms the real candidate behind the always-extreme one gets judged and booked; `test_cycle_paid_skip_does_consume_the_budget` confirms a paid skip still counts, so the budget itself isn't broken), full suite green, `test_coherence.py` green. Item #133 (revert to nominal 12h/3 cadence) still pending -- deliberately NOT done in the same change, since this fix itself needs a few real cycles observed clean before judging the burn-in phase complete (same doctrine as the accelerated-cadence rule itself, CLAUDE.md "Normes permanentes").

------------------------------------------------------------

[DEPLOYE] Subject  : Per-market liquidity filter (not just the event's aggregate) + Polymarket link + win-probability display on bet alerts (Items #224/#225/#226)
Date : 2026.07.30 / Problem : operator, watching a live bet alert, asked "c'est un vrai paris?" and "il me faut le lien sur polymarket" -- investigation found the underlying market ("Company D", part of event "Largest Company end of July?") had $0 volume/$0 liquidity of its OWN, never actually traded, its 50/50 price a pure default rather than a real consensus. `list_liquid_events()`'s docstring claimed "filtered on volume/liquidity" but `min_volume_usd`/`min_liquidity_usd` were only ever checked against the EVENT's aggregate ($1.5M/$5.4M, comfortably above both) -- individual markets inside a liquid event (catch-all placeholders "Company A".."Company T" alongside real named companies like NVIDIA) were never filtered on their OWN volume/liquidity. ARIA judged and bet against "Company D" as if its 50% were a real mispriced edge to exploit.
Solution : `services/polymarket.py`'s per-market loop now rejects (never appends to `candidates`) a market whose OWN `volume`/`liquidity` (already computed as `m_volume_f`/`m_liquidity_f`, previously only carried onto the candidate for display, never gated on) falls below the SAME thresholds already used for the event. Item #225 -- new `market_slug` field on `PolymarketCandidateMarket` (Gamma's per-market `slug`, distinct from `event_slug`) + new `market_url()` (verified empirically via curl HEAD -- `/market/{market_slug}` returns 200, `/event/{market_slug}` 404s -- never assumed from docs), new `market_slug` column on `polymarket_paper_position` (additive hot-migration, same idiom as `_STATE_ADDED_COLUMNS`), wired through `open_bet`'s INSERT and `format_bet_alert` (degrades to no link, never a broken URL, for any position booked before this column existed). Item #226 -- `format_bet_alert` now also shows `win_probability_at_entry` (ARIA's own estimated P(the SIDE SHE BET ON wins) -- already computed in `polymarket_thesis.py` and persisted, but never displayed before this fix), distinct from "Probabilité ARIA" (P(market resolves YES) regardless of side).
`services/polymarket.py`/`polymarket_paper_trader.py` -- 6 new tests (`test_polymarket_client.py`: per-market liquidity filter rejects a $0/$0 market inside an otherwise-liquid event, `market_slug` carried onto the candidate; `test_polymarket_paper_trader.py`: link present/absent, win-probability present/absent). Full suite green (8483 passed, 17 skipped), `test_coherence.py` green.

------------------------------------------------------------

[DEPLOYE] Subject  : "Equity" and "Cash" were mathematically identical -- Polymarket portfolio never marked open positions to market (Item #229)
Date : 2026.07.30 / Problem : operator noticed live in `/polymarket` that "Equite" and "Cash dispo" showed the EXACT same number ($85,738) despite 3 open bets worth $14,262 combined ("c'est propre pour toi ?"). Traced to `portfolio_summary()`: `equity = start - sum(open sizes) + realized_pnl` is LITERALLY `cash_available()`'s own formula -- "equity" never valued an open position at its current market price at all, it just subtracted the cost as a fixed, never-revisited number, identical to cash by construction. Same bug also fed `polymarket_risk_guard.evaluate_portfolio_risk()`'s drawdown circuit breaker (reads `summary["equity"]`) -- it was therefore structurally blind to any LATENT loss on still-open bets, only realized losses (already-resolved markets) could ever trigger it.
Solution : new `_mark_to_market_value(pos)` -- resolves the held side's token_id (`yes_token_id`/`no_token_id` per `side`, same lookup already used by `check_resolutions`), fetches its order book, values the position at `shares * best_bid` (the real resale price, never the spot/reference price -- same doctrine as the existing entry-price resolution and the momentum pipeline's `simulated_fill_price`). Degrades to the position's own `size_usd` (entry cost) on an unavailable/empty book -- never fabricated, never blocking. `portfolio_summary()` now returns `equity = cash + sum(mark-to-market value of every open position)` -- `cash` itself is unchanged (already correct, unaffected by open positions' price moves).
`polymarket_paper_trader.py` -- 2 new tests (`test_portfolio_summary_marks_open_position_to_market`: a bet bought at 0.5 whose market moved to 0.68 raises equity above cash by the unrealized gain; `test_portfolio_summary_degrades_to_entry_cost_when_book_unavailable`: an unavailable book falls back to entry cost, never blocks). Full suite green (8495 passed, 17 skipped), `test_coherence.py` green. No action needed on `polymarket_risk_guard.py` itself -- it already reads `summary["equity"]`, so it inherits the fix automatically.

------------------------------------------------------------

[DEPLOYE] Subject  : Resolution date now shown on `/polymarket` and on the new-bet Telegram alert (Item #244)
Date : 2026.07.30 / Problem : operator, reviewing the open Polymarket positions via a screenshot, asked for the resolution date of the bet ("je veut aussi la date de fin du paris"), then explicitly asked for it on Telegram too ("ajoute les date aussi sur le telegram"). `market.end_date` (`services/polymarket.py`) was already fetched at candidate-build time but never carried onto the persisted position, so neither `format_bet_alert` (new-bet alert) nor `format_portfolio_report` (`/polymarket`) had any way to show it without a live API call -- which `format_portfolio_report`'s own docstring explicitly rules out ("Read-only, no network call -- purely reads what's already persisted, same doctrine as /performance").
Solution : new `end_date` column on `polymarket_paper_position` (additive hot-migration, same `_POSITION_ADDED_COLUMNS` idiom as `market_slug`), populated from `market.end_date` in `open_bet`'s INSERT. New `format_end_date(iso_string)` helper (French `DD/MM/YYYY`, degrades to "date inconnue" on `None`/unparseable -- never a fabricated date). Wired into both `format_bet_alert` ("Date de fin : ...") and `format_portfolio_report`'s open-positions list ("... | fin : ..."). The 3 positions already open when this shipped (all on event "Largest Company end of July?", real `endDate` verified live against Polymarket's Gamma API: `2026-07-31T23:59:00Z`) were backfilled via a one-time direct `UPDATE` against production once the new column existed -- not fabricated, the exact value already confirmed live earlier the same session.
`polymarket_paper_trader.py` -- 7 new tests (`format_end_date` ISO/None/unparseable, `format_bet_alert` with/without the date, `format_portfolio_report` shows the persisted date, `open_bet` persists `market.end_date` onto the position). Full suite green, `test_coherence.py` green.

------------------------------------------------------------

[DEPLOYE] Subject  : `list_liquid_events` paginates past the top-100-by-volume events (Item #196)
Date : 2026.07.30 / Problem : Item #151's own entry noted "~170 other liquid candidates were never reached" -- root cause confirmed while auditing this file: `list_liquid_events` only ever made ONE call to Gamma's `/events` endpoint (`limit=100`, no `offset`), so anything past the 100 highest-volume active events was structurally invisible, no matter how liquid or fresh. #151's rotation guard (`recently_judged`) only varies WHICH of those 100 gets picked each cycle -- it can never reach further.
Solution : `list_liquid_events` gains a `max_pages` kwarg (default 3, up to 300 raw events) -- fetches successive pages via Gamma's `offset` param (verified live with a real curl: `offset=0` and `offset=5` return genuinely disjoint event slugs, not a repeat), stopping EARLY the moment a page returns fewer raw events than `limit` (Gamma's own signal there's nothing further) rather than blindly fetching all `max_pages`. The per-page fetch/parse logic was extracted into a new private `_fetch_liquid_events_page` (returns `(candidates, raw_event_count)` so the caller can tell a genuinely-short final page apart from a full page that just filtered down to few/zero candidates). This pipeline only ever judges a handful of candidates per cycle (`CANDIDATES_PER_CYCLE`), so 3 pages is a deliberately modest ceiling, not an attempt to cover the entire long tail in one pass.
`services/polymarket.py` -- 3 new tests in `test_polymarket_client.py` (paginates past a full first page, stops early on a short page without ever fetching further pages, respects the `max_pages` ceiling even when every page comes back full). All pre-existing `list_liquid_events` tests pass unmodified (their fixed single-event mock naturally short-circuits pagination after page 1). Full suite green, `test_coherence.py` green.

------------------------------------------------------------

[DEPLOYE] Subject  : `max_pages` default raised 3 -> 6 (Item #196 follow-up, same day)
Date : 2026.07.30 / Problem : operator follow-up right after #196 shipped ("on peut l'augmenter encore en taille ?") -- asked whether 300 events was leaving real liquid candidates on the table.
Solution : verified live with 6 real curl passes (offsets 0/100/200/300/400/500) against Gamma's own `/events` endpoint: the liquidity-floor (20,000$) pass rate stays well above zero all the way out -- 97% at offset 0-100, still 50-54% at offset 400-600 -- a real population of tradeable markets, not noise past page 3. Raised the default to 6 (up to 600 raw events) -- API cost is zero (Gamma is free, unlike Tavily's per-call charge) and the added latency is negligible (~2s/page via the existing `min_interval` throttle) against a cycle that runs every 60min (temporary accelerated cadence) to 12h (nominal).
`services/polymarket.py` (docstring + default only) -- no new tests needed, existing pagination tests already cover the mechanism itself (they pass `max_pages` explicitly or rely on early-stop, unaffected by the default's value). Full suite green, `test_coherence.py` green.

------------------------------------------------------------

[DEPLOYE] Sujet    : Second incident of the same family as #224 -- grouped multi-outcome events (missing per-market liquidity key + price pinned at 0.5/0.5) still slipped past the per-market filter, plus a silent-refusal blind spot in `open_bet`
Date : 2026.08.03 / Probleme : operator audit of the live paper portfolio ("sa lair louche") found the entire displayed PnL (+14,262.50$) traced back to the SAME already-known #224 bug (Company A/B/D). Deeper look at `polymarket_judgment_log` found 10 BET decisions since #224 shipped, but only 3 positions ever actually opened -- 7 BET decisions (30/07-02/08, on the event "what will happen before GTA VI?": Jesus Christ return / Trump out / China invades Taiwan / bitcoin $1m / etc.) never became a position, with zero trace of why. Confirmed live via curl: these markets have a real, high per-market `volume` (hundreds of thousands to millions of $, clears #224's own check) but NO `liquidity` key at all on the market (structurally absent, not zero) -- `#224`'s fallback (`m_liquidity_f = m.get("liquidity") or liquidity_f`) silently substitutes the EVENT's own (valid) aggregate liquidity, so the per-market check passes even though nothing was ever traded on that specific market. Their CLOB order book confirmed live: `{"error":"No orderbook exists for the requested token id"}` (HTTP 404) -- never a real traded market. Separately, `open_bet()` returning `None` on refusal (already-positioned / position cap / no valid entry price / size rounds to ~0) was never logged anywhere -- the judgment log records the LLM's BET decision, but nothing records whether the bet actually got booked or why it didn't, making this exact gap invisible without a manual DB/code audit like this one.
Solution : `services/polymarket.py`'s per-market loop gains a second, narrower check (in addition to #224's existing volume/liquidity gate): reject a market whose OWN `liquidity` key is absent from the raw Gamma payload AND whose `yes_price` sits within 1e-6 of exactly 0.5 -- a combination reliably distinguishing "never actually traded, price is the untouched default" from a real market that merely lacks its own `liquidity` field but has a genuine (non-0.5) price. Deliberately narrow (own-liquidity-present-but-pinned-at-0.5 markets are NOT rejected by this check -- a real market pinned by coincidence at 0.5 with real liquidity is not the placeholder pattern) to avoid over-filtering. `open_bet()` now logs (`logger.info`) the specific refusal reason at each of its four `return None` points, closing the observability gap -- no schema/behavior change, purely additive logging.
`services/polymarket.py`/`polymarket_paper_trader.py` -- 3 new tests in `test_polymarket_client.py` (grouped-event pattern is filtered even with a valid event-level liquidity fallback; a real non-0.5 price with a missing liquidity key is NOT filtered; a 0.5-pinned market WITH real own liquidity is NOT filtered -- non-regression against over-filtering). Full suite green (8975 tests collected), `test_coherence.py` green. No new position ever opened in production from the 7 dead BET decisions (confirmed: only 3 rows exist in `polymarket_paper_position`, ids 1-3, all already closed 31/07) -- this fix prevents the pattern going forward, it does not retroactively touch the existing (already-closed) portfolio history.

------------------------------------------------------------
