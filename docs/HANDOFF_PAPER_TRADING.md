# HANDOFF — Paper-trading (portefeuille 1M$, protocole hebdomadaire)

> **Repo PUBLIC — jamais d'IP/secret/token/clé/adresse email personnelle en clair ici.** Noms de variables OK (ex. `GOPLUS_APP_KEY`), leurs valeurs jamais.

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.
> Protocole actif à jour : section "Protocole d'entraînement hebdomadaire" dans CLAUDE.md.

[DEPLOYE] Sujet    : Rappel Telegram horaire tant qu'un coupe-circuit reste arme
Date : 2026.07.31 / Probleme : demande operateur explicite ("si aria arrete de trader parce que les coupes circuit se declenche je veut une notification dans telegram toute les heures tant que j'ai pas traite le probleme") -- les alertes existantes (`format_hard_circuit_breaker_alert`/`format_macro_circuit_breaker_alert`) ne se declenchent qu'A LA TRANSITION (`newly_triggered_hard`/`newly_triggered`), jamais reenvoyees ensuite -- un coupe-circuit reste arme silencieusement des heures/jours si la reprise manuelle (`/riskresume`/`/resume`) n'est jamais donnee.
Solution : nouveau `REMINDER_INTERVAL_SECONDS=3600.0` (1h, valeur explicite operateur) + `should_send_pocket_reminder(wallet)`/`record_pocket_reminder_sent(wallet)`/`format_pocket_blocked_reminder_alert` (per-pocket, `risk_guard.py`) et leurs equivalents macro `should_send_macro_reminder()`/`record_macro_reminder_sent()`/`format_macro_blocked_reminder_alert` -- le macro variant est restreint au cas ou `outgoing_pause` est actif A CAUSE de ce breaker precis (champ `triggered` persiste), jamais un rappel trompeur si l'operateur a lui-meme fait `/stop` pour une raison sans rapport. Nouveau champ `last_reminder_at` dans les fichiers d'etat persistes existants (jamais un nouveau fichier), preserve tous les autres champs (`since`/`reason`/`by`) a chaque ecriture. Branche dans `run_paper_cycle` (paper_trader.py) aux 3 points ou `risk_state`/`macro_state` sont deja evalues chaque cycle (single-pocket swing, boucle 3-pocket, macro) -- jamais une nouvelle cadence heartbeat, reutilise le cycle deja actif. — risk_guard.py/paper_trader.py, 12 nouveaux tests (`TestPocketReminder`/`TestMacroReminder`), suite complete + `test_coherence.py` verts.

------------------------------------------------------------

[CODE] Sujet    : Sortino ajoute au /performance du portefeuille papier (item #150)
Date : 2026.07.27 / Probleme : operateur demande explicitement "je veux que aria est aussi sortino... sur le wallet test [paper]... et un score sortino en temps reel" -- deja calcule pour le wallet-scoring de tiers (services/smart_money.py, `_sortino_ratio`) mais jamais applique au propre historique de trading d'ARIA (`performance_breakdown.py::compute_metrics`).
Solution : nouveau module partage `financial_stats.py` (`sortino_ratio`, sans dependance -- ni DB ni reseau) -- SSOT unique reutilisee des DEUX cotes. `smart_money._sortino_ratio` devient une fine enveloppe qui delegue a `financial_stats.sortino_ratio(returns, min_trades=WEIGHTS.min_closed_trades_for_sortino)`, zero regression (tests existants inchanges, memes valeurs). `compute_metrics()` calcule desormais `sortino` sur `pnl_pct` (deja persiste par position, echelle differente de `return_pct` du wallet-scoring mais le ratio est scale-invariant -- meme resultat numerique verifie par test dedie) -- meme seuil de 5 trades clotures, meme doctrine fail-safe (jamais un chiffre calcule sur un echantillon trop petit). "Temps reel" = recalcule integralement a chaque appel de `/performance`, jamais un cache/valeur figee (deja le comportement de `compute_metrics`, aucun changement necessaire de ce cote). Affiche dans `format_breakdown_report`. Suite directe (meme jour) -- precision operateur : "je veux que le paper test est sont propre sortino par wallet (scalping, swing et vc) et quand les 3 auront un bon sortino alors je pourrai dire quil trade bien" -- la segmentation par poche existait deja (`key_wallet`/dimension "Poche" de `ALL_BREAKDOWNS`, chaque groupe passe deja par `compute_metrics()`), donc le Sortino par poche etait deja CALCULE des le premier correctif -- seul l'AFFICHAGE manquait : la ligne de groupe de la dimension "Poche" (uniquement, pas les 14 autres dimensions -- eviter la verbosite) affiche desormais aussi son propre Sortino. `financial_stats.py` (new)/`services/smart_money.py`/`performance_breakdown.py` -- tests dedies (`test_financial_stats.py` new, `test_format_breakdown_report_shows_sortino_per_pocket` new), suite complete + `test_coherence.py` verts.

------------------------------------------------------------

[CODE] Sujet    : Mode test agressif 24h (item #100) -- cycle court, sizing dynamique, conscience du stress
Date : 2026.07.25 / Probleme : demande operateur explicite -- cloturer toutes les positions et reinitialiser a 1M$, passer le cycle de 7 jours a 24h pour CE cycle precis (test de stress ponctuel, jamais un changement permanent du protocole hebdomadaire), objectif primaire "trader un maximum de tokens meme si elle ne respecte pas tous les signaux", objectif secondaire ~50-100k$ de PnL, retirer le sizing fixe a 1% du mode plancher (`FLOOR_TRADE_ALLOC_PCT`), et qu'ARIA sache combien elle a dans son portefeuille et son PnL reel par rapport a l'objectif ("pour s'auto mettre un stress").
Solution : (1) `paper_trader.reset_portfolio()` cloture/archive tout, remet a 1M$ -- execute directement. (2) nouvelle colonne `paper_state.cycle_duration_days` (NULL par defaut = doctrine normale 7 jours inchangee) -- `reset_portfolio(cycle_duration_days=...)` la pose pour CE cycle seulement, `weekly_cycle_due()` la lit via `_effective_cycle_days()`, `run_weekly_reset()` la remet a NULL a la fin du cycle (jamais un changement permanent, le cycle suivant revient a 7 jours sauf nouvelle demande explicite). (3) `FLOOR_TRADE_ALLOC_PCT` supprime -- le mode plancher utilise desormais la MEME formule de sizing dynamique risque/ATR qu'un trade de conviction normal (`compute_entry_alloc`), jamais un plafond fixe a 1%. (4) `DAILY_TRADE_FLOOR` 5->30, `FLOOR_MAX_OPENS_PER_CYCLE` 2->5, `MAX_POSITIONS` 15->30 pour permettre reellement d'atteindre un volume eleve sur 24h (le cash reel reste le vrai frein naturel, pas un plafond artificiel). (5) nouveau `DAILY_FLOOR_TARGET_PROFIT_USD=75_000` (milieu de la fourchette 50-100k$ demandee) -- le mode plancher construit desormais un vrai `weekly_context` (equity reelle, objectif, progression, jour 1/1) reutilisant le mecanisme DEJA existant (`_weekly_pacing_line`, "Contexte de rythme") pour l'injecter dans le prompt LLM -- ARIA "voit" litteralement son equity et sa distance a l'objectif a chaque decision, au lieu de `weekly_context=None` comme avant. — paper_trader.py, tests dedies (test_daily_floor_feeds_a_real_pacing_context_to_the_analyzer + 3 tests corriges), suite complete + test_coherence.py verts. Reste a faire (chantier separe, timeframe 15-30min + prise de profit rapide) : voir HANDOFF_PIPELINE_MOMENTUM.md.

------------------------------------------------------------

[CODE] Sujet    : Suivi des trades perdants par lot de 10 (#92, pattern + ajustement de trajectoire)
Date : 2026.07.24 / Probleme : demande opérateur explicite -- une alerte par trade perdant isolé a été écartée d'abord (bruit statistique, réaction à l'anecdote) au profit d'un suivi groupé qui évite l'isolement d'une malchance.
Solution : nouveau `trade_loss_batch_review.py` -- accumule les positions clôturées PERDANTES jamais encore batchées, déclenche une analyse LLM (DeepSeek R1, même doctrine que le Devil's Advocate) tous les 10, cherche un pattern RÉCURRENT (jamais forcé sur un lot hétérogène), produit un ajustement de trajectoire one-way injecté dans le security-guard momentum aux côtés des leçons du Devil's Advocate. Générique via `positions_fetch` (paper aujourd'hui, wallets réels demain, #41) -- skills/trade_loss_batch_review.py (nouveau), heartbeat.py, momentum_entry.py

------------------------------------------------------------

[DEPLOYE] Sujet    : trade_devils_advocate_cycle jamais exécuté malgré un gate correctement activé
Date : 2026.07.24 / Probleme : trouvé en câblant le suivi de pertes ci-dessus -- `_sync_x_curiosity_enabled()` avait été corrigé plus tôt le même jour pour activer `task.enabled` selon `ARIA_TRADE_DEVILS_ADVOCATE_ENABLED`, mais `_run_task()` n'avait AUCUNE branche de dispatch pour cet id -- la tâche pouvait être `enabled=True` et rester un no-op total à chaque tick (tombe au travers de tout le elif, aucun else, aucune erreur visible).
Solution : ajout de la branche de dispatch manquante (`run_trade_devils_advocate_cycle`, notifie `_notify_telegram_trading` sur verdict flawed) + celle du nouveau `trade_loss_batch_review_cycle` -- heartbeat.py, tests/test_heartbeat_trade_risk_cycles.py (nouveau, régression)

------------------------------------------------------------

[DEPLOYE] Sujet    : /feedback -- section CLÔTURÉE encore verbeuse après le correctif du 24/07
Date : 2026.07.24 / Probleme : le correctif du 24/07 (plus tôt le même jour) avait rendu compacte la section OUVERTE de /feedback mais laissé la section CLÔTURÉE intacte (thèse/close_notes complètes) sur l'hypothèse "jamais réclamé" -- l'opérateur a signalé juste après avoir encore toutes les données qu'il avait dit ne plus vouloir.
Solution : nouveau `_render_closed_compact()` (même densité/lien collé que la section ouverte), utilisé UNIQUEMENT par `build_positions_detail_block` (/feedback) -- `/ledger` (`build_report`) garde `_render_closed` verbeux, divergence désormais délibérée (bilan rapide vs dossier détaillé) -- paper_ledger_report.py, tests/test_paper_ledger_report.py

------------------------------------------------------------

[CODE] Sujet    : Sealed Ledger v0 (#214) — registre de trades scellé cryptographiquement, ISOLÉ
Date : 2026.07.19  /  Probleme : —
Solution : registre append-only (chaînage SHA-256, JSON canonique, PnL toujours recalculé sur le VWAP des prix d'exécution réels, jamais le prix de décision), proposé et conçu par ARIA elle-même en conversation. Livré en version ISOLÉE (`sealed_ledger.py`/`sealed_ledger_export.py`, jamais câblée au paper-trading réel — décision opérateur explicite pour ce premier tour), preuve exécutée et vérifiée bout en bout sur 4 trades fictifs explicitement marqués comme tels (`PROOF-v0-hand-filled-not-a-real-ARIA-decision`). Reste ouvert : câbler ce registre sur `paper_trader.py`, endpoint API public, décision Postgres vs SQLite — aucun feu vert opérateur encore donné au-delà du v0 isolé.

------------------------------------------------------------

[DEPLOYE] Sujet    : reset_portfolio() effacait l'historique sans archive (DROP brut)
Date : 2026.07.24 / Probleme : audit 5-agents -- reset_portfolio() (reset manuel, ex. apres un incident forçant un redémarrage hors cycle, cf. CNX le 22/07) faisait un DROP TABLE direct sans jamais archiver dans paper_position_archive au préalable, contrairement à run_weekly_reset() qui archive toujours avant de vider -- confirmé en base : le Cycle #2 (18-22/07) n'a laissé aucune trace archivée après le reset manuel du 22/07.
Solution : reset_portfolio() archive désormais tout le contenu de paper_position (ouvert ET clôturé) sous le cycle_number courant avant le DROP, même doctrine non-destructive que run_weekly_reset -- paper_trader.py, tests dédiés (cf. historique git 24/07)

------------------------------------------------------------

[CONFIG] Sujet    : Test 1M$ relancé proprement
Date : 2026.07.16  /  Probleme : —
Solution : reset complet, jour 1 officiel du protocole hebdomadaire — action opérateur, pas de commit

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Cycle #1 clôturé manuellement
Date : 2026.07.18  /  Probleme : —
Solution : -1,98%, 25% de réussite sur 8 trades, objectif +10% non atteint. Protocole d'entraînement hebdomadaire formalisé le même jour (remplace le 30j/7j/14j initial)

------------------------------------------------------------

[DEPLOYE] Sujet    : Incident PLAZM/ESHARE — sélection de paire confondue
Date : 2026.07.19  /  Probleme : le contrat interrogé était confondu avec un autre token dont il n'est que le quote-token, +32950% fictif affiché
Solution : root cause dans 3 endroits distincts (momentum_entry, paper_trader, /vc, vitrine publique) corrigés partout ; position neutralisée manuellement (P&L remis à 0), capital corrigé — momentum_entry.py, paper_trader.py (a122b522)

------------------------------------------------------------

[DEPLOYE] Sujet    : Bug de comptabilité trouvé en corrigeant l'incident ci-dessus
Date : 2026.07.19  /  Probleme : le P&L des paliers de prise de profit déjà réalisés disparaissait du capital total à la clôture finale d'une position
Solution : pnl_usd final inclut désormais le P&L déjà réalisé par palier — paper_trader.py (cf. historique git 19/07)

------------------------------------------------------------

[CONFIG] Sujet    : Reset manuel du portefeuille
Date : 2026.07.20  /  Probleme : —
Solution : décision opérateur ("reset le portfolio") après le marathon de correctifs des rounds 5/6/7 — action opérateur, pas de commit

------------------------------------------------------------

[DEPLOYE] Sujet    : Reset hebdomadaire vulnérable à une mèche isolée
Date : 2026.07.20  /  Probleme : clôturait les positions sur un tick spot brut pile au moment du reset
Solution : prix de clôture robuste — médiane des 5 dernières bougies OHLCV — paper_trader.py (cf. historique git 20/07)

------------------------------------------------------------

[DEPLOYE] Sujet    : Endpoint diagnostic /api/aria/diagnostics/paper-ledger (registre de trades depuis le cloud)
Date : 2026.07.16  /  Probleme : Une session cloud sans acces VPS direct ne pouvait pas consulter le registre reel des positions ouvertes/cloturees (these, prix, P&L) sans relais manuel de l'operateur.
Solution : endpoint GET /api/aria/diagnostics/paper-ledger, meme patron que /diagnostics/pool-status et /diagnostics/agent-wallet-ledger — gate ARIA_DIAGNOSTIC_TOKEN (header X-Diagnostic-Access), exempte du gate Privy/operateur, lecture seule — vanguard/backend (commit d81ba5a)

------------------------------------------------------------

[DEPLOYE] Sujet    : P&L des paliers de prise de profit déjà réalisés disparaissait à la clôture finale
Date : 2026.07.19  /  Probleme : portfolio_summary() ne lit realized_pnl_partial que pour les positions encore open ; close_position() ne sommait que le P&L de la DERNIÈRE tranche — le P&L des paliers déjà réalisés via reduce_position (prise de profit échelonnée) disparaissait silencieusement du capital total au moment de la clôture finale, sous-estimant le vrai capital sur toute position avec sortie par tiers.
Solution : pnl_usd final = P&L de la dernière tranche + realized_pnl_partial déjà accumulé (celui-ci reste par ailleurs visible séparément sur la ligne) — paper_trader.py (commit 5365b643).

------------------------------------------------------------

[CONFIG] Sujet    : Correction manuelle du capital paper-trading après le gain fictif PLAZM/ESHARE
Date : 2026.07.19  /  Probleme : le bug de mislabeling quote-token (cf. HANDOFF_PIPELINE_MOMENTUM) avait figé equity_high_water_mark à ~12,5M$ (faux pic), bloquant déjà toutes les nouvelles entrées en prod via le coupe-circuit dur de risk_guard (faux drawdown de -61%).
Solution : correction en 3 volets, uniquement sur les DONNÉES de la position concernée (aucun changement de code) — sauvegarde de aria.db prise avant écriture ; pnl_usd/pnl_pct/realized_pnl_partial de la position remis à 0 (jamais supprimée, annotation dans close_notes) ; equity_high_water_mark réinitialisé à 1 000 000$ ; risk_guard.resume_new_entries() appelé. Capital vérifié après coup : 997 685$ (-0,23%), coupe-circuit levé — cf. historique git 19/07 (/opt/aria-data/backups/).

------------------------------------------------------------

[DEPLOYE] Sujet    : Scorecard objective de readiness capital réel (/feuvert)
Date : 2026.07.10  /  Probleme : question directe opérateur ("tu ferais confiance à ARIA pour 100k$ ?") sans outil pour y répondre objectivement plutôt que par avis subjectif.
Solution : skills/real_money_readiness.py calcule les cases du barème docs/protocole-argent-reel.md depuis le vrai journal vc_predictions (integrity/robustness/sample_size/benchmark/risk/judge/lawyer) ; commande /feuvert Telegram admin-only — real_money_readiness.py (cf. historique git 10/07)

------------------------------------------------------------

[CODE] Sujet    : Poche satellite du reset hebdomadaire (Tâche 2, option 3 confirmée par l'opérateur)
Date : 2026.07.22  /  Probleme : le reset hebdomadaire force-clôturait TOUTE position ouverte sans exception, même une position au potentiel encore clairement intact (régime Euphorie, stop ATR loin d'être touché, R/R restant solide) — coupait un vrai gagnant au milieu de son mouvement uniquement parce que le calendrier des 7 jours tombait pile ce jour-là. Exempter purement et simplement entrait en conflit avec la règle gravée "ARIA repart à 1M$ CHAQUE semaine" (comparabilité semaine/semaine).
Solution : poche SÉPARÉE et PLAFONNÉE (5% du capital de départ fixe = 50 000$, cumulé), hors du verdict hebdomadaire principal. Éligibilité (`_satellite_pocket_eligible`) : `strategy == "momentum"` (Formule B/vc_thesis non couverte) + régime ratchet (min(entrée, maintenant) — jamais un assouplissement) == Euphorie + stop ATR pas touché + R/R RESTANT (pas celui de l'entrée) >= 1.5, calculé via `_compute_active_stop` (extrait de la boucle de gestion pour être réutilisé sans dupliquer). Meilleurs R/R restants admis en premier si plusieurs candidats dépassent le plafond ; le reste force-clôturé comme avant. Verdict de la semaine calculé sur `cash + coût immobilisé en poche satellite` (jamais `equity` complète, qui inclurait la valorisation flottante) — neutralise totalement l'effet de la poche satellite sur `validated`/`return_pct`, ni bonus ni pénalité. Nouvelle colonne `pocket` ('main'/'satellite') sur `paper_position`/`paper_position_archive` — une position satellite n'est JAMAIS wipée par l'archivage hebdomadaire, ni réévaluée une fois promue (sort uniquement par sa propre clôture normale). Limite connue, documentée dans le code plutôt que cachée : `risk_guard` lit l'équité COMPLÈTE (poche satellite incluse) pour son coupe-circuit de drawdown — plafond bas (5%) pour borner cet impact en v1, séparer les deux poches dans `risk_guard` resterait un chantier distinct si le besoin se confirme. `paper_trader.py` — 16 nouveaux tests (`test_paper_weekly_cycle.py`), suite complète 6781 passed / 17 skipped, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Décote de liquidité sur le PnL affiché des positions ouvertes (mark-to-market)
Date : 2026.07.23  /  Probleme : `portfolio_summary()` valorisait chaque position ouverte au prix spot exact (`price_lookup(contract)`), comme si toute sa taille pouvait être liquidée sans le moindre glissement — un x50 fictif était possible sur un pool devenu mince (point trouvé par le stress-test, jamais corrigé jusqu'ici).
Solution : nouveau `risk_guard.simulated_exit_price` (symétrique de `simulated_fill_price` déjà utilisé à l'achat, même formule `_price_impact_pct` — jamais un second calcul divergent) — dégrade le prix affiché d'une position ouverte selon l'impact qu'une vente de sa taille aurait sur le pool. Liquidité utilisée : `last_liquidity_usd` (positions `vc_thesis`, watermark déjà tenu à jour depuis le 22/07) sinon repli sur `entry_liquidity_usd` (toutes stratégies, figé à l'entrée) — approximation honnête plutôt qu'aucune décote du tout ; `None` sur les deux (position ouverte sans liquidité connue) -> fail-open, comportement historique inchangé. `risk_guard.py` / `paper_trader.py` — 9 nouveaux tests (`test_risk_guard.py`, `test_paper_trader.py`), suite ciblée 325 passed, suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Subject  : Daily trade FLOOR -- force >= 5 momentum trades/day (diagnostic)
Date : 2026.07.23 / Problem : operator wants ARIA to make at least 5 trades/day "for now, so we can judge the tokens she picks even if she loses" -- the pipeline's selectivity produced too few opens/day to evaluate her selection. Root cause is selectivity, not scan frequency (the WebSocket already covers ~30s detection).
Solution : new INDEPENDENT additive cycle `paper_trader.run_daily_trade_floor_cycle` (never touches the normal `run_paper_cycle` decision path -- zero risk to normal entries). Paces `DAILY_TRADE_FLOOR=5` across the day (`_daily_floor_target` = ceil(5 * fraction-of-day-elapsed)); when behind, it evaluates the same momentum candidates in RELAXED mode and opens small tagged trades. Relaxed mode (`momentum_entry.evaluate_momentum_entry(relaxed=True)` -> `evaluate_hard_gates(relaxed=True)`, both default False = strictly unchanged normal behavior, proven by the full 213-test momentum suite still green) waives ONLY the two QUALITY gates (24h-volume floor, established-project-profile) + the R/R-floor / RVOL-reject; it NEVER waives the SAFETY gates (blacklist, liquidity floor, wash-trading, holder concentration, honeypot) NOR the parabolic cap (kept, matching the operator's "never buy the top" instinct) NOR the final LLM security guard. Forced trades: SMALL (`FLOOR_TRADE_ALLOC_PCT=1%` of start), capped `FLOOR_MAX_OPENS_PER_CYCLE=2`/cycle (no burst), tagged `discovery_channel="floor"` + `conviction_tier="floor"` so `/performance` separates them from real conviction picks. Respects the risk circuit breaker (operator decision 07/23: stops forcing on drawdown/consecutive-loss hard stop -- observing her risk management is itself diagnostic), MAX_POSITIONS, cash, `/stop`. Wired to heartbeat `daily_trade_floor_cycle` (60min), gated `ARIA_PAPER_TRADING_ENABLED` AND `ARIA_DAILY_TRADE_FLOOR_ENABLED` (both required, OFF by default). Same commit lowered `momentum_discovery_cycle` 60->30min (operator "on peut baisser le delai" -- WebSocket already covers speed, 30min catches the trending universe faster at modest GoPlus cost, not lowered to 15min per the rate-limit doctrine). `momentum_entry.py`/`paper_trader.py`/`heartbeat.py` -- 9 new floor tests (`test_paper_trader.py`), full suite to confirm, `test_coherence.py` green (not yet committed at the time of this entry).

------------------------------------------------------------

[DEPLOYE] Subject  : Real strategy label on Telegram alerts instead of a fixed "(mode trading)" (Item #131)
Date : 2026.07.27 / Problem : operator noticed a real tETH position opened while the portfolio-wide switch was already on scalping, but its buy/sell alerts still said "(mode trading)" -- traced to the source: `format_buy_alert`/`format_sell_alert`/`format_partial_exit_alert` (and the tracked-position header) all hardcode this literal string, never reading `pos.get("mode")`/`pos.get("strategy")` at all -- confirmed by grepping every occurrence and re-reading each function line by line, not a bug elsewhere silently overwriting a correct value.
Solution : new `_strategy_label(pos)` -- `strategy == "vc_thesis"` wins first (a fully separate pipeline, never the momentum mode switch) -> "venture capital"; else `mode == "scalping"` -> "scalping"; else -> "swing trading" (the friendly name for what was internally called "standard"). Wired into the 3 single-position alerts' header. For the periodic multi-position tracking alert (`format_position_tracking_alert`), the label goes PER POSITION inside `_format_tracked_position_line` instead of the shared header -- a real case observed live: AERO (mode="standard", opened before the scalping switch) and a scalping position can both be open at once, so a single header label would misrepresent one of them. Both `tracked` construction sites (`build_open_positions_tracking_lines`, the main cycle loop) now propagate `mode`/`strategy` alongside the fields already carried. `format_summary` (portfolio-wide aggregate, dead code -- never called anywhere in the repo) intentionally left untouched, out of scope. `paper_trader.py` -- 8 new dedicated tests (`test_paper_trader.py`: label resolution for all 3 cases + missing-fields default, propagation into each of the 3 single-position alerts, per-position labeling with 2 heterogeneous positions in the same tracking alert), full suite green (8032 passed, 17 skipped), `test_coherence.py` green.

------------------------------------------------------------

[CODE] Subject  : paper_state migrated to a per-wallet schema -- Phase 1 of the 3-pocket architecture plan (scalping/swing/VC, each an independent $1M paper wallet)
Date : 2026.07.27 / Problem : the operator wants scalping/swing/VC to run as 3 permanently-concurrent independent $1M portfolios instead of today's single portfolio with an exclusive `trading_mode` switch (approved plan, `fizzy-plotting-map.md`). `paper_state`'s old schema (`id INTEGER PRIMARY KEY CHECK(id=1)`) structurally only supports ONE row -- a hard prerequisite before any second/third wallet can exist.
Solution : SQLite can't ALTER a PRIMARY KEY in place, so `_ensure_tables()` now detects the old schema (has an `id` column, no `wallet` column) and migrates it: renames the table aside, recreates it with `wallet TEXT PRIMARY KEY`, copies the single legacy row's FULL state (every hot-migrated column, not just starting_capital/created_at -- a live prod portfolio's real cycle_number/equity_high_water_mark/trading_mode must never be silently reset) forward under `wallet='swing'` (migration decision: the existing $1M portfolio's full history becomes the swing pocket), then seeds fresh `'scalping'`/`'vc'` rows at `STARTING_CAPITAL_USD` each. New `wallet` column (default `'swing'`) added to `paper_position`/`paper_position_archive` via the same additive hot-migration pattern already used for every other column in this file. ~12 accessor functions gained a `wallet` parameter -- scalar per-wallet state (`starting_capital`/`cash_available`/`get_equity_high_water_mark`/`set_equity_high_water_mark`/`get_current_cycle_number`/`cycle_started_at`/`portfolio_summary`/`reset_portfolio`/`run_weekly_reset`) defaults to `wallet="swing"` (no sensible "all pockets combined" semantics for a scalar); read-side list queries (`get_open_positions`/`get_closed_positions`/`get_archived_closed_positions`) gained an OPTIONAL `wallet: str | None = None` filter (`None` = across all pockets, unchanged behavior for every existing caller since only 'swing' has ever traded). Deliberately deferred out of THIS step (zero trading-behavior change, matching the plan's own Phase 1 boundary): `get_trading_mode`/`set_trading_mode` kept as-is on the swing pocket (retiring the switch needs Phase 2's real 3-analyzer sourcing loop first, not yet built); `MAX_POSITIONS_VC`/`MAX_POSITIONS_SWING`/`MAX_POSITIONS_SCALPING` per-pocket constants (Phase 2/3); `risk_guard`/`limit_orders.py`/`momentum_websocket.py` per-pocket scoping (still global, unaffected by this step since no second wallet trades yet). `reset_portfolio` also had to change internally from DROP+recreate to a scoped `DELETE ... WHERE wallet = ?` -- the table is now shared across 3 wallets, so a DROP would have wiped the other 2 pockets' state; every field the old drop+recreate reset to its default (cycle_number, last_tracking_alert_at, trading_mode) is now reset explicitly in the UPDATE to preserve identical behavior. Known limitation, documented in code, deliberately deferred: `paper_weekly_cycle.cycle_number` is still a GLOBAL primary key, not yet scoped per wallet -- harmless today (only 'swing' ever calls `run_weekly_reset`), would need widening if a future pocket (Phase 4) also adopts this same reset. `paper_trader.py` -- full suite green (8043 passed, 17 skipped, including a 2-test fix in `test_paper_weekly_cycle.py`'s own `_age_cycle` helper which manipulated `paper_state` via raw SQL), `test_coherence.py` green. Phase 2 (3-analyzer sourcing loop) not started.

------------------------------------------------------------

[CODE] Subject  : Real 3-pocket sourcing loop -- Phase 2 of the 3-pocket architecture plan (built via the Workflow tool, 3-agent pipeline: core refactor -> mirror files -> adversarial verify)
Date : 2026.07.27 / Problem : Phase 1 only laid the schema/primitives (zero trading-behavior change) -- ARIA still traded as ONE portfolio via the exclusive `trading_mode` switch. The operator wants scalping/swing/VC to source and trade INDEPENDENTLY every cycle, the same contract legally open in 2-3 pockets at once.
Solution : new `multi_pocket_sourcing_enabled()` gate (`ARIA_MULTI_POCKET_SOURCING_ENABLED`, OFF by default, fail-closed -- same idiom as `daily_trade_floor_enabled()`). `open_position(..., *, wallet: str, ...)` -- `wallet` is now MANDATORY, no default, forcing every caller to say explicitly which pocket a new position belongs to; its own `has_open`/position-count checks are scoped to that SAME pocket only (an open position in a DIFFERENT pocket on the same contract never blocks or counts against this one). New `MAX_POSITIONS_VC=5`/`MAX_POSITIONS_SWING=15`/`MAX_POSITIONS_SCALPING=None`. New `_open_new_entries_for_wallet(wallet, candidates, analyzer, ...)` -- pure extraction of the existing sourcing loop (loss-streak guard, re-entry conviction multiplier, freshness re-check/limit-order fallback, sizing, funnel/counterfactual tracking, unchanged decision logic), reused once (gate OFF) or 3 times independently (gate ON, never mixing candidates/analyzers across pockets). `_run_paper_cycle_locked` branches on `multi_pocket_sourcing_enabled() and default_sourcing` (candidates=None AND analyzer=None, the real heartbeat case) -- gate OFF or any explicit caller (tests, momentum_websocket's own gate-OFF path) keeps the exact historical single-pocket behavior, byte-for-byte; gate ON + default case runs scalping+swing (sharing ONE momentum discovery pass, different analyzer `mode` only) and vc (`candidate_ranking.top_candidates()`, a wholly separate universe) as 3 independent calls. `limit_orders.py`/`momentum_websocket.py` mirrored: `pending_limit_order` gained a `wallet` column (default 'swing', additive hot-migration), `has_active_order`/`create_pending_order` default `wallet="swing"` (unchanged for every existing caller); `momentum_websocket._drain_new_candidates` branches on the same gate, with a new `_drain_multi_pocket` that DELIBERATELY bypasses `run_paper_cycle()` (passing it WebSocket-detected candidates would hit its "explicit caller" branch and silently re-fetch its own momentum candidates via REST, discarding the real-time detection this module exists for) -- calls `_open_new_entries_for_wallet` directly per pocket instead, under the same `_run_cycle_lock`.
2 real bugs found and fixed by the adversarial verify pass (not caught by the existing suite): `_run_daily_trade_floor_locked` used unscoped `get_open_positions()`/`has_open(contract)` -- would have raised `RuntimeError` under gate ON as soon as a candidate was legitimately already open in a DIFFERENT pocket (the multi-pocket ambiguity guard in `_get_open`); fixed by scoping both to `wallet="swing"` (this diagnostic floor cycle only ever books into swing, unaffected by the gate). `count_positions_opened_today()` counted positions across ALL pockets instead of just "swing"; fixed by adding a `wallet: str = "swing"` parameter. Both got dedicated regression tests.
Known limitation, found by 2 of the 3 agents independently, confirmed by direct re-read of the code (`risk_guard.py:685-708`, `paper_ledger_report.py:130-133`) -- documented honestly rather than hidden, NOT fixed here, required BEFORE this gate is ever flipped ON in production: `risk_guard.evaluate_portfolio_risk()` remains wallet-blind -- equity/drawdown computed from `portfolio_summary()`'s default `wallet="swing"` only, but its consecutive-loss counter reads `get_closed_positions(limit=...)` with no wallet filter (all 3 pockets combined). Under gate ON, this means: (a) the drawdown circuit breaker never sees scalping/vc's own equity swings, only swing's; (b) a loss streak split across pockets (e.g. 3 losses in scalping + 2 in vc) can trip the GLOBAL circuit breaker (`risk_guard_state.json`, a single file, not per-pocket) even though no single pocket individually crossed `HARD_CONSECUTIVE_LOSSES`; (c) that same global breaker then blocks new entries for ALL 3 pockets at once, not just the one actually struggling. Same blindness applies to `weekly_context`/`current_regime`/`daily_target_reached`, resolved once (implicitly from "swing") and shared across all 3 pockets' sourcing calls. `paper_ledger_report.build_report()` similarly mixes scopes: `starting_capital()`/`portfolio_summary()` default to "swing" alone while `get_open_positions()`/`get_closed_positions()` (no wallet arg) default to `None` = all pockets -- a display/reporting inconsistency, a product decision (how should the report present 3 pockets?) not a mechanical bug. Phase 3 (per-pocket risk_guard + macro circuit breaker) directly addresses this and is a hard prerequisite before enabling the gate.
`paper_trader.py`/`limit_orders.py`/`momentum_websocket.py` -- 11 new Phase 2 tests plus the verify pass's 2 regression tests (`test_paper_trader.py`), plus mirrored coverage in `test_limit_orders.py`/`test_momentum_websocket.py`/`test_brain_trade_status.py`/`test_paper_ledger_report.py`/`test_performance_breakdown.py`/`test_risk_guard.py`/`test_paper_weekly_cycle.py`/`test_telegram_feedback_command.py` + backend tests (`vanguard/backend/tests/test_aria_diagnostics.py`/`test_aria_paper_wallet.py`). Full suite: 8081 passed, 17 skipped, 1 unrelated failure (`test_tavily.py::test_crawl_success_parses_multiple_pages`, a live-network test failing on an exhausted Tavily monthly budget, no relation to this chantier's files), `test_coherence.py` green (80 passed, independently re-run). Gate stays OFF -- Phase 3 (per-pocket risk_guard) is a hard prerequisite before ever enabling it in production. Partie A of the plan (Polymarket pocket 100k$ -> 1M$) not yet started, independent of this phase.

------------------------------------------------------------

[CODE] Subject  : Per-pocket risk_guard + aggregate macro circuit breaker -- Phase 3 of the 3-pocket architecture plan (built via the Workflow tool, 3-agent pipeline: core refactor -> mirror files -> adversarial verify)
Date : 2026.07.27 / Problem : Phase 2's known limitation (documented above) -- `risk_guard.evaluate_portfolio_risk()` stayed wallet-blind, so a drawdown/loss-streak in ONE pocket (scalping/swing/vc) could trip a single global circuit breaker and block new entries for all 3 pockets at once. This was the documented hard prerequisite before `ARIA_MULTI_POCKET_SOURCING_ENABLED` can ever be flipped ON in production.
Solution : `risk_guard.py` section 2 rewritten -- `_state_path`/`_read_raw`/`_write`/`new_entry_block_status`/`block_new_entries`/`resume_new_entries`/`blocks_new_entries`/`evaluate_portfolio_risk` all take a MANDATORY `wallet` (no default, forcing every caller to state its pocket explicitly), one dedicated JSON state file per pocket (`risk_guard_state_<wallet>.json`), `get_closed_positions`/`portfolio_summary`/HWM calls scoped by wallet -- fixes the cross-pocket consecutive-loss counting bug directly. `PortfolioRiskState` gained a `wallet` field; `format_soft_drawdown_alert`/`format_hard_circuit_breaker_alert` label the pocket in the Telegram text. New section 3: `MACRO_CIRCUIT_BREAKER_LOSS_PCT=0.15`, `MacroRiskState`, `evaluate_macro_risk()` (aggregates equity across scalping/swing/vc, own persisted HWM file `risk_guard_state_macro.json`, arms the REAL global kill-switch `outgoing_pause.pause()` on first breach -- protects against a correlated crash hitting all 3 pockets at once, an angle a per-pocket-only design misses), `format_macro_circuit_breaker_alert`. Callers updated: `open_position` -> `blocks_new_entries(wallet)`; `_run_daily_trade_floor_locked` -> `evaluate_portfolio_risk(wallet="swing")` (this cycle only ever books into swing); `run_weekly_reset` -> `resume_new_entries(wallet, by=...)`; `limit_orders._execute_trigger` -> `evaluate_portfolio_risk(wallet)` scoped to the order's own pocket. `_run_paper_cycle_locked`/`momentum_websocket._drain_multi_pocket`: the swing-scoped snapshot's early `return` is now guarded by `not multi_pocket_mode` (the actual bug fix -- a swing-only drawdown no longer blocks scalping/vc); the multi-pocket branch calls `evaluate_macro_risk()` ONCE before the 3-pocket loop (returns on a macro trigger), then each pocket computes its OWN `pocket_risk_state` inside the loop, skipping only that pocket (`continue`) when blocked rather than a global return. `gateway/telegram_bot.py`'s `/riskresume` rewritten: no argument = checks/resumes all 3 pockets in one reply; `/riskresume <pocket>` targets just one. `paper_ledger_report.py`'s known scope-mixing bug (documented in the Phase 2 entry above) also fixed here: new `_build_pocket_section(wallet, ...)` produces a fully self-scoped section per pocket (equity AND winrate from the SAME pocket), report becomes 3 sections (SCALPING/SWING/VC), `machine["pockets"][wallet]` replaces the old mixed-scope top-level keys (no prod caller reads `machine`, so no external signature broken).
Adversarial verify pass found zero real bugs (independent line-by-line re-read + its own exhaustive grep, no reliance on prior agents' lists) -- confirmed: macro aggregation math is real (3_000_000 total, not a 1M/3M scale error), single-pocket gate-OFF path confirmed byte-for-byte unchanged, `continue` never `return` in both multi-pocket loops, macro HWM ratchets up-only, per-pocket state files never collide with the macro one, every `evaluate_*` call properly awaited. One real operational incident during the run, self-resolved: a concurrent session (the cloud command session, doing unrelated work) committed a fix to 4 test files' `DB_PATH` monkeypatches and `git stash`'d the rest of the working tree mid-workflow -- the verify agent detected it, confirmed the stash's base was a strict ancestor of HEAD (1 commit apart, redundant with its own already-applied fix), `git stash pop`'d cleanly with no conflict, and re-verified the full diff afterward. `packages/aria-core/src/aria_core/risk_guard.py`/`paper_trader.py`/`limit_orders.py`/`momentum_websocket.py`/`gateway/telegram_bot.py`/`paper_ledger_report.py` -- new dedicated tests in `test_risk_guard.py` (per-pocket drawdown isolation, per-pocket consecutive-loss scoping, macro breaker: no false positive at rest, triggers on aggregated loss below each pocket's own hard threshold, resume allows a later independent re-trigger) + `test_paper_ledger_report.py` (`test_build_report_never_mixes_scopes_across_pockets`) + mirrored coverage across `test_limit_orders.py`/`test_momentum_websocket.py`/`test_paper_weekly_cycle.py`/`test_telegram_risk_resume_command.py`. Full suite: 8091 passed, 17 skipped, 0 failed (independently re-run by the verify agent, not just trusted from prior reports), `test_coherence.py` green (80 passed). Gate `ARIA_MULTI_POCKET_SOURCING_ENABLED` stays OFF -- its hard prerequisite (this phase) is now satisfied; Phase 4 (per-pocket weekly reset) and Phase 5 (observability) remain before any real activation is considered. Known residual, deliberately out of scope: `docs/gestion-risque-portefeuille.md`/`docs/HANDOFF_PAPER_TRADING.md` (this file, historically) still reference the old single `risk_guard_state.json` filename in prose -- to update on a future doc pass, zero functional impact.

------------------------------------------------------------

[DEPLOYE] Subject  : Periodic tracking alert could show a stale (pre-reduction) capital right after its own partial-exit alert, same cycle
Date : 2026.07.27 / Problem : real bug spotted live by the operator (Telegram screenshots) on a real AERO position: the partial-exit alert at 14:58 ("Palier de profit 1/3 atteint... prise de 33%... ~67% restant") was immediately followed, same cycle, by a tracking alert still showing the FULL pre-reduction capital (35 000$/3.5%) -- the correct 23 333$/2.3% only appeared ~30 minutes later on the NEXT cycle. Root cause: `tracked` (the periodic-alert snapshot list) appends each position's `qty`/`cost_usd` at the TOP of the management loop, BEFORE that same position's TP-stage/Take-Seed `reduce_position()` call further down updates those same fields in the DB -- the in-memory snapshot was never refreshed after the reduction, so the tracking alert built later in the same cycle read stale data. Confirmed live against the real prod DB (`opened_at`/`entry_price`/`cost_usd` read directly, `tp_stage_hit=1`, `qty` matching the reduced position exactly) before writing any fix.
Solution : `reduce_position()` now also returns `remaining_cost_usd` (the real post-reduction cost, previously only exposed via `**pos` which is the STALE pre-reduction dict). New `_refresh_tracked_after_partial(tracked, contract, partial)` helper (looks up the entry by `contract`, not a raw `[-1]` index -- a robustness suggestion from an independent Grok API review, verified harmless today since no sort/filter happens between `tracked.append()` and the reduction call, but safer against a future refactor) is called at both partial-exit sites (Take Seed 2x, momentum staged TP) right after a successful `reduce_position()`. Same fix also lands the operator-requested entry price + hold duration in `format_sell_alert`/`format_partial_exit_alert`/the periodic tracking line (new `_format_hold_duration()` helper) -- these fields were simply never displayed before, even though already persisted on every position. `paper_trader.py` -- 1 new dedicated regression test (`test_tracked_snapshot_reflects_partial_exit_from_the_same_cycle`), full suite green (258 passed), `test_coherence.py` green.

------------------------------------------------------------

[CODE] Subject  : Per-pocket weekly reset + multi-pocket observability -- Phase 4+5 of the 3-pocket architecture plan, closes the plan (Partie B)
Date : 2026.07.27 / Problem : Phase 3 satisfied the hard prerequisite (per-pocket risk_guard) but two gaps remained before `ARIA_MULTI_POCKET_SOURCING_ENABLED` could ever be considered: `paper_weekly_cycle.cycle_number` was still a GLOBAL primary key (documented limitation from Phase 1) -- scalping and swing would collide on cycle #1 the moment both reset independently -- and every operator-facing surface (`/feedback`, `/performance`) still showed a single aggregate, silently hiding 2 of the 3 pockets' real numbers.
Solution : Phase 4 -- `paper_weekly_cycle` migrated to a composite PK `(wallet, cycle_number)` via the same rename-aside/recreate/copy-forward/drop pattern already used elsewhere in `_ensure_tables` (legacy single-wallet rows migrate forward as `'swing'`, idempotent). `heartbeat.py`'s `paper_weekly_review_cycle` now loops over `("swing", "scalping")`, resetting each pocket independently on its own 7-day clock -- VC deliberately never resets (judged on a rolling history instead, the plan's own decision), not in this loop. `format_weekly_cycle_report` gained a `wallet` param, labels the pocket (SWING/SCALPING) in the report header. Phase 5 -- `/feedback` (Telegram) rewritten to show all 3 pockets side by side (own equity/PnL/open+closed counts each) instead of one merged figure; position detail block stays unified (each line already carries its own strategy label, Item #131). `performance_breakdown.py` gained `key_wallet` (`t.get("wallet") or "swing"`, never "unknown" -- a correct attribution for any pre-Phase-1 trade, not a data gap), confirmed `"wallet"` is actually in `_POS_FIELDS` before wiring it (would have silently returned `None` for every trade otherwise). `paper_trader.py`/`heartbeat.py`/`paper_ledger_report.py`/`gateway/telegram_bot.py`/`performance_breakdown.py` -- 4 new dedicated tests in `test_paper_weekly_cycle.py` (per-wallet cycle isolation, independent due-dates, legacy schema migration + idempotency, per-pocket report labeling) + new `test_heartbeat_paper_weekly_review.py` (confirms the loop resets swing+scalping, never vc) + `test_feedback_shows_all_3_pockets_distinctly` in `test_telegram_feedback_command.py`. Full suite green (8096 passed, 17 skipped), `test_coherence.py` green (2 unrelated pre-existing failures fixed in the same session -- `test_token_dossier_operator_gated_and_read_only`/`test_cockpit_operator_secret_never_persistent` still referenced the /cockpit page removed earlier this session, own commit `7817d102`). Gate `ARIA_MULTI_POCKET_SOURCING_ENABLED` stays OFF pending operator confirmation after deployment -- this closes Partie B of the plan (`fizzy-plotting-map.md`); Partie A (Polymarket pocket 100k$ -> 1M$) remains a separate, not-yet-started item.

------------------------------------------------------------

[DEPLOYE] Subject  : Hold duration silently missing from every periodic tracking alert since Item #137 shipped (Item #223)
Date : 2026.07.30 / Problem : operator observation, live screenshot ("je vois pas le temps de détention dans feedback min") -- Item #137 (27/07) added `_format_hold_duration()` and wired it into `_format_tracked_position_line` (the per-position line inside the periodic "SIMULATION -- suivi positions ouvertes" alert) via `t.get("opened_at")`. The dict actually appended to `tracked` inside the main cycle's position-management loop (`tracked.append({"contract": ..., "symbol": ..., ...})`) never carried an `opened_at` key -- so every single call since #137 shipped silently got `None` -> `_format_hold_duration` returns `""` -> the whole "· détenue ..." segment is dropped, even though the exact same field displays correctly on close/partial-exit alerts (those build their own dict from the position row directly, unaffected).
Solution : one-line fix -- `"opened_at": p.get("opened_at")` added to the dict (`p` is the live `get_open_positions()` row, `opened_at` is a `NOT NULL` column, always present). Existing end-to-end test `test_run_cycle_notifies_position_tracking_for_still_open_positions` (already exercising the real cycle loop, not a hand-built dict) gained an assertion (`assert any("détenue" in a for a in alerts)`) that would have caught this on day one had it existed then.
`paper_trader.py` -- 1 assertion added to an existing test, full suite green (8477 passed, 17 skipped), `test_coherence.py` green.

------------------------------------------------------------
