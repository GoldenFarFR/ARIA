# HANDOFF — 2026-07-09 — Backlog #14/#33/#39 livrés, mentor Claude, durcissement SSH en pause, déploiement confirmé

Reprise directe : lire ce fichier + `CLAUDE.md` + `docs/etat-systeme-cable.md`. Tout est fusionné
dans `main` ET déployé sur le VPS (backend), commit `d6c7385aaf2d` confirmé par health check
(`{"status":"ok","commit":"d6c7385aaf2d",...}` + `GET /api/aria/sepolia-status` propre). Marqueur
`.claude/last-deployed-ref` recalé. Aucun changement vitrine ce lot (pas de `deploy-vitrine.sh`
nécessaire).

## Durcissement SSH (clé + TOTP) — EN PAUSE, pas commencé
L'opérateur voulait remplacer le mot de passe fixe IONOS par une connexion clé SSH + TOTP
(tâche #22/#17 durcissement). Plan validé avec lui :
- Filet de sécurité confirmé disponible : console à distance IONOS (portail authentifié, pas
  exposée sur le port SSH).
- Le mot de passe fixe IONOS reste utilisable UNIQUEMENT via cette console, jamais en SSH direct
  une fois le durcissement fait (sinon ça annule tout l'intérêt du TOTP).
- Codes de secours TOTP (scratch codes générés par `google-authenticator`) = la vraie procédure
  "j'ai perdu mon téléphone", pas le mot de passe fixe.
**Bloqué avant même l'étape 2** (créer la clé SSH) : l'opérateur s'est retrouvé sur un PC
d'entreprise, pas un appareil de confiance à lui — bon réflexe de sa part de ne pas y laisser de
clé/mot de passe VPS. À reprendre entièrement quand il est sur son PC perso. Rien n'a été touché
côté serveur, aucun risque, le mot de passe IONOS actuel continue de marcher normalement.

## Chat à 3 (opérateur/ARIA/Claude) — limite technique clarifiée, palliatif construit
Demande initiale : une vraie conversation à 3 comme entre humains. Vérifié et expliqué : **impossible
avec un seul token de bot Telegram** (une seule identité/avatar par bot). Palliatif déjà en place
(préfixe `🤖 Claude — ` sur toute réponse de Claude, ARIA jamais préfixée) + nouveau ce soir :
un message opérateur commençant par `@claude` (`_handle_message`, `telegram_bot.py`) n'active
PLUS le pipeline LLM d'ARIA — accusé de réception court seulement ("transmis à Claude"), le texte
complet reste journalisé dans le relais pour que Claude le lise à sa prochaine lecture. **Limite
non résolue** : rien ne réveille automatiquement une session Claude en temps réel — il faut soit
que l'opérateur ouvre la session VPS résidente au bon moment, soit un futur cron headless (pas
construit : coût récurrent + risque de boucle à cadrer avant de coder).

## Revue de performance ARIA par Claude — repensée en cours de route
Idée initiale de l'opérateur : "Claude bavarde avec ARIA sur Telegram pour la former". Repensé
avec lui vers un vrai mécanisme d'entraînement, ancré sur des PREUVES plutôt que du bavardage :
`skills/claude_mentor.py` lit les données de performance RÉELLEMENT mesurées d'ARIA
(`vc_predictions.metrics()`, `paper_trader.portfolio_summary()`,
`sepolia_autonomous.autonomous_status()` — fail-closed par source, jamais de donnée inventée),
puis appelle le VRAI Claude Opus 4.8 déjà câblé en prod via la profondeur "develop" de la
passerelle Virtuals (`spark_config.DEFAULT_MODEL_DEVELOP`) — **découverte clé : aucun nouveau
secret nécessaire**, ce modèle est déjà accessible via `VIRTUALS_API_KEY`. Deux sorties : remarque
dans le relais (ARIA y répond en vrai) + proposition d'issue GitHub `aria-knowledge-proposal` si le
constat est jugé durable (même doctrine stricte que `knowledge_inbox.py`). Throttle interne ~1x/jour.
Heartbeat `claude_mentor_cycle`, gaté `ARIA_CLAUDE_MENTOR_ENABLED` (activé ce soir sur le VPS).

## Vérification demandée par l'opérateur : ARIA trade-t-elle sur Sepolia ?
Demande explicite "VERIFIE vraiment tout". `sepolia_autonomous.py` relu ligne à ligne : **non,
aucun trade**. Aucun swap/DEX câblé pour Sepolia dans tout le dôme onchain (seuls fichiers
existants : wallet, autonomous, rehearsal, anchor, wallet_guard). Le testnet n'a même pas de pool
DEX indexé pour un token Base arbitraire. Ce qui se passe réellement sur un signal BUY : décision +
sizing Kelly sur un capital FICTIF (10 000 $ de répétition), puis ancrage on-chain du hash de la
décision (signature réelle, gas réel) — un test d'ingénierie logicielle, jamais un trade. Reste en
`skipped_no_ledger` tant qu'`AriaLedger.sol` n'est pas déployé sur Sepolia (étape Foundry séparée,
toujours pas faite).

## Wallet ARIA partagé pour suivi Zerion
Opérateur a demandé l'adresse pour la suivre depuis Zerion. Donnée avec double mise en garde :
c'est le wallet **Sepolia testnet** (`0x8c8c163DA8099Ef7B553Ee9D4D56EdE8c205Cae5`), aucune valeur
réelle, ne fait que de l'ancrage (pas de swap) — Zerion (orienté mainnet) pourrait ne rien
afficher dessus, BaseScan/Blockscout Sepolia sont plus fiables pour ce réseau. ARIA n'a PAS de
wallet mainnet avec de vrais fonds aujourd'hui (architecture l'empêche même de tenir une clé privée
réelle côté serveur).

## Backlog livré ce soir (3 tâches fermées, code + tests + doc, tout mergé sur main)

### Tâche #33 — Alertes proactives haute-conviction
`skills/high_conviction_alerts.py`, heartbeat `high_conviction_alert_cycle` (60 min), gaté
`ARIA_HIGH_CONVICTION_ALERTS_ENABLED` (activé ce soir sur le VPS). Pousse une alerte Telegram dès
qu'un candidat `candidate_ranking` (existant, rien dupliqué) franchit SAFE + score ≥80/100. Un
contrat n'est alerté qu'une seule fois. Signal de tri, jamais un ordre — renvoie vers `/vc`.

### Tâche #39 — Stop suiveur + prise de profit échelonnée (paper trading)
`paper_trader.py` : remplace la sortie binaire (100% à la cible OU à l'invalidation) par une
gestion de position réelle. **Stop suiveur** (`TRAIL_STOP_PCT=15%`) : se resserre avec le plus
haut atteint depuis l'entrée, ne se relâche jamais sous l'invalidation d'origine
(`active_stop = max(trailing, invalidation)`). **Prise de profit échelonnée**
(`TP_STAGES = (+50%, +100%, +200%)`, 1/3 de la position initiale par palier) : `reduce_position()`
vend une fraction, garde le reste ouvert, P&L partiel visible immédiatement
(`realized_pnl_partial`, sans attendre la clôture complète). Dernier palier = clôture du reliquat,
jamais de position résiduelle qui traîne. Migration de schéma à chaud non-destructive (même patron
que `vc_predictions.py`). Changement de comportement VOLONTAIRE de `run_paper_cycle` — tests
existants mis à jour dans le même commit (doctrine `test_coherence`).

### Tâche #14 — Overlay macro "Contexte marché"
Réutilise `btc_cycles.py` (rien dupliqué) : `current_phase_summary()` (fonction pure, dernier
segment du cycle Bitcoin en cours) + `fetch_current_macro_phase()` (async, cache 1h, dégradation
douce). **Aucun appel LLM** — chiffres déterministes, zéro coût ajouté à chaque rapport. Câblé via
`VCResult.market_context` (data-gated) + `_attach_extras` (TA+ROI+macro, chacun indépendant).
Rendu dans `vc_report.py`, section premium, même patron visuel que ROI/TA. i18n FR/EN.
**Géopolitique/réglementaire reste un seam volontairement VIDE** — aucune source fiable branchée,
jamais de donnée inventée pour combler la case (décision à prendre avec l'opérateur : quelle
source, quel coût, avant de coder quoi que ce soit). Conformément à `architecture-extensibilite.md`
("nouvelle section = preview validé avant prod"), un rapport d'exemple a été envoyé et **validé
visuellement par l'opérateur** ("le html est magnifique") avant de clore la tâche.

## Piège de test évité (à retenir)
En ajoutant l'appel réseau du contexte macro dans `analyze_vc_with_context`, DEUX fichiers de test
qui déclarent explicitement "aucun appel réseau réel" (`test_vc_analysis.py`, `test_vc_cache.py`)
auraient silencieusement commencé à taper la vraie API CoinGecko à chaque test. Repéré avant de
committer (le docstring du fichier de test l'annonçait) — fixture autouse ajoutée dans les deux
fichiers pour couper `fetch_current_macro_phase` par défaut. À vérifier systématiquement : tout
nouvel appel réseau ajouté à un chemin déjà testé doit être mocké dans TOUS les fichiers qui
exercent ce chemin, pas seulement le fichier de test dédié à la nouvelle fonctionnalité.

## État des tests
Suite complète verte tout du long : dernière mesure ~1332 passed / 7 skipped / 1 échec connu
(`test_web_verify_rugby.py`, appel réseau live DuckDuckGo bloqué par l'environnement, hors
périmètre, non lié au code).

## Ce qui reste en attente (pas bloquant, pas commencé)
- Clé SSH + TOTP sur le VPS — en pause, reprendre sur PC perso de l'opérateur.
- `AriaLedger.sol` sur Sepolia (Foundry) — le rehearsal Sepolia reste `skipped_no_ledger` sans ça.
- Nettoyage dexpulse/Aria Market — toujours cartographié (79 fichiers), rien appliqué.
- Décision opérateur : source géopolitique/réglementaire pour compléter l'overlay macro (#14).
- Cron headless pour que Claude soit réveillé en temps réel par `@claude` — pas construit, à cadrer.
- Backlog restant sans blocage : #8, #9, #10, #11, #13, #17, #19-23, #29, #32, #34, #40.
