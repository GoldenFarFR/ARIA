# HANDOFF — Moteur de légitimité (score de sécurité, mint_authority, safety_screen)

> **Repo PUBLIC — jamais d'IP/secret/token/clé/adresse email personnelle en clair ici.** Noms de variables OK, leurs valeurs jamais.

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.

[CODE] Sujet    : B20 -- cache court (3h) des verdicts, exception délibérée à la doctrine "toujours frais" (prérequis x402)
Date : 2026.07.31 / Probleme : premier prérequis identifié avant de vendre le moteur B20 en x402 (opérateur : "aussi plus tard aria proposera d'autres x402 en plus" -- infra durable) -- vendre une route payante qui refait un scan complet (36s même avec Alchemy) à CHAQUE requête, y compris sur le même token demandé deux fois de suite, gaspille du crédit RPC pour rien. Tension trouvée et soumise à l'opérateur AVANT de coder : `external_signal_cache.py` exclut explicitement `safety_screen`/`security_score` de tout cache ("un honeypot/rug peut arriver en quelques minutes") -- B20 est structurellement de la même famille (véto dur câblé juste à côté du honeypot GoPlus dans les deux cribles). Décision opérateur explicite (31/07) : cache quand même, mais TRÈS COURT ("quelques heures"), pas les TTL en jours du reste de ce cache.
Solution : `evaluate_b20_safety` devient cache-first (`external_signal_cache`, `_CACHE_TTL_HOURS=3.0`) -- scan réel renommé `_evaluate_b20_safety_uncached`, jamais appelé directement par les cribles (momentum_entry.py/safety_screen.py continuent d'appeler `evaluate_b20_safety`, transparent, aucun changement de leur côté). Un verdict "opaque" (scan raté/incomplet) n'est JAMAIS mis en cache -- toujours un nouvel essai frais, jamais coincé sur "inconnu" pendant des heures (contrairement à "safe"/"risky"/"not_b20", des réponses réelles et confirmées). Exception documentée en clair dans `external_signal_cache.py` (jamais un silence sur la dérogation à la doctrine existante). Bug de pollution entre tests trouvé et corrigé au passage (`DB_PATH` du cache est une constante figée à l'import du module -- sans isolation explicite par test, tous les tests de `test_b20.py` auraient partagé le même cache réel et pollué leurs verdicts respectifs) -- même patron de fixture que `test_external_signal_cache.py`. `services/b20.py` / `services/external_signal_cache.py` -- 6 nouveaux tests dédiés (hit safe/risky/not_b20, jamais de hit sur opaque, résilience aux pannes lecture/écriture du cache), suite complète 8703 passed / 17 skipped, `test_coherence.py` vert.

------------------------------------------------------------

[CONFIG] Sujet    : B20 -- bascule sur Alchemy (`ARIA_BASE_RPC_URL`), goulot RPC public résolu pour de vrai
Date : 2026.07.31 / Probleme : suite directe de l'entrée juste en dessous (RPC public gratuit non fiable sur un scan profond, 488-572s, parfois "opaque"). Diligence comparative demandée par l'opérateur avant de choisir un fournisseur durable (sert aussi les futurs produits x402) : Alchemy (déjà utilisé ailleurs dans le projet), Infura, QuickNode, Coinbase CDP Node, Dwellir comparés sur tarif/débit réel pour `eth_getLogs` spécifiquement (notre méthode dominante). Dwellir semblait le meilleur sur le papier (débit annoncé le plus élevé, coût uniforme par réponse) mais testé en conditions réelles (compte créé, clé API) : `eth_getLogs` renvoie un 403 Forbidden systématique sur son plan gratuit (même en appel unique, hors concurrence) alors que `eth_getCode`/`eth_getBalance` fonctionnent -- disqualifié pour cet usage. Alchemy testé en conditions réelles : 150 requêtes `eth_getLogs` concurrentes OK (vs 26-28 max sur le RPC public), scan complet du même B20 de 23 jours en 37,5s hors prod / 36,2s confirmé en prod.
Solution : `ARIA_BASE_RPC_URL` configurée dans `vanguard/backend/.env` (jamais commitée, jamais affichée -- construite fichier-à-fichier via `sed`/`cat`, sans jamais faire transiter la clé Alchemy par une variable shell affichable, contournant un premier essai bloqué à raison par `block-secret-display.sh`) vers l'URL Alchemy `v2/<ALCHEMY_API_KEY>` -- réutilise la clé DÉJÀ existante dans le projet (`wallet_transfers_fast.py`), aucune nouvelle ressource créée. Variable PARTAGÉE avec `doppler.py`/`basenames.py`/`base_onchain.py` (vérifié avant bascule : usage RPC léger sur ces 3 modules, aucun scan de logs en masse comme B20 -- la bascule les bénéficie aussi, jamais un risque de saturer leur usage). Conteneur redéployé, variable confirmée chargée (existence vérifiée sans afficher sa valeur), scan réel en prod revérifié après bascule : verdict cohérent (risky, MINT_ROLE actif), 36,2s au lieu de 488-572s/échec. `vanguard/backend/.env` (CONFIG, pas de commit -- fichier hors repo par nature) -- `_PARALLEL_BATCH_SIZE`/le retry sur 429 de l'entrée précédente restent inchangés dans le code (toujours utiles en repli si `ARIA_BASE_RPC_URL` venait à manquer). Cache des verdicts + garde-fous anti-abus (validation d'entrée, débit par payeur, jamais de liste complète) restent la suite nécessaire avant toute vente x402 -- pas encore construits, tâche de suivi posée.

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : B20 -- rate-limit RPC réel sur les scans profonds, PAS résolu, limite documentée
Date : 2026.07.31 / Probleme : suite directe de l'entrée juste en dessous ("verdict tranché" affirmé après UN SEUL run réussi) -- un second run réel sur le même B20 de 23 jours a échoué en "opaque" après 572s (429 épuisant les 3 tentatives de retry), puis un troisième run a réussi en 488s -- confirme que le RPC public gratuit `mainnet.base.org` a un plafond réel de requêtes concurrentes (mesuré en direct : 26 OK, 28 échouent en HTTP 429) ET une instabilité de charge partagée qu'un simple réglage de concurrence ne peut pas garantir à 100%.
Solution (partielle, honnête) : `_PARALLEL_BATCH_SIZE` réduit de 20 à 8 fenêtres (16 requêtes concurrentes/lot au lieu de 40, sous le seuil mesuré) + retry avec backoff sur 429 dans `_window_logs` (3 tentatives, jamais sur une erreur non transitoire) -- réduit le risque sans l'éliminer sur un token ancien. **Vérifié séparément, rassurant pour l'usage réel** : un token FRAIS (~6h, ~1800 blocs -- le cas réel du pipeline momentum, candidats fraîchement découverts) scanne en 0,6s à chaque fois, aucun problème constaté. Le goulot ne touche que les tokens de plusieurs semaines -- pertinent pour le crible VC manuel (`/vc <contrat>` sur n'importe quel token) et un futur produit x402, pas pour le trading actif. **Vraie solution non construite** : un RPC Base dédié à débit vérifié (Alchemy, déjà utilisé ailleurs dans le projet) -- tâche de suivi posée, jamais un nouveau réglage de constante deviné pour compenser. `services/b20.py` (docstring mise à jour avec la limite honnête) -- suite complète 8697 passed / 17 skipped, `test_coherence.py` vert.

------------------------------------------------------------

[CODE] Sujet    : B20 -- fenêtre de scan de rôles trop courte, "opaque" faux pour tout token >5,5 jours
Date : 2026.07.31 / Probleme : trouvé en vérifiant `services/b20.py` en conditions réelles depuis le conteneur prod (demande opérateur explicite avant de considérer le chantier fini) -- `evaluate_b20_safety` sur un vrai B20 de 23 jours renvoyait "opaque" au lieu d'un verdict tranché. Cause : `MAX_LOG_SCAN_WINDOWS=300` × 800 blocs = 240 000 blocs, calibré en supposant un temps de bloc non vérifié -- le vrai temps de bloc Base est 2,0s (mesuré en direct sur 100 000 blocs), donc la fenêtre réelle ne couvrait que 5,5 jours. Aggravant : Blockscout n'a AUCUN bloc de création pour un token B20 (`creation_transaction_hash` reste `null` même pour un B20 reconnu -- precompile, pas un déploiement `CREATE` classique), donc une simple augmentation de la constante n'aurait fait que déplacer le problème plus tard.
Solution : nouvelle `_find_creation_block` -- recherche dichotomique via `eth_getCode` (code absent avant création, présent après -- vérifié fonctionner sur un precompile malgré son bytecode minimal de 1 octet) pour trouver le VRAI bloc de création du token, résolue UNE SEULE FOIS par `evaluate_b20_safety` (jamais 3x par rôle) et réutilisée pour borner exactement le scan des 3 rôles -- `MAX_LOG_SCAN_WINDOWS` redevient un simple garde-fou de secours (relevé à 10 000, ~185 jours) au lieu du plafond réel. Les 3 scans de rôles passent aussi de séquentiel à concurrent (`asyncio.gather`, indépendants une fois le bloc de création partagé connu). Revérifié en direct depuis le conteneur prod après correctif : `is_b20(USDC)=False` (faux positif écarté) et le même B20 de 23 jours renvoie désormais un verdict tranché au lieu de "opaque". `services/b20.py` -- 8 nouveaux tests dédiés (`test_b20.py`, dont la recherche dichotomique elle-même), suite complète 8697 passed / 17 skipped, `test_coherence.py` vert.

------------------------------------------------------------

[CODE] Sujet    : GoPlus aveugle sur les tokens B20 natifs Base -- nouveau moteur de sécurité dédié (backlog #228)
Date : 2026.07.31 / Probleme : Base a activé le 08/07 un nouveau standard de token natif (precompile Rust, factory `0xB20f...`), confirmé structurellement invisible pour GoPlus (aucun bytecode Solidity à analyser -- chaque champ honeypot/hidden_owner/etc. reste silencieusement absent, jamais une erreur explicite) -- un candidat B20 passait donc les deux cribles durs (VC et momentum) comme "propre" alors que GoPlus n'avait jamais réellement regardé.
Solution : nouveau `services/b20.py` -- vérifie `isB20()` sur la factory officielle (jamais un simple préfixe d'adresse, deux faux positifs trouvés en direct sur des lookalikes), puis rejoue l'historique RoleGranted/RoleRevoked (MINT_ROLE/PAUSE_ROLE/BURN_BLOCKED_ROLE) via `eth_getLogs` fenêtré (calibré empiriquement à 800 blocs -- la doc RPC annonçant "10 000" s'est révélée fausse en test réel) et parallélisé (`asyncio.gather`, 118,7s→3,5s par rôle mesuré sur un vrai B20 de 23 jours) pour déterminer qui détient encore ces pouvoirs -- "safe" seulement si tous les rôles sont confirmés renoncés, "opaque" (fail-closed) si un scan est incomplet, jamais un "safe" silencieux sur donnée manquante. Câblé comme véto dur dans les DEUX cribles : `momentum_entry.evaluate_hard_gates` (après le honeypot GoPlus gratuit/caché, avant le scan de rôles coûteux -- détail momentum : `docs/HANDOFF_PIPELINE_MOMENTUM.md`) et `safety_screen.py`/`acp_onchain_scan.scan_base_token` ("risky" = hard_fail confirmé, "opaque" = soft fail réévalué au prochain scan) -- jamais un rejet automatique par préfixe d'adresse (correction opérateur explicite en cours de chantier : "on va pas rejetter les token car ils sont b20, il faut analyser si il sont safe"). `services/b20.py` (nouveau) / `momentum_entry.py` / `skills/safety_screen.py` / `skills/acp_onchain_scan.py` -- 20 tests dédiés (`test_b20.py`) + 9 tests d'intégration (`test_momentum_entry.py`/`test_safety_screen.py`/`test_goplus.py`), suite complète 8689 passed / 17 skipped, `test_coherence.py` vert. Piste ouverte, pas encore décidée : si ce moteur se révèle fiable en prod, il pourrait devenir le premier produit qu'ARIA vend en x402 (x402 seller reste dormant, deux gates OFF, décision de gouvernance séparée).

------------------------------------------------------------

[CODE] Sujet    : Faux positif "usurpation probable" -- la bio X native du token jamais vérifiée (Item #171)
Date : 2026.07.28 / Probleme : re-test réel de HOLO (bonding, contrat 0x6f8c2Eb585a93B29721B17E050bEabd3125fA937) après le chantier bonding v2 (#155-167) a produit un jugement "Contrat Base non corroboré... signalant une usurpation probable" (potential_score=2.0/10) alors que HOLO est un projet légitime (backed by @playartsdotai + @virtuals_io, vérifié manuellement sur X). Cause racine : `conviction_research.py` cherchait la corroboration du contrat UNIQUEMENT via une recherche web générique (Tavily) sur l'adresse EVM brute -- pour un token en bonding, cette adresse n'apparaît presque jamais verbatim sur le web, et Tavily est tombé sur un site homonyme sans rapport ("holo.host", contrat Ethereum différent) qu'il a pris pour le "site officiel". La VRAIE preuve de légitimité était disponible depuis le début : la bio X native du compte (@HoloStudio_AI, déjà déclaré dans `token.socials`) cite EXPLICITEMENT le lien Virtuals du token (`app.virtuals.io/virtuals/47656`) -- jamais consultée jusqu'ici.
Solution : `services/twitterapi_io.py` (déjà utilisé pour X Substance) gagne `bio`/`bio_urls` (les liens t.co de la bio DÉJÀ dépliés par l'API, `entities.description.urls[].expanded_url`, vérifié en direct). `conviction_research.research_project_potential` gagne un paramètre optionnel `known_launchpad_id` -- une fois le handle X résolu, sa bio est vérifiée pour un lien `app.virtuals.io/virtuals/<id>` correspondant ; un match force `contract_corroborated=True` (preuve la plus forte : le compte natif du token, pas un tiers), RETIRE le snippet du site homonyme contradictoire du contexte LLM (sinon le LLM garde un score bas malgré la corroboration, bug réel observé), et le prompt LLM reçoit une formulation dédiée insistant sur la primauté de cette preuve. `bonding_entry.py` transmet `known_launchpad_id=token.virtual_id` pour chaque token bonding (déjà connu nativement). Étendu au pipeline momentum standard (`momentum_entry.py`, opérateur go-ahead le jour même) : avant l'appel diligence, tente une résolution best-effort du virtual_id via `virtuals_client.fetch_by_address(contract)` (fonctionne pour un token gradué ET en bonding, Base uniquement, jamais bloquant). Résultat vérifié en conditions réelles sur HOLO : score composite 24,6→73,7/100, verdict HOLD→BUY, potential_score 2,0→7,0/10. `services/twitterapi_io.py`/`conviction_research.py`/`bonding_entry.py`/`momentum_entry.py` -- nouveaux tests dédiés (`test_twitterapi_io.py` bio/bio_urls, `test_conviction_research.py` 8 tests sur la corroboration bio X, `test_bonding_entry.py`/`test_momentum_entry.py` sur la transmission du virtual_id), 12 fakes `fake_research` existants dans `test_momentum_entry.py` élargis (`**kwargs`) pour tolérer le nouveau paramètre, 2 nouveaux fixtures autouse (bio-check neutre par défaut, résolution Virtuals neutre par défaut) pour éviter tout vrai appel réseau accidentel dans les tests existants. Suite complète 872 tests verts sur les fichiers touchés ; suite intégrale en cours de vérification au moment de cette entrée.

------------------------------------------------------------

[CODE] Sujet    : Cache persisté pour les 4 signaux Substance (item #40)
Date : 2026.07.24 / Probleme : les 4 signaux Substance (`_fetch_github_substance`/`_fetch_website_substance`/`_fetch_docs_substance`/`_fetch_x_substance`, `vc_analysis.py`) déclenchaient un scan RÉEL (Tavily crawl, GitHub API, TwitterAPI.io) à chaque `/vc` ou passage heartbeat sur le même token, alors que les faits sous-jacents changent peu sur quelques jours -- pur gaspillage d'appels payants/limités. `vc_cache.py` existant (in-memory, TTL 300s, vidé à chaque redéploiement) est structurellement insuffisant pour ce besoin (jours, pas minutes).
Solution : nouveau `services/external_signal_cache.py` (SQLite, `aria.db` partagé, clé `(signal_type, target_key)` normalisée, jamais de cache sur un scan `available=False`) — TTL différenciées (GitHub/X = 7j, Website/Docs = 15j, doctrine opérateur du 23/07). Les 4 fonctions `_fetch_*_substance` consultent le cache AVANT tout scan réel et y écrivent après un scan `available=True` réussi (clé X = le handle, jamais l'URL du lien déclaré — stable). `security_score`/`safety_screen` et le délai anti-front-running (#39) restent volontairement EXCLUS de ce cache (doctrine distincte, toujours frais). `services/external_signal_cache.py` (nouveau) / `skills/vc_analysis.py` — 8 nouveaux tests dédiés (`test_external_signal_cache.py`) + 4 nouveaux tests de câblage (`test_vc_analysis.py`), suite complète en cours de vérification au moment de cette entrée, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[DEPLOYE] Sujet    : is_proxy (GoPlus) jamais consulté -- risque de confusion sur les émetteurs institutionnels
Date : 2026.07.24 / Probleme : opérateur montre une capture GoPlus sur EURC ("Proxy contract: Yes", "Ownership renounced: No", tous deux en rouge) et demande si c'est dangereux -- vérifié : NON pour Circle/Tether (architecture proxy volontaire, sert la conformité réglementaire, gel d'adresses sanctionnées), mais is_proxy (services/goplus.py, capturé depuis le début) n'était consulté NULLE PART dans le code (safety_screen/momentum_entry/acp_onchain_scan) -- ARIA elle-même n'avait aucun moyen de contextualiser ce signal si on lui posait la question, risque de confusion demandé explicitement à corriger par l'opérateur.
Solution : ctx.is_proxy ajouté à TokenScanContext, peuplé dans _apply_honeypot_signals (acp_onchain_scan.py) -- JAMAIS de malus mécanique sur le score (un proxy seul n'est pas une confirmation de danger), mais un risk_flag toujours présent avec un texte CONTEXTUALISÉ : "normal et attendu" si le contrat est un émetteur stablecoin déjà reconnu (_is_recognized_stablecoin, smart_money.py), "point d'attention" sinon -- même doctrine que mint_authority.py (jamais un drapeau brut, toujours un jugement de contexte). 2 tests dédiés (cas reconnu/inconnu) -- acp_onchain_scan.py (cf. historique git 24/07)

------------------------------------------------------------

[CODE] Sujet    : Malus mint (-30) aveugle à l'autorité réelle (#164)
Date : 2026.07.22  /  Probleme : le score de sécurité (`acp_onchain_scan._apply_onchain_signals`) appliquait -30 dès qu'une fonction mint était détectée dans l'ABI, sans jamais consulter `mint_authority` — contrairement au crible dur (`safety_screen._mint_is_dev_controlled`), qui neutralise déjà correctement un mint renoncé/launchpad/contrat (timelock). Un projet légitime avec un vesting sain (mint_authority="contract") tombait sous le seuil de score 70 exigé par `safety_screen` (min_score) et ratait le sourcing automatique de la poche VC — confirmé concrètement lors d'un stress-test à 24 scénarios simulés (scénario "LEDGR", x50 raté). Aggravant : `_resolve_mint_authority` n'était appelée qu'APRÈS `_score_and_verdict` dans `scan_base_token` — `ctx.mint_authority` valait encore `None` au moment du calcul, donc une simple correction de la formule seule n'aurait rien changé sans réordonner les appels.
Solution : `SAFE_AUTHORITIES` (renommé depuis `_SAFE_AUTHORITIES`, devenu public) déplacé dans `mint_authority.py` comme source unique, importé par `safety_screen.py` (qui avait sa propre copie `_MINT_AUTHORITY_OK`, dupliquée) ET par `acp_onchain_scan.py`. `_apply_onchain_signals` reçoit désormais `mint_authority` et neutralise le malus si l'autorité ∈ `SAFE_AUTHORITIES` (renounced/launchpad/contract) — fail-closed inchangé pour `eoa`/`unknown`/non résolu. Dans `scan_base_token`, la résolution de `mint_authority` a été déplacée AVANT l'appel à `_score_and_verdict` (elle se faisait après). 6 nouveaux tests (3 paramétrés "neutralisé", 3 paramétrés "malus conservé", 1 intégration bout-en-bout qui verrouille l'ordre d'appel) — `acp_onchain_scan.py` / `safety_screen.py` / `mint_authority.py` / `test_acp_onchain_blockscout.py` (non commité au moment de cette entrée — suite complète 6749 passed / 17 skipped, `test_coherence.py` vert).

------------------------------------------------------------

[CODE] Sujet    : Trois trous du crible VC (`safety_screen`) comblés — wash-trading, slippage caché, plancher de liquidité rigide
Date : 2026.07.22  /  Probleme : `safety_screen.py` n'avait AUCUN garde-fou anti-manipulation de volume (contrairement au pipeline momentum) ; le champ GoPlus `slippage_modifiable` (le dev peut changer taxe/slippage après coup, sans reprise de propriété visible — pouvoir caché distinct de `hidden_owner`/`can_take_back_ownership`) n'était jamais consulté ; et le plancher de liquidité (30 000$) rejetait à tort un token dont le score ET le verdict sont déjà propres, sans jamais regarder si le reste du dossier compensait.
Solution : (1) réutilise TEL QUEL `momentum_entry.MAX_VOLUME_TO_LIQUIDITY_RATIO`/`_wash_trading_ratio_confirmed` (fenêtre de confirmation soutenue partagée par contrat/chaîne) — échec MOU, jamais `hard_fail` (comportement de marché, pas un mécanisme confirmé dans le contrat) ; (2) `TokenScanContext.slippage_modifiable` peuplé dans `_apply_honeypot_signals` (malus -15) + barrière `hard_fail` dans `safety_screen` (même famille que `hidden_owner`/`can_take_back_ownership`) ; (3) le plancher de liquidité est assoupli SEULEMENT si `security_score >= min_score` ET `lite_verdict == "SAFE"` ET mint propre — jamais silencieux, une raison "liquidité faible ... tolérée" reste visible même sur un passage réussi. `acp_onchain_scan.py` / `safety_screen.py` — 12 nouveaux tests (`test_safety_screen.py`, `test_goplus.py`), suite ciblée 82 passed, suite complète 6781 passed / 17 skipped, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : `owner_change_balance` (GoPlus) jamais consulté — vecteur de perte totale non couvert
Date : 2026.07.22  /  Probleme : trouvé en observant une position momentum RÉELLEMENT ouverte en prod (CNX) — GoPlus signalait `owner_change_balance=True` (l'owner du contrat peut modifier directement le solde de n'importe quel wallet) sur ce token, un pouvoir DISTINCT du honeypot classique (qui bloque la revente) puisqu'il permet un vol/effacement direct de solde sans même passer par un blocage de revente. Ce champ est bien capté par le client GoPlus (`services/goplus.py::TokenSecurity.owner_change_balance`) depuis le début, mais n'était exploité NULLE PART dans le pipeline de décision (ni score, ni barrière, ni pipeline momentum) — seulement utilisé en interne pour la propriété `ownership_verifiably_renounced` (qui ne sert qu'à calibrer la durée du cache, jamais à bloquer un token).
Solution : `TokenScanContext.owner_change_balance` ajouté et peuplé dans `_apply_honeypot_signals` (malus -40, verdict `DANGER` forcé — même gravité que `is_honeypot`/`cannot_sell_all`, pas un simple malus modéré comme `hidden_owner`) ; barrière `hard_fail` ajoutée dans `safety_screen.py` (même famille que `hidden_owner`/`can_take_back_ownership`/`slippage_modifiable`). Détail du pendant momentum (`_check_honeypot`) dans `docs/HANDOFF_PIPELINE_MOMENTUM.md`. `acp_onchain_scan.py` / `safety_screen.py` — 4 nouveaux tests, suite complète 6801 passed / 17 skipped, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Signal "sortie de liquidité déguisée" — insiders hors du wallet 'creator' (repris du stress-test)
Date : 2026.07.22  /  Probleme : `dev_wallet.py` ne surveille QUE le wallet déployeur explicitement labellisé (`creator_address`) — un insider ayant reçu une part significative de la distribution initiale (mint direct ou transfert du déployeur, jamais passé par un DEX) peut revendre intégralement sa dotation sans qu'aucun signal existant ne le capte, puisqu'il ne porte jamais l'étiquette 'creator'. Confirmé nécessaire par une vraie requête Dune sur le contrat CNX (déjà scanné en prod) : le déployeur a reçu le mint initial puis distribué à plusieurs wallets tiers dans les heures/jours suivants.
Solution : nouvelle table Dune exploitée, `erc20_base.evt_transfer` (transferts ERC-20 bruts, distincte de `dex.trades` qui ne couvre que les trades DEX) — `services/dune.py::build_insider_recipients_query`/`get_insider_recipients` (fenêtre de temps obligatoire, littéraux hex nus vérifiés en conditions réelles). Nouveau module `skills/insider_wallets.py` (`gather_insider_wallet_facts`/`judge_insider_wallets`, même doctrine que `dev_wallet.py` : signal pondéré concern/neutral/unknown, jamais un rejet d'office) — repère les wallets ayant reçu ≥1% de ce qu'a reçu le plus gros récipiendaire (déployeur) dans les 14 jours suivant la création de la paire, et vérifie via les holders Blockscout DÉJÀ récupérés par le scan (zéro appel réseau supplémentaire) s'ils détiennent encore quelque chose aujourd'hui. Holders indisponibles → `available=False` (fail-closed, jamais un "a tout vendu" déduit d'une absence de donnée). Câblé dans `acp_onchain_scan.scan_base_token` (`include_insider_check`, off par défaut — un appel Dune facturé de plus) et activé sur le chemin `/vc` réel (`vc_analysis.py`) ; consulté dans `_build_untrusted_context`, purement consultatif LLM. `services/dune.py` / `skills/insider_wallets.py` (nouveau) / `skills/acp_onchain_scan.py` / `skills/vc_analysis.py` — 45 nouveaux tests (`test_dune_client.py`, `test_insider_wallets.py` nouveau, `test_acp_onchain_insider_wallets.py` nouveau), suite complète 6884 passed / 17 skipped, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Signal insider_wallets absent sur les tokens en phase de bonding (repli ajouté)
Date : 2026.07.22  /  Probleme : `_resolve_insider_wallets` dépendait exclusivement de `ctx.best_pair.pair_created_at` pour borner la fenêtre de recherche Dune — or un token Virtuals en bonding pré-graduation n'a AUCUNE paire DexScreener (`ctx.best_pair` reste `None`, normal à ce stade), donc le signal restait silencieusement `unknown` sur toute cette niche. `deployer_history` n'est PAS affecté (dépend uniquement de `creator_address` via Blockscout, jamais de la paire).
Solution : nouveau champ `TokenScanContext.token_created_at_ms`, peuplé dans `_resolve_bonding_phase` depuis `VirtualToken.created_at` (déjà récolté par le même appel réseau Virtuals, zéro coût supplémentaire) via un nouveau helper `_iso_to_epoch_ms`. `_resolve_insider_wallets` utilise désormais `ctx.best_pair.pair_created_at` en priorité, avec repli sur `ctx.token_created_at_ms` si aucune paire n'existe — jamais l'inverse (un token gradué garde toujours sa vraie date de paire). `skills/acp_onchain_scan.py` — 4 nouveaux tests (`test_acp_onchain_bonding.py`, `test_acp_onchain_insider_wallets.py`), suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Signal "réputation du déployeur" — récidive cross-token confirmée par ARIA elle-même
Date : 2026.07.22  /  Probleme : `dev_wallet.py` juge le comportement du déployeur SUR ce token précis, mais rien ne vérifie s'il a déjà déployé un AUTRE contrat par ailleurs confirmé scam (le contrat peut être propre, le déployeur récidiviste). Volet "activité GitHub" de la même graine (`improvement_seeds.yaml` ligne 140) déjà pleinement couvert depuis le 19/07 (`services/project_activity.py::fetch_github_diligence_snapshot`, consommé par `conviction_research.py` sur les deux pipelines VC ET momentum) — aucun trou restant sur ce volet, seule la réputation on-chain manquait. Vérifié en direct (22/07) : Blockscout n'a AUCUN endpoint bon marché pour lister "les contrats créés par une adresse" (pas de paramètre `type=contract_creation`, pas de tri) — seule option viable : parcourir l'historique de transactions déjà existant et lire `created_contract` (présent dans la réponse réelle mais jamais mappé dans `Transaction`). Découverte notable au passage sur le cas réel CNX : l'adresse 'creator' peut être un compte délégué EIP-7702 (`proxy_type: "eip7702"`, confirmé par appel direct) — donc pas toujours une identité stable d'un déploiement à l'autre, une prudence de plus documentée dans le nouveau module.
Solution : `Transaction.created_contract` ajouté (champ additif, `None` par défaut) et peuplé dans `get_transactions` ET `get_transactions_bounded` (même client Blockscout réutilisé, zéro doublon). Nouveau `services/deployer_history.py` (`gather_deployer_history_facts`/`judge_deployer_history`) — parcourt l'historique BORNÉ (3 pages, `get_transactions_bounded`, doctrine "jamais garanti exhaustif" déjà en place ailleurs dans le projet) du wallet déployeur, dédoublonne les contrats créés (hors le token courant), croise chacun contre `momentum_blacklist.is_blacklisted` (gratuit, donnée de première main ARIA, zéro appel réseau) — récidive confirmée → signal `concern` ; sinon `neutral` (avec mention honnête si l'historique est tronqué). Signal purement consultatif (même doctrine que `dev_wallet.py`/`insider_wallets.py`), jamais un véto — l'instabilité potentielle de l'identité 'creator' (compte délégué) justifie cette prudence. Câblé dans `acp_onchain_scan.scan_base_token` (`include_deployer_reputation`, off par défaut) et activé sur `/vc` (`vc_analysis.py`), consulté dans `_build_untrusted_context`. `services/blockscout.py` / `services/deployer_history.py` (nouveau) / `skills/acp_onchain_scan.py` / `skills/vc_analysis.py` — 22 nouveaux tests (`test_deployer_history.py` nouveau, `test_acp_onchain_deployer_reputation.py` nouveau, `test_blockscout_client.py`), suite complète 6884 passed / 17 skipped, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : dev_wallet.py ne détectait jamais un achat/vente sur un pool Uniswap V4
Date : 2026.07.23  /  Probleme : calibration en live demandée (docstring existant : "heuristique À CALIBRER en live"). Vérifié sur le contrat CNX déjà scanné en prod : son pool réel (dexId "uniswap" côté DexScreener, sans plus de précision) est un pool Uniswap V4 — sur cette architecture, TOUS les swaps de TOUS les pools transitent par un seul contrat `PoolManager` SINGLETON (`0x498581ff718922c3f8e6a244956af099b2652b2b` sur Base, confirmé officiel via BaseScan), jamais par une adresse de pool dédiée comme sur V2/V3. `gather_dev_wallet_facts` comparait `frm`/`to` au seul `lp_address` (l'identifiant logique de pool renvoyé par DexScreener, pas une vraie adresse de contrat sur V4) — un achat/vente réel du déployeur sur un pool V4 n'était donc JAMAIS classé "bought"/vente, toujours à tort "allocation".
Solution : nouvelle constante `_UNISWAP_V4_POOL_MANAGER_BASE` (adresse officielle vérifiée), ajoutée systématiquement aux candidats de comparaison EN PLUS de `lp_address` (jamais à sa place — un pool V2/V3 classique continue de fonctionner exactement comme avant). `skills/dev_wallet.py` — 3 nouveaux tests (`test_dev_wallet.py`), suite ciblée 128 passed, suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Cluster Sybil parmi les holders — au-delà de la convergence pairwise
Date : 2026.07.23  /  Probleme : chantier documenté à plusieurs reprises comme LE plus important, jamais résolu — `_holder_concentration` (acp_onchain_scan.py) ne regarde que le TOP HOLDER individuel et le cumul des 10 premiers, jamais si ces holders partagent une origine commune. Une distribution factice (40 wallets à 2% chacun, tous financés par la même source) passe chaque barrière individuelle alors que 78% de l'offre est en réalité concentrée entre les mains d'un seul acteur déguisé en communauté. Distinct de `smart_money._pairwise_convergence` (scopé aux 1-3 wallets soumis ENSEMBLE à `/walletscore`, jamais au pool complet de holders d'un token).
Solution : nouveau `skills/sybil_cluster.py` (`gather_sybil_cluster_facts`/`judge_sybil_cluster`) — regroupe les top holders déjà récupérés par source de financement commune, réutilisant TEL QUEL `smart_money._funding_source` (jamais dupliqué). Cluster ≥5 holders ET ≥20% cumulé de l'offre → signal `concern` ; sinon `neutral`. Coût réseau RÉEL et assumé : jusqu'à 15 appels Blockscout bornés par scan (un par holder vérifié) — nettement plus coûteux que les autres signaux consultatifs de ce chantier (qui réutilisent des données déjà en main sans appel supplémentaire). Câblé dans `acp_onchain_scan.scan_base_token` (`include_sybil_check`, off par défaut) mais **volontairement PAS activé sur `/vc`** (contrairement à insider_wallets/deployer_history) — la poche VC automatique reste dormante (0% du capital, décision du 15/07), le coût réseau ne se justifie pas tant qu'elle n'est pas réactivée ; à activer sur décision opérateur explicite le moment venu. `skills/sybil_cluster.py` (nouveau) / `skills/acp_onchain_scan.py` — 17 nouveaux tests (`test_sybil_cluster.py` nouveau, `test_acp_onchain_sybil_cluster.py` nouveau), suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Signal "substance GitHub" — juge la qualité réelle du développement, pas la fréquence
Date : 2026.07.23  /  Probleme : `project_activity.py` juge la FRAÎCHEUR (jours depuis le dernier commit), jamais la SUBSTANCE — un projet qui spam des commits cosmétiques (README, formatage) passerait le même signal qu'un vrai développement technique. Point de priorité #23 du stress-test.
Solution : nouveau `skills/github_substance.py` — 6 critères pondérés (ratio code/cosmétique 30%, densité de diff 20%, présence de tests 15%, diversité par CATÉGORIE fonctionnelle contract/tests/js_ts/backend 15%, régularité temporelle 10%, qualité des messages 10%). Conception initiale vérifiée AVANT codage contre une proposition externe : coût réel confirmé par appel direct (GitHub ne donne stats/fichiers que via un appel SÉPARÉ par commit, jamais dans la liste), plafonné à 30 commits analysés en détail sur une fenêtre RÉELLE de 90 jours (paramètre `since`, corrigé après relecture — la constante n'était initialement jamais appliquée à la requête). Authentifié via `GITHUB_TOKEN` existant (vérifié par appel réel qu'il peut lire n'importe quel dépôt public tiers, rate limit 5000/h vs 60/h anonyme que `project_activity._fetch_github` utilise aujourd'hui — gap distinct noté, hors scope). Deuxième revue croisée externe intégrée après vérification : garde-fou précoce (<5 commits bruts -> indisponible avant tout appel détail) et diversité par catégorie fonctionnelle plutôt que par extension brute (adoptés) ; échantillonnage stratifié par lignes techniques (écarté -- suppose des données qu'on n'a pas encore sans payer le coût complet). Validé par 4 appels réels : Uniswap v4-core (aucun commit dans la fenêtre, repo mature -> unavailable honnête), ethereum/go-ethereum (score 89,5/100, positive), sindresorhus/awesome -- liste markdown non technique -> unknown (1 seul commit technique, échantillon trop faible), GoldenFarFR/ARIA (score 83,5/100, positive, cohérent avec l'activité réelle de cette session). Câblé dans `vc_analysis._fetch_github_substance` (réutilise `ctx.best_pair.project_links` déjà collecté, jamais un second parsing de lien), activé sur `/vc`, purement consultatif. `skills/github_substance.py` (nouveau) / `skills/vc_analysis.py` — 16+6 nouveaux tests (`test_github_substance.py`, `test_vc_analysis.py`), suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[CODE] Sujet    : Confirmation de stabilité temporelle sur la liquidité — crible VC
Date : 2026.07.23  /  Probleme : `safety_screen()` lit la liquidité/volume de façon INSTANTANÉE au moment du scan — une manipulation temporaire synchronisée sur la fenêtre de scan (liquidité gonflée juste avant, retirée juste après) passerait le crible sans que rien ne le détecte. Contrairement au wash-trading momentum (état en mémoire process, confirmé sur des scans répétés en boucle continue de 15 min), le crible VC (`token_absorber.absorb`) n'est pas un cycle continu — un contrat peut n'être scanné qu'une seule fois.
Solution : nouveau module `skills/liquidity_stability.py` (`record_and_check_liquidity_stability`) — table persistante `vc_liquidity_snapshots` (survit aux redémarrages, contrairement à un dict process), compare la liquidité actuelle au dernier snapshot connu de CE contrat (fenêtre 60 min par défaut) : chute ≥40% → `confirmed=False` (soft-fail, comportement de marché, jamais hard_fail) ; stable/hausse → `True` ; AUCUN antécédent (premier scan) → `None`, jamais un rejet sur une absence de comparaison (même doctrine fail-open que le reste du projet). Câblé dans `token_absorber.absorb()` juste avant `safety_screen()`, nouveau paramètre `safety_screen(liquidity_stability_confirmed=...)`. `skills/liquidity_stability.py` (nouveau) / `skills/safety_screen.py` / `token_absorber.py` — 13 nouveaux tests (`test_liquidity_stability.py` nouveau, 3 tests bout-en-bout dans `test_token_absorber.py`, fixture d'isolation DB dédiée), suite complète à confirmer, `test_coherence.py` vert (non commité au moment de cette entrée).

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Flag "transfert vers adresse CEX connue" (extension insider_wallets) — vérifié, PAS construit
Date : 2026.07.22  /  Probleme : le stress-test proposait un flag dédié sur les transferts insiders vers un dépôt CEX tagué connu, en complément de `insider_wallets.py`.
Solution : vérifié empiriquement AVANT de coder (appels Blockscout réels) — Blockscout Base ne tague PAS les adresses CEX connues (`name`/`public_tags` restent vides même sur un hot wallet Coinbase documenté publiquement sur Ethereum mainnet, contrairement à Etherscan qui le nomme explicitement "Coinbase 1"). Sans source de labels fiable côté Blockscout, ce flag nécessiterait soit une liste blanche maison (fragile, les dépôts CEX changent par utilisateur, jamais exhaustive), soit un service tiers payant (Etherscan Labels/Arkham/Nansen, déjà écartés ou à chiffrer séparément). Différé jusqu'à ce qu'une vraie source de labels CEX fiable sur Base soit identifiée — jamais construit sur une donnée qui n'existe pas. Aucun fichier modifié.

------------------------------------------------------------

[DEPLOYE] Sujet    : Substance GitHub aveugle sur un lien d'organisation seule (CNX/crynux-network)
Date : 2026.07.23  /  Probleme : question opérateur directe ("aria détecte bien les liens d'un projet ?") sur une capture réelle des liens CNX — vérifié que `github_substance` renvoyait "indisponible" sur `github.com/crynux-network` alors que ce repo a un développement actif réel (272 étoiles sur `crynux-node`). Cause : le projet déclare une URL d'ORGANISATION (pas de repo précis), que `parse_github_repo` ne sait pas résoudre par construction.
Solution : `resolve_github_repo` (project_activity.py) résout aussi une organisation seule — charge les repos triés par activité (`sort=stars` n'existe PAS côté API, vérifié contre la doc officielle), exclut forks/archivés/repos de config (`.github`/`profile`), puis PRÉFÈRE un candidat dont le nom partage une racine avec le nom de l'organisation (préoccupation opérateur explicite : une org peut héberger des projets sans rapport entre eux) avant de départager par étoiles. Vérifié en conditions réelles sur le conteneur : `crynux-network` → `crynux-node` (signal positive, 75.5/100) au lieu d'indisponible. `github_substance.py` fenêtre aussi désormais réellement les commits sur 90 jours (`_WINDOW_DAYS` déclaré mais jamais appliqué à la requête avant ce correctif). `services/project_activity.py` / `skills/github_substance.py` (commit `d926d9a9`) — 12 nouveaux tests, suite complète 6969 passed / 17 skipped, `test_coherence.py` vert.
Note (même session) : opérateur a aussi proposé 3 formules externes (X/Website/Docs Substance) — évaluées et testées en conditions réelles sur CNX (twit.sh, site_snapshot) avant tout code : la majorité de leurs axes (âge de compte X, ratio followers, cohérence visuelle d'un site, profondeur d'un whitepaper au-delà de 600 caractères) demandent des données qu'ARIA n'a structurellement pas aujourd'hui (twit.sh ne renvoie que des métriques par tweet, jamais de profil ; `site_snapshot.py` est plafonné à 600 caractères, conçu pour enrichir un prompt LLM, pas pour auditer un site de 500Ko+) — jamais codé sur cette base, en attente d'une décision opérateur sur les capacités à construire en plus (crawler multi-page, parsing PDF, source de données de profil X).

------------------------------------------------------------

[CODE] Sujet    : Signaux "substance Website/Docs/X" — construits via Tavily extract/crawl
Date : 2026.07.23  /  Probleme : suite directe de l'entrée ci-dessus — opérateur a confirmé vouloir câbler TOUS les liens DexScreener (sauf Discord/Telegram) après avoir vérifié en réel que Tavily résout le blocage constaté ("il faut utiliser tavily" ; "elle doit pouvoir extraire tout pour noter" ; "la doc doit aussi être lue en entière... https://docs.crynux.io/").
Solution : `skills/website_substance.py` (crawl Tavily sur le lien Website, 4 critères : profondeur en mots/sections clés trouvées/nombre de pages atteintes/HTTPS — score 100/100 réel sur CNX, 12 pages, 9135 mots) ; `skills/docs_substance.py` (crawl Tavily sur le lien Docs DÉCLARÉ séparément — jamais une découverte incidente via le crawl Website — 4 critères : profondeur technique/mots-clés techniques/roadmap/tokenomics+risques — score 80/100 réel sur CNX, 15 pages, 13456 mots) ; `skills/x_substance.py` (Tavily extract sur la page profil X). **Trouvaille réelle importante** : la formule X proposée par l'opérateur a été RÉDUITE après un test concret — l'extraction Tavily d'une page profil X renvoie des tweets "highlights" (les plus vus/engagés, 21K/54K/81K vues observées), PAS le fil chronologique récent réel (aucune date 2026 alors que le compte postait la veille, confirmé via twit.sh en parallèle) — testé 2 fois (extract_depth basic ET advanced), même résultat. La régularité de publication a donc été ÉCARTÉE du signal X (fabriquer un score dessus aurait produit un faux négatif sur un compte réellement actif) ; seul l'âge du compte reste ("Joined October 2023", confirmé exact contre une capture réelle du profil, 34 mois). Les 3 signaux sont consultatifs (jamais un véto), câblés dans `vc_analysis._build_untrusted_context`/`analyze_vc_with_context` via `_find_link_by_label` (Website/Docs, par label déclaré) et `conviction_research._extract_x_handle` réutilisé tel quel (X, par domaine d'URL, jamais dupliqué). `services/tavily.py` gagne `extract()`/`crawl()` (authentification par header `Authorization: Bearer`, vérifiée réelle sur les 3 endpoints Tavily ; coût sourcé contre la doc officielle dans `services/tavily_budget.py`). `skills/website_substance.py` / `skills/docs_substance.py` / `skills/x_substance.py` / `services/tavily.py` / `services/tavily_budget.py` / `skills/vc_analysis.py` — 40+ nouveaux tests, suite complète à confirmer, `test_coherence.py` vert.

------------------------------------------------------------

[CODE] Sujet    : TwitterAPI.io comble le trou X Substance (followers/following réels)
Date : 2026.07.23  /  Probleme : X Substance restait réduit à 1 seul axe (âge du compte) — ni twit.sh ni Tavily n'exposent de compteurs followers/following. Opérateur a partagé un tableau comparatif de 4 fournisseurs tiers avec un plafond dur 0,20$/scan.
Solution : diligence réelle des 4 candidats (workflow 4 agents, WebSearch+WebFetch) : TwitterAPI.io retenu (0,18$/1000 profils = 0,00018$/scan, followers+following+createdAt en UN appel, ScamAdviser "legit and safe", Trustpilot positif, skill MCP officielle) contre Apify (0,004$/profil, risque ToS réel — X a déjà poursuivi des scrapers), X API officielle (0,01$/profil, plus de palier gratuit en 2026), Refetcher (0,0009$/scan mais SANS `created_at`, légitimité faible), ChirpAPI (**n'existe pas**, vérifié). Piste alternative **Xquik** (fournisseur x402 natif listé dans `awesome-x402`, 0,00015$/scan) évaluée puis ÉCARTÉE : son défi de paiement pointe vers le réseau "Tempo" (Stripe/Paradigm, chainId 4217, très légitime mais PAS Base/USDC) — incompatible avec l'infra CDP existante, chantier disproportionné. Nouveau `services/twitterapi_io.py` (throttle 5,5s, sourcé sur le VRAI dashboard opérateur — palier Free 0,2 QPS jamais payé, capture réelle) tenté EN PREMIER dans `x_substance.py`, repli Tavily (âge seul) inchangé si absent/panne. Vérifié en conditions réelles PAR L'OPÉRATEUR (curl manuel depuis son terminal, jamais moi — clé dans `.env`, contrainte de sécurité de session) : `@crynuxio` → 3676 followers, 242 following, créé le 27/10/2023, cohérent avec Tavily et une capture réelle du profil.

------------------------------------------------------------

[CODE] Sujet    : X Substance enrichi — activité/régularité + engagement normalisé
Date : 2026.07.23  /  Probleme : suite directe de l'entrée ci-dessus — opérateur a demandé d'utiliser aussi l'engagement et l'activité (déjà dans son tableau comparatif, confirmant que twit.sh les a aussi). Réutiliser twit.sh ici aurait dupliqué un appel payant déjà fait par `conviction_research.py` (cadence de publication) sur le même compte.
Solution : nouveau `services/twitterapi_io.fetch_last_tweets` (`/twitter/user/last_tweets`, 0,15$/1000 tweets, vérifié réel : `createdAt`+`likeCount`/`replyCount`/`retweetCount`/`quoteCount` par tweet) — même fournisseur déjà branché, zéro nouveau client, zéro couplage avec conviction_research.py (qui garde son propre chemin X officiel→twit.sh, inchangé). Formule à 4 axes quand tout est disponible : âge (30%) + ratio following/followers (25%, seuils repris de la proposition externe) + régularité (25%, semaines actives sur 12 dans une fenêtre de 90j, seuil plein à 8 semaines) + engagement moyen normalisé par followers (20%, seuils ≥1%→100/≥0,3%→70/≥0,05%→40/moins→0, calibrage approximatif documenté comme tel). Poids redistribués proportionnellement si un axe manque (jamais un chiffre fabriqué) — ex. panne sur last_tweets seul dégrade UNIQUEMENT activité/engagement, âge+ratio restent intacts. `services/twitterapi_io.py` / `skills/x_substance.py` — 20+11 nouveaux tests, suite complète à confirmer.

------------------------------------------------------------

[DEPLOYE] Subject  : Virtuals bonding index client only ever saw the last ~200 tokens -- real project (HoloStudio) invisible past that window
Date : 2026.07.27 / Problem : operator asked to find the bonding project with the strongest potential; a scan of `fetch_prototypes()` found nothing substantial among the ~200 most recent bonding tokens. Operator then supplied a real contract (HoloStudio AI Ecosystem, created 25/02, 307 holders, a real Seedify hackathon win + NEXUS partnership independently confirmed by external sources) that the scan had never surfaced. Root cause traced live: `build_prototypes_url`'s `page_size` was capped at 200 and never requested a page beyond the first -- verified this was NEVER a server-side limit (direct API calls with `pagination[pageSize]=500`/`=2000` both returned full pages quickly, `=10000` timed out; the real index holds ~54,500+ tokens across ~110 pages via `pagination[page]=N`, confirmed by fetching page 73 and finding HoloStudio's exact contract in the raw response).
Solution : `build_prototypes_url`/`build_graduated_url` gain a `page` param (only appended to the URL when `page > 1` -- the default-page URL stays byte-for-byte identical, zero behavior change for every existing caller); page_size cap raised 200 -> 500 (the calibrated safe ceiling, not guessed). `VirtualsClient.fetch_prototypes`/`fetch_graduated` gain `max_pages` (default 1, unchanged historical behavior) via a shared `_fetch_paginated` walk that stops using the API's own `meta.pagination.pageCount` (never guessed from response length) and keeps pages already fetched successfully even if a LATER page then fails (only the very first page failing still returns `[]`, identical to before). Real residual limit found while verifying, documented rather than hidden: a full 110-page walk is NOT reliably fast in one pass -- server latency grows with pagination depth, two live attempts timed out at different depths (page 7 once, past page 72 another time) -- never a deterministic bug, a genuine server-side characteristic. Partial results are always preserved (never all-or-nothing), but a single-pass full-history scan isn't guaranteed complete -- the planned persistent bonding watchlist (next chantier) will need a resumable checkpoint across cycles, not a single from-scratch pass, to fully exploit this fix. Also checked the rest of the repo for the same unverified-cap pattern (`min(x, 200)`): 2 other occurrences found are unrelated (a local SQL LIMIT on an internal log table, a bounded score calculation); a 3rd (`fetch_recent_trades`, a DIFFERENT Virtuals endpoint for individual trades) was already verified live by a prior session and left untouched. `services/virtuals.py` -- 8 new tests (`test_virtuals_client.py`), targeted suite green (116 passed), full suite green (8072 passed, 17 skipped).

------------------------------------------------------------

[DEPLOYE] Sujet    : screened_token n'a jamais eu de ligne "active" -- barème de score plafonné à 70/95 (Item #200)
Date : 2026.07.29 / Probleme : opérateur a demandé pourquoi il ne voit jamais la poche VC agir ("et vc je le vois jamais aussi"). Diagnostic par agent dédié (Explore) sur la vraie base de prod (`/opt/aria-data/aria.db`) : 2984 lignes dans `screened_token` en 3 semaines, 2189 `pending`, 795 `rejected`, **0 `active`**. Cause racine réelle : `_score_and_verdict` (acp_onchain_scan.py) part d'une base de 50 et ne pouvait ajouter au mieux que +20 (liquidité +10, volume +5, flux d'ordres acheteur +5) -- plafond théorique atteint 70/95, exactement le `DEFAULT_MIN_SCORE` exigé par `safety_screen.py` (contrat vérifié, mint neutralisé, Gini bas, GoPlus propre n'apportaient chacun AUCUN bonus, seulement l'absence de malus). Résultat mesuré : seulement 3 candidats sur 2984 ont jamais touché 70, et les 3 ont ensuite été bloqués par un second problème (contract_verified/top_holder_pct jamais renseignés par Blockscout ~98% du temps, fail-closed sur donnée indisponible -- non corrigé dans ce commit, banqué séparément).
Solution : ajout de vrais bonus positifs pour des signaux CONFIRMÉS bons, sans jamais toucher aux malus existants (un mauvais token garde exactement les mêmes pénalités qu'avant) -- contrat vérifié +10 (`_apply_onchain_signals`), mint proprement neutralisé (renoncé/launchpad connu) +5 net au lieu de 0, distribution des holders équitable (Gini <0,5/<0,7) +10/+5 (calculé mais purement informatif jusque-là), lecture GoPlus VRAIMENT propre (aucun signal, y compris ceux seulement contextualisés comme is_proxy/is_mintable/is_blacklisted/transfer_pausable, jamais juste `delta==0`) +10. Nouveau plafond théorique : 95, cohérent avec l'affichage "score X/95" déjà partout dans le code. `skills/acp_onchain_scan.py` (commit `744f4fc8`) -- tests existants corrigés avec les nouveaux deltas attendus (`tests/test_acp_onchain_blockscout.py`, `tests/test_goplus.py`), suite complète 8402 passed / 17 skipped, `test_coherence.py` vert. Cause #2 (Blockscout fail-closed sur donnée indisponible) et cause #3 (déploiements bonding qui polluent la poche VC avec des candidats structurellement sans paire DEX) restent ouvertes, pas encore corrigées.

------------------------------------------------------------

[DEPLOYE] Sujet    : Item #200 suite -- cause #2 (Blockscout "indisponible") était en fait un retry trop lent, pas une vraie panne
Date : 2026.07.29 / Probleme : opérateur a demandé d'attendre d'avoir corrigé Blockscout avant de déployer le fix ci-dessus. Test empirique EN DIRECT (pas une supposition) sur les 3 seuls candidats ayant jamais atteint score 70 (RWAGMI, GRUG, BRIAN2) : `check_contract_flags`/`get_token_holders` réussissent en fait très bien MAINTENANT (rafale de 15 appels réels, 15/15 succès, ~1s chacun, holders + Gini correctement calculés) -- y compris RWAGMI, marqué "abandonné après 3 tentatives (11.8j)" en base. Blockscout n'est donc PAS structurellement indisponible ~98% du temps comme le suggérait la table `screened_token` -- la vraie cause est le RYTHME du retry : `retry_stale_pending` (base_crawler.py) ne retentait que 20 candidats/cycle de 6h avec un abandon à `max_age_days=7` -- avec 2189 `pending` réels, un cycle complet prenait ~27 jours, largement au-delà du délai d'abandon. Un candidat qui essuie un simple échec réseau transitoire une seule fois n'avait donc statistiquement quasiment aucune chance d'être retenté avant d'être abandonné en `rejected`.
Solution : `absorb_direct_candidate` (bonding_absorber.py) qui relaie des déploiements tout frais dans le pool VC standard est un comportement VOULU et documenté (décision opérateur du 10/07 : liquidité/vérification/pair sont des aspects qui MÛRISSENT avec le temps, jamais un rejet définitif) -- vérifié dans le code, PAS un bug, laissé inchangé (cause #3 du diagnostic initial écartée après vérification). Le vrai correctif : `heartbeat.py` (cycle `vc_crawl`) surcharge désormais `retry_stale_pending(limit=60, max_age_days=21)` au lieu des défauts (20, 7) -- un cycle complet passe de ~27 jours à ~9 jours, et un candidat a maintenant plusieurs vraies chances de retry avant l'abandon, plutôt qu'effectivement zéro. Choix dimensionné avec prudence (x3, pas plus) : Blockscout répond vite (~1s/appel) donc le temps d'exécution du cycle de 6h n'est pas en cause, mais le budget Pro partagé (90 000 crédits/jour, déjà "nearly exhausted" au moment du test, repli automatique déjà en place vers l'endpoint gratuit) est un facteur limitant réel à surveiller avant d'aller plus loin. `heartbeat.py` (commit à suivre) -- suite complète 8402 passed / 17 skipped, `test_coherence.py` vert.

------------------------------------------------------------

[CODE] Subject  : Limit-order path never re-checked holder concentration -- a >=80%-concentrated token could still be bought after a long "watching" window
Date : 2026.08.02 / Problem : real security gap found via an adversarial cross-review workflow, then implemented from a pre-written correction plan. `momentum_entry.evaluate_momentum_entry` defers its holder-concentration guardrail (`_check_holder_concentration`, top 10 EOA holders excluding pool/burn/verified contracts >= 80%) via `evaluate_hard_gates(defer_holder_concentration=True)`, then only re-runs it once `signal.present` is True (the confirmed golden-pocket + RSI setup, buy-now path). A candidate routed to the limit-order path instead (`signal.present` is False by construction -- that is the whole premise of placing a limit order) returns from `evaluate_momentum_entry` before ever reaching that check. Neither `limit_orders.reanalyze_for_watching` (honeypot-only re-check at the `pending`->`watching` transition) nor `limit_orders._execute_trigger` (the buy itself) ever called it. The `watching` window can last up to 30 days for an `rsi_divergence_pending` order (`watch_expiry_hours`) -- wide enough for a distribution to concentrate past 80% in insider hands after the one-time honeypot re-check, with ARIA still buying at trigger.
Solution : new `limit_orders._reanalyze_holder_concentration(order)` -- re-fetches a FRESH pair (`momentum_entry.fetch_token_pairs` + `_best_pair`, same pattern already used by `check_rsi_divergence_watching_order`, never the signal's original possibly-stale `pool_address`) and calls `momentum_entry._check_holder_concentration` AS-IS, zero duplicated logic. Doctrine matches that function's own (its docstring: "only the honeypot check is fail-closed in this pipeline") -- FAIL-OPEN on any missing/unresolved data (pair lookup failure, no pair found), deliberately NOT copying the honeypot's fail-closed behavior onto this specific guardrail. Never applied to a bonding-curve order (`chain == bonding_entry.CHAIN_MARKER`) -- no separate DEX pool pre-graduation, `_reanalyze_bonding_for_watching` already covers the structural equivalent (`dev_holding_pct`). Wired at BOTH re-analysis points (defense in depth, chosen over a single point after weighing the tradeoffs in the plan): `reanalyze_for_watching` (after the existing honeypot/dex-quality check in both non-bonding branches -- cheapest/hardest guardrail first, never spent on an order about to be cancelled for a cheaper reason) and `_execute_trigger` (right before `paper_trader.open_position`, after sizing and all duplicate/cap/circuit-breaker guards -- the last, hardest-to-bypass point). A confirmed-concentrated trigger returns `None`, same soft-fail as every other `_execute_trigger` guard -- the order stays in `watching`, never lost, may still resolve on a later pass if concentration clears. `limit_orders.py` -- new autouse test fixture (`_bypass_holder_concentration_reanalysis`) neutrally stubs the guardrail for every unrelated test in the file (avoids ~20 existing tests needing individual mocks, single point of patch); 12 new dedicated tests (`test_limit_orders.py`: 5 unit tests on `_reanalyze_holder_concentration` itself including the exact hole scenario -- a `>=80%`-concentrated token confirmed rejected -- plus fail-open-on-network-failure and bonding-never-checks; 4 wiring tests on `reanalyze_for_watching` including the cost-ordering guarantee (honeypot failure short-circuits before spending the holder-concentration call); 3 end-to-end tests on `_execute_trigger` via `process_active_orders`, covering blocked/clear/fail-open). Targeted suite green: `test_limit_orders.py` 125 passed, `test_momentum_entry.py` 340 passed, `test_limit_orders.py`+`test_momentum_entry.py`+`test_paper_trader.py` 807 passed, `test_coherence.py` 86 passed.

------------------------------------------------------------
