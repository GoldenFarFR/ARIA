Format: `[STATUT] Sujet` puis `Date : AAAA.MM.JJ / Probleme : ...` puis
`Solution : ... — fichier.py (hash court)`. `[STATUT]` in `DEPLOYE` / `CODE`
(tested, not yet deployed) / `CONFIG` (manual action, no commit) / `ETAT ACTUEL`
(current snapshot, not a fix). Covers the operator mobile fallback channel
(Item #201, plan at `/root/.claude/plans/fizzy-plotting-map.md`): account,
sessions, chat, kill-switch.

------------------------------------------------------------

[CODE] Subject  : Mobile chat always answered in English regardless of the operator's own language (Item #201)
Date : 2026.07.30 / Problem : real production bug caught by the operator's own end-to-end test of the just-deployed Phase 1/2 (login + chat) — he asked ARIA a question in French from the Android app and got an English answer back ("0 open positions across all pockets. Paper-trading capital stands at 1,000,000 $ with zero P&L."). Traced to `AriaBrain.process(user_message, lang=LANG_EN, ...)` -- English is the function's own default, and `/aria/ops/chat` never passed `lang` at all. Never caught before because Telegram auto-translates the display for the operator (documented elsewhere in this file); the Android app translates nothing, so this is the first surface where the gap was actually visible. Not cosmetic: the operator does not read English (CLAUDE.md, operator profile) -- a fallback channel meant to work when Telegram is down answering in a language its only user can't read is a functional failure of the channel's entire purpose.
Solution : `ChatBody.lang: Literal["fr", "en"] = LANG_FR` (French default, deliberately diverging from `AriaBrain.process()`'s own English default -- commented in place so a future session doesn't "fix" the divergence back). Passed through explicitly to `aria_brain.process(..., lang=body.lang, ...)`. Optional field -> the already-installed APK gets French with zero rebuild; a future client can still pick English.
`vanguard/backend/app/api/routes/operator_mobile.py` -- 3 tests (`test_operator_mobile_routes.py`: absent field defaults to "fr", explicit "en" honored, unsupported locale -> 422 without ever reaching the brain). Full suite green (aria-core 8496 passed/17 skipped, backend 208 passed), `test_coherence.py` green.

------------------------------------------------------------

[CODE] Subject  : Phase 3 -- kill-switch REST (/stop, /resume, /history), TOTP anti-replay (Item #201)
Date : 2026.07.30 / Problem : the mobile fallback channel had login/session/chat (Phases 1-2) but no way to arm or lift the kill-switch from the app -- the whole point of a channel meant to work when Telegram is down. The plan's core requirement: the sliding session alone (fine for chat) is far too permissive for the one control that can really hurt.
Solution : `POST /aria/ops/stop` / `POST /aria/ops/resume` now require BOTH the mobile session AND a fresh TOTP code on every call (`_require_fresh_totp`), checked via the account's own `verify_totp` then consumed by a new anti-replay claim -- never cached, never derived from the session. Anti-replay is a dedicated table (`operator_used_totp_codes`, new `app/auth/operator_totp_replay.py`) with `UNIQUE(account_id, totp_code)`: the DB itself rejects a second insert of the same pair, so two concurrent calls with the same code can never both pass a check-then-mark race (proven by a test firing 8 concurrent claims of one code -- exactly 1 winner). A dedicated throttle (`KILL_SWITCH_MAX_ATTEMPTS=15`/5min, `check_rate_limit`) bounds brute-forcing a stolen session's TOTP guesses, always self-releasing -- the plan forbids a hard lock on a fallback channel. The legacy server-to-server path (`X-Admin-Secret`/`X-Admin-Totp`, a different `ADMIN_TOTP_SECRET`) is exempt from this second factor -- it already presented its own TOTP to `is_operator_request`, an assumed difference in level (serves scripts, not a human app reusing one code repeatedly), documented in the plan. Both routes are idempotent (arming an already-armed pause, or resuming an already-resumed one, returns `changed=false` and logs nothing -- a network retry must never look like a failure or pollute the audit trail). `record_incident` failures never block the real action (audit is observability, never a gate) -- proven by a test that makes the log call raise and confirms the pause still armed. `GET /aria/ops/history?limit=N` reuses `kill_incident_log.list_incidents`, capped server-side (`HISTORY_MAX_LIMIT=100`) regardless of what the client asks for.
Real edge case found and handled deliberately: a corrupted `pause_state.json` reads as `paused=False` (fail-open on the read) but freezes spending fail-closed (`outgoing_pause.money_block_reason`) -- treating that as "already resumed" would leave the operator with no way to unfreeze money from this channel. `/resume` now also repairs an unreadable state (writes a clean one), and that IS logged as a real change.
`vanguard/backend/app/auth/operator_totp_replay.py` (new), `operator_mobile.py` -- 27 new tests across `test_operator_totp_replay.py` (new) and `test_operator_mobile_routes.py`, +1 structural invariant in `test_coherence.py` (the only one of these actually wired to CI -- see residual below). Full suite green (aria-core 8496 passed/17 skipped, backend 208 passed), `test_coherence.py` green. Not yet deployed -- reviewed and committed by the command session, never blind-trusted from the subagent's own report.

------------------------------------------------------------

[ETAT ACTUEL] Sujet : vanguard/backend/tests/ n'est câblé à AUCUN workflow CI
Date : 2026.07.30 / Probleme : `.github/workflows/ci.yml` ne couvre que `packages/aria-core/**` (voir son propre `paths:`) -- les 208 tests backend (Phase 1-3 du canal opérateur mobile incluses) tournent en local mais jamais en CI. Trou préexistant à Phase 3, pas introduit par elle -- trouvé en relisant le rapport de l'agent qui a construit Phase 3.
Solution : pas encore corrigé -- nécessite un nouveau job CI dédié (`vanguard/backend`, son propre `.venv`/dépendances), décision/scope à trancher séparément plutôt qu'ajoutée en silence à ce chantier. En attendant, l'invariant structurel du kill-switch mobile est verrouillé dans `test_coherence.py` (celui-là réellement en CI) -- filet partiel, pas un vrai substitut à des tests d'exécution réelle.

------------------------------------------------------------

[DEPLOYE] Subject  : Mobile chat confabulated a kill-switch action -- "/stop" as free text got a fake "Stop confirmed" (Item #201)
Date : 2026.07.30 / Problem : real incident caught by the operator himself testing Phase 3 in prod -- he typed "/stop" as a plain chat message in the app and ARIA answered "Stop confirmed. Aucune session ou process actif à interrompre. 🟠 payant (5123 tok)". Verified live: `outgoing_pause.pause_status()` never changed, and a routine limit-order alert arrived normally right after -- proof the pause never actually armed. The free-text chat only ever calls `aria_brain.process()`; there is nothing in that path that could arm `outgoing_pause.pause()`, so the LLM had simply generated a plausible-looking confirmation for a real security action that never happened. Telegram never has this problem: its `/stop` is a dedicated `CommandHandler` intercepted BEFORE the brain (`gateway/telegram_bot.py::_handle_stop`) -- the mobile chat route had no equivalent interception.
Solution : `_control_command_reply(message, lang)` checks the message's FIRST WORD against `{"/stop", "/resume", "/pause", "/start"}` (case-insensitive) BEFORE `/chat` ever calls `aria_brain.process()` -- on a match, returns a fixed, non-generated `ChatResponse` explaining the chat can't arm/lift the kill-switch yet and pointing to Telegram, `aria_brain.process()` is never invoked at all. Matched on the first word only, never a substring search -- a genuine question containing "stop" (e.g. "why no stop loss on this token?") still reaches the brain normally. Deliberately conservative in scope: only fixes the confabulation risk on this specific action class, not a general anti-hallucination mechanism for the whole chat.
`vanguard/backend/app/api/routes/operator_mobile.py` -- 4 new tests (`test_operator_mobile_routes.py`: /stop never reaches the brain, all 4 commands intercepted case-insensitively via parametrize, reply respects `lang`, "stop" mid-sentence still reaches the brain unmodified) + 1 structural invariant in `test_coherence.py` (guard runs before the brain call, the exact two commands from the real incident stay in the guarded set). Full suite green (aria-core 8499 passed/17 skipped, backend 216 passed), `test_coherence.py` green. The real UI fix -- a dedicated STOP button in the app calling `POST /aria/ops/stop` directly with a TOTP prompt -- remains open, lower priority per the operator (Phase 2 mobile UI work, not yet scheduled).

------------------------------------------------------------

[CODE] Subject : EAS Android build failed on `npm ci` -- peer-dependency conflict between permissionless and viem's `ox`
Date : 2026.08.07 / Probleme : the 07/08 EAS build (`c2fe74a5`, commit `78fde919`) errored after 59 min, queued an hour first so it initially read as a transient Expo incident. It was not: the "Install dependencies" phase failed with `npm ci --include=dev exited with error code 1`. Reproduced identically on the VPS -- `permissionless@0.2.57` declares `peerOptional ox@^0.8.0`, while `viem@2.55.10` pulls `ox@0.14.33`, and the committed `package-lock.json` had evidently been generated with peer checks bypassed. `npm ci` on EAS runs strict, so a lockfile that installs fine locally with `npm install --legacy-peer-deps` breaks the build. No `mobile/` file had changed since that commit, so a plain retry would have failed the same way (and the 180-minute retry window had lapsed anyway). The last APK that actually built dates from 07/29 -- the operator's phone has none of the fixes since.
Solution : `mobile/.npmrc` with `legacy-peer-deps=true` -- EAS reads the project's own npmrc, so the build now resolves exactly as a local install does, with no CLI flag to remember. The conflict itself is benign (`peerOptional`, and permissionless' range is simply stale against current viem), so pinning `ox` down to 0.8 would have meant downgrading viem for no real gain. Verified end to end on the VPS before committing: `npm ci --include=dev` completes (558 packages, 0 vulnerabilities) and `tsc --noEmit` still passes.
`mobile/.npmrc` (new). Build to be relaunched from expo.dev with base directory `mobile`.

------------------------------------------------------------

[CODE] Subject : EAS build then failed one phase later -- Metro resolved jose's NODE build, which imports the `buffer` built-in
Date : 2026.08.07 / Probleme : with the npm ci fix in place (entry above), the next EAS build got past "Install dependencies" and died in "Bundle JavaScript" instead: `Unable to resolve module buffer from node_modules/jose/dist/node/esm/runtime/base64url.js`. Import chain is App.tsx -> @privy-io/expo -> @privy-io/js-sdk-core -> jose. `jose@4.15.9` ships separate node and browser builds and declares both in its package exports; Metro was picking the "import" condition, landing on dist/node/esm, whose `import { Buffer } from 'buffer'` targets a Node built-in that does not exist in React Native. Reproduced locally with `npx expo export --platform android` before changing anything -- the same failure, same import stack.
Solution : new `mobile/metro.config.js` asking for the `browser` condition first (`unstable_conditionNames = ['browser', 'react-native', 'require', 'import']`, with `unstable_enablePackageExports`), which hands us jose's own dependency-free build. Preferred over polyfilling `buffer` + aliasing it in `extraNodeModules`: that ships a shim to work around a build we should never have been resolving, and would leave every other node-only import in that same build (crypto, util...) queued up to break the same way later. Verified locally: bundle succeeds (2325 modules, 4.9 MB), `tsc --noEmit` still clean. Note for any future session: the project had NO metro.config.js at all until now, so this is also the file to reach for the next time a dependency resolves to the wrong build.
`mobile/metro.config.js` (new).

------------------------------------------------------------

[CODE] Subject : Read-only session scope -- a token stolen off the operator's PC can no longer drive the action routes
Date : 2026.08.17 / Probleme : operator decision ("je pense quil faut empecher toute interactif sur le dashboard sa devrait etre qune lecture"), from an explicit threat model he stated himself: a worm on his PC. Restricting the dashboard's own routes would have been THEATRE -- `operator_dashboard.py` was already GET-only (2 routes). The real hole, found by auditing what the SHARED session unlocks: the dashboard authenticates with `require_operator_or_session`, the exact same session as the mobile action routes, so a token lifted off that machine could call them. Audit of the 4 action routes showed `/stop` and `/resume` were already covered by a fresh-TOTP requirement (a stolen token alone never fired them), but `/chat` and `/push-token` had NO second factor at all -- `/chat` being the dangerous one, since it is open-ended natural language to an agent with real capabilities.
Solution : new `scope` column on `operator_sessions` (additive migration, same pattern as `last_totp_reverify_at`) + `sessions.is_read_only()` + `require_full_session()` posted on the 4 action routes. Fail-safe design: a NULL scope (every pre-17/08 session) stays FULL so the operator's live phone session and kill-switch are never silently downgraded, while any UNRECOGNISED value fails CLOSED to read-only (a tampered column costs read access, never grants write). A bogus scope passed at creation is stored read-only for the same reason. `read_only` is opt-IN on /login, so no existing client changes. 10 dedicated tests (6 unit + 4 end-to-end proving the brain is never reached, that reads still work, that the kill-switch is refused, and that a normal login is unaffected); full backend suite green (309). HONEST BOUND, stated in the code: this protects against a stolen TOKEN, not stolen CREDENTIALS -- an attacker with password + TOTP can still mint a full session at /login. — `app/auth/operator_session.py`, `app/api/routes/operator_mobile.py` (89a70834)
