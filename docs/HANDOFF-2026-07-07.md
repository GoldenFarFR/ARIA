# Handoff de session — 2026-07-07

> Pour la prochaine session Claude. Branche : `claude/session-context-files-ofl85l`
> (PR #12). Répondre en **français**. Tout ci-dessous est committé + poussé sauf
> mention contraire.

## 1. Direction produit (rappel — SSOT dans VISION.md §8)

ARIA = investisseuse VC, **gamme LUXE** du domaine. Le moat est **l'analyse (la
décision prouvée), pas l'exécution**. Abonnement boutique ~500 $/mois × 4-5
clients. Éditions **premium** (navy/or/émeraude) + **standard** (rose/vert/or).
Funnel « échelle de risque » : web2 → BTC/ETH → confiance via track record →
low-caps qualité / pré-bonding Virtuals. « Un Nansen avec de la qualité. »

## 2. Livré cette session (committé)

- **Rapport** : éditions premium/standard (palette rose) + correctif scroll
  mobile (radiaux aplatis). VALIDÉ par l'utilisateur (« les deux sont correct »).
- **VISION.md** §8 : bloc « ARIA investisseuse VC — gamme luxe ».
- **docs/conformite-dossier-avocat.md** : dossier de questions pour un avocat
  (facturation = possible « conseil en investissement » AMF/MiFID/MiCA →
  **aucun encaissement avant validation d'un avocat**).
- **docs/recherche-launchpads-base-2026.md** : Virtuals = seule vraie niche
  pré-bonding sur Base ; Flaunch 2e ; Clanker/Zora sautent le bonding.
- **4 moteurs construits + testés** (offline) :
  - `skills/vc_judge.py` — **proof engine** (juge adverse, facts-only, fallback
    déterministe, dôme). ✅ **Branché en MODE TEST** `/vc <adr> test` (admin).
  - `skills/ta_levels.py` + `skills/chart_render.py` — niveaux TA déterministes +
    graphique PNG email-safe (Pillow, data-URI). ⏳ **pas encore dans le rapport.**
  - `services/virtuals.py` — client api.virtuals.io + parsing UNDERGRAD/AVAILABLE.
    ⏳ **pas encore de mode d'analyse dédié.**
  - Mode test `/vc … test` (raisonnement complet, sans email ni track-record). ✅
- **Vitrine durcie** : `robots.txt` + `sitemap.xml` + en-têtes de sécurité
  (`vanguard/public/_headers`). Bénin, pas de CSP.

## 3. À faire ensuite (priorité haute → basse)

1. **#14 Section « Contexte marché » (overlay macro)** dans le rapport. NB :
   pas de source macro branchée → à faire en **additif, data-gated** (rendu
   UNIQUEMENT si data fournie, sinon rapport identique). Ajouter un **test de
   non-régression** (sortie inchangée sans data) + un **preview** pour validation
   de l'utilisateur avant mise en prod. Facts-only : jamais d'événement inventé.
2. **#9 Graphique + niveaux TA dans le rapport** : brancher `chart_render` +
   `ta_levels` dans `vc_report.py`, même approche additive + preview. Dépend d'un
   **pipeline OHLCV réel** (GeckoTerminal gratuit + Alchemy pour tokens morts) —
   à construire ; c'est aussi la dépendance de #5 (projection) et #8 (moteur 24/7).
3. **#10 Mode d'analyse Virtuals pré-bonding** : câbler `services/virtuals.py`
   (détection statut + progression graduation) dans le flux ; testable en live
   seulement sur le VPS (réseau bloqué dans l'env cloud).
4. **#11** (barres même-échelle + thèse enrichie) : **NE PAS toucher** sans le
   feu vert de l'utilisateur (il a validé le visuel actuel).
5. **#12 Backend (URGENT)** : déploiement VPS — voir checklist scratchpad
   (`CHECKLIST_backend_demain.md`, transmise). Nécessite l'utilisateur (choix du
   périmètre : déployer code / activer SMTP / lancer collecte 24/7) + secrets VPS.

## 4. Décisions SÉCURITÉ pour l'utilisateur (audit anti-bot du site)

Verdict : **par défaut le site ne bloque pas les bots.** Correctifs bénins
appliqués (robots/sitemap/headers). Décisions de **modèle d'accès** à trancher
(détail technique transmis hors repo — ne pas committer d'exploit dans un repo
potentiellement public) :
- Le mur d'auth Privy (`AccessCodeMiddleware`) est **désactivé par défaut**
  (`aria_public_mode=True`) → produit + données ouverts. L'activer si accès
  réservé aux membres.
- Rate-limit ancré sur `X-Visitor-Id` (fourni par le client) → contournable.
  L'ancrer sur l'IP réelle / une session vérifiée ; migrer vers Redis.
- `/api/aria/chat` (agent LLM) public → surface d'abus de coût.
- Tokens/secret acceptés en query string → fuite via logs/Referer.
- Endpoints `/api/auth/*` sans rate-limit.
- Vrai anti-bot = au niveau hébergeur (Cloudflare + Bot Fight/Turnstile), pas
  dans le code.

## 5. Pièges / notes

- **Échec de test pré-existant, SANS rapport** avec cette session :
  `tests/test_llm_fallback.py::test_virtuals_with_groq_fallback_chain`
  (résolution de routes LLM : 1 route au lieu de 2). Non causé par nos changements
  (on n'a pas touché `llm.py`). À investiguer séparément.
- Réseau externe **bloqué** dans l'env cloud (403 policy) : DexScreener,
  api.virtuals.io, blockscout, etc. → tout ce qui a besoin de données live se
  teste sur **fixtures** ici, et se valide en live **sur le VPS**.
- **Garde-fous à ne jamais modifier** sans validation : `permission_mode`,
  `wallet_guard`, `regles-uniques`, `config.toml`, flag `aria_llm_enabled`.
  Jamais d'exécution de trade (validation humaine Telegram). Facts-only partout.
- **PR #12** : au dernier check, propre (mergeable_state=clean, 0 CI, 0 review).

## 6. Environnement de test

Venv : `/home/user/ARIA/packages/aria-core/.venv-test/bin/python -m pytest …`
depuis `/home/user/ARIA`. Dép manquante → `uv pip install --python <venv> <pkg>`.
