# HANDOFF — Automatisation continue (cron VPS, veille, observabilité)

> **Repo PUBLIC — jamais d'IP/secret/token/clé en clair ici.**

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.

Regroupe l'historique (conception, premiers tests réels, incidents rencontrés et corrigés) des
mécanismes cron VPS indépendants listés dans CLAUDE.md ("Automatismes en place") : veille
continue Research, Avocat du Diable, promotion backlog, watchdog paper-trading 1M$, surveillance
santé logs. CLAUDE.md garde l'état opérationnel court (quel fichier, quel cron, quel gate) — ce
fichier garde le "comment on en est arrivé là" et les leçons apprises en route.

------------------------------------------------------------

[CONFIG] Subject: goplus-security-watch re-flagged already-handled topics -- tweet_id-only dedup insufficient
Date: 2026.08.25 / Probleme: operator reported 6 duplicates the same day (issues #227-231) -- same underlying topic (ClickFix/PowerShell, address poisoning, wallet rotation, increaseAllowance, Google 2FA cloud-sync) already documented 18/08 in docs/HANDOFF_SECURITE.md/HANDOFF_COINBASE_CDP.md, but a NEW tweet_id (GoPlus periodically republishes the same generic alerts) fully escaped step 1's dedup (which only compares already-seen tweet_id, never already-resolved topics).
Solution: new step 3bis in verdict_prompt.txt -- before any TRAITER verdict, grep the relevant security HANDOFF files for the topic's keywords; if already documented, verdict NON with an explicit pointer to the existing entry, never a TRAITER that re-requests already-done work. File outside the git repo (VPS only, edited in place, same doctrine as shadow_persistent.py) -- /opt/aria-data/goplus-security-watch/verdict_prompt.txt.

------------------------------------------------------------

[ETAT ACTUEL] Subject : Backlog CLAUDE.md purge -- 42 items RESOLVED archived here, index kept to open/active items only
Date : 2026.08.19 / Probleme : CLAUDE.md's numbered backlog index (#256-#335) had accumulated 43 RESOLVED entries alongside 34 still-open ones, defeating the index's own purpose ("pick up in any dev session") -- a session scanning the list had to read through resolved noise to find real open work.
Solution : all RESOLVED entries purged from CLAUDE.md's backlog index, archived here verbatim (one item = one line, full detail already lives in `docs/backlog-technique.md` for anything needing more than this): #256 Aerodrome/Velodrome merger, #257 Robinhood Chain security coverage, #260 x402 V2 CAIP-2 multi-chain, #262 ReasoningBomb spend ceiling audit, #263 Odos aggregator (CDP already routes via 0x), #264 GitLost CVE audit, #265 shared-credential risk, #266 Monte-Carlo/walk-forward overfitting check, #267 Sonnet 5 pricing hike, #269 cadence Polymarket (superseded by burn_in_cadence.py), #270 Three-Layer multi-agent pattern, #271 Base Beryl B20 freeze/seize, #272 CryptoJS weak-RNG, #273 Base MCP routing comparison, #274 FinHarness comparison, #275 CVE-2026-22708 PATH-poisoning, #276 Clanker/Neynar governance, #277 indirect prompt-injection audit, #278 StartupHub.ai (live-tested, low value, not connected), #281 "Comment and Control" CVSS 9.4, #282 delayed-activation honeypot, #283 428-LLM-router security study, #284 CVE-2026-9198 Langflow (does not apply), #285 contractshark malicious VS Code extension, #291 DeepMind AI Agent Traps taxonomy, #292 Bankrbot/Grok NFT+morse attack, #294 CrowdStrike fragmented-instruction technique, #301 candle_history staleness fix, #302 resource_budget.py consolidation, #303 CVE-2026-48710 BadHost Starlette, #307 npm Shai-Hulud/CHAINDROP worm, #308 Robinhood Chain Blockscout coverage, #309 Robinhood Chain Stock Tokens, #311 headless claude -p turn/cost ceiling, #312 Coinbase Agentic.Market re-audit, #313 Coldcard incident added to Devil's Advocate checklist, #316 FINSABER LLM-trading-strategy flag, #317 both mobile bugs fixed, #318 anti-ReDoS clamp gap closed, #319 3/4 Devil's Advocate critiques fixed, #320 llm_usage.py monthly-cost self-correction, #324 LiteLLM PyPI worm (zero exposure). Note: #289 was NOT purged despite matching a naive "RESOLVED" grep -- its own text explicitly says "pas RESOLVED, action dev reste ouverte", stays in CLAUDE.md's active index. — `CLAUDE.md` (backlog index section).

------------------------------------------------------------

[ETAT ACTUEL] Subject : Devil's Advocate -- migration OpenRouter->Anthropic direct API and push/deploy threshold history
Date : 2026.08.04→10 / Problem : the review was originally built on OpenRouter (18/07); a real credit-exhaustion incident on 10/08 forced an emergency migration to the direct Anthropic API mid-session. Separately, the push/deploy threshold (cumulative raw line count held back before both `git push` and `./vanguard/deploy.sh`) started at 2000 lines and proved too tight in practice, generating reviews on nearly every small commit.
Solution : model officialized as Claude Fable 5 on 04/08 (second opinion sought before committing to the switch), migrated off OpenRouter entirely on 10/08 after the incident -- same review script/prompt, only the API client changed. Threshold raised 2000->8000 the same day (10/08), with an explicit operator correction that deploy must be gated by the SAME threshold as push, not just the API-call trigger -- an exception carved out the same day for pushes touching ONLY `.github/**` (surveillance infra must never sit disarmed, goes out immediately regardless of line count). — `scripts/devils-advocate-review.sh`, `scripts/devils-advocate-lib.sh`, `scripts/devils-advocate-precommit.sh`.

------------------------------------------------------------

[ETAT ACTUEL] Subject : Accelerated observation cadence doctrine -- first real application (Item #108 Polymarket) and its auto-revert mechanism
Date : 2026.07.27→08.10 / Problem : the doctrine (run a new heartbeat cycle/gate on a deliberately fast temporary cadence on first activation, to catch failures early) was carved into CLAUDE.md as a permanent norm on 27/07, applied for the first time the same day to Item #108 (Polymarket paper trading, cadence tightened from a nominal 12h/3 candidates down to 1h/1 candidate) -- but the restoration back to nominal cadence had to be tracked manually (Item #133, a plain reminder), and stayed forgotten/unactioned for close to 2 weeks in practice.
Solution : `burn_in_cadence.py` built 10/08 as a generic, reusable mechanism -- auto-reverts an accelerated observation cadence back to its documented nominal value after N consecutive clean heartbeat cycles (no exceptions, no silent no-ops), closing Item #133 without needing a human to remember and manually revert it. Any future first-activation of a new cycle/gate can reuse this same mechanism instead of relying on a manually-tracked backlog reminder. — `burn_in_cadence.py`.

------------------------------------------------------------

[ETAT ACTUEL] Subject : Self-healing throttles/bypasses batch (5 mechanisms) -- the real operator frustration that triggered the batch, and the incidents each one replaces
Date : 2026.08.10 / Problem : operator frustration ("c'est chiant... je vais finir par le supprimer") with recurring manual `.env`/code-edit/redeploy cycles every time the SAME class of external-provider failure or throughput ceiling needed adjusting -- 3 concrete recurring incidents motivated the batch: the holder-concentration guardrail going down for extended real outages with no auto-bypass, GeckoTerminal's throttle needing hand recalibration after every real rate-limit shift, and wallet-scan concurrency requiring 3 separate manual recalibrations including one confirmed 6-day live-lock.
Solution : 5 self-healing mechanisms built the same day, all following the same tighten-fast/ease-slow doctrine where applicable, none needing a human to notice/edit/redeploy for the SAME class of recurring event going forward: (1) `holder_concentration_outage_bypass.py` -- auto-arms after 3 sustained real failures, disarms on first real success, one-time Telegram notice on first armament; (2) `goplus_quota_suspension.py` -- auto-suspends on a real GoPlus rate-limit signal (429/code 4029), exponential backoff 12h->48h, one-time Telegram notice; (3) `services/geckoterminal.py`'s throttle made adaptive (tightens fast on a real 429, eases slowly only after 30 sustained successes, never past the operator's own last hand-calibrated floor); (4) `burn_in_cadence.py` (see the dedicated entry above); (5) `wallet_scan_concurrency.py` -- adaptive `MAX_WALLETS_PER_CYCLE`, same tighten-fast/ease-slow doctrine one layer up (concurrent consumers of the shared GeckoTerminal throttle, not the throttle's own rate). — `holder_concentration_outage_bypass.py`, `single_row_state.py` (shared atomic read-modify-write, fixed a real lost-increment race the same day), `goplus_quota_suspension.py`, `services/geckoterminal.py`, `burn_in_cadence.py`, `wallet_scan_concurrency.py`.

------------------------------------------------------------

[DEPLOYE] Subject : GoPlus Security X watch -- direct Telegram notification added, automation ceiling found while extending it further, first 7 TRAITER verdicts actually processed
Date : 2026.08.18 / Problem : same day, operator correction after the version below shipped: "non comme je tes deja ddit je reouvre des session tres rarement" -- a mechanism that only surfaces at SessionStart (`system_issues` + the existing reminder hook) is not good enough on its own for something the operator actually wants to see, since they don't reopen sessions often. Then, once the Telegram leg worked: "tu dois le traiter toi moi je le lis pas" -- the operator does not want to be the relay reading the notification and deciding; the actual implementation should happen without depending on them.
Solution : two distinct changes. (1) Telegram leg -- `promote_verdicts.py` extended with a `_dedup_key_ever_used()` check (queries `system_issues` for the dedup_key across ANY status, not just open) before sending, so a tweet already promoted once (open or closed) never re-fires a duplicate Telegram message even across repeated cron runs; `telegram_notify.py` copied verbatim from `solana-robinhood-shadow/telegram_notify.py` (same standalone-script pattern, same fix -- throwaway `telegram.Bot`, credentials from the real backend `.env`, best-effort). Tested in isolation (`sent: True`) then end-to-end through the real promotion logic with a synthetic issue (opened, notified, closed as a test artifact) before touching the real 7-issue backlog. (2) Attempted a further "implement automatically" stage (export open issues -> a headless `claude -p` implements/commits/pushes doc-only fixes -> a deterministic script closes the issue + notifies) so the operator's "traite chaque sujet qui remonte" would need zero session to actually resolve. **Blocked by the safety classifier 3 times running** -- twice on the implement prompt (describing autonomous commit+push), once on the deterministic close+notify script, which has NO LLM call and does not touch git at all. The pattern across all 3 denials: the boundary is "no human present at the moment of the action", not narrowly "public push" -- `scripts/research-log-promotion.sh` already commits+pushes to the public repo autonomously via cron and has never been blocked, the difference being that it runs AS the nominal Claude Code session itself (its own prompt frames it as "tu es la session commandement du projet ARIA"), whereas the GoPlus attempt was building a SEPARATE downstream stage with no session framing at all. Abandoned rather than routed around (explicitly against tool-denial guidance to never work around a safety block). (3) Processed the real backlog manually in this same session instead, proving the "reaches a real decision" requirement can be met by a live session without needing unsupervised infrastructure: all 7 open TRAITER issues (#128-#134) read, actually worked (live code audit for #128 Robinhood Chain GoPlus coverage -- found code-complete but never empirically exercised, GoPlus quota currently suspended so live-tested deferred; live code audit for #132 approve/increaseAllowance spenders -- confirmed clean, cross-referenced the existing #310 Permit2 verdict; #134 DeepScan diligence -- confirmed same product as the already-banked #289, merged rather than duplicated; #129/#130/#131/#133 -- OPSEC doctrine added to `HANDOFF_SECURITE.md`/`HANDOFF_AGENT_WALLET.md`, #133 required a live clarifying question to the operator about their real fiat-onramp funding chain, answered and documented generically without naming the specific provider), each closed with the real resolution as the reason. Established going forward: no further unsupervised implement stage will be attempted; the operator-facing commitment is that a real session proactively works through every open `goplus-security-watch` issue at the start of its work, not just when asked. — `goplus-security-watch/promote_verdicts.py`, `goplus-security-watch/telegram_notify.py` (both outside the public repo, VPS-only); `docs/HANDOFF_SECURITE.md`, `docs/HANDOFF_AGENT_WALLET.md`, `docs/HANDOFF_COINBASE_CDP.md`, `docs/backlog-technique.md` (public repo, this commit).

------------------------------------------------------------

[DEPLOYE] Subject : GoPlus Security X watch -- per-tweet TRAITER/NON verdict + proposed solution, promoted to system_issues
Date : 2026.08.18 / Problem : same day, operator follow-up after the fetch-only version below: "je veut pas que le systeme sarrete a ramener les tweet, je veut une lecture de chacun deux et une decision, oui a traiter ou non, si oui proposer une solution" -- collecting raw tweets alone wasn't the point, every tweet needed an explicit judged verdict, not just being folded into the Research Watch's generic scan (which never renders a per-item verdict). No urgency required (operator: nothing needs sub-48h handling) but the system must not silently stop at collection.
Solution : two more stages chained after the existing fetch in the same `run.sh` (same daily cron, `30 6 * * *`, no new cron entry needed). Stage 2, `verdict_prompt.txt` -- a SECOND headless `claude -p` pass, same hard tool restrictions as `research-loop` (`WebSearch WebFetch Read Write` only, `Bash Edit Agent Task` explicitly disallowed, `--max-budget-usd 1`, `--no-session-persistence`) -- reads every tweet in `tweets-log.md` not yet judged (dedup against `tweets_id`s already present in `verdicts.md`), reads CLAUDE.md for what ARIA actually uses, and renders an explicit TRAITER/NON verdict per tweet with reasoning, PLUS a concrete text-only proposed solution when TRAITER (never code, never an implementation -- a future Claude Code session decides). Stage 3, `promote_verdicts.py` -- deterministic, zero LLM call, parses `verdicts.md` for TRAITER blocks and opens a `system_issues` entry per tweet (`source="goplus-security-watch"`, `dedup_key` on the tweet_id so re-running is idempotent, never a duplicate open issue) -- surfaces automatically at the start of every future session via the already-existing `.claude/hooks/system-issues-reminder.sh` hook, same proven pattern as `vc-watch/run.sh`. Tested end-to-end for real: the verdict regex parser validated first on a synthetic sample, then a real promotion test opened+verified+closed a throwaway issue before touching the real registry. Real run against the 20-tweet backlog already logged: all 20 judged (7 TRAITER, 13 NON) despite hitting the $1 ceiling right at the very end (didn't block the actual work) -- verdict quality independently reviewed: correctly cross-referenced real CLAUDE.md backlog items (#313 Coldcard, #308/#309 Robinhood Chain), correctly declined tweets with no real ARIA link rather than fabricating one, and proposed genuinely specific solutions (e.g. verifying GoPlus's real Robinhood Chain chain_id coverage in the actual client code, not just the tweet's own partner-listing claim). 7 real `system_issues` entries opened and independently re-verified via a fresh `list_open()` call. — `goplus-security-watch/verdict_prompt.txt`, `goplus-security-watch/promote_verdicts.py`, `goplus-security-watch/run.sh` (all outside the public repo, VPS-only).

------------------------------------------------------------

[DEPLOYE] Subject : GoPlus Security X watch -- RAW tweet text via x402 twit.sh, never WebFetch/summarized
Date : 2026.08.18 / Problem : operator, now subscribed to @GoPlusSecurity (a security vendor ARIA already uses, `services/goplus.py`), proposed an automated watch of its feed to progressively reinforce security awareness. Initial proposal (dedicated hook every 6h) evaluated then rejected: the existing Research Watch (2h cron, `WebSearch`/`WebFetch` only, never the paid X budget) already covers this need in principle -- but the operator explicitly required the tweet text come back RAW, "as written". Live test: `WebFetch` on x.com fails (HTTP 402), and even browser-based reading (`get_page_text`, claude-in-chrome) auto-translates to French and truncates ("Show more") -- neither option gives genuinely raw text. The operator then pointed to a tool that already existed but had been overlooked in this conversation: `services/twitsh.py` (x402, `x402.twit.sh/tweets/user`, $0.01/call) -- tested live, returns the tweet's exact JSON text (`text` field), never passed through a summarization model. First attempt blocked fail-closed (`usdc_balance_usd` returned `None`) -- real cause: the test script hadn't loaded the backend `.env`'s CDP keys, not a prod bug; fixed, 20 real tweets fetched successfully, real cost $0.01.
Solution : new independent VPS cron (same pattern as the other `*-watch/run.sh` mechanisms, outside the public repo) -- `goplus-security-watch/run.sh` (1 call/day, operator: "not urgent") sources the backend `.env`'s CDP keys then calls `fetch_tweets.py`, which uses `services/twitsh.fetch_user_tweets('GoPlusSecurity')`, dedupes against `tweet_id`s already present in `tweets-log.md` (plain grep, no separate state file needed at this low volume) AND restricts to the last 24h (operator follow-up request, avoids a big historical backfill on top of the ID dedup), appending only genuinely new tweets, full raw text. The existing Research Watch (`research-loop/prompt.txt`) now reads this file as an extra source -- explicitly scoped as read-only, never a call of its own, so it never contradicts its own "never a paid API" boundary (which stays about ITS OWN actions, not content it's handed to read). Real cost: $0.01/day (~$0.07/week) on the shared x402 budget (`x402_budget.py`, $5/week cap, plenty of margin -- $4.72 remaining at deploy time). Tested end-to-end twice under real conditions (first pass: 20 tweets logged; second pass: dedup confirmed, 0 new). Cron added (`30 6 * * *`). — `goplus-security-watch/run.sh`, `goplus-security-watch/fetch_tweets.py`, `research-loop/prompt.txt` (all outside the public repo, VPS-only).

------------------------------------------------------------

[DEPLOYE] Sujet    : Backup DB — échec silencieux 2 jours de suite, jamais détecté
Date : 2026.08.14 / Probleme : backlog #41 (issu de la revue Avocat du Diable du brainstorming multi-agents du 14/08) — le backup quotidien (`/opt/aria-data/db-backup/run.sh`, cron `10 4 * * *`) a réellement échoué le 11/08 et le 12/08 (`sqlite3: Error: database is locked`), laissant des fichiers `.db` à 0 octet, et `set -euo pipefail` a tué le script AU PREMIER échec (aria.db) sans jamais tenter auth.db/dexpulse.db, sans écrire de ligne dans `backup.log`, sans alerter personne — confirmé en relisant l'historique du log (les 11/08 et 12/08 sont absents des lignes "backup ok"). Personne ne l'a su jusqu'à cette lecture.
Solution : 3 couches, recommandation directe de la revue Fable 5 : (1) chaque DB traité indépendamment dans la boucle — l'échec d'un DB n'empêche plus les autres d'être tentés ; (2) vérification post-backup avant de faire confiance au fichier produit (taille > 0 ET `PRAGMA integrity_check` == "ok") — un backup non-vide mais corrompu est désormais aussi détecté qu'un fichier vide ; (3) toute erreur écrit une ligne explicite dans `backup.log` ET déclenche une alerte Telegram immédiate (pas de garde anti-flapping — cron quotidien, chaque jour raté est un risque cumulatif réel, même pattern que `outgoing-pause-watch`). Testé en conditions réelles (pas juste `bash -n`) : exécution manuelle confirmée, 3 DB backupés+vérifiés avec succès, ligne "backup ok: aria auth dexpulse" écrite. `/opt/aria-data/db-backup/run.sh` (hors repo public, même statut que les autres crons VPS). Non fait dans ce passage (backlog N1 de la revue Fable 5, distinct) : test de restauration périodique du backup — un backup jamais restauré n'est pas prouvé restaurable.

------------------------------------------------------------

[DEPLOYE] Sujet    : Veille continue automatisée "VPS Research" — conception et premier test réel
Date : 2026.07.18  /  Probleme : décision opérateur explicite ("h24 7/7, jamais deux fois la même, 50 idées/jour, tu les ajoutes au plan") de mettre en place une veille de recherche continue, indépendante de toute session/app ouverte.
Solution : cron VPS natif (initialement `0 */3 * * *`, `/opt/aria-data/research-loop/run.sh`) — `claude -p` headless, outils bridés en dur (`--allowedTools "WebSearch WebFetch Read Write"` + `--disallowedTools "Bash Edit Agent Task"`, aucune commande exécutable même sur injection de prompt). Cadence de démarrage volontairement prudente (3h, pas 2h) car ce VPS partage le même quota MAX 5x que le reste de l'ingénierie — le texte prévoyait déjà de la monter "une fois l'impact réel observé sur 1-2 jours". Premier test réel : 7 entrées propres en ~3 min, 0 outil interdit invoqué, dédoublonnage validé en conditions réelles (une piste déjà connue correctement écartée). Distinction actée avec l'opérateur (discussion croisée Gemini) entre Research (scout, pensée divergente, aucun ancrage code) et l'Avocat du Diable (critique convergente, bornée au diff — cf. entrée suivante) : les deux rôles ne fusionnent jamais dans le même agent.

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Cadence veille Research relevée 3h→2h, jamais mise à jour dans CLAUDE.md
Date : 2026.08.03  /  Probleme : le crontab réel (`crontab -l`) tournait déjà à `0 */2 * * *` (2h, 12 passages/jour) alors que CLAUDE.md documentait encore "toutes les 3h (8 passages/jour)" avec un texte justificatif qui se contredisait lui-même ("cadence de démarrage volontairement prudente, pas 2h" alors que 2h était déjà la cadence réelle) — dérive de doc jamais rattrapée après un relevage de cadence.
Solution : `crontab -l` revérifié en direct le 03/08 (passe de compaction CLAUDE.md, étape 8/9) — confirmé `0 */2 * * *`. CLAUDE.md corrigé pour refléter la cadence réelle. Réflexe à graver : toute modification de cadence cron doit être accompagnée, dans le même geste, d'une mise à jour du texte CLAUDE.md qui la documente — sinon la doc dérive silencieusement comme ici.

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Procédure réutilisable — audit workflow avant de compacter une section sensible de CLAUDE.md
Date : 2026.08.03  /  Probleme : —
Solution : lors de la passe de compaction du 03/08, une première tentative manuelle de compresser la section "Règles absolues" a silencieusement perdu 12 éléments substantiels (états par défaut de gates, verrous `test_coherence`, bornes chiffrées, une clause de sécurité sur une adresse codée en dur, un jalon futur) — une seule relecture humaine ne les a pas tous vus non plus avant qu'un workflow dédié les trouve. Procédure qui a fonctionné, réutilisable telle quelle pour toute future compaction d'une section sensible (garde-fous, capital réel) :
1. Un premier agent (`fetch-original`) lit le fichier réel et retourne le texte ORIGINAL verbatim de la section concernée — jamais de mémoire/résumé.
2. Un deuxième agent (`diff-audit`) reçoit l'ORIGINAL + la version PROPOSÉE dans son prompt, et audite élément par élément (chaque gate, chaque borne chiffrée, chaque nom de fichier/fonction cité) — consigne explicite : lister toute perte réelle avec citation des deux côtés, ou dire "aucune perte" si c'est le cas ; terminer par un verdict tranché SÛR À COMMITTER / CORRECTIONS NÉCESSAIRES.
3. Un troisième agent (`pointer-check`) vérifie que chaque nouveau pointeur `docs/HANDOFF_*.md` introduit par la compaction résout vers du contenu réel (lit les fichiers cibles), et propose une entrée HANDOFF prête à coller pour tout ce qui manque.
Coût : 2 agents pour l'audit + 1 pour le pointer-check (respecte le plafond de 2 agents/workflow en les regroupant en 2 phases, `Diff-audit`/`Pointer-check`, chacune sous ce plafond). Sur les points où l'audit trouve un vrai choix (pas juste une perte accidentelle) — ex. le périmètre exact d'une exception de gouvernance, ou un déclencheur qui contredisait une autre règle du texte source — l'agent doit explicitement dire "à faire valider par l'opérateur", jamais trancher seul en silence.

------------------------------------------------------------

[DEPLOYE] Sujet    : "Avocat du Diable" — critique architecturale post-push, conception et premier test
Date : 2026.07.18  /  Probleme : feu vert opérateur direct (après conception croisée avec Gemini) pour un critique automatique de chaque push sur `main`, distinct de la veille Research (pensée convergente, bornée au diff, pas de pivot).
Solution : hook `.git/hooks/pre-push` (stub non versionné) appelle `scripts/devils-advocate-review.sh` (versionné), envoie le diff en arrière-plan détaché à DeepSeek R1 via OpenRouter (modèle/lab différent de celui qui écrit le code), reçoit aussi une carte légère de `docs/aria-learning-inbox/` (noms de fichiers seulement). Rapport écrasé à chaque push (`/opt/aria-data/architect-report.md`, hors repo public), log technique séparé append-only. Échec de génération → marqueur `[ÉCHEC DE GÉNÉRATION DU RAPPORT]` explicite, jamais un contenu halluciné silencieux. Agent "Architecte" (relecture de plan avant codage) volontairement pas construit — tester d'abord la valeur du seul critique post-push. Modèle migré vers Gemini 3.1 Pro le 02/08 (panne de facturation DeepSeek, voir `docs/HANDOFF_LLM.md`), puis vers Claude Fable 5 le 04/08 (entrée suivante) — cette entrée reste la trace du design original, jamais réécrite.

------------------------------------------------------------

[DEPLOYE] Sujet    : Avocat du Diable — file pending/archived (fin du fichier unique écrasé) + modèle officialisé sur Claude Fable 5 + check synchrone pré-commit
Date : 2026.08.04 / Probleme : opérateur a repéré des appels payés répétés (~0,08$/push) jamais suivis d'une lecture réelle ("je paye pour rien"). Diagnostic : le rapport unique écrasé à chaque push perdait silencieusement tout rapport intermédiaire sur deux pushs rapprochés, et le rappel de lecture ne suivait que le dernier état — gap confirmé indépendamment par Gemini ET Claude Fable 5 sur le même diff de test (comparaison directe demandée par l'opérateur). CLAUDE.md/ce fichier disaient aussi encore "DeepSeek R1" alors que le modèle réel en prod était déjà Gemini 3.1 Pro depuis le 02/08 (migration jamais documentée durablement, déjà signalé non-corroboré par un audit externe le 02/08, jamais corrigé depuis).
Solution : (1) `scripts/devils-advocate-lib.sh` (nouveau) — modèle/prompt/appel API extraits, partagés entre le hook async et un nouveau `scripts/devils-advocate-precommit.sh` (vérification SYNCHRONE, demande opérateur explicite : capter la critique AVANT un commit officiel, réponse relayée à l'opérateur dans la conversation, jamais un fichier à lire "plus tard"). (2) Rapports écrits en file `/opt/aria-data/architect-reports/pending/<sha>.md` (un fichier par push, jamais écrasé) au lieu d'un fichier unique ; "lu" = déplacement explicite vers `archived/`. (3) `.claude/hooks/session-checkpoint.sh` : rappel qui liste tout ce qui reste en attente, se répète sur changement de liste OU tous les 150 messages (backstop périodique — un throttle purement "liste changée" ne se répète jamais si la session ignore le rappel, gap trouvé par Fable 5 sur ce diff lui-même le même jour). (4) Modèle officialisé sur `anthropic/claude-fable-5` (décision opérateur explicite, casse délibérément la doctrine cross-lab d'origine après une comparaison directe où Fable 5 a trouvé le même angle mort que Gemini sans son erreur factuelle + une meilleure proposition d'architecture) — payload corrigé en conséquence (max_tokens 96000 + `reasoning.effort=medium`, sinon réponse vide garantie sur un diff long, leçon déjà connue de `consult-gemini.sh`). Coût accepté explicitement à condition de batcher les push (jamais un push isolé par petit correctif). Backlog noté, pas construit : déduplication par hash de diff entre le check précommit et le hook post-push (évite de payer deux fois pour un cycle vérif→correction→commit→push), modèle de repli si Fable 5 indisponible sur OpenRouter — `scripts/devils-advocate-lib.sh`/`devils-advocate-review.sh`/`devils-advocate-precommit.sh`/`.claude/hooks/session-checkpoint.sh`, testé en conditions réelles (pipeline bout-en-bout vérifié, HTTP 200, comparaison Gemini/Fable5 sur diff réel).

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Mécanisme de "Routine" de l'app desktop ne peut pas atteindre le filesystem du VPS
Date : 2026.07.18  /  Probleme : deux tentatives (promotion backlog, watchdog paper-trading) via le mécanisme de tâche planifiée de l'app desktop échouaient systématiquement dès que le dossier ciblé était `/opt/aria` — confirmé par test opérateur, fonctionne seulement sur un dossier local du PC.
Solution : les deux routines ont été supprimées, remplacées par du cron VPS natif (indépendant de toute app/PC ouvert). Ce mécanisme desktop reste utilisable uniquement pour des tâches sans besoin d'accès filesystem VPS (pur appel HTTPS vers une API déjà déployée) — ne jamais lui donner `/opt/aria` comme dossier cible.

------------------------------------------------------------

[DEPLOYE] Sujet    : Promotion backlog automatisée — conception et premier test réel
Date : 2026.07.18  /  Probleme : `research-log.md` grossit en continu (cron veille ci-dessus) sans mécanisme pour promouvoir les trouvailles actionnables vers le backlog CLAUDE.md/aria-learning-inbox.
Solution : `scripts/research-log-promotion.sh`, cron VPS quotidien (9h UTC) — relit le journal depuis un marqueur de progression, juge chaque entrée avec un vrai esprit critique (vérifie les faits douteux par WebSearch), outils `Read Write Edit WebSearch WebFetch` + `Bash(git *)` uniquement (jamais de shell arbitraire). Premier test réel (15h47-15h54 UTC) : 4 items promus + 1 fiche, bien sourcés. **Incident réel observé et corrigé** : la session a décrit un de ses propres commits antérieurs comme une "exécution concurrente" — aucune preuve d'un second processus, explication la plus probable : perte du fil de ses propres actions après une compression de contexte interne (le prompt exige de lire l'intégralité de CLAUDE.md, potentiellement deux fois). A mené à une contradiction mineure (un item écarté puis re-promu dans la même session), résultat final correct. Prompt corrigé pour vérifier `git log` soi-même avant de conclure à une interférence externe.

------------------------------------------------------------

[DEPLOYE] Sujet    : Watchdog paper-trading 1M$ — conception et premier test réel
Date : 2026.07.18  /  Probleme : le test paper-trading 1M$ tourne en continu sans qu'aucune session ne surveille son état entre deux reprises de fil.
Solution : `/opt/aria-data/paper-watchdog/run.sh`, cron VPS toutes les 3h (décalé de 30min de la veille Research pour ne pas cogner sur le même quota). Destiné aux sessions Claude Code, pas à l'opérateur — aucune notification Telegram. Calcule équité/P&L approximatifs, signale toute position ouverte >24h sans thèse ou clôturée <24h sans notes de clôture, refuse de conclure sur la qualité du trading sous 20 trades clôturés. Écrit en append-only dans `watchdog-log.md`. Premier test réel : portefeuille encore vide, cohérent avec un cycle relancé ~3h30 plus tôt.

------------------------------------------------------------

[DEPLOYE] Sujet    : Surveillance santé logs production — conception et calibration
Date : 2026.08.03  /  Probleme : demande opérateur explicite après une vidéo sur les tests automatisés — la CI vérifie le CODE, `/api/health`/autoheal est une pure sonde de vivacité HTTP sans conscience des tâches heartbeat (angle mort confirmé par un workflow de validation).
Solution : `/opt/aria-data/log-health-watch/run.sh`, cron VPS horaire — scanne les vrais logs du conteneur `aria-api` depuis le dernier checkpoint, cherche `Traceback`/`CRITICAL` uniquement (délibérément pas un grep générique "error" : mesuré en direct sur 6h de logs réels, 19 occurrences génériques, toutes du bruit). Vérifie aussi `architect-review.log` (les 3 dernières tentatives du hook Avocat du Diable en échec = anomalie) — c'est ce 2e check qui aurait détecté une vraie panne de 30h de ce hook (compte OpenRouter à sec, corrigée le 02/08), invisible à un simple scan de `docker logs`. Notification Telegram réservée à l'anomalie. Validé par un workflow de conception : pas de LLM, pas de dashboard à niveaux — un projet à un seul opérateur avec un seul canal d'alerte n'a pas de lecteur pour un tableau de bord dédié.

------------------------------------------------------------

[DEPLOYE] Sujet    : Logs permanents des circuit breakers externes + alerte Telegram sur panne soutenue
Date : 2026.08.04  /  Probleme : demande opérateur explicite après la perte des logs Docker bruts (stdout/stderr, effacés au recyclage du conteneur) à un redéploiement ("si sa peut ameliorer le suivie oui implemente les log permanent") — aucun historique persistant de l'état ouvert/fermé des circuit breakers externes, ni d'alerte quand une API reste vraiment cassée.
Solution : cartographie complète des 16 fichiers `services/*.py` (Agent Explore) : seuls 5 ont un VRAI état ouvert/fermé avec cooldown bloquant — `blockscout.py` (par chaîne), `dexscreener.py`, `goplus.py` (2 mécanismes distincts : circuit normal code 4029, cooldown auth code 4012), `wallet_transfers_fast.py` (alchemy/moralis), et `momentum_entry._record_provider_outcome` (cascade OHLCV, 6 providers) — 12 états trackés au total. Les 12 autres fichiers (compteur sans cooldown ou sans seuil) restent hors périmètre, honnêtement absents plutôt que faussement "fermés". Vérifié ligne par ligne contre le code réel (revue croisée Fable, deux corrections actées : l'étiquette 4029/4012 était inversée dans le premier jet ; `_record_provider_outcome` manquait au périmètre initial). Nouveau module `circuit_breaker_log.py` (table `circuit_breaker_event` append-only, écriture fire-and-forget via `asyncio.create_task` — jamais bloquant pour l'appel réseau réel) + `circuit_breaker_status.py` (agrège l'état mémoire live des 5 modules + dernier événement DB + `sustained_outage` = ouvert ET rouvert ≥2x dans la dernière heure). Endpoint `/api/aria/diagnostics/circuit-breakers` (même gate `ARIA_DIAGNOSTIC_TOKEN` que les autres diagnostics). Cron `/opt/aria-data/circuit-breaker-watch/run.sh` (hors repo, même patron que `log-health-watch`) — alerte Telegram réservée à `sustained_outage`, jamais un cooldown isolé normal. Fixture `conftest.py` ajoutée (`_drain_circuit_breaker_background_tasks`, autouse) pour éviter qu'une tâche fire-and-forget en vol ne fuite après la fermeture de la event loop d'un test — corrige un warning qui serait sinon apparu dans des dizaines de tests pré-existants sur ces 5 fichiers, pas seulement les nouveaux. Suite complète verte (9069 passed) avant déploiement. Déployé (commit e4a181ec), endpoint vérifié en direct par curl HTTPS réel (pas seulement le texte de sortie de `deploy.sh`) -- piège trouvé au passage : le backend vit sur le sous-domaine dédié `api.ariavanguardzhc.com`, pas `ariavanguardzhc.com/api/...` (confondu une première fois, `/api/aria/holding` et `agent-wallet-ledger` retournaient aussi le SPA sur le mauvais domaine -- pas une régression de ce déploiement, juste une mauvaise URL de test). `/opt/aria-data/circuit-breaker-watch/run.sh` (cron horaire `45 * * * *`, décalé de `log-health-watch` à `15 * * * *`) installé et testé manuellement -- premier passage propre, aucun breaker ouvert.

------------------------------------------------------------

[DEPLOYE] Sujet    : Surveillance mémoire/swap VPS + alerte Telegram sur seuil combiné
Date : 2026.08.05  /  Probleme : demande opérateur explicite après un incident réel observé en session (swap monté à 65%, cause = plusieurs processus ccd-cli orphelins tournant sans connexion réseau active vers Anthropic) — aucune alerte n'existait pour signaler une dérive mémoire/swap avant qu'elle devienne critique.
Solution : `/opt/aria-data/memory-watch/run.sh`, cron VPS toutes les 15 min (`5,20,35,50 * * * *`, distinct des minutes déjà prises par log-health-watch/circuit-breaker-watch). Métrique combinée (RAM utilisée + swap utilisé) / (RAM totale + swap totale) plutôt que RAM seule — le swap peut monter fort même quand la RAM affichée semble correcte, exactement le cas observé (RAM 62%, swap 65%). Alerte au franchissement de 75%, hystérésis à 65% pour ne jamais re-notifier tant que ça reste au-dessus (même doctrine anti-bruit que log-health-watch/circuit-breaker-watch). Message inclut les 5 processus les plus gourmands en mémoire + un rappel de vérifier les sessions ccd-cli orphelines avant toute libération manuelle. Testé en conditions réelles (seuil temporairement abaissé à 1%, message Telegram reçu confirmé, remis à 75% ensuite).

------------------------------------------------------------

[CONFIG] Subject : rtk (Rust Token Killer) installed on the VPS -- Bash output compression for Claude Code sessions
Date : 2026.08.05 / Problem : operator watched a video mentioning Claude-token-saving tools (initially misattributed to a GitHub profile, real project identified from a screenshot: headroom, then rtk surfaced alongside). Session Bash outputs (9000+-test pytest runs, docker logs, ls) are a large share of the MAX 5x quota burn.
Solution : rtk v0.44.2 (rtk-ai/rtk, Apache-2.0, 74.9k stars, JetBrains-benchmarked) installed to /usr/local/bin/rtk from the official GitHub release binary (sha256 checksum verified; strace showed zero internet calls; telemetry opt-in/off by default, GDPR-explicit). Quality validated before adoption: pytest -72% with failures fully preserved (+ full log kept in ~/.local/share/rtk/tee/), docker ps -63%, ls -48%, git diff --stat intact, already-compact outputs untouched. MANUAL prefix discipline only (rtk init --global hook deliberately NOT installed until a few days of real use prove zero information loss). headroom (65k stars, proxy/wrap/MCP) evaluated too -- banked, oversized for our use (15-20% gain on coding agents for much more integration surface). Usage note in CLAUDE.md "Token economy" section.

------------------------------------------------------------

[CONFIG] Subject : sqlite3 CLI + ripgrep + osv-scanner installed on the VPS (session-efficiency + continuous security tooling)
Date : 2026.08.05 / Problem : follow-up to the rtk adoption same day -- session-level inefficiencies (prod DB queried via verbose inline python3 -c through docker exec; plain grep walking .venv/node_modules) and no way to re-check the freshly pinned lockfiles against known CVEs over time.
Solution : sqlite3 3.46.1 + ripgrep 14.1.1 (official Ubuntu repos) and osv-scanner v2.4.0 (Google release binary, sha256 verified) in /usr/local/bin. First real scan: 980 packages across the 3 lockfiles (requirements-lock.txt 132, product-frontend 206, vanguard 642) -- zero known vulnerabilities, retroactively validating the versions pinned earlier today. All three are PASSIVE on-demand tools (no daemon/hook/cron) -- operator explicitly cautioned against stacking too much at once, so the weekly osv-scanner cron + Telegram alert is deliberately NOT created yet (separate decision once the tool has proven itself). Discipline: sqlite3 always with -readonly on the prod DB.

------------------------------------------------------------

[CODE] Subject : daily DB backup cron + CodeQL + Dependabot alerts (delegation push, operator request)
Date : 2026.08.05 / Problem : operator asked what else could be automated/delegated to focus on ARIA's trading. Audit found THE critical gap: the trading DB (the whole track record, paper_position_archive 136 rows) lived on ONE disk with zero automated backup (only 3 stale manual copies from migrations). Also missing: static analysis of our own code (CI covered deps/secrets/runtime but not code flaw classes), and CVE alerting on the freshly pinned lockfiles.
Solution : (1) /opt/aria-data/db-backup/run.sh, daily cron 04:10 UTC (quiet slot, no collision) -- sqlite3 online .backup (WAL-safe, never locks prod) of aria/auth/dexpulse, gzip (~11 MB/day), 14-day rotation; restore INTEGRITY-TESTED for real (integrity_check ok + 136 archive rows readable from the restored copy). Off-VPS externalization = separate operator-gated decision (needs external storage account). (2) .github/workflows/codeql.yml -- python + javascript-typescript, on push to main (code paths) + weekly sweep, runs on GitHub's servers, purely additive (Security tab alerts, never blocking). (3) .github/dependabot.yml -- security ALERTS only (open-pull-requests-limit 0 kills routine bump PRs), the watch layer that makes lockfile pinning livable. NOTE: (2)+(3) only take effect once pushed to GitHub (still batched under the 2000-line push rule).

------------------------------------------------------------

[CODE] Subject : external uptime watch via GitHub Actions (4th delegation item, operator "ok pour les 4")
Date : 2026.08.05 / Problem : structural blind spot -- every monitor (log-health, memory-watch, watchdog, autoheal) runs ON the VPS: a full-VPS outage alerts no one at all.
Solution : .github/workflows/uptime-watch.yml -- GitHub-side probe every ~15 min (schedule drift acceptable at this granularity) on the showcase + /api/health, 3 attempts 30s apart inside one run (blips never alert), ONE deduped labeled issue on sustained failure (GitHub emails the operator natively), auto-close with comment on recovery. Zero external account, zero VPS load. Effective only once pushed (batched under the 2000-line rule, same as CodeQL/Dependabot).

------------------------------------------------------------

[CONFIG] Subject : log-health-watch false positive on Devil's Advocate skip lines (batch-push threshold interaction)
Date : 2026.08.07 / Problem : Telegram alert fired at 21:15:01Z claiming "the last 3 attempts of the Devil's Advocate hook all failed (no HTTP 200)". The check only looked for "HTTP 200" among the last ARCHITECT_TAIL_N=3 raw lines of architect-review.log, with no distinction between a real failed API call and a "SKIP sous le seuil" line (no call attempted at all, normal behavior under the 2000-line batch-push threshold). Three consecutive small pushes under threshold produced 3 SKIP lines in a row, tripping the false alarm -- the last REAL attempt (2026-08-07T19:18:25Z) was in fact HTTP 200.
Solution : /opt/aria-data/log-health-watch/run.sh (outside the public repo, no commit) -- filter out "SKIP sous le seuil" lines with grep -v before taking the last N lines, so only real call attempts count toward the failure check. Verified against the real log file before deploying: TOTAL=3, OK=3, ARCHITECT_ALL_FAILING=false.

------------------------------------------------------------

[CONFIG] Subject : signal-cascade-watch -- permanent cron replacing manually re-armed session Monitor calls
Date : 2026.08.09 / Problem : during the signal cascade build (GitHub/Farcaster/web/X columns, see docs/HANDOFF_SIGNAL_CASCADE.md), cycle-pass and triage-queue-convergence visibility was covered by session-local `Monitor` calls (DB polling loops) -- these have a hard 1h timeout and had to be manually relaunched by the operator's own request ("relance la surveillance de logs aussi") every time they expired, with no coverage between sessions at all.
Solution : /opt/aria-data/signal-cascade-watch/run.sh (outside the public repo, no commit) -- bash-only, no LLM call (the detection itself is mechanical: compare current DB state to a persisted state file, log only on a real change). VPS cron every 15 min, same interval as the fastest source columns. Two things tracked, state files never touching the app DB (`cycle-state.tsv`, `queue-state.tsv`, both under the watchdog's own directory): (1) `MAX(last_evaluated_at)` per source-column watchlist table -- a cycle pass; (2) `signal_cascade_triage_queue` rows with `status='pending'` -- a new candidate entering, or `convergence_count` rising on one already queued (the actual trigger for a Claude Code triage decision). Silent on an unchanged state (verified manually: first run logs the baseline, immediate second run logs zero events). No Telegram -- same doctrine as paper-watchdog: a triage decision needs real reasoning a cron cannot produce, so this stays a session-facing log (`watch-log.md`), never an operator interruption.

------------------------------------------------------------

[DEPLOYE] Sujet : Avocat du Diable -- bascule OpenRouter vers l'API Anthropic directe, condensation Haiku au lieu de troncature, suivi de cout reel
Date : 2026.08.10 / Probleme : credits OpenRouter epuises le 08-09/08, 3 revues reelles mortes en silence (HTTP 402) sans que l'operateur ou une session ne le remarque avant relecture explicite du journal -- meme classe d'incident deja vecue avec le budget X402. En parallele, la troncature brute a 60000 caracteres perdait silencieusement la fin de tout diff au-dela de ~2000-2500 lignes, et le seuil de batching (2000 lignes) ne rendait aucun compte du cout reel par appel (l'operateur demandait explicitement un suivi).
Solution : `scripts/devils-advocate-lib.sh`/`devils-advocate-review.sh`/`devils-advocate-precommit.sh` -- (1) appel direct `api.anthropic.com/v1/messages` (`x-api-key`, modele `claude-fable-5` sans prefixe OpenRouter), premier test reel a revele que Fable 5 exige `thinking.type: adaptive` + `output_config.effort` (pas le format extended-thinking classique -- corrige apres une vraie erreur HTTP 400, jamais suppose) ; (2) `devils_advocate_diff_for_review()` condense via Claude Haiku 4.5 (structure/fichiers/logique, jamais un resume qui saute une section) tout diff depassant 60000 caracteres au lieu de le tronquer -- le rapport couvre desormais TOUJOURS le diff entier ; (3) `devils_advocate_cost()`/`devils_advocate_log_cost()` -- tarifs verifies en direct (`platform.claude.com/docs/en/about-claude/pricing`, jamais devines : Fable 5 $10/$50 par MTok in/out, Haiku 4.5 $1/$5), chaque appel (revue, precommit, condensation) journalise dans `/opt/aria-data/architect-reports/cost-log.csv` ; (4) seuil de batching releve 2000->8000 lignes (decision operateur, affine en direct : d'abord propose a 10000, mais une cible de condensation FIXE (20000 chars) aurait fait grimper le ratio de compression sans limite avec la taille -- `devils_advocate_condense_target_chars()` scale desormais la cible avec la taille reelle du diff (1/8e, plancher 15000, plafond 45000), et le seuil est redescendu a 8000 pour rester dans une plage ou le ratio ne depasse jamais ~12:1). Les 3 rapports 402 regeneres avec le VRAI diff d'origine (memes plages de commit, jamais reconstruit de memoire) : `c68b63c4c968`, `ac99a0dd112d`, `bc1099777869` dans `architect-reports/archived/`, cout reel du backfill $1.22.
Bonus reel : le premier appel de la migration a lui-meme trouve un vrai bug (race condition perte d'increment dans `single_row_state.SingleRowStore` sous appels concurrents, confirme deux fois independamment) -- corrige le meme jour via `SingleRowStore.mutate()` (transaction `BEGIN IMMEDIATE` atomique), test de concurrence dedie (`test_single_row_state.py`, 20 appels simultanes, zero increment perdu).
Auto-critique honnete (le mecanisme s'est teste sur son propre diff de migration) : la condensation reste un resume AVEC PERTE -- une passe suivante, la revue de rapports a trouve elle-meme un angle mort reel (le seuil de batching et la qualite de condensation "se renforcent negativement" si la cible etait restee fixe, corrige ci-dessus) et propose une alternative plus fidele jamais construite (map-reduce par fichier : chaque chunk brut relu par un modele avant toute synthese, jamais un resume de resume) -- bancable si la condensation s'avere insuffisante en usage reel, pas construite maintenant (cout/complexite pas justifies sans signal concret d'echec).
Filet mecanique ajoute le meme jour (demande operateur "tu as pas peur que Haiku zappe des trous importants ?") : la liste des fichiers touches est extraite du diff brut par un simple grep sur les en-tetes `diff --git` (jamais un modele) et accolee au resume Haiku, avec instruction explicite a Fable 5 de signaler tout fichier absent du resume -- couvre l'omission COMPLETE d'un fichier (le cas le plus grave, verifiable a cout nul), pas la perte de detail a l'interieur d'un fichier present (risque reel, non couvert, reste dans le map-reduce banque ci-dessus).
Test comparatif Haiku seul (meme jour, meme diff, meme prompt de critique que Fable 5, $0.03) : Haiku produit une critique structurellement comparable (memes 4 sections, points pertinents) mais commet une vraie erreur factuelle absente des rapports Fable 5 (confond le budget de sortie max_tokens avec une limite de contexte d'entree, alors que les modeles Claude recents supportent jusqu'a 1M tokens de contexte) -- confirme la repartition des roles : Haiku pour l'extraction factuelle (condensation), jamais pour le jugement final.

------------------------------------------------------------

[CODE] Sujet    : Avocat du Diable — decoupage par tranches, le signal concret d'echec banque plus haut ("map-reduce par fichier, pas construit maintenant") est arrive
Date : 2026.08.10  /  Probleme : le rapport genere sur le commit 8e01e6fb a lui-meme trouve un vrai gap sur son propre pipeline -- la condensation Haiku a echoue (HTTP 400, fenetre de contexte 200k tokens depassee par un diff cumule de 720341 caracteres), et le repli de secours tronquait au plafond FIXE existant (45000 car., cf. entree ci-dessus), donc Fable 5 n'a reellement vu que ~6% du diff -- contredit silencieusement la doctrine "jamais tronque" (vrai pour le cas nominal, faux pour le cas d'echec).
Solution : root cause verifiee via WebSearch (jamais suppose) avant tout correctif. `devils_advocate_split_diff_by_file()` decoupe le diff sur les frontieres de fichiers (jamais un fichier coupe en deux, bin-packing par fichier COMPLET -- un bug d'estimation trouve et corrige en test local gratuit avant tout usage reel, cf. commentaire du commit) ; `devils_advocate_condense_chunk()` condense chaque tranche independamment avec 1 retry, marqueur explicite `[TRANCHE N NON CONDENSEE]` + liste de fichiers (grep mecanique) si une tranche echoue quand meme ; couverture reelle (tranches reussies/total) ecrite dans `DA_COVERAGE_NOTE`, affichee dans le header du rapport de facon MECANIQUE (jamais dependante du texte libre de Fable 5) — `scripts/devils-advocate-lib.sh`/`devils-advocate-review.sh` (6935fc5d). Rapport 8e01e6fb archive apres traitement. Test reel de bout en bout fait le soir meme (script isole appelant directement `devils_advocate_diff_for_review()` avec de vrais appels Haiku, jamais via un faux push -- la cle API lue par le script lui-meme, jamais par une commande Bash de la session, cf. garde-fou "jamais toucher un .env via Bash") : diff synthetique de 2.4M caracteres (~3.3x le pire cas reel connu), ~20 tranches condensees avec succes, cout reel $0.20. Ce test a lui-meme trouve un second bug reel : `DA_COVERAGE_NOTE` etait une variable globale fixee DANS la fonction, mais les deux vrais appelants invoquent toujours cette fonction via `$(...)` (substitution de commande = sous-shell) -- toute variable fixee a l'interieur y reste enfermee, jamais remontee. Le mecanisme de couverture du commit precedent n'aurait donc jamais fonctionne en prod. Corrige en ecrivant la couverture dans un fichier temporaire fourni par l'appelant (seul canal qui survit a un sous-shell), verifie gratuitement (reseau mocke) — `scripts/devils-advocate-lib.sh`/`devils-advocate-review.sh`/`devils-advocate-precommit.sh` (3680d1c0).

------------------------------------------------------------

[DEPLOYE] Subject : VC reports/decisions watchdog (backlog #91)
Date : 2026.08.11 / Problem : operator wants permanent system-coherence oversight ("un intermediaire qui sont seule job c'est de tout verifier aleatoirement... un truc a etait oublier et sa tourne pour rien") -- nothing was watching whether the VC cycles (crawl/radar_x/thesis_review/resolve/weekly_forecast/self_report) stayed alive over time; a silently broken cycle could go unnoticed for weeks.
Solution : `/opt/aria-data/vc-watch/run.sh`, same pattern as `signal-cascade-watch`/`memory-watch` (pure bash, hourly VPS cron, never an LLM call, append-only log, meant for Claude Code sessions, no Telegram). Source of truth : `/opt/aria-data/heartbeat_state.json` (`last_runs`), the SAME mechanism `heartbeat.py` itself uses internally to decide whether a task is due -- a real trap avoided while building it : `vc_operator_last` (misleadingly named SQL table) is NOT written by `vc_self_report`; a first version built on it would have produced a false alarm. Tolerance 2x the nominal cadence (absorbs jitter/short pauses), hysteresis (one alert per anomaly, auto-cleared once the cycle resumes). Tested under real conditions (simulated state with a 6-week-old timestamp) : detection + anti-spam on the 2nd pass confirmed before any cron deployment. Crontab line added (`0 * * * *`). `/opt/aria-data/vc-watch/run.sh` (outside the public repo, like the other watchdogs).

------------------------------------------------------------

[DEPLOYE] Subject : centralized `system_issues` registry, GitHub-Issues style (explicit operator request)
Date : 2026.08.11 / Problem : direct operator request ("je veut que se soit comme github si il y a un probleme tu a des notification dans une fichier et cest a toi de les fermer ou de les reparer") -- before this, every watchdog (paper-watchdog/signal-cascade-watch/memory-watch/log-health-watch/circuit-breaker-watch/v8-watch/vc-watch) wrote its findings into its OWN markdown, under its OWN directory, with no centralized view or automatic reminder -- only the signal cascade already had a dedicated SessionStart hook (`signal-cascade-queue-reminder.sh`).
Solution : generalizes EXACTLY that same already-proven pattern. `aria_core/system_issues.py` (new `system_issues` table : source/title/detail/severity/status/dedup_key/opened_at/closed_at/closed_reason) -- `open_issue()` dedups via `dedup_key` (a watchdog re-detecting the SAME ongoing anomaly never spams a new row, same hysteresis as `vc-watch/alert-state.tsv`), `close_issue()` requires a non-empty reason (same doctrine as `signal_cascade_convergence.record_triage_decision` : a close is a decision with real reasoning, never a bare status flip). `.claude/hooks/system-issues-reminder.sh` (SessionStart, same structure as `signal-cascade-queue-reminder.sh`) injects the OPEN issues (top 10, most critical first) at the start of EVERY session -- the session is responsible for closing them (false positive/already resolved) or fixing the real problem then closing with the commit as the reason. Bash writes directly via `sqlite3` (no Python dependency for a mechanical watchdog) -- `vc-watch/run.sh` wired as the FIRST pilot producer (opens a `warning` issue on abnormal silence, auto-closes it once the cadence returns to normal). Tested end-to-end under REAL conditions (real `/opt/aria-data/aria.db`, not just unit tests) before wiring : open -> hook surfaces it -> simulated state refreshed -> auto-close confirmed. Real trap hit while testing : `aria_db_path()` defaults to `/opt/aria/data/aria.db` (dev path), never the real prod DB without an explicit `configure_data_dir()` -- already known (session memory), reconfirmed in practice.
`aria_core/system_issues.py` (new, 15 tests)/`.claude/hooks/system-issues-reminder.sh` (new)/`.claude/settings.json` (hook registered)/`/opt/aria-data/vc-watch/run.sh` (outside the public repo, first producer). Full suite green (9934 passed, 17 skipped).

------------------------------------------------------------

[CODE] Subject : system_issues.py had zero DB-failure protection, contradicting its own "never blocking" purpose (backlog #106, robustness-test axis)
Date : 2026.08.11 / Problem : a robustness audit (backlog #106, 3rd axis of the same 4-axis test-expansion mandate as #104/#105) found this same-day module had NO try/except around any of its 4 public functions' `aiosqlite` calls -- multiple bash watchdogs (vc-watch and future producers) plus Python callers can write to `system_issues` concurrently, and a genuine failure (locked/corrupt DB, disk full) would have raised straight into whichever watchdog or session called it, exactly what this module exists to prevent for everyone else.
Solution : wrapped each of `open_issue`/`close_issue`/`list_open`/`count_open` in its own try/except, matching the existing return-type contract instead of inventing new sentinels : `open_issue` returns `-1` (never a real id) on failure, `close_issue`/`list_open`/`count_open` return their already-existing "nothing happened" values (`False`/`[]`/`0`). 5 new tests in `test_system_issues.py` -- 4 simulate a real `aiosqlite.connect` failure (same `_broken_connect` pattern already proven in `test_candle_history.py`, never a mocked business exception) and verify red-then-green against the pre-fix code, plus a 5th confirming a transient failure never leaves the module permanently degraded (the next call on a healthy DB works normally). Full suite green (9944 passed, 17 skipped). — `system_issues.py`, `test_system_issues.py` (+5 tests).

------------------------------------------------------------

[CODE] Subject : circuit-breaker "all closed by default" test flaky under the full suite only -- inter-test state leak found and fixed
Date : 2026.08.13 / Problem : operator asked to investigate why `test_circuit_breaker_status.py::test_get_circuit_status_covers_all_12_tracked_states_closed_by_default` failed only when the FULL suite ran, never in isolation or in small combinations -- a real pollution of shared in-process state between test files, not a functional bug. Traced to `test_circuit_breaker_status.py`'s own local `_reset_breaker_state` autouse fixture, which reset dexscreener/goplus/wallet_transfers_fast/momentum_entry's module-level circuit-breaker counters but OMITTED blockscout entirely -- a test elsewhere in the full suite (one of many pre-existing error/retry tests that deliberately drive a service past its failure threshold) left blockscout's real client state "open", and this file's own reset never cleaned it up before its own "all closed" assertion ran.
Solution : new global `_reset_circuit_breaker_state()` + autouse fixture `_isolated_circuit_breaker_state` in `conftest.py`, covering all 5 modules `circuit_breaker_status.py` tracks (dexscreener, blockscout via `_chain_clients` iteration, goplus, wallet_transfers_fast, momentum_entry's OHLCV cascade), reset both BEFORE and AFTER every test in the whole suite -- replaces `test_circuit_breaker_status.py`'s own incomplete local fixture (removed, now redundant). Two new tests lock the actual regression: `test_blockscout_open_state_from_a_prior_test_never_leaks_here` dirties blockscout's real client state, the next test confirms it comes back clean -- proving the GLOBAL fixture, not per-file bookkeeping, is what keeps the suite isolated. Full suite green (10069 passed, 17 skipped, down from 1 failure). — `tests/conftest.py`, `tests/test_circuit_breaker_status.py` (2 new tests, local fixture removed).

------------------------------------------------------------

[CONFIG] Subject : outgoing-pause-watch -- hourly Telegram reminder while the /stop kill-switch stays armed
Date : 2026.08.13 / Problem : operator discovered, while an unrelated diagnosis session was investigating wallet_scan_queue throughput, that a PRIOR Claude Code session had armed `outgoing_pause.py`'s kill-switch at 15:44 UTC (real reason: CDP wallets not yet configured) -- the operator only found out ~5h later, purely by accident. Nothing in the system ever surfaced an armed kill-switch to them proactively.
Solution : new VPS cron `/opt/aria-data/outgoing-pause-watch/run.sh`, hourly (`0 * * * *`), pure READ of `pause_state.json` (never touches `outgoing_pause.py` itself -- guardrail file). Deliberately NOT the one-shot hysteresis doctrine every other watchdog uses (memory-watch/log-health-watch alert once until the state clears): the operator explicitly asked for a REPEATED nudge every hour for as long as the pause stays armed, so a real freeze never again goes unnoticed for hours. Tested against the real live state (script sent a real Telegram message confirming the 5.4h-armed CDP pause, `{"ok":true,...}`). — `/opt/aria-data/outgoing-pause-watch/run.sh` (new, outside the public repo), crontab entry.

------------------------------------------------------------

[CONFIG] Subject : memory-watch upgraded -- close-watch loop during an alert + targeted auto-kill of orphaned ccd-cli sessions, swappiness lowered 30->10
Date : 2026.08.14 / Problem : real incident this same day -- combined RAM+swap hit 77% (alert threshold), swap alone at 93% (~140MB free), root cause a ccd-cli session (old binary version) running unattended for 8 days straight, confirmed by the operator as not a legitimate session before being closed by hand (SIGTERM). The existing watchdog only polled every 15min with no escalation path -- a single alert, then silence until the next scheduled pass, no way to see the real trajectory or act automatically before the situation got worse.
Solution : (1) `run.sh` now loops internally at 90s intervals once `ALERT_THRESHOLD_PCT` (75%) is crossed, up to a 12min cap (stays well under the 15min outer cron interval), protected by an `flock` lock so the next scheduled cron pass can never overlap a still-running close-watch loop. (2) A new, deliberately narrow auto-kill: past `KILL_THRESHOLD_PCT=90` (kept a real margin under the operator's suggested 95%, given how little swap headroom was left at 93% this same day), the script SIGTERMs only `ccd-cli` processes older than `ORPHAN_AGE_THRESHOLD_SECONDS` (48h -- no legitimate work session runs that long uninterrupted), sends a Telegram notification BEFORE acting with the exact pid/age list, and never touches `aria-api`/`dockerd`/anything serving real traffic (that stays autoheal's job). Logic validated against a real throwaway `sleep` process before deployment (pid extraction + SIGTERM path confirmed working end to end), and the unmodified alert path re-verified live (no regression: 2 consecutive real passes, correct percentages, no false alert). (3) `vm.swappiness` lowered 30->10 (`/etc/sysctl.d/99-aria-swappiness.conf`, applied live via `sysctl -p`) -- a general performance tuning for an application server (avoid swapping active pages when cache alone could be reclaimed instead), explicitly NOT a fix for this incident's root cause (a memory-leak volume problem, not a "when does the kernel swap" problem) -- discussed honestly with the operator before applying. — `/opt/aria-data/memory-watch/run.sh` (outside the public repo), `/etc/sysctl.d/99-aria-swappiness.conf` (VPS system config, outside the repo), `docs/registre-automatisations.md`.

------------------------------------------------------------

[DEPLOYE] Subject : knowledge_inbox_cycle now attributes its issues to the AriaZHC GitHub account, not the operator
Date : 2026.08.14 / Problem : `run_knowledge_inbox_cycle` (already active) opened `aria-knowledge-proposal` issues via `settings.github_token` -- the operator's personal PAT -- so GitHub attributed every proposal to "GoldenFarFR", never to Aria herself, despite the issue text already saying "Proposal generated by ARIA". Operator created a real separate GitHub account (AriaZHC), added as a "Triage" collaborator on GoldenFarFR/ARIA, generated a classic PAT (public_repo scope -- the fine-grained token never offered "Only select repositories" for a plain external-collaborator account, exact root cause unconfirmed, worked around rather than blocking on it).
Solution : new `aria_knowledge_inbox_github_token` field (vanguard/backend/app/config.py + packages/aria-core/src/aria_core/testing.py, same duplication pattern as `aria_brain_github_token`), read first in `knowledge_inbox.py::run_knowledge_inbox_cycle` with a fallback to `github_token` when unset (zero regression dev/local). Token stored in `vanguard/backend/.env` (never seen by the Claude Code session, never displayed via Bash). Verified via a real live API call (one-off script, discarded after use): issue #59 opened on GoldenFarFR/ARIA, `author_login=AriaZHC` confirmed. 2 regression tests added (`test_cycle_prefers_dedicated_aria_token_over_operator_token`, `test_cycle_falls_back_to_operator_token_when_dedicated_unset`) — `packages/aria-core/tests/test_knowledge_inbox.py`.

------------------------------------------------------------

[DEPLOYE] Subject : file-staleness-watch -- periodic re-read of living docs (15d), counter carried by an explicit review
Date : 2026.08.14 / Problem : repeated operator request (3x, refined through the session) -- any doc file with no activity for 15 days must be re-read and corrected if stale, "even if nothing needs changing, at least it resets the counter". `git log`/mtime alone can't carry that counter (only moves when CONTENT changes), and the operator clarified the re-read stays a Claude Code session's job (not a separate headless-LLM cron) -- final design simplified accordingly.
Solution : `/opt/aria-data/file-staleness-watch/run.sh` (daily VPS cron, pure bash+sqlite3, same pattern as vc-watch/memory-watch -- NO LLM call). Dedicated `file_review_log` table (path, last_reviewed_at, reviewed_by) distinct from git mtime -- that's what carries the counter. Scope deliberately limited to living docs (*.md at root+docs/+packages/aria-core/docs, AGENTS.md/VISION.md/CLAUDE.md/README.md/SECURITY.md, vanguard/AGENTS.md/VISION.md/README.md) -- not stable .py code, already covered by tests+CI, including the whole repo would have multiplied volume for near-zero signal. Capped at `MAX_NEW_PER_RUN=8` new flags per pass (oldest-first), since 82 files were already stale on the first pass -- spamming 82 issues at once would have drowned the SessionStart hook. Dedup'd via `system_issues`'s own mechanism (`dedup_key`), so idempotent (verified: 3 consecutive passes, never a duplicate). `mark-reviewed.sh <path> <reason>` atomically closes the matching system_issues row AND updates `file_review_log` -- both in the same script so the counter reset (the operator's actual concern) is never forgotten. Real bug found during the live test: apostrophes in the detail message silently broke the SQL INSERT (`2>/dev/null` masked the failure) -- fixed with systematic escaping (`${VAR//\'/\'\'}`). — `/opt/aria-data/file-staleness-watch/run.sh` + `mark-reviewed.sh` (new, outside the public repo), `docs/registre-automatisations.md`.

------------------------------------------------------------

[CONFIG] Subject : db-backup rotation moved to grandfather-father-son -- a 20-day-old problem had no surviving backup
Date : 2026.08.17 / Probleme : the daily backup kept a flat 14 days (`find -mtime +14 -delete`). Real consequence, spotted while answering an operator question about a destructive worm: any corruption or bad manipulation noticed more than 14 days later had NO healthy copy left to restore from -- the window was silently too short for the slow-corruption class of incident. Second issue: rotation keyed on `mtime`, which any copy/restore operation rewrites, so a restored-then-recopied file could be deleted on the wrong schedule.
Solution : GFS rotation -- 14 dailies + 8 weeklies (Monday) + 12 monthlies (1st) = one year of depth for ~35 files instead of 90, well within the VPS's 58 GB free (measured). Decision now keyed on the DATE IN THE FILENAME, never `mtime`. Fail-safe: an unparsable filename is KEPT, never deleted (doubt costs disk, never data). Logic unit-tested on synthetic dates across 400 days BEFORE touching the live script, then the extracted block was run against a throwaway directory (16 files -> 9, exactly the expected survivors, including the deliberately-corrupt filename). Verified against the 36 REAL backups: 0 would be wrongly deleted at the next pass. Previous script kept as `run.sh.bak-20260817`. Also closed the "backlog N1" item left open in the 14/08 entry above: a real restore test was run this pass (`zcat` + `PRAGMA integrity_check` = ok + 173 rows readable from `paper_position_archive`). — `/opt/aria-data/db-backup/run.sh` (outside the public repo, same status as the other VPS crons)

------------------------------------------------------------

[ETAT ACTUEL] Subject : off-VPS backup still MISSING -- the only irreversible risk left
Date : 2026.08.17 / Probleme : the operator asked what happens if a worm destroys the machine. Audit answer, verified: `DEST=/opt/aria-data/db-backup/daily` -- the backup lives on the SAME disk as the data it protects. It covers corruption and bad manipulation; it covers NOTHING against a destructive worm, ransomware, a dead disk, or a suspended/deleted VPS. What would be lost is the track record (paper_position_archive, weekly verdicts) -- the very asset the strategy calls the moat, and the one thing GitHub does not hold. Disk space was never the constraint (58 GB free, measured): location is.
Solution : NOT SOLVED -- operator-gated, needs an external storage account. Decision taken 17/08: Backblaze B2, chosen over Google Drive and an external disk on one criterion that decides everything -- an application key restricted to WRITE can deposit but cannot delete or overwrite, so a fully-compromised VPS still cannot erase the history (Drive keeps a deletable copy, an external disk depends on the operator remembering to plug it in). Volume measured: 42 MB/day compressed, ~2 MB/day growth, ~1 GB/year at current retention -- inside B2's free 10 GB tier. Design agreed, not built: (1) encrypt locally BEFORE upload (B2 must never be able to read the track record), (2) upload with a write-only key, (3) decryption key stored in Bitwarden, NEVER on the VPS -- otherwise an attacker taking the server gets both the data and the means to read it. Bitwarden itself is unfit to hold the backups (Send caps at 100 MB and expires; Premium attachments cap at 1 GB total = 24 days at 42 MB/day). Next step is the operator's: create the B2 account and add the key to `vanguard/backend/.env` himself (session Bash access to that file is blocked by the security classifier, by design).

------------------------------------------------------------

[CONFIG] Subject : log-health-watch/memory-watch/outgoing-pause-watch now mirror into system_issues, not Telegram-only
Date : 2026.08.17 / Probleme : operator explicit request ("se serait bien quue les alerte te soit remonter a toi... tu te creer des hook ou des cron qui te tenvoi ici les erreur") -- these 3 watchdogs alerted Telegram only, so a session only ever saw an anomaly if the operator manually relayed the message (confirmed live: 3 real anomaly alerts already missed this way in this same session before the ask). `vc-watch`/`file-staleness-watch` were the only ones already mirroring into `system_issues` (read at every session start via `.claude/hooks/system-issues-reminder.sh`).
Solution : added the same `open_system_issue`/`close_system_issue`/`ensure_system_issues_table` bash helper trio (source-tagged, dedup_key-gated) to all 3, additive alongside the unchanged Telegram push. Real bug found and fixed live while testing the first one (log-health-watch): `-cmd "PRAGMA busy_timeout=5000;"` ECHOES its value ("5000") to stdout in CLI batch mode -- this silently poisoned the `existing=$(...)` capture in `open_system_issue` (a non-empty "5000" made the dedup check always think an issue was already open, so the INSERT never ran, exactly the silent-failure class of bug `2>/dev/null` is supposed to guard against but here masked instead). Fixed with `.timeout 5000` (the CLI-native equivalent, verified silent) instead of the PRAGMA form -- applied identically to memory-watch and outgoing-pause-watch from the start, so they never carried the bug. Also incidentally applies the WAL-era busy_timeout fix (same session, `aria.db` journal_mode switched delete->WAL after a real "database is locked" production incident) to these bash CLI callers specifically, which -- unlike the app's own `aiosqlite.connect()` calls -- had no timeout at all by default. Each of the 3 tested end-to-end live (open + close, isolated dedup_key, cleaned up after) before considering it done. `outgoing-pause-watch` deliberately keeps calling `open_system_issue` on every repeated hourly pass (not just the first) -- the function's own dedup_key check already no-ops a re-open, so this stays consistent with that watchdog's intentional non-hysteresis Telegram doctrine without duplicating rows. — `/opt/aria-data/log-health-watch/run.sh`, `/opt/aria-data/memory-watch/run.sh`, `/opt/aria-data/outgoing-pause-watch/run.sh` (all outside the public repo, no commit).

------------------------------------------------------------

[DEPLOYE] Subject : `_SettingsProxy` (aria_core/runtime.py) leaked a read-only-property shadow across the whole test session -- pre-push regression check blocked a push on an unrelated file
Date : 2026.08.18 / Probleme : `scripts/pre-push-regression-check.sh`'s own targeted test run (a ~90-file subset selected by grepping every file that mentions a changed module) deterministically failed one test never touched this session (`test_agent_wallet_monitor.py::test_send_outflow_confirmation_creates_approval_and_sends_keyboard`) -- reproducible twice in a row on the SAME subset, yet always green standalone, as a whole file, and in a full 10,642-test repo run. Bisected the ~90-file subset down to a 2-file minimal repro (`test_telegram_scan_command.py` + `test_agent_wallet_monitor.py`). Root cause: `_SettingsProxy.__setattr__` (documented, INTENTIONAL fallback for read-only computed properties like `admin_ids`) writes `object.__setattr__(self, name, value)` directly onto the proxy's own `__dict__` when the real settings object refuses the write with `AttributeError`. Since the proxy is a MODULE-LEVEL SINGLETON shared by every importer, and `__getattr__` is only ever consulted when normal attribute lookup fails, the FIRST test in a session to do `monkeypatch.setattr(settings, "admin_ids", ...)` permanently shadows `admin_ids` on the proxy -- every later test's `settings.admin_ids` read returns that stale shadowed value instead of recomputing the property against ITS OWN fresh settings object (each test gets a brand-new `AriaRuntimeSettings()` via `conftest.py`'s autouse `_isolated_runtime` fixture, but the proxy's shadow survives that swap untouched). `test_telegram_scan_command.py:47` shadows `admin_ids=[999]`, monkeypatch's own teardown restores it to whatever stale value it had captured as "old" -- `test_agent_wallet_monitor.py`'s later test only ever sets the REAL field (`telegram_admin_ids`, which has a normal pydantic setter, no fallback triggered) expecting `admin_ids` to recompute from it, but reads the shadowed value instead, gets an empty list, and `_send_outflow_confirmation` correctly (from ITS OWN perspective) treats "no admin_ids" as a hard block and returns `False`.
Solution : `runtime.configure()` (called by `bootstrap.configure()`, which `configure_test_runtime()` calls, which the autouse `_isolated_runtime` fixture calls before EVERY test) now clears the proxy's `__dict__` on every call -- `globals()["settings"].__dict__.clear()` (the plain name `settings` is shadowed by `configure()`'s own `settings` parameter, hence the explicit `globals()` lookup). Scopes every property shadow to "since the last configure()" instead of "for the whole process," matching what a fresh settings object is supposed to mean, closing the leak at its one real entry point rather than patching each affected property (`admin_ids`, `owner_chat_id`, and any future one) individually. Verified: the 2-file minimal repro, the full ~90-file targeted subset, and a full repo-wide run (10,642+ tests) all green after the fix. This bug is NOT specific to `admin_ids` -- any test anywhere that monkeypatches a read-only property on `settings` was equally capable of poisoning any later test reading that same property, regardless of file/module boundaries; this class of bug is now closed for good, not just for this one symptom. — `runtime.py`.

------------------------------------------------------------

[DEPLOYE] Subject : sample-size alert for the LATE-BONDING pocket
Date : 2026.08.21 / Probleme : operator asked to be told when there is enough data to settle the open calibration questions (trailing-stop distance, bonding-progress band). Leaving that to a session's memory guarantees it gets dropped -- sessions end, and this one had already reset the epoch four times in a day, each reset restarting the count.
Solution : `/opt/aria-data/late-bonding-sample-watch/run.sh`, cron every 10 min, Telegram at 100/250/500/1000 closures under the CURRENT configuration. Two design points that matter more than the threshold list. (1) State auto-resets when `CONFIG_EPOCH` changes -- without it a reset epoch would inherit "palier 250 already announced" and stay silent through a fresh sample. (2) Every message carries the raw mean AND the mean without its two best trades side by side: announcing the mean alone would invite precisely the reading the statistical mandate exists to prevent, and this pocket produced five such false positives in two days. Read-only, no verdict -- it says there is enough to decide, never what to decide. — `run.sh`, `docs/registre-automatisations.md`.

------------------------------------------------------------

[CODE] Sujet    : column migrations raced between processes (moved here 24/08, was misfiled in `docs/HANDOFF_SECURITE.md` -- no perfect home in any of the 26 component files, this is the closest: infra reliability across VPS processes, not a secrets/access/CI incident)
Date : 2026.08.21  /  Probleme : every module that grows a table reads PRAGMA table_info then issues ALTER TABLE ADD COLUMN. That read-then-write is a race: two processes starting together (API container + standalone shadow process, or two test cases) both see the column missing, both ALTER, and the loser raises sqlite3.OperationalError "duplicate column name". Surfaced as a flaky test (test_concurrent_reservations_never_both_succeed_past_the_cap, green in isolation, red inside the full suite). An audit found 33 modules using this pattern and NONE guarding against it -- the real risk is a process failing to start right after an incident, exactly when several come up at once.
Solution : db_migrations.ensure_columns() -- idempotent, treats losing the race as a normal zero-added outcome while any OTHER OperationalError (locked, disk full) still surfaces, so a real failure never becomes a silently half-migrated table. 5 regression tests including a simulated race and a real-failure case. Applied to the three most exposed modules (x402_budget, solana_late_bonding_shadow, shadow_snapshot_archive); the remaining 30 keep the hand-written form and should migrate opportunistically — db_migrations.py (f459185c)

------------------------------------------------------------

[DEPLOYE] Sujet : goplus-security-watch re-flagged 5 topics already closed on 18/08 -- `already_seen` computed but never used to gate `open_issue()`
Date : 2026.08.26 / Probleme : operator noticed 6 fresh `system_issues` rows (goplus-security-watch, opened 06:30 UTC) covering topics already resolved weeks earlier -- confirmed live: `verdicts.md` still held its ORIGINAL 2026-08-18T14:20:00Z blocks (the file is never truncated after promotion), and today's cron re-read the whole file, re-matching those same old TRAITER blocks. `promote_verdicts.py` DID compute `already_seen = await _dedup_key_ever_used(dedup_key)` (checks system_issues across ANY status, not just open, exactly as its own docstring promises) -- but only used that flag to gate the Telegram notification, never the `system_issues.open_issue()` call itself. `open_issue()`'s own internal dedup only matches an issue with `status='open'` (by design, for an ongoing-anomaly watchdog that must re-collapse into the same row every passage) -- so once the original #128-#134 issues were closed weeks ago, their dedup_key became "free" again and every later re-read of the same old verdicts.md block opened a brand-new duplicate row.
Solution : one-line fix -- `if already_seen: continue` before the `open_issue()` call, so a tweet whose dedup_key exists anywhere in the table (open OR closed) is skipped entirely, matching the module's own already-written docstring guarantee instead of only half-implementing it. The 6 duplicate issues (#251-256) closed with a pointer to their original resolution (`docs/HANDOFF_SECURITE.md`/`docs/HANDOFF_COINBASE_CDP.md` 2026.08.18 entries, `docs/backlog-technique.md` #334 for the DeepScan one). `verdicts.md` deliberately left untruncated (append-only audit trail, same doctrine as every other watch mechanism's log) -- the fix is in the promotion step, not in pruning the source file. — `goplus-security-watch/promote_verdicts.py` (VPS-only, outside the public repo).
