# HANDOFF — 2026-07-08 (session sécurité + onchain + normes)

Reprise directe : lire ce fichier + `CLAUDE.md` + `docs/etat-systeme-cable.md`. Tout est sur
`main`, testé, verrouillé par `test_coherence` (CI). Rien n'est encore déployé sur le VPS.

## Ce qui a été construit aujourd'hui (commits sur main)
1. **Correctifs sécurité prod** (`edff6b1`) : webhook Telegram **fail-closed** (secret exigé,
   temps constant) ; secret opérateur **header-only** (plus de `?secret=`) ; **anti-usurpation**
   du champ `handle` (un visiteur ne peut plus poster sur @Aria_ZHC en se disant opérateur) ;
   **plafond rate-limit par IP** + `--proxy-headers`.
2. **Checkpoint auto de session** (`ca6d085`) : hook `UserPromptSubmit` → propose de mettre à
   jour les résumés tous les 20 messages. **Rappel de déploiement VPS** (`b295be9`, `df1266b`) :
   au-delà de 2500 lignes non déployées, UNE ligne de rappel, non bloquant, commandes sur demande.
   Marqueur suivi `.claude/last-deployed-ref`.
3. **2FA site** (`dea7211`) : bouton d'enrôlement **MFA natif Privy** + **Google** ajouté aux
   méthodes de connexion. *(Nécessite activation dans le dashboard Privy : Google OAuth + MFA.)*
4. **2FA opérateur** (`cc2d882`) : TOTP RFC 6238 (`aria_core/admin_totp.py`), **opt-in via
   `ADMIN_TOTP_SECRET`** (OFF par défaut, aucun lock-out), header `X-Admin-Totp` + **verrou
   anti-force-brute par IP**. Enrôlement : `python vanguard/operator/gen-admin-totp.py`.
5. **Normes permanentes** (`61d2f25`) : qualité, fluidité, graphique/UX, robustesse,
   testabilité, sobriété, accessibilité, **protection des données utilisateur**. + norme
   « vérif sécurité après chaque construction » (Règles absolues).
6. **Simulation d'attaque quotidienne** (`99e5fe7`) : `vanguard/backend/security_sim/` +
   workflow GitHub Actions (03:17 UTC). Introspecte toutes les routes, tire des milliers de
   requêtes hostiles, baseline différentiel. **Faille réelle trouvée + corrigée** : corps
   non-UTF8 → 500 sur tout POST (handler `RequestValidationError` dans `main.py`).
7. **Preuve onchain du track-record** (`11f88af`) : attestation Merkle
   (`aria_core/onchain/attestation.py`, 16 tests dont détection d'altération/backdating) +
   contrat `contracts/AriaLedger.sol` (aucun transfert de valeur).
8. **@base source d'opportunités** (`40a4f47`) + **radar d'opportunités**
   (`aria_core/opportunity_radar.py`, `ed9d64c`) : mine posts + **commentaires** → idées à
   fusionner dans ARIA (langage d'opportunité + accroches techniques, digest opérateur).

## Décisions en attente de l'opérateur
- **Clé d'ancrage onchain (bloque le déploiement onchain)** : 1) manuel/local · 2) clé
  attestation dédiée · 3) relayer. Rien n'est signé/déployé tant que non tranché.
- **Lecture X = ACTIVE** (confirmé via `/status` : « read bearer ✅ »). `X_BEARER_TOKEN` est
  bien en place. Donc le **radar d'opportunités** (`opportunity_radar.py`) peut être branché
  sur les vrais fils/commentaires @base : reste à câbler fetch → mine → digest opérateur
  (heartbeat ou à la demande). **À surveiller** : `/status` affiche `Heartbeat: never` — la
  boucle périodique n'a jamais tourné ; à vérifier (sinon aucun cycle auto ne s'exécute).
- **Privy dashboard** : activer Google OAuth + MFA pour que les boutons du site fonctionnent.

## Déploiement (à faire)
- Backend : `./vanguard/deploy.sh` (prérequis `TELEGRAM_WEBHOOK_SECRET` dans le `.env` sinon
  bot muet). Optionnel : `ARIA_PAPER_TRADING_ENABLED=1` (chrono 20 jours), `X_BEARER_TOKEN`.
- Vitrine (Google + bouton 2FA) : `./vanguard/deploy-vitrine.sh`.
- Après déploiement : mettre `.claude/last-deployed-ref` = `git rev-parse main` + commit.

## Stratégie Base (financement)
Plan et arbitrage dans le fil de session : viser le gros (Batches Startup ~50k$) par paliers,
preuve avant promesse. La brique onchain (preuve inviolable) est utile **que le grant tombe ou
non**. Builder Rewards (Talent Protocol) = palier bas à activer.

## Session 08/07 (après-midi) — showcase relai humain, /github repair, ACP dormant, Base
- **Showcase PR (`skills/showcase_pr_watcher.py`) refondu** : auto-réponse SEULEMENT sur feu
  vert net (merge/LGTM sans négation/question/technique) ; sinon **relai public taguant
  @GoldenFarFR + ping Telegram privé** avec brouillon (ARIA n'invente ni ne tranche rien).
  Signature « Response generated by ARIA (an autonomous AI owned by GoldenFarFR) », zéro
  em-dash. Testé en réel sur `Virtual-Protocol/acp-cli-demos#37`.
- **`/github repair`** (admin-gated) : `GitHubClient.edit_issue_comment` + `repair_last_reply`
  éditent un commentaire déjà posté (owner/repo dérivés de l'URL pour l'état legacy). Testé en
  réel : édition acceptée par GitHub (le token a le droit).
- **Bug corrigé** : commande `/github` **jamais enregistrée** (muette) → `CommandHandler` ajouté
  + route texte-libre repair. Verrouillé dans `test_coherence`.
- **ACP** : abandonné mais **préservé en seam dormant** → `docs/acp-reactivation.md` (réveil :
  flags env + signer local + rebuild). La cause du 500 (donnée par le mainteneur LauJoeYing) =
  signer non autorisé → relancer `acp agent add-signer` **en local** (jamais sur le serveur).
- **Base (financement) — direction confirmée par l'opérateur, on pousse à fond.** Briques
  livrées (« on fait tous ») : **seam x402** (`services/x402.py`, gaté OFF, dôme, aucune dépense
  auto) · **ancrage onchain** (`onchain/anchor.py` prépare la racine → signature LOCALE, serveur
  sans clé ; `contracts/DEPLOY.md`) · **dossier** `docs/base-funding-dossier.md` (plan d'emploi
  des fonds). **Aperçus design** (artefacts) : démonstrateur Base (dégradé olive/navy/lavande,
  FR/EN, validation Telegram OU centre de commandement) + **cockpit IA/humain** (#21, look
  nouvelle génération : bento verre, jauges, sparklines). **Prochain** : endpoint public
  read-only `/api/pulse` (pouls coarse, aucun secret/candidat) pour le suivi live + le cockpit
  (opérateur a validé le principe : je ne peux pas avoir d'accès privilégié, un pouls public sûr
  est la voie). Onchain attestation + `opportunity_radar` @base déjà en soute.
- **Backlog capté (ne pas perdre)** : #39 stop-loss suiveurs / take-profit échelonnés (monitor +
  propose, humain valide ; auto en paper) ; #40 profil X @GoldenFarFR pro (perso fondateur, pas
  page entreprise ; marque = @Aria_ZHC).
- **Norme opérateur** : rappels checkpoint/déploiement = **1 ligne isolée**, jamais noyés.
- **Déploiements VPS** : dernier commit déployé confirmé **`c1a90ad`** (health OK). Marqueur
  `last-deployed-ref` recalé à chaque fois.
- **Normes opérateur ajoutées** : rappels checkpoint/déploiement = **1 ligne isolée** (jamais
  noyés dans un pavé multi-sujets). Ne pas garder la mémoire des sessions passées au-delà des
  fichiers/artefacts (dit honnêtement).

## Session 08/07 (soir) — cockpit EN LIGNE + /watchlist + rapport /vc en PDF sécurisé multilingue + déploiement
Tout est **fusionné dans `main` ET déployé sur le VPS**, validé visuellement par l'opérateur.

1. **Cockpit (#21) construit et câblé** : `vanguard/src/pages/CockpitPage.tsx` +
   `CockpitPulsePanel`/`CockpitGate`/`CockpitDossierPanel`, route `/cockpit` dans `App.tsx`.
   Pouls public (`GET /api/pulse`, sans auth) + dossier par contrat (`GET
   /api/aria/dossier/{contract}`, gaté opérateur). Secret opérateur **`sessionStorage` uniquement**
   (jamais `localStorage`), transmis en header (`X-Admin-Secret`/`X-Admin-Totp`), jamais en
   query-string — verrouillé par `test_coherence`.
2. **`/watchlist [n]` Telegram** (admin, n∈[1,30], défaut 10) : répond à « comment je vois la
   checklist des contrats qu'ARIA suit de près ? » — classement `candidate_ranking.top_candidates`.
3. **Rapport `/vc` : refonte email/PDF/langue complète** :
   - `/vc <contrat>` (mode normal) affiche désormais des **boutons Telegram FR/EN** pour choisir
     la langue du rapport AVANT de lancer l'analyse LLM (le mode `test` est inchangé, pas
     interactif). Callback `vclang:<lang>:<adresse>`.
   - Email : plus de rapport complet en clair. Le corps est un **teaser court** (badges, R/R,
     mention de la pièce jointe) ; le rapport COMPLET (thèse, méthodologie, scénarios) part en
     **PDF sécurisé** en pièce jointe (`skills/vc_report_pdf.py`, reportlab) — chiffré via
     `pypdf` avec permissions limitées à l'**impression seule** (mot de passe propriétaire jetable,
     généré à chaque envoi, jamais réutilisé) + **filigrane nominatif traçable** (destinataire +
     empreinte SHA-256). Dissuasif, explicitement documenté comme **pas inviolable** (pas un vrai
     chiffrement fort) — la vraie protection anti-fuite reste le filigrane.
   - i18n : `skills/vc_i18n.py` → `report_strings(lang)` (~85 clés, FR/EN), réutilisé par le
     rendu HTML et le PDF. **Scope volontairement limité à FR/EN** (ES/IT/ZH demandés par
     l'opérateur mais pas encore supportés — l'infra LLM/i18n ne couvre que FR/EN aujourd'hui).
   - Nouvelles dépendances : `reportlab`, `pypdf` (aucune dépendance native, sûr pour Docker/CI).
   - Bug trouvé et corrigé en cours de route : la langue choisie via les boutons n'était jamais
     transmise à `send_vc_report(...)` (paramètre `lang` manquant) — corrigé.
4. **Déploiement** : backend (`deploy.sh`) confirmé sur commit `25be86fd3af1`, marqueur
   `.claude/last-deployed-ref` mis à jour. **Piège découvert** : `deploy.sh` ne redéploie QUE le
   backend — la vitrine statique a son propre script **`deploy-vitrine.sh`**, jamais lancé au
   premier passage, d'où `/cockpit` qui affichait encore l'ancienne page d'accueil. Une fois
   `deploy-vitrine.sh` lancé, cockpit + vitrine confirmés en ligne et fonctionnels par l'opérateur.
5. **Suivi (non codé ce soir, discuté avec l'opérateur)** :
   - Verdict AVOID/EXTREME mal fondé sur un token Virtuals **pré-bonding** (pas de paire DEX =
     normal pour cette catégorie, pas un vrai signal de danger) + le bruit X n'est pas consulté
     par `/vc` à la demande (le radar X est un mécanisme de découverte asynchrone séparé).
     Rattaché à la tâche #10 (déjà `in_progress`, jamais vraiment commencée). **Priorisation en
     attente de l'opérateur** — pas encore tranchée.
   - Formulaire Base Ecosystem Fund : champs équipe/expérience rédigés avec l'opérateur (parcours
     réel assumé : 2 ans investisseur crypto, pas d'expérience de gestion d'équipe classique,
     présenté comme différenciateur du modèle AI-operated holding plutôt que caché).
   - **Vitrine pas encore "gamme luxe" sur grand écran** : `.page-shell` plafonne toutes les
     sections à `max-w-2xl` (672px), centré — sur un moniteur large (~1920px) ça laisse ~700px de
     vide de chaque côté. Le dégradé d'ambiance (`.vanguard-minimal`) existe mais à ~7% d'opacité,
     quasi invisible. Correspond à la tâche #19 (déjà `pending`, non commencée) — **priorisation
     en attente de l'opérateur**, pas encore tranchée non plus.
- **Déploiements VPS** : dernier commit déployé confirmé **`25be86fd3af1`** (backend + vitrine,
  health + rendu visuel confirmés). Marqueur `last-deployed-ref` recalé.
