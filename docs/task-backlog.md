# Task backlog — persistent across sessions

> Distinct from `docs/backlog-technique.md` (research-watch leads, auto-fed by
> the daily promotion cron, narrative format). This file is a plain task
> tracker: concrete dev/audit items with just two states, open or done.
> `TaskCreate`/`TaskList` (the Claude Code session tool) is NOT persistent
> across sessions on its own — replicate any task created there into this
> file to survive past the current session. When starting a session, read
> this file and re-create the open tasks via `TaskCreate` for in-session
> tracking; when a task completes, mark it `[x]` here (never delete — keep
> the history) and close it via `TaskUpdate` in the session.

## Open
- [ ] Add Robinhood Chain to Blockscout's `CHAIN_IDS` (#308) — Robinhood Chain (one of the 3 active chains in the 1M$ test) has a confirmed live official Blockscout explorer (`robinhoodchain.blockscout.com`), but `services/blockscout.py::CHAIN_IDS` doesn't include it — `pocket_spec.py` already documents this exact gap. Verify the real numeric chain_id and API URL format before coding.
- [ ] Filter RWA Stock Tokens from momentum sourcing on Robinhood Chain (#309) — Robinhood Chain launched 200+ tokenized US stocks (NVDA/AAPL/GOOG, native Chainlink feed) tradable alongside the memecoins the momentum pipeline already sources. No asset-class filter exists. Verify momentum sourcing on Robinhood Chain can't mistakenly source a Stock Token as a regular momentum candidate.
- [ ] Add tiering to `candle_history.due_for_refresh` (#301) — at 20 tokens/pass × 15min cadence, a full pass over the real watchlist (640 tokens) takes ~8h (confirmed by Devil's Advocate report b5ed70d2). No real-capital impact today (only shadow mechanisms read this), but must be fixed before any real pocket starts reading `candle_history` to decide. Add a tiering parameter (open positions + active watchlist vs. tokens untraded for N days relegated to a slower cycle) — a parameter, not a rewrite.
- [ ] Audit synchronized multi-pocket risk and `wallet_guard` truncation (#291) — DeepMind "AI Agent Traps" taxonomy, two angles never audited: (a) could a single external signal trigger a synchronized reaction across several active pockets at once (v8/v9/scalping/swing/vc, partially shared sources)? (b) could a truncated/misleading summary get a Telegram `wallet_guard.py` escalation approved without the operator seeing the real transaction content?
- [ ] Audit passive asset receipt as a permission-change vector (#292) — real Bankrbot/Grok incident (Base, ~174-200k$, 04-05/05/2026): an NFT sent to the agent's wallet auto-upgrades it to VIP status unlocking real transfers — privilege changed by mere passive receipt of an incoming asset. Verify no ARIA mechanism (wallet_guard, agent-wallet pilot) can have its permission level changed by receiving an asset (NFT/token/transfer) without explicit validation.
- [ ] Verify fragmented-instruction recomposition across multi-source synthesis (#294) — CrowdStrike's public prompt-injection taxonomy (PT0197-PT0201): "Algorithmic Payload Decomposition", a malicious instruction split into individually-benign fragments, reassembled into an actionable command only once the model has accumulated all fragments. Verify no multi-source synthesis (aggregated WebSearch results, successive `website_scraper.py` crawls) implicitly recomposes a fragmented instruction before reaching `_llm_confirm`/`_llm_security_gate`.
- [ ] Evaluate Sybil-Defender/Bubblemaps for `smart_money.py`'s clustering limit (#295) — Sybil-Defender (github.com/forkoooor/Sybil-Defender, live on Forta since Nov 2023, 7 EVM chains) and Bubblemaps are the two candidates closest to immediate deployment for `smart_money.py`'s already-documented structural limit #1 (Sybil clustering beyond pairwise convergence). Verify real Base coverage and label freshness first.
- [ ] Evaluate a turn/cost ceiling for headless `claude -p` crons (#311) — the headless crons (research-loop, backlog-promotion, any future similar one) have no cap on the NUMBER of WebSearch/WebFetch calls within a single run (`--allowedTools` already restricts Bash/Edit/Agent/Task, nothing bounds call count). "Economic denial-of-wallet" angle (2026 study: a chain of individually-legitimate tool calls can inflate cost up to 658x while the task still succeeds). Evaluate adding `--max-turns` or a cumulative per-run cost cap.
- [ ] Verify signature/resource binding and atomicity on the 2 real x402 routes (#314) — "Free-Riding in the AI Economy" (arXiv 2605.30998) quantifies an x402 resource-leak ratio up to 100% on production middleware, from 2 invariants: payment signature detachable from the precise resource, and no atomicity allowing a double grant via race condition. Verify ARIA's 2 real x402 routes (`/api/x402/walletscore`, `/api/x402/b20score`) bind the authorization signature to the precise resource and that no race condition allows multiple grants for one payment.
- [ ] Audit artificial-consensus risk on multi-LLM vote/judge mechanisms (#315) — "Security in LLM-as-a-Judge" (arXiv 2603.29403) isolates the multi-LLM vote/judge mechanism itself as a distinct security surface. Two live mechanisms concerned: Polymarket paper (`VOTE_COUNT=3` votes must converge, `MAX_VOTE_SPREAD=0.15`) and v8's 3 LLM gates. Verify no fetched content (WebSearch/`website_scraper`) could simultaneously steer several votes of the same set toward an artificial consensus rather than a legitimate disagreement.
- [ ] Add the Coldcard case to the Fable 5 report human-review checklist (#313) — the Coldcard incident (~$116M stolen, weak RNG) confirms Claude Fable (same model powering the Devil's Advocate/`consult-fable5.sh`) did NOT catch this bug during a dedicated AI-assisted code review, precisely because it hid at a build/submodule boundary. Add this case explicitly to the existing checklist — never treat a clean Fable 5 report as proof of absence of a bug on logic crossing a build/submodule/dependency boundary.
- [ ] Check for leftover "Cockpit" references after the organism-first frontend redesign — found while auditing branch `vps-cockpit-wallet-76` (16/08): `CockpitPage.tsx` no longer exists (replaced by `ClientSite.tsx`/`VanguardSite.tsx`), and `OrganismHero.tsx` already documents the conscious removal of the wallet panel (08/02). Verify no other "Cockpit"/orphaned component reference lingers in `vanguard/src` after this redesign (dead imports, dead routes) — never explicitly audited after the architecture change.
- [ ] Verify native CDP guardrails and the allowance on `account.swap()` (#310) — two distinct angles on the real CDP swap (`agent_wallet_pilot.py`/`agent_wallet_cdp_adapter.execute_swap`): (1) "Coinbase Agentic Wallets" reportedly advertises native TEE-level guardrails (session cap, per-transaction cap, contract allowlist) enforced before the Python layer — verify in the real CDP docs whether ARIA already benefits from this; (2) SwapNet (~$13.4M stolen 25/01/2026 via an arbitrary call capability on a DEX aggregator from the same 0x/Matcha family) — verify `account.swap()` never grants an unlimited/persistent allowance beyond the current transaction amount. No code change before this documentation check.
- [ ] Scope the unified `resource_budget.py` project with the operator (#302) — 7 near-identical budget/quota guard modules confirmed (`blockscout_credit_budget.py`, `firecrawl_budget.py`, `tavily_budget.py`, `firecrawl_overspend_suspension.py`, `goplus_quota_suspension.py`, `coinmarketcap_quota_guard.py`, `twitterapi_io_budget.py`) — Firecrawl even has 2 separate mechanisms. Same pattern reimplemented per incident instead of factored. The next provider (Arkham x402, #299) would write an 8th module. Proposed structural project: single declarative `resource_budget.py` + single `resource_budget_ledger` table + one consolidated "API runway" Telegram command, migrated in 3 safe steps. NOT to be started without explicit operator "ok" (structural change to active guardrails) — this task is to propose/scope, not to code directly.
- [ ] Fix the mobile notification-tap bug + offline TOTP deadlock (Devil's Advocate 384c13e2) — two real bugs in `mobile/`, found 07/08, never tracked elsewhere: (1) `consumeInitialNotificationTap` doesn't consume anything — `getLastNotificationResponseAsync` returns the SAME response every call, re-triggered on every return to `phase==='home'` — a tap from hours ago re-opens the chat in a loop on every unlock cycle; (2) the local mirror timestamps with client-side `Date.now()` and duplicates the backend's 30-day policy — a drifting device clock bypasses/wrongly-triggers the policy, AND creates a deadlock for an operator who updates the app then launches offline (no local timestamp ever written, routed to `totp_reverify` which requires a network POST — impossible offline, the exact lockout the offline branch was meant to avoid). Full report: `/opt/aria-data/architect-reports/archived/384c13e23b66459479aa04843af38921225b1836.md`.
- [ ] Extend the anti-ReDoS clamp past polynomial-only and the single `AriaBrain.process` entry point (Devil's Advocate 08ac3e9a) — found 06/08, never tracked elsewhere. Current clamp (bound input length to 8192 chars) only neutralizes POLYNOMIAL ReDoS — an EXPONENTIAL backtracking pattern (`(a+)+b`-style) explodes in seconds at ~30-40 chars, so 8192 chars remains an unlimited playground for any future badly-written pattern. The "single choke point" only exists at one call site (`AriaBrain.process`) — any regex running on EXTERNAL NON-USER data (DexScreener token names, on-chain descriptions, webhook payloads) stays out of scope, exactly where an attacker controls the text without going through Telegram. Full report: `/opt/aria-data/architect-reports/archived/08ac3e9a7264320684be6d192398d018b48ba452.md`.
- [ ] Verify the candle-cache's current state against 4 untracked Devil's Advocate reports from 06/08 — 4 consecutive reports on the same subsystem (`_candles_cache`/`_fetch_candles_impl`), possibly already partly fixed by same-day iteration, re-check against current code before acting: `list(candles)` copied on every cache READ turns an O(1) hit into O(n) with allocation (355f1335); the cache stores LIVE references shared across callers — one caller mutating its candles silently corrupts the cache entry for everyone else for the remaining TTL (de5f91ec); the provenance ContextVar isn't populated on a cache HIT — `get_last_candle_provenance()` returns stale/None data on the most common path (94655696); the "15m/30m fixed fallback" silently injects wrong-granularity candles into the scalping RSI/MFI calc with zero marking in the signal/log/position, and the whole watchlist falls back simultaneously on a GeckoTerminal rate-limit (319031e6). Reports: `/opt/aria-data/architect-reports/archived/{355f1335e7bb7f8c4b1bc04f93f6ecd391a8778d,de5f91ec4dc0273cc2af677ab6338e433ceb3a53,94655696b68c21dd0383fe038bd39e3d2f3d5f37,319031e6bef5dcc49339dd94adb52ddcedfe0f61}.md`.
- [ ] Fix the multi-writer drift in `llm_usage`'s running monthly total (Devil's Advocate 7aff8afe) — found 06/08, never tracked elsewhere. The incremental monthly total only survives under the assumption "single writer process, never a rescan" — the "future periodic sanity check" mentioned in a comment isn't wired. On the first multi-writer scenario (overlapping-deploy processes, a second worker, an import script appending to the JSONL), the total silently drifts for the rest of the month with zero detection. Also, `_running_month_total` never evicts past months (the leak blamed on the old cache is reduced, not eliminated). Full report: `/opt/aria-data/architect-reports/archived/7aff8afefaf0b640b4c63e605a020186cd24fee2.md`.

## Done

- [x] Fix the scraper's `contract_confirmed` false negative (href not extracted) — `website_scraper.py::_extract_page_text` only kept visible text, so a contract referenced only in an `href` (e.g. a "Buy on Uniswap" button) was wrongly reported as "not found on site". Found live 16/08 on fetchr.guru (cascade candidate #196). Fixed by also including `href` attribute values in the returned text, verified against the real fetchr.guru site, 2 new tests, full suite green (10222 passed), deployed — `website_scraper.py` (cc90b0da).
