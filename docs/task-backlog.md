# Task backlog — persistent across sessions

> Distinct from `docs/backlog-technique.md` (research-watch leads, auto-fed by
> the daily promotion cron, narrative format). This file is a plain task
> tracker: concrete dev/audit items with just two states, open or done.
> `TaskCreate`/`TaskList` (the Claude Code session tool) is NOT persistent
> across sessions on its own — replicate any task created there into this
> file to survive past the current session. When starting a session, read
> this file and re-create the open tasks via `TaskCreate` for in-session
> tracking; when a task completes, mark it `[x]` here (never delete — keep
> the history) and close it via `TaskUpdate` in the session.

## Open
- [ ] **Investigate real (non-mocked) network calls hanging the full `pytest tests/` run (found 16/08)** — the full suite (10000+ tests) reproducibly hangs mid-run (confirmed twice, both times stuck in `epoll_wait` with zero CPU progress for 5-18+ minutes). `ss -tnp` on the stuck worker processes shows real ESTABLISHED TCP connections on port 443 to external IPs (Cloudflare ranges, likely a provider like DexScreener/GeckoTerminal/CoinGecko) — some test(s) somewhere in the suite are NOT fully mocking their HTTP client, violating the project's own "no real network calls in tests" doctrine. Isolating the exact culprit wasn't completed this session: `tests/test_acp_onchain_dev_behavior.py` was initially suspected (it was the last line logged before one hang) but passes cleanly in isolation (3/3, 24s) and even with `--ignore`d, the full run still hung elsewhere with the same real-connection signature — so this is likely more than one test, or state leaking from an earlier file into a later one, not a single fixable line. Every file this session's changes actually touched was verified clean via a fast, targeted, all-green run (368 tests, 11s: `test_llm_usage.py`, `test_candle_history.py`, `test_website_scraper.py`, `test_telegram_channel_verify.py`, `test_polymarket_thesis.py`, `test_pocket_spec.py`, `test_blockscout_client.py`, `test_coherence.py`, `test_heartbeat_gate_wiring_regression.py`) — this is a real, separate, pre-existing gap in the test suite's mocking discipline, not something introduced this session. **Action needed**: bisect which test(s) open the real connection (try running large alphabetical slices of `tests/` with `-n 4` and `ss -tnp` polling to narrow it down, or grep for `AsyncClient(` calls across `tests/` missing a `monkeypatch.setattr` on the same module) and fix the missing mock.
- [ ] **Fix x402 resource binding + add a local payment-nonce ledger (#314) — CONFIRMED real gap, diagnosed 16/08, needs explicit operator "ok" before touching the live payment path.** The 2 live x402 routes (`/api/x402/walletscore`, `/api/x402/b20score`) share byte-identical PaymentRequirements (no per-product discriminator) — a payment signed for one is structurally valid on the other, since the installed x402 V2 SDK has no resource/URL field at all. Atomicity is partially covered (response gated on facilitator `settle()` success) but ARIA keeps no independent nonce/idempotency ledger. Fix: per-product discriminator in `ResourceConfig.extra` (`aria_core/x402_seller.py::build_resource_config`) enforced on the route-matching side (`vanguard/backend/app/x402_seller.py`), plus a local single-use payment-nonce table (same `BEGIN IMMEDIATE` pattern as `x402_budget.py::try_reserve`). Low urgency given real traffic (2 lifetime sales, same test payer) but close before any Bazaar/agentic.market discoverability push increases real third-party traffic.
- [ ] **Create and validate a real CDP Policy against the live pilot wallet (#310, operator-requested 16/08) — capital-real-adjacent, needs explicit operator "ok" before touching the live wallet.** `agent_wallet_cdp_policy.py` (built 16/08, `build_pilot_bounded_policy()`/`create_pilot_bounded_policy()`) is fully coded and tested but DORMANT — never created against the real CDP wallet. Confirmed this session: the Policy Engine (`cdp.policies.create_policy`, TEE-level session/per-tx/contract-allowlist guardrails) is exposed directly in the standard `cdp-sdk` ARIA already uses, no separate product needed — the lever is real and available now. Do this properly later: validate against a tiny real amount first (the module's own documented next step), never assume it works from the dormant code alone.
- [ ] Extend the anti-ReDoS clamp to the 2 remaining lower-priority call sites (#318) — `smart_money_leaderboard.py`, social-link regexes in `signal_cascade_x.py`/`services/farcaster.py`/`services/project_activity.py` (fixed-format patterns, low real risk). The 3 higher-priority sites (`x_insight_relevance.py::_prefilter_junk`, `conviction_research.py`'s `_extract_x_handle`/`_contract_mentioned`, `opportunity_radar.py::extract_opportunities`) are done — see `## Done` below.

## Done

- [x] Add Robinhood Chain to Blockscout's `CHAIN_IDS` (#308) — chain_id=4663 sourced from the official docs (docs.robinhood.com/chain/connecting, cross-checked against a second source), confirmed registered in the Pro API aggregator (`api.blockscout.com/4663/api/v2/stats` → HTTP 402, not 404) and the standalone explorer live-tested (`robinhoodchain.blockscout.com/api/v2/stats` → real data). Added to `services/blockscout.py::CHAIN_IDS` AND `pocket_spec.py::_BLOCKSCOUT_CHAINS` (separate manually-synced set, both needed for the guardrail to recognize the new coverage) — `FULLY_GUARDED_CHAINS` now includes "robinhood", 3 tests updated.
- [x] Add tiering to `candle_history.due_for_refresh` (#301) — a full pass over the real watchlist (~640 tokens) took ~8h at 20 tokens/pass × 15min cadence. Added a FAST tier (favorites/never-fetched/refreshed within `stale_after_days=7`) and a SLOW tier (`stale_batch_fraction=0.2` of the batch guaranteed, either tier backfills the other's leftover slots) — `candle_history.py::due_for_refresh`, 6 new tests.
- [x] Audit synchronized multi-pocket risk and `wallet_guard` truncation (#291, 8-agent workflow, 16/08) — no gap confirmed. A dedicated macro circuit breaker (`risk_guard.evaluate_macro_risk`, `MACRO_CIRCUIT_BREAKER_LOSS_PCT=0.15`) already guards against a correlated crash across shared-sourcing pockets, arming the real kill-switch; v9 is structurally isolated on its own watchlist. `wallet_guard`'s Telegram escalation mirrors the real transaction payload 1:1 with no truncation step. Optional low-priority cleanup only (not done): log cross-pocket contract overlap; route `send_approval_keyboard` through the same chunking `send_message` already uses.
- [x] Audit passive asset receipt as a permission-change vector (#292, 8-agent workflow, 16/08) — no gap confirmed. Audited `agent_wallet_pilot.py`/`agent_wallet_cdp_adapter.py`/`wallet_guard.py`: no mechanism derives a permission level, cap, or allowed-action from a merely-received asset. `usdc_balance_usd()` filters strictly by exact USDC address (an NFT/spam token is structurally invisible to it); every real threshold is a hardcoded cap that blocks, never an unlock; zero NFT-specific code path exists anywhere in the repo.
- [x] Verify fragmented-instruction recomposition across multi-source synthesis (#294, 8-agent workflow, 16/08) — no gap confirmed. `conviction_research.py` does aggregate multiple external sources into one prompt, but the prompt-injection dome (delimiting tag + ignore-embedded-instructions rule) wraps the FULL aggregate, not per-source — a payload split across sources gets reassembled and evaluated together at the single semantic judgment point, not smuggled past a per-fragment-only check.
- [x] Evaluate a turn/cost ceiling for headless `claude -p` crons (#311) — `--max-turns` doesn't exist in this CLI version (verified live against `claude --help`, 2.1.222); the real mechanism is `--max-budget-usd` ("only works with --print"). Added to both `research-loop/run.sh` ($2) and `scripts/research-log-promotion.sh` ($3) — starting values, calibrate down once real per-pass cost is observed in `run.log`.
- [x] Verify signature/resource binding and atomicity on the 2 real x402 routes (#314, 8-agent workflow, 16/08) — **real gap confirmed, not fixed** (needs operator "ok" before touching the live payment path): the 2 routes share byte-identical PaymentRequirements, no resource binding at all in the installed x402 V2 SDK schema; atomicity partially covered (response gated on `settle()` success) but no independent nonce ledger on ARIA's side. Moved to `## Open` above with the concrete fix plan.
- [x] Audit artificial-consensus risk on multi-LLM vote/judge mechanisms (#315, 8-agent workflow, 16/08) — real gap confirmed and partially fixed: Polymarket's `VOTE_COUNT=3` votes all received the byte-identical unsanitized Tavily research context (only the system-prompt lens differed). Fixed the plain-injection half: `polymarket_thesis.py::_research_context` now runs through `sanitize_untrusted_text` before entering the shared prompt (2 new tests). NOT fixed (larger change): independently re-sourcing the context per vote; flagging suspiciously tight convergence as a possible manipulation signal. Also corrected: "v8's 3 LLM gates" doesn't exist in the code — `scalping_v9`/`scalping_variants` are fully deterministic, zero LLM calls.
- [x] Add the Coldcard case to the Fable 5 report human-review checklist (#313) — added to CLAUDE.md's Devil's Advocate "Absolute rule" and as item 11 of `docs/HANDOFF_LLM.md`'s Fable 5 checklist.
- [x] Check for leftover "Cockpit" references after the organism-first frontend redesign (8-agent workflow, 16/08) — `vanguard/src`/`vanguard/product-frontend` trees confirmed clean (no dead file/import/route). One real live residue found: `content/site_copy.py`'s public API payload (`GET /api/aria/content/site`) still served `"cta_primary": "Explore the Cockpit"`, pointing at a page removed since 27/07 — unconsumed by any frontend today but genuinely stale. Fixed: changed to "Explore the Vitrine".
- [x] Reduce the anti-ReDoS clamp gap beyond `AriaBrain.process` (Devil's Advocate 08ac3e9a, partial, #318) — a dedicated audit found the 2 most exposed unclamped HTML scrapers (unauthenticated third-party content, no bound at all): `knowledge/web_verify.py::_parse_ddg_html` and `services/telegram_channel_verify.py`. Both now cap raw HTML at 200k chars before any regex runs, same doctrine as `website_scraper.py`/`site_snapshot.py`. 5 more lower-priority call sites found and left open — see `## Open` above.
- [x] Clamp the 3 remaining higher-priority anti-ReDoS call sites (#318, 16/08) — `knowledge/x_insight_relevance.py::_prefilter_junk` now clamps the raw X mention text (`_MAX_RAW_TEXT_CHARS=200_000`) before `_SPAM_ONLY`/`contains_injection_marker` run; `conviction_research.py::_extract_x_handle`/`_contract_mentioned` clamp `text_blob` at the shared regex choke point (covers both the `combined` Tavily blob and the detective-mode `detective_texts` join, single point protects every current and future caller); `opportunity_radar.py::extract_opportunities` clamps the incoming text before `_segments` runs, closing the case where a post has no sentence delimiter at all and reaches `_OPP_RE`/`_HOOK_RE` as one unbounded blob. One test per site (real signal placed past the cutoff, confirmed NOT found, elapsed time asserted fast) — `test_x_insight_relevance.py`, `test_conviction_research.py`, `test_opportunity_radar.py`. 2 lower-priority sites (`smart_money_leaderboard.py`, social-link regexes in `signal_cascade_x.py`/`services/farcaster.py`/`services/project_activity.py`) left open — see `## Open` above.
- [x] Fix the multi-writer drift in `llm_usage`'s running monthly total (Devil's Advocate 7aff8afe) — added `reconcile_monthly_cost()`/hourly heartbeat task `llm_usage_reconcile_cycle`: rescans the real JSONL total, self-corrects the cached running total if it drifted past $0.01, logs a warning, evicts stale months from the accumulator. 4 new tests.
- [x] Fix the mobile notification-tap bug + offline TOTP deadlock (Devil's Advocate 384c13e2, 16/08 re-check) — both already fixed by same-day/next-day iteration, verified live against current code: `consumeInitialNotificationTap` (`mobile/push.ts:130-139`) now calls `Notifications.clearLastNotificationResponseAsync()` after reading the response, with a comment explicitly citing the bug it closes; the offline TOTP deadlock is fixed in `mobile/totpReverifyStore.ts` (`localReverifyStatus()`) by distinguishing "never recorded" (degrades to trusting the session, no lockout) from "recorded and genuinely stale" (still fails closed) — comment explicitly cites report 384c13e2 by hash. Remaining optional refinement, not a live bug: the local mirror still timestamps with client-side `Date.now()` instead of a server-signed `totp_reverify_deadline` claim (would remove the device-clock-drift dependency) — noted, not built, no active exploit path since the deadlock itself is closed. Separate real gap found while checking (not part of this task): `mobile/` has no test infrastructure at all (no `.test.*` files, no jest config in `package.json`) — worth a future dedicated backlog item if mobile logic keeps growing.
- [x] Verify the candle-cache's current state against 4 untracked Devil's Advocate reports from 06/08 (16/08 re-check) — 3 of the 4 critiques were already fixed by same-day iteration (07/08), verified live against `momentum_entry.py`: cache mutation-corruption risk closed by a defensive copy on both write and read (de5f91ec); provenance ContextVar now snapshotted into the cache entry and re-armed on a hit (94655696); the 15m/30m degraded fallback now computes and logs a real `degraded` flag, persisted per-row in `scalping_v9.py`'s `v9_cycle_log` table and surfaced in the open-position message (319031e6). Remaining open item (355f1335, `list(candles)` copied on every read = O(n) instead of an immutable tuple) is a documented, deliberate perf tradeoff against the mutation risk, not an active bug — negligible at current candle-list sizes, not fixed. One adjacent point from 94655696 also still open but currently inert: `_ensure_cycle_log_table`/`_log_cycle_evaluation` open a fresh aiosqlite connection + commit per row/per cycle instead of a batched `executemany` — the report's contention concern was sized for ~50 tokens, the real v9 watchlist is documented at <=5 (comment in `scalping_v9.py:152`), so no real risk today; revisit if the watchlist grows past ~20-30 tokens.
- [x] Fix the scraper's `contract_confirmed` false negative (href not extracted) — `website_scraper.py::_extract_page_text` only kept visible text, so a contract referenced only in an `href` (e.g. a "Buy on Uniswap" button) was wrongly reported as "not found on site". Found live 16/08 on fetchr.guru (cascade candidate #196). Fixed by also including `href` attribute values in the returned text, verified against the real fetchr.guru site, 2 new tests, full suite green (10222 passed), deployed — `website_scraper.py` (cc90b0da).
