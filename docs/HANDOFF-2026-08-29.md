# Session checkpoint — 2026-08-29

Auto-checkpoint (5250+ messages). Détail factuel complet dans les pointeurs cités
plus bas — ce fichier est un résumé de session, pas un journal supplémentaire.

## État en sortie de session

- **Capteurs on-chain (Briques 1-3)** : Brique 1 (vrai Swap V2) et Brique 2
  (buy/sell flow) validées en production sur Base ET Robinhood. Brique 3
  (Mint/Burn/liquidity delta) validée sur Robinhood (Mint+Burn réels, zéro
  contamination des compteurs swap) — **toujours 🟡 sur Base** (zéro Mint/Burn
  observé sur ~1h48/2781 lignes au dernier checkpoint, 29/08 21:55 CEST).
  Brique 4 (oracle USD) et le backfill historique global restent bloqués tant
  que Brique 3 n'est pas close sur les deux chaînes.
- **Prochain geste validé par l'opérateur** : backfill CIBLÉ sur un pool Base
  connu pour un Mint/Burn historique, en test de preuve du décodeur
  uniquement — jamais une validation production. Brique 3 Base reste 🟡 tant
  qu'un événement live n'est pas apparu.
- **Budget RU Chainstack recalibré** : le circuit breaker Base s'était ouvert
  (plafond 25k/jour trop bas face à plusieurs restarts le même jour, pic
  4161 RU/h). Nouveau plafond Base 100k/jour, Solana ramené à 0 (décision
  opérateur explicite). Déployé et vérifié (Base réouvert automatiquement).
- **Diligence tokens Base** : AGNT SOCIAL (utilité non documentée, risque
  d'homonymie avec $AGNTS signalé) et Basemate (produit fintech légitime
  confirmé via Base APAC, mais le token $BASEMATE lui-même déconnecté du
  produit réel qui fonctionne en USDC) — rapport publié en artifact, aucune
  action de trading engagée.
- **Diligence WachAI/$WACH** : protocole de vérification agentique (ERC-8004,
  TokenSense, WachAI Swap), token micro-cap sur Base (paire WACH/VIRTUAL,
  ~1.8k$/24h de volume) — recherche seule, rien d'intégré.

## Décisions opérateur (non re-demandées à l'avenir)

- Jamais fusionner deux briques capteurs dans un même déploiement — séquence
  stricte tests → checkpoints → déploiement → observation, une brique à la fois.
- Un backfill ciblé pour prouver un décodeur reste distinct d'une validation
  production — ne jamais transformer l'un en l'autre.
- Circuit breaker RU : Base 100k/jour, Solana 0/jour (jusqu'à nouvelle
  décision explicite).

## Commits clés de la session (chronologique)

```
f202d289 fix(evm-swap-ws): decode real V2 Swap event (brique 1)
7f51006e docs(roadmap): add on-chain activity sensors roadmap, brick 6
1eecd507 feat(evm-swap-ws): brique 2/5 -- buy/sell direction classified
7e762c7c docs(handoff): brique 2 deployed, real T0 logged
aa8c1ab1 docs(handoff): close brique 2 production checkpoint -- validated
bcdca05a docs(onchain-sensors): lock brique 3 formal mini-spec
0d6b3443 feat(onchain-sensors): real Mint/Burn/ModifyLiquidity decoded (brique 3)
0f7917fd chore(handoff): log brique 3 deployment
9ec1d5b8 fix(chainstack-ru-budget): redistribute per-chain caps (base 25k->100k, solana->0)
12c49df5 docs(handoff): log chainstack RU cap redistribution
45213391 chore: update last-deployed-ref after 9ec1d5b8 deploy
d183968b docs(onchain-sensors): document the exact observation-row contract
b8074bd5 docs(handoff): close robinhood's brique 3 checkpoint, base partial
```

Journal complet (36 commits du jour, incident #364/backlog inclus) :
`git log --oneline --since="2026-08-29 00:00"`.

## Pointeurs (détail factuel, ne pas dupliquer)

- `docs/roadmap-capteurs-onchain.md` — vision, discipline, mini-specs.
- `docs/HANDOFF_PIPELINE_MOMENTUM.md` — checkpoints réels, T0 par brique.
- `docs/HANDOFF_RESOURCE_BUDGET.md` — recalibration RU Chainstack.
- `docs/etat-systeme-cable.md` — état câblage à jour (édité même session).
- `CLAUDE.md` — section "Active state — on-chain activity sensors roadmap" éditée en place.

## Prochaine étape

Backfill ciblé Base (identifier un pool + événement Mint/Burn historique réel,
décoder, comparer à la source on-chain) — en cours, résultat à journaliser
séparément une fois obtenu.
