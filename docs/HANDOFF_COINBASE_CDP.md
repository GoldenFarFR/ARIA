# HANDOFF — Coinbase CDP (agent wallet, capital réel)

> **Repo PUBLIC — jamais d'IP/secret/token/clé/adresse email personnelle en clair ici.** Noms de variables OK (ex. `GOPLUS_APP_KEY`), leurs valeurs jamais.

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.
> Capital réel — toujours vérifier l'état actuel (`/agentwallet`,
> `agent_wallet_log.list_transactions()`) avant de supposer quoi que ce soit au-delà de la
> date de ce fichier.

[ETAT ACTUEL] Subject  : Item #41 unblocked diagnostically -- TWO real blockers found, neither is the card (nothing signed, no capital moved)
Date : 2026.07.28 / Problem : reviving the paused Item #41 ("dry-run then real Tangem tap"). The 25/07 commit message claims the dry-run was validated live, but NOTHING documents an `isApproved()==true`, so the state was genuinely unknown. Two independent blockers found, both verified live, both invisible until now:
Solution : **(1) ERC-6492 derivation mismatch (kills the coded grant path).** Free read-only call to `CoinbaseSmartWalletFactory.getAddress([owner_bytes], nonce)` on Base: `(SDK factory, TANGEM_ST_OWNER, salt 0)` derives `0x21F778A4e1bDEE8B6AA86773e72b200cc4B2Ada5`, NOT `aria-smart-st` (`0x800027f6...`). Salts 0-5 all tested, none match. So the ERC-6492 wrapper in `agent_wallet_smart_swing_grant.py` (which locally reconstructs `createAccount([owner], 0)` against `_COINBASE_SMART_WALLET_FACTORY`) would have deployed/validated against a DIFFERENT address -- `isApproved()` would have stayed false with no explicit error, exactly the "prime suspect" D1 the module's own docstring flagged but never tested. Root cause: `aria-smart-st` was created SERVER-SIDE via the CDP API (`create_evm_smart_account`, cf. the funding entry further down), so its counterfactual address cannot be reproduced by a local factory call. Consequence: the "deploy the account first, then grant plainly" alternative is ALSO broken as written (it would deploy `0x21F778A4...`). The real path must go through the CDP API itself for deployment/signing -- code not written. This cost ZERO gas and ZERO taps to find, precisely because it was checked before asking for the card. **(2) The Tangem bridge is a network zombie.** `tangem-bridge-shared-net` shows 2 days uptime and a healthy startup log, but was started with `--network container:aria-api` -- and every blue-green `deploy.sh` DELETES the old `aria-api` container. Its network namespace now points at a container ID that no longer exists: unreachable from the host AND from the current `aria-api` (HTTP 000 both ways, verified). Its logs contain literally nothing beyond the 2 startup lines -- no client has ever reached it since. This very likely explains the 25/07 real-grant attempt never completing: if any deploy happened between bridge start and the attempt, the bridge was already orphaned, and the failure had nothing to do with the card or the signature. Structural trap to fix durably: attaching the bridge to `container:aria-api` makes it silently die on every deployment. Operator decided (28/07) to restart the bridge later (a restart invalidates the WalletConnect session currently paired on their phone). NOT done, nothing restarted, no session touched. Still-unproven D2 (does the card accept `eth_signTypedData_v4`) remains untestable until the bridge is reachable again.

------------------------------------------------------------

[CODE] Subject  : Real CDP bodies for the smart-swing injected seams -- new adapter module wiring the spender's live SDK calls (still DORMANT)
Date : 2026.07.25 / Problem : `agent_wallet_smart_swing.py` (entries below) is pure orchestration + guards over INJECTED seams (`BalanceFn`/`token_balance_fn`/`SpendPullFn`/`SwapFn`x2/`ReturnTransferFn`/`QuoteFn`) -- no seam had a real CDP body yet, so nothing could actually pull/swap/return on the spender. Needed the concrete cdp-sdk 1.47.1 implementations, targeting the delegated SPENDER (`aria-spender-smart-st`), without touching the already-audited orchestration.
Solution : new `agent_wallet_smart_swing_cdp_adapter.py` -- thin, faithful wrappers only (no business logic, no new safety decision; the gate/kill-switch/loss-breaker/per-tx-cap/balance-check/forced-slippage all stay in the orchestration and run BEFORE these seams). Sibling of `agent_wallet_cdp_adapter.py`, same doctrine (lazy `cdp` import, `CdpClient()` reads creds from env, never logs a secret, reuses its `_get`/`_parse_balance_entry`). Functions: `usdc_balance_usd(address=)`/`token_balance(address=,token_address=)` (address-generic reads) + zero-arg `BalanceFn` bindings `smart_st_usdc_balance_fn` (BUY affordability = aria-smart-st USDC, the pull SOURCE), `spender_usdc_balance_fn` (recovery/inspection), `make_spender_token_balance_fn(token)` (SELL/canary); `spend_pull_fn` -> `spender.use_spend_permission(spend_permission=build_spend_permission_input(), value, network) -> str` (REUSES the module's builder, never reconstructs the input); `buy_swap_fn`/`sell_swap_fn` -> one shared `_execute_spender_swap` (quote via `cdp.evm.create_swap_quote(taker=SPENDER)` -> execute `spender.swap(AccountSwapOptions(swap_quote=quote))`), two wrappers because the seam kwargs differ (buy given USD->parse_units 6dec; sell given exact `amount_in_tokens_atomic`); `return_transfer_fn` -> `spender.transfer(to=SMART_ST_ADDRESS HARDCODED, ...)`; `quote_fn` -> raw read-only `create_swap_quote` (never `.execute()`). Spender resolved by `get_account(address=SPENDER_ADDRESS)`, never by name (immune to the 21/07-23/07 rename incidents). All SDK signatures read from the installed source (evm_client.py:437/1199, evm_server_account.py:269/342/658, swap/types.py) -- never guessed. `agent_wallet_smart_swing_cdp_adapter.py` (new) / `test_agent_wallet_smart_swing_cdp_adapter.py` (new, 34 tests: every function + a failure-path each + the 5-address return-pin refusal parametrize + a no-production-importer dormancy guard). Full suite (7726 passed) + `test_coherence.py` green. (branch claude/smart-swing-real-cdp-adapter-temp)
**RECONFIRMED DORMANT:** gate `ARIA_SMART_SWING_ENABLED` still OFF, no Spend Permission granted (needs a future Tangem tap, out of scope), `aria-smart-st` holds no swing funds, NO production caller/heartbeat/cron imports this module (grep-verified + a dedicated test asserts it) -- no funds moved, no guard-rail (wallet_guard/permission_mode/config.toml/regles-uniques/the gate default) touched. Writing this adapter arms nothing.
**KEY DEVIATIONS / HONEST LIMITATIONS (verified, not guessed):** (1) The buy's `BalanceFn` reads aria-smart-st's USDC (the pull SOURCE), NOT the spender's -- the spender holds ~0 USDC before a pull, so the spender's balance would block every buy; `run_swing_entry_canary`'s own docstring confirms "balance_fn = USDC balance of aria-smart-st". (2) Amount-out is sourced from the pre-execution QUOTE's EXPECTED `to_amount`, because `AccountSwapResult` carries ONLY `transaction_hash` (verified swap/types.py:366) and CDP swaps submit-and-return (`send_transaction` returns the hash without waiting for the receipt -- verified), so a post-swap balance read is not reliably settled. `to_amount` (not the tolerance-bounded `min_to_amount`, which is ~10% below expected by construction and would make a legit round-trip look like a ~19% loss) matches `precheck_swap_roundtrip`'s own documented choice. Bounded/loud/recoverable consequence: if the real fill lands below the quoted expected, the immediate sell/return can revert -> surfaced via `funds_stranded` (spender-held, Policy-bounded), never a loss. Before live arming, source the EXACT fill from the tx receipt (needs a settlement-wait, out of scope) for precise P&L + zero-strand round-trips. (3) A buy's bought-token human `amount_out` uses an 18-decimal default (a quote carries no decimals) -- informational only; the load-bearing `amount_out_atomic` is exact and the sell's USDC `amount_out` (6 decimals -> P&L-critical) is exact. (4) `sell_swap_fn` REQUIRES `amount_in_tokens_atomic` (can't infer the sold token's decimals) -- fail-closed raise, handled by the seam as a failed/NOT-stranded sell.

------------------------------------------------------------

[CODE] Subject  : Swing pocket hold-verify hardening -- temporal-hold buy/sell split + real sell leg + dual-quote pre-check + executed micro-canary + Policy calldata-hole fix (4 volets, operator-approved 07/25)
Date : 2026.07.25 / Problem : the swing safety envelope (entries below) had two real holes the operator called out. (1) The CDP Policy's allow-swap-router rule accepted ANY calldata to the router with no value bound, so a compromised spender key could route out up to the FULL Spend Permission allowance (the docstring's "never stealable" was false). (2) A scam token engineered to PASS ARIA's existing filters (GoPlus honeypot etc. can be misreported by a well-configured scam) would be bought at full size before any real verification -- operator: "attention si c'est un scam bien configuré pour passer le filtre... éviter l'achat" (avoid committing the full amount, not just detect after the fact). Tangem signs only fund TRANSFERS; swaps are autonomous, so the guard must live in the swap path itself. Still DORMANT: gate `ARIA_SMART_SWING_ENABLED` OFF, no Tangem tap, wired to nothing -- code only, no real money touched.
Solution : all injected-seam / pure-function, same doctrine as the existing file (`balance_fn`/`spend_pull_fn`/`swap_fn`/`return_transfer_fn`/`quote_fn` all injected, no live CDP call in production paths), all against really-installed cdp-sdk 1.47.1 (never guessed). **VOLET 1 (calldata hole):** `build_swap_only_policy` rule 1 now ANDs the router-address criterion with a `NetUSDChangeCriterion(type="netUSDChange", operator="<=", changeCents=_per_tx_cap_cents())` bounding net USD moved to the per-tx cap (reuses `SPEND_PERMISSION_ALLOWANCE_USD`, no new number) -- loss under a spender compromise is now BOUNDED by the per-tx + period caps, stated honestly (never "impossible"). **VOLET 2 (temporal hold, operator "oui parfait"):** BUY (`execute_smart_swing_swap`) no longer auto-returns the token -- it deliberately STAYS in the spender (this is what lets a real sell exist: the USDC-only Spend Permission could never pull an arbitrary token back to sell it). New SELL primitive `execute_smart_swing_sell` (held token -> USDC, NO pull, MANDATORY automatic return of the USDC to aria-smart-st) with the SAME security checkpoint as the buy, factored into `_trading_blocked_reason` (gate->kill-switch->breaker) + `_resolve_real_balance` so the two real-money paths can't drift. `agent_wallet_positions` re-verified against this first-ever real sell leg (buy `amount_in`=USD/`amount_out`=qty, sell `amount_in`=qty/`amount_out`=USDC -> `pnl_usd` exact) and its "closed is empty by construction" gap note updated -- closed positions can now genuinely occur. **VOLET 3 (dual-quote pre-check):** `precheck_swap_roundtrip` fetches a BUY and a SELL quote (`quote_fn` mirrors `cdp.evm.create_swap_quote`) for the same notional, measures the implied round-trip cost from the EXPECTED amounts (not the slippage-bounded min, to avoid tolerance stacking), ACCEPT iff `<= MAX_SLIPPAGE_BPS`; fail-closed on any error/unavailable/zero. Catches PRICE/liquidity issues only -- NOT an active honeypot (a quote is read-only), so it is a cheap first pass, never a replacement for VOLET 4. **VOLET 4 (executed micro-canary):** `run_swing_entry_canary` does a REAL small buy (`CANARY_TEST_AMOUNT_USD`=3$) then immediately a REAL sell of exactly what was received, measures the ACTUAL combined buy+sell tax (more trustworthy than GoPlus's self-reported tax), and REFUSES the full-size entry (loud/logged) if the sell fails outright (the exact honeypot signature) OR the round-trip loss exceeds `CANARY_MAX_ROUNDTRIP_LOSS_BPS`=2000 (20%: a round trip is 2 swaps ~= 2x one-way, worst legit ~19% at the 10% per-swap ceiling). Canary legs log under a SEPARATE `wallet_product` (`cdp_smart_account_swing_canary`) so probe round-trips never pollute the real-position P&L feed the loss breaker consumes. Exposed standalone (no single "open a position" entry point exists yet) with docstring guidance that a future orchestrator MUST gate the full-size buy on `result.passed` -- no broader heartbeat wiring invented. `agent_wallet_smart_swing.py`/`agent_wallet_positions.py` + `test_agent_wallet_smart_swing.py` (90 tests, +30) / `test_agent_wallet_positions.py`, full suite + `test_coherence.py` green. (branch claude/smart-swing-hold-verify-temp)
**HONEST GAP DELIBERATELY NOT SOLVED (out of scope, documented):** while a position is OPEN (token held in the spender, not yet sold), there is still NO real-time mark-to-market. The loss circuit breaker (`evaluate_swing_risk`) only reacts to REALIZED losses on CLOSE (paired buy+sell P&L from `agent_wallet_positions`) -- it never sees an UNREALIZED drawdown on a still-held position. A token could bleed 60% while held and the breaker stays silent until the position is actually sold at a loss. Live mark-to-market needs the network price of every held token (a call the pure-read pairer must never make) plus a valuation cycle -- a separate, larger piece the operator has NOT scoped. Explicitly left for a future chantier, never faked here.

------------------------------------------------------------

[CODE] Subject  : Real closed-position feed for the swing pocket -- pairs the flat swap journal into positions with realized P&L (closes gaps (a)/(b) of the entry below)
Date : 2026.07.25 / Problem : the swing safety envelope (entry below) left `evaluate_swing_risk`'s `recent_pnls`, `run_swing_post_mortem`'s `recent_buys`, and `trade_loss_batch_review`'s `positions_fetch` as INJECTED seams -- no real feed existed because `agent_wallet_log` records individual swap ATTEMPTS, never paired entry/exit trades with realized P&L. This is the feed the future ~250$ real capital LOSS circuit breaker depends on -- an invented or wrong P&L here would mis-trip (or fail to trip) a real-money brake.
Solution : new pure-read module `agent_wallet_positions.py` -- pairs `ok` `swap` rows for one `wallet_product` into positions. A buy leg is `USDC -> X`, a sell leg is `X -> USDC`; `pnl_usd = proceeds_usd(sell.amount_out) - cost_usd(buy.amount_in)` (both already USD in the journal, so P&L is exact regardless of token qty). FIFO, one buy leg to one sell leg -- a DESIGN ASSUMPTION, corrected after an audit finding (07/25): the swing path (`execute_smart_swing_swap`) can currently only ever produce BUY legs, since its pull step is hardcoded to the USDC-only Spend Permission regardless of `token_in`/`token_out` -- no SELL leg (`X -> USDC`) exists anywhere in the codebase yet, so `closed` is empty by construction until a real exit mechanism is designed (open question: per-token permission doesn't scale; likely needs its own orchestration, e.g. via the Tangem owner directly). The FIFO logic is still the right generalization for whenever a real sell exists (a stray extra buy safely stays OPEN, never a double close). Public feeds: `closed_positions_fetch`/`open_positions_fetch` (most-recent-first, same shape/order as `paper_trader.get_closed_positions`), `recent_realized_pnls` (newest-first, drives the consecutive-loss branch), `pairing_health`. Wired entry points (swing feed ALWAYS explicit, paper defaults untouched): `run_swing_loss_batch_review_cycle` (passes `positions_fetch=swing_closed_positions_fetch`) and `evaluate_swing_risk_from_log` (feeds real `recent_pnls`+`recent_buys` into smart_swing, `equity_usd` stays caller-injected). Nothing executes a swap or touches CDP -- read of `agent_wallet_tx_log` only. `agent_wallet_positions.py` (new) / `test_agent_wallet_positions.py` (new, 28 tests), full suite + `test_coherence.py` green. (cf. git log 2026-07-25, `feat(smart-swing): real closed-position feed`)
**HONEST GAPS / DECISIONS (documented, never hidden):** (a) **Sell with no matching buy** -> collected in `PairingResult.unmatched_sells`, NEVER a P&L (a phantom entry at price 0 would fabricate a huge profit). (b) **No trade CONTEXT in the journal** -> `thesis`/`discovery_channel`/`conviction_tier`/`entry_regime`/`close_reason`/`close_notes` are returned `None` (paper-portfolio concepts the swap log doesn't record) -- so a batch/adversarial review of real swing trades sees prices+P&L only until the log (or a sibling table) also records the entry rationale. Real follow-up to consider. (c) **True mark-to-market equity is NOT produced here** -> needs the live value of still-held tokens (a network call a pure-read module must never make); `evaluate_swing_risk_from_log` keeps `equity_usd` caller-injected, supplying only the fully-real `recent_pnls`/`recent_buys`. (d) **Shared-table leak found (not introduced silently)** -> `trade_loss_batch_review` persists into the `trade_loss_batch` tables and `momentum_entry` injects their `active_trajectory_adjustments()` GLOBALLY into the paper momentum prompt. Position-id collisions ARE prevented (swing ids are negative = the negated closing-swap row id, disjoint from paper's positive AUTOINCREMENT ids), but a CONFIRMED swing adjustment would still surface in the paper momentum prompt because that active-set read is not source-scoped. So `run_swing_loss_batch_review_cycle` is deliberately NOT wired to any heartbeat -- wiring it to production needs that leak resolved first (a source/pocket column filtered by `active_trajectory_adjustments`, or a separate table for the real-capital feed). (e) **Partial/accumulated fills are not quantity-reconciled** -> one sell closes exactly one buy lot; exact for full round-trips, becomes approximate if future orchestration adds partial sells (revisit with quantity-aware matching then).

------------------------------------------------------------

[CODE] Subject  : Smart Account swing pocket -- Policy + guarded execution + loss circuit breaker built (safety envelope completed)
Date : 2026.07.24 / Problem : the Model B foundation (entry below, 07/23) had only the identities + spend-permission builder. The next chunk was still open: the swap-only Policy, the guarded execution path, and (operator's 07/24 doctrine) a real safety brake on LOSS rather than on spend -- since the operator explicitly wants the Spend Permission set LARGE and FIXED (2500$/week ~= 10x the ~250$ real capital) so it NEVER blocks a legitimate trade.
Solution : `agent_wallet_smart_swing.py` (still DORMANT, gate `ARIA_SMART_SWING_ENABLED` OFF, wired to nothing) extended with, all against the really-installed cdp-sdk 1.47.1 (never guessed): (1) `SPEND_PERMISSION_ALLOWANCE_USD` 50 -> 2500 (structural ceiling `_MAX_SANE_ALLOWANCE_USD=10000` unchanged, 2500 stays below it). (2) `build_swap_only_policy(router_address)` -- a real `CreatePolicyOptions(scope="account", rules=[allow-swap-router, allow-token-agnostic-return-transfer-to-aria-smart-st])`, everything else default-denied; rule1 = `SendEvmTransactionRule(accept, EvmAddressCriterion(addresses=[router], operator="in"))`, rule2 = `EvmDataCriterion(abi=KnownAbiType.ERC20, conditions=[EvmDataCondition(function="transfer", params=[EvmDataParameterConditionList(name="to", operator="in", values=[SMART_ST_ADDRESS])])])`; fail-closed ValueError on a malformed router; validated by actually constructing the SDK objects. Docstring is explicit: real enforcement MUST be validated against live CDP on a tiny amount before trusted (NOT done). (3) `execute_smart_swing_swap(...)` -- guarded path with the SAME 5 app-layer guards as agent_wallet_pilot.py (gate -> `/stop` strict -> per-tx cap bounded by the allowance -> real-balance check fail-closed -> forced 10% slippage -> pull `use_spend_permission` -> `swap` -> MANDATORY return-transfer to aria-smart-st -> logging). Real CDP calls INJECTED (no key/network here, same doctrine as the EOA pilot). Distinct `WALLET_PRODUCT="cdp_smart_account_swing"` in `agent_wallet_log` (never mixed with the pilot's rows -- the wallet_product column separates them, no schema change). `funds_stranded` flag surfaces loudly if a step fails AFTER the pull (funds in the spender, recoverable-only-to-aria-smart-st via the Policy's return-transfer carve-out). **CORRECTION (07/25): the "never stealable" claim once here was WRONG** -- rule 1 (allow-swap-router) accepted ANY calldata to the router, so a compromised spender key could route value out through router-shaped calls up to the full Spend Permission allowance; the loss was BOUNDED by the caps, never zero. That calldata hole was closed 07/25 by adding a `NetUSDChangeCriterion` (per-tx USD cap) to rule 1 -- see the hold-verify entry at the top of this file. (4) Dedicated LOSS circuit breaker (`evaluate_swing_risk`/`block_swing_swaps`/`resume_swing_swaps`/`blocks_swing_swaps`) -- reuses risk_guard's `HARD_DRAWDOWN_PCT`/`HARD_CONSECUTIVE_LOSSES` by IMPORT (single source of truth, can never silently become laxer than the paper breaker), own state file `smart_swing_risk_state.json` (never mixed with the paper `risk_guard_state.json`), MANUAL resume only. (5) On a fresh trip: adversarial post-mortem reusing `trade_devils_advocate._REVIEW_SYSTEM` (same "sound"/"flawed" doctrine, DeepSeek R1 via OpenRouter) + Telegram notify with the result -- never a mute arming. 60 tests in `test_agent_wallet_smart_swing.py`, full suite + `test_coherence.py` green. `agent_wallet_smart_swing.py` / `test_agent_wallet_smart_swing.py` (b79e3f36).
**HONEST GAPS FOUND (documented, never invented):** (a) No "closed real position with P&L + thesis" feed exists for this wallet -- `agent_wallet_log` records individual swap ATTEMPTS, not paired entry/exit trades with realized P&L. So `evaluate_swing_risk`'s `equity_usd`/`recent_pnls` and `run_swing_post_mortem`'s `recent_buys` are INJECTED seams (the mechanism is built + tested; the real feed is a later step). Building that feed = building a real closed-position ledger on top of agent_wallet_log -- NOT done, not faked. (b) `trade_loss_batch_review.py` is ALREADY fully generic over `positions_fetch` (verified -- only paper coupling is the overridable default fallback, prompt uses generic dict keys) -- so it needs NO change to run on a real feed, but that real feed is blocked by the same gap (a). (c) `build_swap_only_policy` does NOT allowlist a preceding ERC-20 `approve(router, amount)` tx that a real router swap may require -- flagged in the docstring, to verify during live validation and add deliberately if confirmed, never guessed. **Still open (unchanged from below, hardware/verification session):** live-validate the Policy on a tiny amount; the one-time Spend Permission grant needs the Tangem<->BaseAccount wrapper (step 2, still OUT OF SCOPE -- raw round-trip proven 07/24, wrapper not built); fund aria-smart-st; end-to-end test before flipping the gate; build the real closed-position feed for gaps (a)/(b); wire `execute_smart_swing_swap`'s injected fns to a real CDP adapter (`use_spend_permission`/`swap`/`transfer` on the spender EOA).

------------------------------------------------------------

[DEPLOYE] Sujet    : x402_cdp_signer.py oublié lors du fix get_or_create_account du 23/07
Date : 2026.07.24 / Probleme : audit 5-agents -- x402_cdp_signer.py:80 appelait encore cdp.evm.get_or_create_account(name=WALLET_NAME) directement, le pattern dangereux (creation silencieuse d'un wallet fantome vide si WALLET_NAME desynchronise du vrai nom CDP) corrige partout ailleurs le 23/07 (commit 0dbbc214, _get_wallet_account fail-closed dans agent_wallet_cdp_adapter.py) apres 2 incidents reels (21/07, 23/07) -- ce signataire x402, actif sur un flux de paiement reel (33 spends/24h au moment de l'audit), avait ete oublie. Verifie en direct (cdp.evm.list_accounts()) avant correction : exactement 3 comptes reels existent, aucun wallet fantome cree par ce chemin a ce jour -- risque latent, pas de degat constate.
Solution : reutilise _get_wallet_account(cdp) (import agent_wallet_cdp_adapter) au lieu de get_or_create_account -- meme echec bruyant (RuntimeError explicite) qu'ailleurs si WALLET_NAME ne correspond plus a rien - x402_cdp_signer.py (cf. historique git 24/07)

------------------------------------------------------------

[CONFIG] Sujet    : Wallet CDP financé
Date : 2026.07.16  /  Probleme : —
Solution : 1 USDC + ETH gas. Découvert au passage : le compte CDP standard n'a pas d'intégration Paymaster/Smart Account, chaque swap/transfert consomme du vrai ETH

------------------------------------------------------------

[DEPLOYE] Sujet    : Pilote agent-wallet activé en prod
Date : 2026.07.18  /  Probleme : —
Solution : boucle de décision autonome câblée (réutilise le pipeline momentum), sizing 3% du solde réel plafonné 15$ — agent_wallet_pilot.py (0d3f5933)

------------------------------------------------------------

[DEPLOYE] Sujet    : Swap réel bloqué à répétition
Date : 2026.07.19→21  /  Probleme : 5 tentatives de swap, toutes échouées (erreur Pydantic gasFee) — bug du SDK CDP officiel, pas de notre code
Solution : tx_hash toujours vide, aucune perte de fonds (fail-closed) ; cooldown structurel 7 jours pour ne pas boucler sur un token cassé — agent_wallet_pilot_cycle.py (b00d108c)

------------------------------------------------------------

[CONFIG] Sujet    : Authentification CDP rejetée (401)
Date : 2026.07.21  /  Probleme : allowlist IP côté CDP restreinte à l'ancienne IP, après migration VPS
Solution : nouvelle IP ajoutée à l'allowlist — config CDP dashboard, pas de commit

------------------------------------------------------------

[CONFIG] Sujet    : Export de clé privée réalisé proprement
Date : 2026.07.21  /  Probleme : besoin d'exporter la clé privée du wallet pour un usage ponctuel
Solution : via une clé API TEMPORAIRE séparée (scope Export uniquement), jamais la clé de prod ; supprimée après usage ; clé privée du wallet jamais vue par Claude Code — config CDP dashboard, pas de commit

------------------------------------------------------------

[DEPLOYE] Sujet    : Wallet mal résolu — solde affiché à 0$
Date : 2026.07.22  /  Probleme : /agentwallet affichait 0 USDC alors que le vrai solde on-chain était 12,80$ — get_or_create_account(name=...) résolvait vers un 2e compte CDP vide, créé par erreur le 21/07 lors de la régénération de clé API
Solution : WALLET_NAME corrigé vers le nom réel du wallet historique ("aria-wallet") ; vérifié empiriquement avant de committer ; aucune perte de fonds à aucun moment — agent_wallet_cdp_adapter.py (6cddf739)

------------------------------------------------------------

[DEPLOYE] Sujet    : Nom de wallet dupliqué dans un 2e fichier
Date : 2026.07.22  /  Probleme : x402_cdp_signer.py avait sa propre copie de WALLET_NAME, jamais synchronisée avec le fix ci-dessus
Solution : import depuis agent_wallet_cdp_adapter.py (source unique) — ne peut plus se désynchroniser — x402_cdp_signer.py (6cddf739)

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : État du wallet
Date : 2026.07.22  /  Probleme : —
Solution : 0xF046...37Ef ("aria-wallet"), ~12,80 USDC + 0,0166 ETH. Second wallet 0x584b...57Ef gardé comme secours, jamais utilisé par le code

------------------------------------------------------------

[CODE] Sujet    : Migration vers Smart Account CDP (Spend Permissions)
Date : 2026.07.22  /  Probleme : plafond de dépense actuel existe seulement en Python, aucune protection au niveau du contrat lui-même
Solution : direction actée par l'opérateur, RIEN construit — explicitement différé à une session dédiée (pas enchaîné juste après l'incident ci-dessus)

------------------------------------------------------------

[ETAT ACTUEL] Sujet    : Coinbase Agentic Wallets retenu face à MetaMask Agent Wallet
Date : 2026.07.14 vers 15  /  Probleme : choisir un produit d'agent-wallet pour le futur pilote capital réel (~10-15$) — MetaMask Agent Wallet nommément compatible Claude Code mais accès anticipé jamais ouvert.
Solution : Coinbase Agentic Wallets retenu (gratuit à créer, testable dès ~20$ USDC, MCP officiel compatible Claude, x402 natif, accessible immédiatement) — constat commun aux 4 options comparées (MetaMask/Coinbase/Trust Wallet/Cobo) : toutes fonctionnent sur plafond + liste blanche accordés une fois, jamais une confirmation humaine par transaction individuelle — plan complet docs/pilote-agent-wallet-10usd.md, rien codé à cette date (construit plus tard, voir la suite du fichier).

------------------------------------------------------------

[CONFIG] Sujet    : Clé API CDP créée par le dashboard avec 4 permissions par défaut (Transfer inclus)
Date : 2026.07.15 / Probleme : le dashboard Coinbase Developer Platform coche par défaut les 4 permissions (View/Trade/Transfer/Receive) sur une nouvelle clé API - Transfer actif aurait permis à tout outil connecté à cette clé (skill, MCP) de déplacer des fonds sans passer par aucun garde-fou applicatif, avant même que le wrapper de sécurité du pilote (plafond, slippage, kill-switch) n'existe.
Solution : clé corrigée en direct à View (lecture seule) uniquement avant toute validation - réflexe à répéter pour toute future clé créée via une interface web tierce, une permission cochée est un pouvoir actif immédiatement, indépendant de ce que le code fait aujourd'hui - docs/pilote-agent-wallet-10usd.md (cf. historique git 15/07)

------------------------------------------------------------

[DEPLOYE] Sujet    : Wallet agent CDP rejetait l'authentification (401) apres migration VPS
Date : 2026.07.21 / Probleme : le compte CDP restreint la cle API a une IP precise (allowlist) - l'ancienne IP (VPS pre-migration du 20/07) etait restee seule autorisee, toute authentification echouait silencieusement depuis la bascule.
Solution : allowlist IP mise a jour avec l'IP reelle du VPS actuel (detail dans aria-ops, prive) - cf. historique git 21/07

------------------------------------------------------------

[DEPLOYE] Sujet    : Collision de nom de wallet apres regeneration de cle API CDP
Date : 2026.07.22 / Probleme : get_or_create_account(name="aria-agent-wallet-pilot") resolvait vers un SECOND compte vide cree automatiquement lors de la regeneration de cle du 21/07, distinct du wallet historique (adresse 0xF04625162b616c5ad9788811b7be8CDd425B37Ef) qui detient le vrai solde - /agentwallet affichait 0 USDC alors que le solde reel on-chain etait non nul. x402_cdp_signer.py avait sa propre copie dupliquee du nom de wallet, non synchronisee.
Solution : WALLET_NAME = "aria-wallet" fixe en dur dans agent_wallet_cdp_adapter.py (verifie empiriquement, resout la bonne adresse) ; x402_cdp_signer.py importe desormais ce nom depuis ce meme module plutot que de le dupliquer - ne peut plus se desynchroniser. Aucune perte de fonds (le solde restait sur la blockchain, seule la lecture/signature pointait au mauvais endroit). Second wallet (vide, 0x584b2B35dac347B2317da0d21b95063de51257Ef) garde comme secours, jamais supprime - agent_wallet_cdp_adapter.py, x402_cdp_signer.py (commit 6cddf739)

------------------------------------------------------------

[DEPLOYE] Sujet    : Surveillance temps reel wallet agent (lecture seule) + commande /agentwallet
Date : 2026.07.16  /  Probleme : Aucune visibilite automatique sur les mouvements du wallet CDP reel (depots/sorties) et aucune commande Telegram pour consulter le solde reel (USDC/ETH/autres tokens).
Solution : agent_wallet_monitor.py (lecture seule, classification known/external_deposit/unexpected_outflow via Blockscout, registre append-only agent_wallet_movement_log, cycle heartbeat 10min gate ARIA_AGENT_WALLET_MONITOR_ENABLED) + commande /agentwallet (solde USDC+ETH+autres tokens avec valeur $ via dexscreener.fetch_tokens_batch) — agent_wallet_monitor.py (commits 16d2a505ce9c, 4c521e37c29e, 04356b851744)
------------------------------------------------------------
[CODE] Sujet    : execute_swap envoyait un montant en dollars au lieu de l'unite la plus petite du token
Date : 2026.07.16  /  Probleme : agent_wallet_cdp_adapter.execute_swap envoyait from_amount=str(amount_in_usd) (un montant en $) alors que l'API CDP attend une quantite en plus petite unite du token (ex. wei) — jamais exerce contre un vrai appel avant d'etre trouve, aurait fait echouer/mal-interpreter chaque swap reel des le premier essai.
Solution : bug trouve en revue avant le codage du declencheur swap/transfert, correction prevue dans le meme chantier (ETH natif exclu comme jambe de swap, aucune convention CDP documentee pour un sentinel ETH) — cf. historique git 16/07, agent_wallet_cdp_adapter.py

------------------------------------------------------------

[DEPLOYE] Sujet    : Pilote agent-wallet réel activé en prod (capital réel, décision autonome)
Date : 2026.07.18  /  Probleme : n/a — activation de fonctionnalité, pas un bug.
Solution : ARIA_AGENT_WALLET_PILOT_ENABLED=true en prod, vérifié en direct sur le conteneur (agent_wallet_pilot_enabled() == True). Doctrine "Option 2" : ARIA décide ET exécute seule, sans clic Telegram, dans les bornes déjà gravées (Règles absolues, Exception #3/#4) — agent_wallet_pilot_cycle.py (commit c9550624). Toute session future doit revérifier l'état réel du wallet/journal (agent_wallet_log) avant de supposer quoi que ce soit.

------------------------------------------------------------

[CODE] Sujet    : SDK CDP officiel — ValidationError Pydantic sur gasFee pour un swap trop petit
Date : 2026.07.19  /  Probleme : le SDK CDP (modèle CommonSwapResponseFees.gas_fee, champ déclaré non-nullable) plante en ValidationError quand l'API CDP omet ce champ — observé systématiquement sur des swaps de quelques centimes (probablement une contrainte de liquidité/quote de gas non calculable pour un montant minuscule), contradiction interne au SDK officiel, pas un bug côté ARIA.
Solution : échec catché et journalisé status="failed" (fail-closed, aucune perte de fonds réels dans les cas observés) — ne jamais fabriquer une valeur de gasFee pour contourner cette validation. Cooldown structurel dédié ajouté (is_structural_swap_failure, détection par sous-chaîne "validation error"/"pydantic", 7 jours — distinct du cooldown transitoire 60min et de momentum_blacklist.py) pour éviter de retenter en boucle un token structurellement cassé — agent_wallet_log.py/agent_wallet_pilot_cycle.py (commit b00d108c). Cause la plus probable identifiée mais non confirmée à l'époque : solde du wallet trop bas (sizing 3% d'un solde très inférieur au plan initial 10-15$) pour qu'un devis de gas fiable soit calculable. **Corrigé le 26/07, voir entrée ci-dessous — hypothèse infirmée, cause réelle confirmée.**

------------------------------------------------------------

[CODE] Subject  : Real root cause of the URANUS incident confirmed and fixed -- CDP's own API, not the wallet balance
Date : 2026.07.26 / Problem : the pilot has been gated ON since 18/07 (`ARIA_AGENT_WALLET_PILOT_ENABLED=true`, confirmed live) but had NEVER executed a single real swap -- all 5 real attempts (19/07, `URANUS` incident above) failed with the same Pydantic error, and the 7-day structural cooldown then silently kept it quiet for a full week (found while investigating the operator's request to look at this pilot's current state). The 19/07 entry's hypothesis ("wallet balance too low for a reliable gas quote") was never confirmed and turned out to be wrong.
Solution : traced the failure through the ACTUALLY INSTALLED `cdp-sdk` (1.47.1, still the latest release -- no version bump fixes this) rather than guessing. Confirmed identical on BOTH the "inline" `account.swap(...)` pattern ARIA uses AND the SDK's own documented "quote-then-execute" pattern (`account.quote_swap()` then `account.swap(swap_quote=...)`) -- both funnel through the exact same `create_swap_quote()` -> `CreateSwapQuoteResponse.from_dict()` -> `CommonSwapResponseFees` parse, so switching patterns would NOT have fixed it. Real cause: Coinbase's own create-swap-quote endpoint can return `liquidityAvailable: true` with a null `gasFee`/`protocolFee` for some low-liquidity routes -- unrelated to the swap AMOUNT (the 19/07 hypothesis), a genuine inconsistency in Coinbase's own API response for certain token routes. Fix: `agent_wallet_cdp_adapter._patch_cdp_swap_fee_validation()` relaxes `CommonSwapResponseFees.gas_fee`/`protocol_fee` to `Optional[TokenFee] = None` on the live Pydantic model (verified live against the installed SDK: the exact same `gasFee: null` payload that used to raise now parses cleanly) -- safe because neither field is ever read by ARIA's own code (`execute_swap` only reads `transaction_hash`/`to_amount`), so this only unblocks parsing of an otherwise-valid quote, changing nothing about transaction building, Permit2 signing, or broadcast. Applied lazily right before a real swap attempt (idempotent module-level guard), never at import time. `agent_wallet_cdp_adapter.py` -- 6 new dedicated tests (`test_agent_wallet_cdp_adapter.py`, exercising the REAL installed SDK, not a mock, per this project's "never write blind against the evolving API" doctrine), full suite green, `test_coherence.py` green. Not yet observed against a real end-to-end swap (the fix unblocks PARSING, the next real momentum BUY candidate found by the pilot will be the true confirmation) -- next real attempt should be watched via `agent_wallet_log.list_transactions()`.

------------------------------------------------------------

[CODE] Sujet    : Cooldown swap agent-wallet insensible à la casse du contrat
Date : 2026.07.18  /  Probleme : la requête SQL de cooldown après un échec de swap ne matchait pas token_out en ignorant la casse — un même contrat écrit avec une casse différente aurait pu re-déclencher un swap pendant la fenêtre de cooldown, contournant la protection.
Solution : requête corrigée en LOWER(token_out) = ? — agent_wallet_log.py (cf. historique git 18/07, commit c9550624).

------------------------------------------------------------

[CONFIG] Sujet    : Protection MEV pour le pilote — évaluée, DIFFÉRÉE (backlog #14/#216)
Date : 2026.07.22  /  Probleme : le stress-test VC (Codex Partie 11) proposait une protection MEV pour le pilote agent-wallet, touchant potentiellement l'exécution de capital réel — évaluation demandée AVANT tout code, jamais un chantier lancé à chaud.
Solution : DIFFÉRÉ après recherche sourcée (pas construit). Base n'a pas de mempool public (séquenceur centralisé FCFS) — le vecteur sandwich attack classique n'a structurellement rien à observer ; une étude dédiée sur les L2 à mempool privé confirme des rendements nets négatifs pour ce type d'attaque. Le slippage 10% déjà en place borne le pire cas absolu à ~1$ sur un swap de 10-15$, un montant qui ne couvre généralement même pas le coût de gas d'un attaquant. Solutions existantes écartées : Flashbots Protect/MEV-Blocker ne couvrent pas Base (doc officielle) ; RPC protégés tiers (BlockRazor/dRPC/GetBlock) payants et ajoutent une dépendance externe ; 1inch Fusion exigerait une réécriture complète du flux de signature (intention hors-chaîne + dépendance résolveurs tiers, intérêt économique incertain sur un ordre de quelques dollars) ; le Smart Account CDP (Spend Permissions/Paymaster, piste séparée #216/#225) n'apporte aucune protection MEV documentée. **À revisiter si** : le capital du pilote grossit significativement au-delà de quelques centaines de dollars, OU Base bascule vers un séquençage partagé/décentralisé (jalon 2026, Espresso/Flashbots — déjà en veille CLAUDE.md), OU une perte anormale est observée dans `agent_wallet_log.py` (prix attendu vs exécuté). Aucun fichier modifié — recherche uniquement.

------------------------------------------------------------

[CONFIG] Subject  : Smart Account migration — step 1, both smart accounts created (owner = existing EOAs)
Date : 2026.07.23  /  Problem : n/a — deliberate infrastructure step, not a bug. Prior note above ("Smart Account CDP... RIEN construit") is now partially superseded by this entry.
Solution : Two `EvmSmartAccount`s created via `cdp.evm.create_smart_account(owner=..., enable_spend_permissions=True)` (cdp-sdk 1.47.1, confirmed real Python API — `create_smart_account(owner: BaseAccount, name, enable_spend_permissions=False)`). A Smart Account requires an EOA owner by design — the two existing EOAs were reused as owners rather than freed up, which reverses an earlier assumption in this same conversation that they'd become available for another purpose (they remain in active use as the signing key behind each contract). Mapping: `aria-smart-wallet-one` (address `0x81e26A7552e15D3B4cE50b505A382773b1CA0089`) owned by `aria-wallet` (`0xF04625162b616c5ad9788811b7be8CDd425B37Ef`); `aria-smart-wallet-two` (address `0xDa1a87f38E78Eb9564D135804414E089519C2B1c`) owned by `aria-agent-wallet-pilot` (`0x584b2B35dac347B2317da0d21b95063de51257Ef`). Created with a dedicated new CDP API key (Manage-only permission, IP-allowlisted to the VPS) — separate from whatever key/scope the two owner EOAs were originally created under. `enable_spend_permissions=True` only turns the CAPABILITY on at the contract level — no concrete spend policy/limit has been configured yet, that remains a separate step. **Not yet wired to any ARIA code** (`agent_wallet_cdp_adapter.py` still operates on the original EOAs directly) — this is purely the wallet-creation step of the migration; rewiring the pipeline to actually transact through the Smart Accounts, funding them with USDC, and configuring real spend-permission policies are all still open, deliberately sequenced steps.

------------------------------------------------------------

[CONFIG] Subject  : Smart Account migration — step 1 REVISED same day: owners switched from CDP EOAs to operator's Tangem hardware wallets, purpose-named (VC vs swing-trading)
Date : 2026.07.23  /  Problem : n/a — design revision before any funding happened (both prior smart accounts were still empty), not a fix. The two CDP-EOA-owned smart accounts from the entry above were superseded within the same session, before any transfer was ever executed against them.
Solution : Operator decided the two smart accounts should be owned by physical Tangem hardware wallets instead of CDP-managed EOAs, for offline/emergency control independent of CDP, AND split by PURPOSE rather than arbitrary numbering (VC investment pocket vs swing-trading/momentum pocket) — matching the project's existing 85% VC / 15% trading split. `create_smart_account` only reads `owner.address` internally (verified in cdp-sdk 1.47.1 source — `owners = [owner.address]`, no signature required from the owner at creation time), so the Tangem's PUBLIC address alone was sufficient; no private key ever transited through Claude Code or the VPS.

The two prior CDP-EOA-owned smart accounts (`aria-smart-wallet-one` owned by `aria-wallet-X402`, `aria-smart-wallet-two` owned by `aria-wallet-transfert`) were renamed to `aria-smart-wallet-orphan-one`/`aria-smart-wallet-orphan-two` and abandoned (no delete capability exists in the CDP SDK for a deployed smart-account contract — same precedent as the earlier orphaned EOA documented above). Never funded, so no migration was needed.

New mapping:
- `aria-smart-st` (address `0x800027f61363EF304c5C2Afee811d9d4074B474c`) — owned by Tangem `0x33783cCb570Cb279C25F836806B5c4C3C8309777` ("tangem-01"). **This same address is `agent_wallet_pilot.ALLOWED_TRANSFER_ADDRESS`** (Exception #4, existing hardcoded transfer destination) — operator confirmed this reuse is intentional (same physical device), not a mix-up.
- `aria-smart-vc` (address `0x9C72AedD2836Edc24566E8B0Fd1825e0E1eFbF07`) — owned by a second, distinct Tangem address `0x85e3D8128a9b7be14065A4E36C1845041BF65d7F`.

Design refined in the same conversation (not yet built): on `aria-smart-st`, only SWAPS should run unattended (via a granted CDP Spend Permission — see `cdp.spend_permissions`/`EvmSmartAccount.use_spend_permission`, verified real in the SDK: grant requires one Tangem-signed transaction via `EvmClient.create_spend_permission`, actual spending afterward is signed by the granted "spender" — a separate CDP-managed EOA — with NO Tangem tap needed per trade); any TRANSFER (funds leaving the wallet to another address) requires a manual Tangem confirmation on BOTH `aria-smart-st` and `aria-smart-vc`, no exception. `aria-smart-vc` itself requires a Tangem tap for every action, investments included — matches (and hardware-enforces, beyond a Telegram click a code bug could bypass) the project's existing absolute rule that VC real capital always needs human validation.

**Open question, not yet verified**: whether the CDP Spend Permission mechanism can scope the granted spender to ONLY call a swap/DEX-router-style interaction (as opposed to a generic token transfer) at the contract level, or whether that swap-vs-transfer distinction still needs to be enforced in application code (same pattern as the existing separate `attempt_swap`/`attempt_transfer` functions for the current EOA-based pilot) rather than guaranteed by the on-chain permission alone. Needs checking before any real spend-permission policy is configured and relied upon.

Still open, nothing built: (1) fund `aria-smart-st`/`aria-smart-vc` (still empty), (2) verify the swap-vs-transfer scoping question above, (3) configure a real spend-permission policy for the `st` swap path, (4) write the code that executes via `use_spend_permission` instead of direct owner signing, (5) design the Tangem-gated direct-owner-tx path for `vc` (and for transfers on `st`), (6) rewire `agent_wallet_cdp_adapter.py` once the wave-2 translation pass is done (the file is in its batch). Real capital involved at every step — operator confirmation required before each one goes live.

------------------------------------------------------------

[DEPLOYE] Subject  : ALLOWED_TRANSFER_ADDRESS (Exception #4) changed to aria-wallet-transfert
Date : 2026.07.23  /  Problem : n/a — deliberate operator-directed change to a named guardrail constant, not a bug. The prior address turned out to be the operator's personal Tangem (`tangem-01`, meanwhile also reused as `aria-smart-st`'s owner, same day) — operator decided the pilot's allowed transfer destination should instead be a dedicated CDP wallet.
Solution : `agent_wallet_pilot.ALLOWED_TRANSFER_ADDRESS` changed from `0x33783cCb570Cb279C25F836806B5c4C3C8309777` to `0x584b2B35dac347B2317da0d21b95063de51257Ef` (`aria-wallet-transfert`, the renamed ex-"aria-agent-wallet-pilot" EOA — see the Smart Account migration entries above for its full history). Single source of truth confirmed before editing (`grep` across the codebase): only one constant declaration, every call site references it by name, no hardcoded duplicate to miss. `tests/test_agent_wallet_pilot.py` imports `pilot.ALLOWED_TRANSFER_ADDRESS` rather than hardcoding the value, so it needed no changes and passed unmodified — `agent_wallet_pilot.py` (commit pending). Full suite for the 3 affected test files (92 tests) green after the change.

------------------------------------------------------------

[CONFIG] Subject  : aria-smart-st / aria-smart-vc funded with small real test amounts
Date : 2026.07.23  /  Problem : n/a — deliberate first funding step, not a bug.
Solution : 2 USDC + ~$2 of ETH sent to EACH smart account from `aria-wallet-X402` (`account.transfer(token=..., network="base")`, cdp-sdk 1.47.1) — ETH amount (0.00104253 ETH/wallet) computed from a live spot price fetched just before the transfers ($1918.435/ETH, Coinbase public spot API). Confirmed via `list_token_balances` after the transfers: both `aria-smart-st` (`0x800027f61363EF304c5C2Afee811d9d4074B474c`) and `aria-smart-vc` (`0x9C72AedD2836Edc24566E8B0Fd1825e0E1eFbF07`) hold exactly 2 USDC + 0.00104253 ETH each. Source wallet (`aria-wallet-X402`) balance before this: 0.0166 ETH + 12.728007 USDC — plenty left for gas on future transactions. No spend-permission policy configured yet, no swap/transfer code wired to these wallets — purely a funding step ahead of the still-open items in the entry above (#41: verify swap-vs-transfer scoping, configure real spend-permission policy, write `use_spend_permission`-based execution code, rewire `agent_wallet_cdp_adapter.py`).

------------------------------------------------------------

[DEPLOYE] Subject  : Live pilot wallet-name bug fixed in prod + duplicate EOA incident + final orphan naming convention
Date : 2026.07.23  /  Problem : discovered live via `agent_wallet_monitor.py`'s own Telegram alerts ("SORTIE NON INITIÉE PAR ARIA") + a new "aria-wallet" EOA (`0x8e71C3e9396ded76AdA6EA56cD3c315C3D67D79b`) auto-created today by the STILL-RUNNING prod container, which had the OLD `WALLET_NAME`/`ALLOWED_TRANSFER_ADDRESS` baked into its already-loaded code — editing the source file alone never reaches a running container (per this project's own established fact: rebuild required, `git pull` + restart isn't enough). Exact same failure mode as the 21/07 incident, this time self-inflicted by the same-day rename rather than an API key change. Separately, the two "TOKEN SUSPECT" alerts seen in the same Telegram feed at the same time are a REAL address-poisoning attack (lookalike addresses `0x80005B5aCEB5cc0164c069Db15E0af4A1B7B474C` / `0x9c727dA07Ec5e785a4926860250FC7Aebc9Fbf07` mimicking `aria-smart-st`/`aria-smart-vc`'s real prefixes/suffixes) — correctly caught and labeled by the monitor, no action needed beyond never manually copying an address from transaction history without full character-by-character verification.
Solution : Committed + pushed (`d19b0513`) + deployed (`./vanguard/deploy.sh`, blue-green, verified live through nginx) the fix already made to `agent_wallet_cdp_adapter.py`/`agent_wallet_pilot.py` earlier the same day. The accidental duplicate EOA and the two now-superseded CDP-EOA-owned smart accounts were renamed to a clear "abandoned" convention (final form, after CDP's naming regex rejected underscores — alphanumeric + hyphens only, `^[A-Za-z0-9][A-Za-z0-9-]{0,34}[A-Za-z0-9]$`): `0-OLD-wallet-aria` (was `0x8e71C3e9...`), `0-OLD-smart-wallet-one` / `0-OLD-smart-wallet-two` (were `aria-smart-wallet-orphan-one`/`-two`). No delete capability exists in the CDP SDK for either EOAs or smart accounts (both are real on-chain addresses/contracts) — renaming to an unambiguous "0-OLD-" prefix, which sorts to the top of the dashboard list, is the best available substitute for deletion. **Lesson for any future CDP account rename**: a source-level fix to a hardcoded account name is NOT sufficient by itself — the running production container must also be redeployed before the rename is safe, or the live process will repeat this exact incident on its next heartbeat cycle.

------------------------------------------------------------

[CONFIG] Subject  : Smart Account design refined -- swap-vs-transfer split now resolved via CDP Policy Engine (not just Spend Permission)
Date : 2026.07.23  /  Problem : the open question from the entries above (can a Spend Permission be scoped to swap-only vs generic transfer at the contract level) was investigated properly (real ABI inspection + web research + real SDK method/field inspection, never guessed). Finding: `SpendPermissionManager.spend()` is PURELY a "transfer up to `value` of `token` to the spender" primitive (confirmed via `cdp.spend_permissions.SPEND_PERMISSION_MANAGER_ABI`) -- it has NO concept of "swap," and Coinbase's own docs confirm this explicitly ("this approach does not enable apps to make arbitrary external calls from user accounts"). So a Spend Permission alone cannot guarantee "swap-only" -- the withdrawn value briefly sits in the spender's own custody before the spender executes the actual swap and (if the code is correct) returns the output to the smart account. This is a real, distinct risk from bad-token/rug risk: a code bug or a compromised spender key could misdirect funds during that window, independent of token quality.
Solution (design, nothing built yet) : combine TWO CDP mechanisms rather than relying on Spend Permission alone. (1) Spend Permission on `aria-smart-st` still governs value/token/period (operator wants this UNCAPPED for swaps specifically, capped/blocked for transfers -- see below). (2) A CDP Policy (`cdp.policies.create_policy`, scope="account", confirmed real in cdp-sdk 1.47.1 -- `PoliciesClient.create_policy/delete_policy/get_policy_by_id/list_policies/update_policy`, rule type `SendEvmTransactionRule` with `EvmAddressCriterion`/`EvmDataCriterion` letting a rule match on destination contract address AND call data/method) attached to the SPENDER account, allowlisting ONLY the specific DEX router address + swap method selector, rejecting everything else including a raw ERC-20 `transfer()` call. This is enforced by CDP's own signing-layer infrastructure (Trusted Execution Environment per Coinbase's own docs), not application code discipline -- closes the gap the Spend-Permission-alone design left open.
Operator's actual intent (23/07, clarified after the above): `aria-smart-st` — swaps UNCAPPED in volume/frequency (Spend Permission allowance effectively unlimited for the swap path specifically, restricted to only ever be usable for swaps via the Policy above), but any TRANSFER to another wallet always requires the Tangem owner directly (never delegated to the spender at all -- no Spend Permission/Policy path should ever authorize a raw transfer). `aria-smart-vc` — no delegation of any kind; every action (swap or transfer) requires the Tangem owner's direct signature, no spender, no policy needed.
Still open, nothing built: (1) create the actual spender CDP account (new, dedicated, never reused), (2) create the Spend Permission on `aria-smart-st` (account=aria-smart-st, spender=new account, token=USDC, allowance=effectively unbounded per operator's explicit choice after being warned of the tradeoff, period=TBD), (3) create + attach the CDP Policy on the spender account allowlisting only the intended DEX router/swap method, (4) verify end-to-end on a tiny real amount before trusting this for the live pilot, (5) write the `use_spend_permission`-based execution code in `agent_wallet_cdp_adapter.py` (still pending wave-2 translation completion on that file), (6) confirm whether `cdp.policies.create_policy` needs the "Manage" API-key permission (Non-custodial section) -- not yet tested, prediction only.

------------------------------------------------------------

[CONFIG] Subject  : Option A vs Coinbase Agentic Wallets -- deep comparison completed (3-agent research workflow), design refined with real cdp-sdk source citations
Date : 2026.07.23  /  Problem : n/a -- operator asked to "compare and simulate" before committing further engineering time, given the design above was still speculative on several points (exact custody mechanics, whether the Policy Engine could really enforce swap-only, whether a ready-made Coinbase product might be a better fit).
Solution : Real research (reading the installed cdp-sdk 1.47.1 source directly, file:line citations, not docs summaries) confirmed and refined the plan above, and evaluated Coinbase's separate "Agentic Wallets" product (docs.cdp.coinbase.com/agentic-wallet/welcome, launched ~Feb 2026) as an alternative. Full reports archived in this session's workflow transcript; key confirmed facts below.

**Verdict: Agentic Wallets REJECTED for this design.** It is a genuinely separate product built on plain CDP Server Wallet v2 EOAs (MPC + AWS Nitro Enclave custody), NOT a layer on top of the EVM Smart Accounts already built today. It has no hardware-wallet-owner concept at all -- no field, no mechanism, nothing analogous to `EvmSmartAccount.owner`. It cannot express "transfer requires a physical Tangem tap" at any price -- this is a missing product concept, not a missing config option. Adopting it would mean abandoning the already-created, already-funded `aria-smart-st`/`aria-smart-vc` for new wallets (email-OTP onboarding) plus a real on-chain fund migration, plus bolting a Node.js-only CLI/MCP toolchain onto an all-Python backend (no Python SDK exists for this product). Not recommended.

**Option A (Smart Account + Spend Permission + Policy Engine) confirmed as the only viable path, with the exact mechanics now nailed down from source:**
- Smart Accounts support only ONE owner today (`create_evm_smart_account_request.py`: "Today, only a single owner can be set... array to allow setting multiple owners in the future" -- no `add_owner` method exists in 1.47.1).
- The CDP Policy Engine can ONLY attach to a plain EOA account (`account_policy` field exists on `create_evm_account_request.py`/`update_account`), never directly to a Smart Account (no such field on `create_evm_smart_account_request.py`/`update_evm_smart_account_request.py`). This is *why* the Policy must live on the spender EOA, not on `aria-smart-st` itself -- forced by the SDK, not a stylistic choice.
- Direct smart-account swaps DO keep custody inside the smart account (confirmed: `send_swap_operation.py` -- the smart account is the on-chain taker, output lands back in it) -- but every such call requires a LIVE owner signature via `owner.unsafe_sign_hash(...)` (`send_user_operation.py:79-81`), called synchronously, inline, every time, with zero session-key/cached-authorization escape hatch in the SDK. For `aria-smart-st`/`aria-smart-vc`, `owner` is the Tangem address -- meaning direct smart-account swaps would require a physical Tangem tap per swap, defeating the automation goal entirely.
- **Spend Permission (`spend()` on the SpendPermissionManager, confirmed contract address `0xf85210B21cC50302F477BA56686d2019dC9b67Ad`) is therefore not optional -- it's the ONLY value-movement path in the SDK that does NOT call the owner's signature on every use.** Granting it (one-time) still requires the Tangem's live signature; using it afterward (the spender pulling funds) does not.
- **NEW, previously-unidentified hard prerequisite: a "Tangem-to-`BaseAccount` signing bridge" must be built** -- a custom Python class satisfying `eth_account.signers.base.BaseAccount`'s interface (`.address`, `.sign_message`, `.unsafe_sign_hash`, `.sign_transaction`) that, on each call, gets a real signature from the physical Tangem device (most likely via a WalletConnect v2 session to the operator's phone -- Tangem's consumer app supports standard `eth_signTypedData_v4`/`eth_sendTransaction` this way, but nothing off-the-shelf bridges it to cdp-sdk). Nothing like this exists yet. Needed for: the one-time Spend Permission grant on `aria-smart-st`, AND 100% of every action on `aria-smart-vc` (by design). **This is the single largest unbuilt piece of the whole plan (2-4 days of the 5-9 day total estimate), bigger than the Policy/Spend-Permission wiring itself.**
- Policy Engine mechanics for "swap yes, transfer no", confirmed from `cdp/policies/types.py`: no native "this is a swap" operation exists for our account type (`CreateEndUserEvmSwapRule` exists but is scoped to CDP's *End User/embedded wallet* product line, not applicable here). Must instead combine `EvmAddressCriterion` (allowlist the DEX router CDP's swap backend routes through -- **not hardcoded in the SDK, must be verified empirically per real quotes on Base before trusting it, and re-checked if it ever changes**) with `EvmDataCriterion` (decode calldata against `KnownAbiType.erc20/erc721/erc1155` -- e.g. an explicit `reject` rule matching ERC-20 `transfer`/`transferFrom` regardless of destination, as defense-in-depth). Rule evaluation is top-down first-match, default-deny if nothing matches.
- **Critical gap confirmed in the CURRENT "unlimited" design decision (23/07)**: the Policy Engine and Spend Permission together only ever gate on WHO (spender) and WHERE (destination/method) -- **neither gates on AMOUNT**. With the allowance set to "unlimited" per the operator's own explicit choice, a sizing bug that attempts to swap 100% of the wallet instead of an intended 2% position is NOT stopped by anything in this design -- it would succeed (bounded only by whatever the wallet actually holds, never more, no leverage possible, and a raw transfer-out is still blocked). **A real numeric cap on the Spend Permission allowance (not "unlimited") is needed for this design to actually deliver "a real safety net against errors," which was the operator's explicitly stated goal when this comparison was requested.**
- Concrete build sequence confirmed (see full research for exact code): (1) build the Tangem bridge first, test on a trivial gesture, (2) one-time Spend Permission grant via the bridge (try `cdp.evm.create_spend_permission()` first, but the manual `approve()` FunctionCall via `send_user_operation` is the safer fallback -- unverified whether the convenience wrapper even works for an externally-owned account), (3) create the dedicated spender EOA (never reused), (4) create + attach the Policy to the spender, (5) runtime loop: spender pulls funds via `use_spend_permission`, swaps with `spender.swap()` (plain EOA swap, simpler than the smart-account path), then MUST explicitly transfer the output back to `aria-smart-st` (a real, distinct code step -- if missed/buggy, funds sit stranded in the spender, not stolen, but misplaced and needing manual recovery), (6) transfers on `aria-smart-st` always go through `aria_smart_st.transfer()` directly (Tangem bridge, every time, by design), (7) `aria-smart-vc` never uses Spend Permission/Policy at all -- every action through the Tangem bridge directly.
- Effort estimate: ~5-9 engineering days total, dominated by the Tangem bridge (least confident number, most novel piece, should be prototyped early and cheaply before committing to the rest).

**Recommendation adopted**: proceed with Option A, but do NOT configure the Spend Permission as "unlimited" as originally planned -- replace it with a real, calibrated numeric ceiling before this is trusted for live use. Next concrete steps, in order: (1) prototype the Tangem↔BaseAccount signing bridge on a trivial gesture, (2) decide and configure a real Spend Permission ceiling (not unlimited), (3) create the dedicated spender + Policy, (4) end-to-end test on a tiny real amount, (5) wire the execution code into `agent_wallet_cdp_adapter.py` (blocked on wave-2 translation completing on that file). Nothing built yet beyond this session's research and design.

------------------------------------------------------------

[CODE] Subject  : Smart Account swing pocket -- Model B chosen, safety-envelope foundation built (spend-permission cap)
Date : 2026.07.23 / Problem : the migration design (entries above) was fully researched but nothing was built. Operator confirmed the direction explicitly: the SWING pocket (aria-smart-st) must trade autonomously via the delegated spender (no Tangem tap per swap), while every TRANSFER out and every VC action still requires the Tangem owner. Operator also settled the safety-critical open decision from the research above: the Spend Permission allowance is NOT unlimited -- $50/week auto-renewing ("prudent to start, scale once proven"), which restores the "real safety net against errors" the research flagged as missing.
Solution : new `agent_wallet_smart_swing.py` (DORMANT, gate `ARIA_SMART_SWING_ENABLED` OFF, wired to nothing) holding (1) the verified on-chain identities read from the DEPLOYED `agent_wallet_monitor.MONITORED_WALLETS` (never memory): aria-smart-st `0x800027f61363EF304c5C2Afee811d9d4074B474c`, aria-smart-vc `0x9C72AedD2836Edc24566E8B0Fd1825e0E1eFbF07`, spender `0x8e71C3e9396ded76AdA6EA56cD3c315C3D67D79b` (**already created**, the research's "create spender" step is done), Tangem owners `0x33783cCb...`/`0x85e3D812...`; (2) `build_spend_permission_input()` -- pure, network-free, encodes the $50/week cap as a STRUCTURAL invariant (`_MAX_SANE_ALLOWANCE_USD=10_000` guard makes an unlimited/absurd allowance impossible to produce even from a careless edit -- a real raise is a deliberate operator decision, never silent). 19 tests. Full suite green, test_coherence.py green (not yet committed at the time of this entry).
**SDK surface verified against the really-installed cdp-sdk 1.47.1 (never guessed, this is the reference for the next build step):**
- `SpendPermissionInput(account, spender, token, allowance:int-atomic, period_in_days)` builds/validates; `SPEND_PERMISSION_MANAGER_ADDRESS=0xf85210B21cC50302F477BA56686d2019dC9b67Ad`.
- `EvmClient.create_spend_permission(spend_permission, network, paymaster_url?, idempotency_key?) -> EvmUserOperation` (the one-time grant, needs the Tangem owner's signature).
- The spender (`EvmServerAccount`, plain CDP-managed EOA, NO Tangem) has `use_spend_permission(spend_permission, value:int, network) -> str` (pulls funds, NO owner signature) AND `swap(AccountSwapOptions) -> AccountSwapResult` AND `transfer(...)` (the last is what the Policy must box in).
- **Swap output lands in the SPENDER, not in a chosen recipient** -- `AccountSwapOptions` has NO recipient field. So the spender MUST transfer the output back to aria-smart-st, and since the OUTPUT token varies per trade, the Policy's return-transfer carve-out must allowlist "an ERC-20 transfer whose recipient == aria-smart-st, token-AGNOSTIC" (via `EvmDataCriterion` on the `to` parameter), NOT a fixed token contract. This is the single most delicate, safety-critical piece -- its real enforcement MUST be validated against live CDP on a tiny amount before any grant is trusted.
- Policy shapes confirmed: `cdp.policies.CreatePolicyOptions(scope, description, rules)`, `SendEvmTransactionRule(action:'accept'|'reject', operation:'sendEvmTransaction', criteria)`, `EvmAddressCriterion(type, addresses, operator)`, `EvmDataCriterion(type, abi, conditions)`; top-down first-match, default-deny. The DEX router `to` address is obtainable dynamically from a real quote (`QuoteSwapResult.to`) -- must be confirmed STABLE across several real quotes before hardcoding it into the allowlist.
**Still open, in order (next chunk, hardware/verification session):** (1) build `build_swap_only_policy(router_address)` (rule1 allow swap router, rule2 allow token-agnostic transfer only to aria-smart-st, default-deny) + validate it against live CDP on a tiny amount; (2) build the guarded execution path (`use_spend_permission` -> swap -> return-to-SA) with the same app-layer guards as agent_wallet_pilot.py (per-tx cap, real-balance check, forced 10% slippage, /stop, logging); (3) the one-time Spend Permission grant (Tangem tap) -- still needs the Tangem<->BaseAccount signing bridge (the 2-4 day piece, unbuilt); (4) fund aria-smart-st with real USDC; (5) end-to-end test on a tiny real amount before flipping the gate. `agent_wallet_smart_swing.py` (new) / `test_agent_wallet_smart_swing.py` (new).

------------------------------------------------------------

[CODE] Subject  : Exception #3 granted -- real agent-wallet pilot allowed to decide+execute swaps without a per-transaction Telegram click
Date : 2026.07.16  /  Problem : n/a -- deliberate, repeated operator decision (not a bug), extending the Sepolia-style autonomous-execution doctrine to a REAL mainnet pilot (~10-15$) for the first time.
Solution : Non-negotiable bounds fixed at grant time -- hard 10-15$ cap checked against the real balance before every attempt (fail-closed if balance unavailable), swap-only (no generic transfer/withdraw), slippage forced <=10%, `/stop` checked every attempt, dedicated isolated wallet (never mixed with the main Vanguard ZHC wallet), structurally separate from `wallet_guard.escalate_spend/resolve_spend` (locked in `test_coherence`), every attempt logged via `agent_wallet_log.py`. Full design: `docs/pilote-agent-wallet-10usd.md` §8. Gate `ARIA_AGENT_WALLET_PILOT_ENABLED` built dormant first, activated in prod 18/07 (see entry above, "Pilote agent-wallet réel active en prod").

------------------------------------------------------------

[CODE] Subject  : Exception #4 granted -- real USDC transfer capability added to the pilot, alongside swap
Date : 2026.07.16  /  Problem : n/a -- deliberate operator decision, settled via AskUserQuestion after clarifying the destination address, to keep the pilot from becoming a generic theft vector.
Solution : `agent_wallet_pilot.attempt_transfer()` added, bounded at grant time by: a single destination address hardcoded in `agent_wallet_pilot.ALLOWED_TRANSFER_ADDRESS` (never a free parameter, never a configuration value editable without code review -- value has since changed once, see the "ALLOWED_TRANSFER_ADDRESS (Exception #4) changed to aria-wallet-transfert" entry above), a DISTINCT gate `ARIA_AGENT_WALLET_TRANSFER_ENABLED` (OFF by default, required IN ADDITION to the global pilot gate), the same 10-15$ cap, the same `/stop` kill-switch, and the same logging (`agent_wallet_log.py`, `to_address` column added). Destination verified distinct from the Sepolia testnet wallet (`0x8c8c163DA8099Ef7B553Ee9D4D56EdE8c205Cae5`) to avoid mixing testnet/mainnet.

------------------------------------------------------------

[DEPLOYE] Subject  : Autonomous decision loop ("option 2") wired to heartbeat
Date : 2026.07.18  /  Problem : n/a -- feature delivery, not a bug.
Solution : `agent_wallet_pilot_cycle.py`, heartbeat task `agent_wallet_pilot_cycle` (same gate `ARIA_AGENT_WALLET_PILOT_ENABLED`) -- reuses the already-tested momentum pipeline (honeypot+R/R+LLM guard), sizing per the rule decided 16/07 (#203: 3% of the real balance, capped at the 10-15$ ceiling, never the full balance), open-position detection via actually-held tokens (never an ambiguous balance threshold that could confuse fee dust with a real position), 60min cooldown after a technical swap failure (distinct from `momentum_blacklist.py`, reserved for real threats). v1 scope: one entry at a time, no automatic exit. An "x402 unlocks a decision blocked by missing data" component (requested by the operator the same day) was DEFERRED -- the one endpoint that could have helped (`ethereum-token-verification`) was confirmed still broken since 17/07. Real bug fixed in passing, unrelated to this deferral: `cybercentry_insight.verify_and_remember_wallet()` was paying on every call without ever checking vector memory first -- fixed with a ~7-day cache.

------------------------------------------------------------

[CODE] Subject  : Future milestone noted, not built -- winner's tax routed to ALLOWED_TRANSFER_ADDRESS
Date : 2026.07.16  /  Problem : n/a -- forward-looking note, no activation condition met yet.
Solution : once ARIA reaches several hundred real trades with a winrate >80%, a 30% tax on each winning trade would be routed to `ALLOWED_TRANSFER_ADDRESS` (Exception #4). Activation condition out of reach at this date -- nothing built, no code path exists.

------------------------------------------------------------

[CODE] Subject  : Tangem<->BaseAccount signing bridge -- Node.js prototype built (the 2-4 day unbuilt piece from the migration plan above)
Date : 2026.07.24 / Problem : the Smart Account migration plan flagged a hard prerequisite with no off-the-shelf Python solution: the one-time Spend Permission grant on aria-smart-st, and every action on aria-smart-vc, needs the Tangem owner's real signature. Verified before building (never assumed): the only maintained Python WalletConnect package, `pyWalletConnect`, implements the WALLET role only ("built for the wallet part, which establishes a link with the dapp and receives requests") -- it cannot initiate a session or send a signing request, the exact opposite of what ARIA needs (ARIA must play the DAPP role). No Python SDK exists for the dApp side; the only maintained SDK is the official JavaScript `@walletconnect/sign-client`. Hand-rolling the WalletConnect v2 relay/encryption protocol in Python was judged too risky for a component that will eventually touch real capital (operator's own explicit second opinion, confirmed: "Implémenter WalletConnect v2 à la main en Python... est excessivement risqué").
Solution : new isolated package `packages/tangem-wc-bridge/` (Node.js, own package.json, structurally separate from both aria-core and vanguard) -- a minimal HTTP service using the official `@walletconnect/sign-client` SDK (dApp role), 3 endpoints (`POST /wc/connect`, `GET /wc/status`, `POST /wc/request-signature`), bound to `127.0.0.1` ONLY (verified live via `ss -tlnp`, never `0.0.0.0`), defaults to Base Sepolia (`eip155:84532`) unless explicitly overridden, allowlists only `eth_sendTransaction`/`eth_signTypedData_v4`/`personal_sign` (rejects any other method even if a session technically negotiated more -- keeps blast radius small). No persistent session across restarts, no automatic reconnect loop -- one-shot setup tool, a human is expected to be present for every use (that IS the point of a Tangem tap). Never holds a private key -- only relays WalletConnect JSON-RPC to the Tangem app and returns whatever comes back after the physical NFC tap. Python-side client `aria_core.tangem_bridge` (httpx, same fail-safe dataclass doctrine as every other service client in this project -- `available`/`error` fields, never raises on an expected failure, never invents a signature) -- 15 tests (connect/poll/wait/request-signature success + non-200 + bridge-unreachable paths), full suite green, `test_coherence.py` green.
**Verified real, not assumed**: server binds to 127.0.0.1 only (confirmed via `ss -tlnp`), 404/error routing works, fail-closed without `WALLETCONNECT_PROJECT_ID` set (process exits with a clear message rather than starting half-configured). **Deliberately NOT built yet** (per the plan's own ordering -- this was step 1 only): wrapping this client into an `eth_account.signers.base.BaseAccount` subclass consumable by cdp-sdk for the actual Spend Permission grant. The exact signing payload shape CDP expects for a Smart Account UserOperation is not yet verified against a live Tangem tap -- building that wrapper before the raw round-trip below was proven working would have risked shipping untested glue code. `packages/tangem-wc-bridge/` (new: `package.json`/`server.js`/`README.md`) / `aria_core/tangem_bridge.py` (new) / `test_tangem_bridge.py` (new).

------------------------------------------------------------

[DEPLOYE-LOCAL] Subject  : Tangem bridge END-TO-END round-trip PROVEN with the operator's real card (step 1 success criterion met)
Date : 2026.07.24 / Problem : the bridge above was built and unit-tested but the actual pairing -> physical NFC tap -> signature round-trip had never been exercised against the operator's real Tangem device -- the genuine "does this whole thing actually work" question.
Solution : ran live with the operator (their real Reown project ID + phone). Sequence proven: `/wc/connect` produced a real WalletConnect v2 URI -> pasted into the Tangem app -> connection approved -> `/wc/status` returned `connected` with address `0x33783cCb570Cb279C25F836806B5c4C3C8309777` (exactly `agent_wallet_smart_swing.TANGEM_ST_OWNER`, the aria-smart-st owner) -> `/wc/request-signature` (`personal_sign` of "Hello ARIA!") -> operator tapped their card -> a real 65-byte ECDSA signature came back and was CRYPTOGRAPHICALLY VERIFIED: `eth_account.Account.recover_message` recovered exactly `0x33783cCb...` (MATCH True). So the bridge relays a real hardware-wallet signature end-to-end, verified against the expected address -- not just "a response came back".
**Two real environment constraints discovered live** (both matter for every future use of this bridge): (1) Tangem's WalletConnect does NOT support testnets -- Base Sepolia was rejected with an explicit "réseaux non pris en charge" error, so the bridge had to run on Base MAINNET (`eip155:8453`). Message-signature only was used deliberately (`personal_sign`, never a transaction) so NO funds could move regardless of the network. (2) The pairing/signature flow is timing-sensitive: leaving the phone mid-flow, or Ctrl-C'ing the waiting curl, desyncs/deletes the WalletConnect session ("Record was recently deleted"). Two helper scripts added to make this repeatable without manual juggling: `test-signature.sh` (fully self-contained: connect -> wait-for-approval -> sign -> capture) and `poll-and-sign.sh <connectionId>` (server-side watcher, for when the operator can only touch the phone). QR pairing proved unreliable for a ~230-char URI (camera misreads); the working method was pasting the URI as text into the Tangem app.
**Next milestone (step 2, still open)**: build the `eth_account.signers.base.BaseAccount` wrapper around `aria_core.tangem_bridge` now that the raw round-trip is proven, then use it for the one-time Spend Permission grant on aria-smart-st ($50/week cap, `agent_wallet_smart_swing.build_spend_permission_input`). `packages/tangem-wc-bridge/test-signature.sh`/`poll-and-sign.sh` (new).

------------------------------------------------------------

[DEPLOYE-LOCAL] Subject  : Tangem bridge threat-model hardening -- tx-signing blocked by default (operator concern: no hole that drains a wallet)
Date : 2026.07.24 / Problem : operator asked explicitly for no security hole that could let an attacker "drain the wallets or hijack the hash." Audit run: the bridge is wired to NO automatic execution path (no boot/heartbeat/cron/systemd/docker -- confirmed by grep), the Python client is imported by no production flow, and the Node service is not a daemon (only launched manually for a supervised setup, then stopped). The structural protections are strong (private key never on the server -- it's on the physical Tangem card; every signature needs a physical NFC tap; 127.0.0.1-only bind; non-daemon). The one residual gap in the code: the method allowlist included `eth_sendTransaction` + `eth_signTypedData_v4` -- the only two methods that can actually move funds or grant a token allowance (a Permit) -- so a local attacker present WHILE the service ran and a session was live could in principle slip in a malicious tx-signature request, hoping the operator taps without reading the screen.
Solution : `server.js` now allowlists ONLY `personal_sign` by DEFAULT (a message signature can never move funds, any network). `eth_sendTransaction`/`eth_signTypedData_v4` are blocked unless `TANGEM_BRIDGE_ALLOW_TX_SIGNING=true` is explicitly set -- a deliberate per-session operator action (needed later for the one-time Spend Permission grant, never the default). So even a local attacker, during a live session, can by default only obtain a message signature that moves no funds, not a fund-moving transaction. The `requiredNamespaces.methods` negotiated at connect time uses the same constant, so the session itself only advertises what's allowed. Verified: default -> `["personal_sign"]` only; with the flag -> tx methods added; `node --check` clean. Honest residual documented (README "modèle de menace"): once the tx flag IS enabled, the FINAL defense is the operator reading the Tangem screen before tapping (Tangem always shows exactly what's being signed) + the eventual on-chain $50/week Spend Permission cap + swap-only Policy bounding the worst case. `packages/tangem-wc-bridge/server.js`/`README.md`.

------------------------------------------------------------

[DEPLOYE-LOCAL] Subject  : Tangem bridge -- 3 residuals from an external security audit closed (shared-secret auth, session close, wording)
Date : 2026.07.24 / Problem : an external LLM audit (operator-relayed) validated the design ("no critical flaw") but flagged 3 real residuals: (1) NO auth between the Python caller and the local Node service -- any local process reaching 127.0.0.1:port while it ran could request a signature of an allowed method; (2) a WalletConnect session left open after the first tap could be reused for more signatures while the service still ran; (3) calling a `personal_sign` "harmless" was slightly too strong (it moves no funds, but can still be used for auth/replay in other contexts).
Solution : (1) shared-secret auth -- `server.js` now requires `Authorization: Bearer <token>` on EVERY route (fail-closed 401 before any handler). The token comes from `TANGEM_BRIDGE_TOKEN` or, if unset, is generated at startup and written to a 0600 file (`<tmpdir>/tangem-bridge-token`) that `aria_core.tangem_bridge._auth_token()` + the two helper scripts read. Closes the "any local process" gap against a DIFFERENT local user (not against an already-root attacker -- documented honestly, defense-in-depth not load-bearing). (2) new `POST /wc/disconnect` endpoint + `tangem_bridge.disconnect()`; both helper scripts now call it at the end so a session can't be reused. (3) README wording corrected to "moves no funds" instead of "harmless". `server.js`/`README.md`/`test-signature.sh`/`poll-and-sign.sh` / `aria_core/tangem_bridge.py` -- 6 new tests (`test_tangem_bridge.py`: bearer header on every call, token-from-file fallback, no-header-when-no-token, disconnect success/unreachable), 20 tests total green, `test_coherence.py` green, `node --check` clean. Structural protections unchanged (key on card only, physical tap, 127.0.0.1-only, non-daemon) -- these residuals were all "attacker already local + service deliberately running", now further narrowed.

------------------------------------------------------------

[ETAT ACTUEL] Subject  : "Module Yield" thesis (idle capital -> Morpho/Aave) -- evaluated, construire_plus_tard
Date : 2026.07.24 / Problem : backlog #32 asked to evaluate placing idle cash (paper portfolio between positions, or the real agent-wallet pilot) into a DeFi yield protocol on Base instead of letting it sit idle. Evaluated via a 3-agent research workflow, every critical fact re-verified live before being trusted.
Solution : construire_plus_tard, not now, on either front. Paper (1M$ fictitious): a simulated yield produces no real benefit, only diagnostic value, not worth the effort against the test's actual goal (observing ARIA's trading behavior). Real (agent-wallet pilot, $10-15 cap): Aave V3 Base USDC confirmed live at 3.40% APY -> ~$0.34-0.51/year, negligible against (1) no official Python SDK for Aave or Morpho (raw contract calls + approval + gas handling needed, same effort class as the Basenames RPC work done the same day), (2) two REAL, dated, independently-confirmed security incidents on the Base/Aave ecosystem: the 18/04/2026 Aave/KelpDAO-LayerZero bridge compromise (~$292M rsETH minted fraudulently, ~$177-190M bad debt landed on Aave, rsETH markets frozen on Aave/SparkLend/Fluid) and the 17/02/2026 Moonwell incident ($1.78M lost to an AI-generated oracle-wrapper bug that treated a cbETH/ETH ratio as a direct USD price -- a directly relevant cautionary tale for ARIA, itself an AI that could generate similarly-flawed code), and (3) a NEW governance decision needed (the pilot is structurally swap-only + one bounded transfer capability to a single fixed address -- a DeFi deposit is outside this scope, needs its own named exception, not a simple technical add-on). Coinbase's native "USDC Rewards" program (zero smart-contract risk) re-confirmed even less viable than first thought: 3.35% APY (not the 4.1% sometimes advertised), restricted to "US-based individuals and businesses" AND requires having "previously held a USDC balance in your Coinbase (Retail, Business, or Prime) account" -- two cumulative eligibility barriers.
Conditions to revisit: once real capital meaningfully exceeds the pilot's current $10-15 cap (e.g. a future real VC pocket) -- at that point, check USDC Rewards eligibility FIRST (zero added risk if eligible), only consider a Morpho ERC-4626 vault (reputable curator, never picked on rate alone) or Aave otherwise, and any contract/integration code must be human-written and reviewed, never ARIA alone (the Moonwell precedent is exactly this failure mode). A pure diagnostic overlay (simulated APY applied to the paper ledger's idle cash, zero on-chain calls) remains a near-zero-cost option if the operator wants an informative number before then -- backlog only, never urgent. `docs/aria-learning-inbox/` not used for this entry (self-contained verdict, not an ongoing watch item).

------------------------------------------------------------

[ETAT ACTUEL] Subject  : Sepolia test swap (`onchain/sepolia_wallet.py`) -- set aside, explicit operator decision, code NOT deleted
Date : 2026.07.24 / Problem : backlog item #27 ("unblock the Sepolia test swap -- router/exit-token never verified on-chain") had been open a long time. Operator explicit decision this session: "on sen sert plus et on va pas sen servir" -- no longer needed, no future use planned.
Solution : NOT deleted -- `onchain/sepolia_wallet.py`/`sepolia_rehearsal.py`/`sepolia_autonomous.py` remain in the codebase untouched. Reasoning: `sepolia_autonomous.py` is the code path behind the NAMED, BOUNDED exception in CLAUDE.md's absolute rules (the fully-autonomous Sepolia rehearsal, `ARIA_SEPOLIA_AUTONOMOUS_ENABLED`) -- deleting the code would erase a documented governance mechanism, not just an unused feature, and that's a different, bigger decision than "we don't need this test swap anymore." Backlog #27 closed as abandoned. If the operator later wants the CODE physically removed (not just the backlog item closed), that needs its own explicit go-ahead naming the files.

------------------------------------------------------------

[CODE] Subject  : Smart-swing guarded entry orchestrator -- the missing consumer that gates a full-size buy on the canary verdict (the gap the adversarial audit flagged)
Date : 2026.07.25 / Problem : the safety envelope in `agent_wallet_smart_swing.py` built `precheck_swap_roundtrip` (free dual-quote pre-check), `run_swing_entry_canary` (real ~3$ buy+sell round-trip probe returning `CanaryResult.passed`), and `execute_smart_swing_swap` (real full-size buy) as isolated primitives. The audit's own words: "the canary only produces a verdict; it does no full-size buy. Since no consumer exists yet, there is no path that silently ignores the result... it is a real pre-arming requirement to build that gated orchestrator." A verdict with no caller that gates on it is inert -- nothing chained precheck -> canary -> full buy with a hard abort-early contract.
Solution : one new pure-orchestration function `execute_guarded_swing_entry(*, token, amount_in_usd, quote_fn, balance_fn, spend_pull_fn, buy_swap_fn, token_balance_fn, sell_swap_fn, return_transfer_fn, canary_amount_usd=CANARY_TEST_AMOUNT_USD, canary_max_roundtrip_loss_bps=CANARY_MAX_ROUNDTRIP_LOSS_BPS, precheck_max_roundtrip_loss_bps=MAX_SLIPPAGE_BPS, network=NETWORK, precheck_fn=..., canary_fn=..., full_size_buy_fn=...) -> GuardedSwingEntryResult`. Chains the three primitives in order, aborting the instant any refuses: (1) PRECHECK (free) at the FULL intended `amount_in_usd` (price impact is size-dependent) -> rejected returns immediately at stage="precheck", canary NOT run, no balance/spend/real call touched; (2) CANARY (real ~3$) -> `passed=False` for ANY reason (buy blocked, buy-swap failure/stranded, 0 tokens, sell failed = honeypot signature, round-trip tax too high) returns immediately at stage="canary", the full-size buy is NEVER called; (3) FULL-SIZE BUY (real) with the caller's `amount_in_usd` passed straight through, NEVER resized (sizing is not this orchestrator's decision). New frozen `GuardedSwingEntryResult(opened, stage, reason, precheck_result, canary_result, buy_result)` surfaces WHERE it stopped and carries every stage result that ran (never swallowed). Adds NO new network/CDP call and re-implements NONE of the 5 application guards (gate/kill-switch/breaker/balance/slippage) -- each composed primitive enforces those internally (a gate-off run is caught by the canary's own buy and reported as a canary refusal). The full-size buy logs under the real `WALLET_PRODUCT`; the canary legs under `CANARY_WALLET_PRODUCT` (out of the real-position feed) -- both inherited unchanged. `precheck_fn`/`canary_fn`/`full_size_buy_fn` default to the real module primitives and exist only as composition seams for isolated unit testing (call-tracking) -- never rebound in production.
**Still fully DORMANT / unwired** (verified): gate `ARIA_SMART_SWING_ENABLED` still OFF by default (untouched), no Tangem grant / Spend Permission exists, no Policy validated live. This orchestrator itself has NO caller either -- it is not imported by any heartbeat cycle, cron, or production flow. "Who decides to open a swing position, with what size and thesis" remains a separate, larger chantier explicitly out of scope here (the orchestrator owns no sizing/thesis logic -- `amount_in_usd` is a required caller input). `agent_wallet_smart_swing.py` (new `execute_guarded_swing_entry` + `GuardedSwingEntryResult` + `ENTRY_STAGE_*` markers) -- `test_agent_wallet_smart_swing.py` +13 tests (5 isolated sequencing via call-tracking stubs: happy-path order, amount-never-resized, precheck-reject-skips-canary-and-buy, canary-fail-skips-buy, buy-failure-surfaced-not-masked; 8 integration through the real primitives: end-to-end open with journal assertions, real-precheck-reject-spends-nothing, and the 5 canary failure modes each asserting via a tracked full-size-buy fn that it is NEVER called + a real full-size-buy-swap failure surfaced as stranded), 103 tests in the file, full suite green (7692 passed, 17 skipped), `test_coherence.py` green. Branch `claude/smart-swing-guarded-entry-temp` (not pushed, not merged).

------------------------------------------------------------

[CODE] Subject  : One-time Spend Permission GRANT on aria-smart-st via the Tangem owner -- the `approveWithSignature` route (the UserOp route is a dead end for a hardware owner)
Date : 2026.07.25 / Problem : the "next milestone (step 2)" above -- perform the one-time Spend Permission grant on aria-smart-st -- had no code and an unproven signing path. A sibling-agent memo (source-cited against the installed cdp-sdk 1.47.1) was verified end-to-end this session and CONFIRMED: the obvious high-level path (`cdp.evm.create_spend_permission()` -> sign -> `send_user_operation`) is a DEAD END for a Tangem owner. That path finalizes by signing the raw `userOpHash` with `owner.unsafe_sign_hash` (raw ECDSA over a 32-byte hash, no EIP-191 prefix / no EIP-712 struct -- verified `actions/evm/send_user_operation.py:85-88` + the `send_user_operation` endpoint docstring). The Tangem bridge's allowlist is `personal_sign` / `eth_sendTransaction` / `eth_signTypedData_v4` only (`server.js` ALLOWED_METHODS) -- `eth_sign` (raw hash) is deliberately refused (blind-hash phishing vector). So there is no way to feed a raw `userOpHash` to the card; that whole output must be abandoned for this owner.
Solution : built the SECOND, canonical Coinbase route -- `SpendPermissionManager.approveWithSignature(perm, signature)` (EIP-712 authorization validated by ERC-1271, + ERC-6492 while the account is still counterfactual). New module `agent_wallet_smart_swing_grant.py` (`grant_spend_permission_via_tangem(...)` + pure helpers). Sequence, each external touchpoint an injected seam (unit-testable, no live net/card): (1) freeze the reviewed permission ONCE via `agent_wallet_smart_swing.build_spend_permission_input()` -> `resolve_spend_permission` (random salt + concrete start each call, so it MUST be resolved once and threaded byte-identically to hash/sign/calldata or `isApproved` stays false); (2) read the authoritative permission hash on-chain (`getHash`, web3) and the account's deploy state (`get_code`) -- read-only, safe even in dry-run; (3) build the replay-safe EIP-712 typed data `CoinbaseSmartWalletMessage{hash=getHash}` (structure copied verbatim from `sign_and_wrap_typed_data_for_smart_account.py:231-251`; NOT reusing `create_replay_safe_typed_data` because it recomputes the inner hash -- the on-chain getHash is the safer source, no risk of mis-encoding the manager's own domain); (4) relay that EXACT typed data through the EXISTING `aria_core.tangem_bridge` client (`eth_signTypedData_v4`, params `[owner, json(typed_data)]`, chain `eip155:8453`), after verifying the physically-connected card is `TANGEM_ST_OWNER` (wrong card -> abort before signing); (5) normalize `v` to 27/28, wrap via `create_smart_account_signature_wrapper` (reused as-is), then ERC-6492-wrap when `get_code` is empty (logic mirrored from `evm_smart_account.py:604-623`, factory/selector/magic constants IMPORTED from the SDK so they can never drift); (6) ABI-encode `approveWithSignature` (mirror of the SDK's `contract.encode_abi("spend", ...)`), submit from the CDP-managed spender via `cdp.evm.send_transaction` (a `TransactionRequestEIP1559` -- the memo's bare-dict shorthand would not construct; Coinbase manages gas/nonce); (7) confirm ONLY via the on-chain `isApproved(perm)` view (polled) -- `granted=True` is claimed on nothing else. `dry_run=True` by DEFAULT (an accidental call performs no tap and no submit -- only the read-only reads + returns the exact sign payload for inspection). Guardrails honored: account/spender/token/allowance come ONLY from `build_spend_permission_input()` (no free `allowance`/`spender`/`token` parameter -- asserted by a test), no secret ever read/logged, no signature/key hardcoded, wallet_guard/permission_mode/config.toml untouched.
**Two empirical unknowns the FIRST real run must prove (documented, never assumed): D1** aria-smart-st is almost certainly counterfactual (undeployed) -- its first UserOp would need raw-hash owner signing the Tangem can't do, so it may never deploy via the owner path; `approveWithSignature` will then need the ERC-6492 branch, whose factory calldata `createAccount([owner],0)` is only valid if the account's counterfactual address was derived from exactly (factory, [Tangem owner], salt/index 0) -- if `isApproved` stays false after a 6492 submit, THIS mismatch is the prime suspect (cleaner alt: permissionlessly deploy the account first via the factory, then a plain wrapper suffices). **D2** the Tangem has only ever signed a trivial `personal_sign`; signing this replay-safe typed data (verifyingContract = the smart account itself) is valid EIP-712 but never sent through the card -- the #1 thing to test. **Ordering trap** the default submitter is the spender EOA: run the grant BEFORE attaching the swap-only CDP Policy (which would default-deny this manager call), or inject a `submit_fn` from another funded account; the spender needs a little Base ETH for gas.
**Fully DORMANT / unwired**: nothing in production imports this module; it is a one-time human-supervised setup action, never a heartbeat/cron path. `agent_wallet_smart_swing_grant.py` (new) -- `test_agent_wallet_smart_swing_grant.py` 27 tests (pure helpers: frozen-constants, fresh-salt-each-call, tuple order/types, replay-safe typed-data structure, v-normalization matrix, 6492 vs plain wrap, approveWithSignature selector; orchestrator with fully-faked seams: dry-run-default-no-tap-no-submit, real deployed grant sequence+payload, undeployed uses-6492, frozen-struct threaded identically to getHash/isApproved/calldata, success-requires-confirmed-isApproved, operator-decline=rejected-no-submit, bridge-unreachable/not-connected/wrong-card/submit-failure/read-failure all error-no-submit, and the no-free-parameter guardrail). Full suite green (7753 passed, 17 skipped), `test_coherence.py` green (a CDP `send_transaction` is not a GitHub/X/email external-write, so the write-allowlist guard is not triggered -- verified). Branch `claude/smart-swing-tangem-grant-temp` (not pushed, not merged).

------------------------------------------------------------

[CODE] Subject  : Operator security stress-test on aria-wallet-X402-EVM -- confirmed a real gap in incident investigation speed, alert now interactive, preventive export guard added (Items #187/#190)
Date : 2026.07.29 / Problem : operator deliberately triggered a real, unannounced `unexpected_outflow` alert to stress-test ARIA's incident readiness (a genuine payment: 1 USDC to Codex.io for account verification, settled via a "swapAndExecute" call on Decent v3 UTB -- a real facilitator rail, not a code path this project's `x402_executor.py` uses). Investigation was slow and manual: cross-referencing `x402_budget.list_spends()`, `agent_wallet_log.list_transactions()`, and live Blockscout calls by hand, no single source of truth. Operator's own verdict: the system was "a priori non" ready. Separately, the operator revealed they had exported this wallet's private key via the CDP dashboard and used it directly in MetaMask to make the payment -- confirming empirically that an exported EOA private key bypasses every CDP-side control (IP allowlisting, API restrictions) permanently, since the blockchain never checks who/where a valid signature came from.
Solution : three independent, additive fixes to `agent_wallet_monitor.py` (read-only, no execution capability, unchanged) -- (1) `WalletMovement` gained a `method` field (the on-chain call name, e.g. "swapAndExecute", already available from Blockscout but never surfaced before -- now shown directly in the alert); (2) a new `_THIRD_PARTY_ADDRESS_NAMES` registry (display-only, deliberately NEVER consulted by `_is_own_wallet` -- labeling a manually-identified counterparty like "Codex.io" must never silently reclassify a future movement to that same address as safe); (3) the `unexpected_outflow` alert is now an interactive Telegram confirmation (`_send_outflow_confirmation`, reusing the existing `approvals.py`/`InlineKeyboardButton` pattern already used by `wallet_guard.escalate_spend` -- never duplicated) instead of a passive notification: "Autorisé par moi" just confirms, "PAS autorisé" immediately triggers the existing global kill-switch (`outgoing_pause.pause`, already tested elsewhere in this project, never recoded) so future outgoing spends freeze while the operator investigates -- doesn't undo the flagged movement itself (already on-chain, irreversible by construction), but cuts reaction time from "read a message, act manually" to one tap. Falls back to a plain-text notification on ANY failure (missing admin_ids, bot down, approval creation failure) -- the alert itself is never silently lost. `gateway/telegram_bot.py`'s `send_message` gained an optional `reply_markup` parameter (backward-compatible, no existing caller passes it) and a new `outflow_confirm:` branch in `_handle_callback`.
Separately, `test_coherence.py` gained a preventive structural guard (`test_no_cdp_account_export_call_anywhere`): no file in this codebase may ever call `.export_account()` (the real CDP SDK method, verified live in the installed `cdp` package, `cdp/evm_client.py`) -- confirmed none does today. This guards only against a FUTURE code path silently reintroducing a key-export call; it cannot detect (and no software could) an export performed directly via the CDP dashboard, outside this project's code -- that vector's real mitigation is architectural (Smart Account migration, see the diligence entry below), not a test.
Real test-infrastructure trap hit and fixed while building this (documented for future sessions): `runtime.py`'s `_SettingsProxy` forwards `admin_ids` (a computed read-only pydantic property derived from `telegram_admin_ids`) via a fallback `object.__setattr__` when the real settings object refuses the write -- `monkeypatch.setattr(settings, "admin_ids", ...)` (an already-established pattern elsewhere in this test suite, e.g. `test_wallet_guard_escalation.py`) therefore shadows the property PERMANENTLY on the proxy instance for the rest of the pytest session, never properly restored by monkeypatch's undo. New tests using this pattern in `test_agent_wallet_monitor.py` (a file that sorts alphabetically BEFORE `test_proactive*.py`) broke 7 unrelated tests there when the full suite ran, only when run in that exact ordering. Fixed by monkeypatching `telegram_admin_ids` (the real underlying field, a normal writable attribute) instead of the computed property directly -- confirmed the full suite green afterward with no state leak.
Follow-up items banked, not built this session: #188 (audit trail of gate on/off changes over time -- today only the CURRENT state of `ARIA_X402_SELLER_ENABLED` etc. is inspectable, never what it was AT the time of a past incident), #189 (single-call `wallet_incident_report(address)` diagnostic tool -- this investigation needed ~7 separate manual lookups), #191 (Smart Account + Spend Permissions migration for this wallet -- a real diligence pass confirmed receiving works today with zero blocker, but outgoing x402 payments depend on EIP-3009 `transferWithAuthorization`, whose compatibility with a "pure" ERC-4337 Smart Account is unconfirmed; recommended architecture is a fresh Smart Account as the receiving treasury + a SEPARATE, narrowly-scoped spender key for outgoing x402 payments specifically -- explicitly NOT shared with `aria-smart-st-EVM`/`aria-smart-vc-EVM`'s own future spenders, to keep the blast radius of any one key leak scoped to its own use case), #192 (a second, SEPARATE finding made while verifying the other 2 CDP wallets during this same investigation: `aria-spender-smart-st-EVM`, excluded from `MONITORED_WALLETS` today, received 2 suspicious tokens on 25/07 -- a fake "Claude 9.0" token and an "EṬH" token using a Unicode homoglyph, sent from an address one character off from the real `aria-wallet-X402-EVM` -- classic look-alike phishing/dust, currently invisible to this monitor since that wallet isn't watched).
`agent_wallet_monitor.py`, `gateway/telegram_bot.py`, `test_coherence.py` -- 12 new tests (`test_agent_wallet_monitor.py`: method propagation, third-party label never flips classification, interactive confirmation creates approval/sends keyboard/degrades to plain notification on any failure), full suite green (8336 passed, 17 skipped), `test_coherence.py` green. No real compromise occurred -- confirmed by the operator directly (the deliberate stress-test payment), key rotation left to the operator's judgment given the exported key remains valid indefinitely regardless of any code-side fix.

------------------------------------------------------------

[CODE] Subject : Auto-ban unsolicited scam token deposits from the momentum pipeline
Date : 2026.08.03 / Problem : operator request after spotting a "Malveillant"-flagged phishing token in the Coinbase Wallet app -- confirmed on-chain, 9+ phishing/dust-attack tokens received on the real pilot wallet (0xF04625...) in 3 weeks (DEXHorizon, HOLO, GDWS, a token literally named "Claude", BYZAN, plus lookalike ETH/USDC). The existing guard (`_lookalike_target`) only caught homoglyph ETH/USDC impersonations (4 entries in `phishing_blacklist`), missing every generic scam. Structural check confirmed FIRST: `execute_swap`/`transfer_usdc` (the pilot's only signing paths) can never target an unsolicited token anyway (candidates come only from the legitimate momentum discovery pipeline; transfer is locked to one fixed address) -- so this is a defense-in-depth/hygiene improvement, not a fix to an active signing risk.
Solution : `check_wallet_activity` now runs `_is_unsolicited_scam_token()` on any token deposit classified "external_deposit" (a tx ARIA never initiated herself) -- checks real DexScreener liquidity, bans into `momentum_blacklist` (permanent, `is_blacklisted()` already checked first in `evaluate_momentum_entry`) if none found, silently (no Telegram notification, same as the rest of this cycle). Deliberately does NOT ban a genuine manual deposit: verified live that the operator's own VIRTUAL purchase (via the Coinbase app "Acheter"/"Echanger", never routed through `agent_wallet_log`, so also classified "external_deposit") has 30 real DexScreener pairs (397k$-415k$ liquidity) vs zero liquidity for all 9 confirmed scams -- the liquidity floor (`_UNSOLICITED_SCAM_LIQUIDITY_FLOOR_USD=1000.0`) separates the two reliably; Blockscout's own `reputation`/`is_scam` fields were checked first and found useless (return "ok"/False for every confirmed scam). Official USDC exempted before any network call (never checked, never bannable). Fail-open on DexScreener lookup failure. `agent_wallet_monitor.py` (`a39c10de`) -- 5 new tests (`test_agent_wallet_monitor.py`: no-liquidity banned, low-liquidity banned, real-liquidity operator deposit never banned, lookup-failure never bans, official USDC never checked/banned), 119 tests in the file green, `test_momentum_blacklist.py`/`test_coherence.py` green. Not yet deployed to the VPS.

------------------------------------------------------------

[ETAT ACTUEL] Subject : Backlog #254 audit -- swap execution uses Permit2 (per-swap signature), never a persistent ERC-20 approve() to the router
Date : 2026.08.07 / Problem : #254 (promoted from the research watch 05/08) flagged the real JaredFromSubway.eth hack (7.5M$ drained 20/06/2026 via router allowances left active after use, not a malicious initial signature) as a pattern to check against `agent_wallet_cdp_adapter.execute_swap` -- the real pilot's only swap path, capital already active (#204). The adapter's own code never shows an allowance/approval call, but that alone doesn't prove the underlying CDP SDK doesn't issue one internally.
Solution : verified directly against the actually-installed `cdp-sdk` 1.47.1 (confirmed identical version in both `vanguard/backend/.venv` and `packages/aria-core/.venv`) -- `cdp/actions/evm/swap/send_swap_transaction.py`, the exact function `account.swap()` (called by `execute_swap`) resolves to. Zero `.approve()`/ERC-20-allowance call anywhere in the whole `cdp/actions/evm/swap/` tree (grep confirmed). The mechanism used is Uniswap's Permit2 (`CreateSwapQuoteResponseAllOfPermit2`, an EIP-712 typed message): when `swap_data.requires_signature` is true, the CDP account SIGNS a typed message (`sign_evm_typed_data`) scoped to that one quote, appended to the swap calldata -- there is no standing on-chain allowance granted to the router itself. This is architecturally the mitigation for the exact failure class #254 described: a compromised/malicious router can never replay an old approval, because the router never receives a standing approval in the first place -- every swap requires a fresh signature. The only persistent on-chain state possible is the wallet's own one-time approval to the CANONICAL Permit2 contract itself (Uniswap-deployed infrastructure, same address across every EVM chain, not swap- or router-specific) -- categorically different from the JaredFromSubway vector (a standing approval directly to an exploitable dApp/router contract).
Scope of this verification, stated honestly: covers the SWAP MECHANISM (static SDK code, governs every future swap through this path) -- not a live on-chain snapshot of the wallet's current allowances (not fetched; this codebase's generic Blockscout `methods-read` helper only supports no-argument getters, not a 2-argument read like `allowance(owner, spender)`, and the code-level guarantee already answers the question #254 asked). No code change made -- nothing to fix. #254 struck from the CLAUDE.md backlog.
