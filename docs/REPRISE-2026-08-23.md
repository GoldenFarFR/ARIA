# Reprise — 23/08/2026, 01h00

Écrit pour qu'une session compactée reparte sans rien perdre. Tout ce qui suit
a été **vérifié en direct**, pas cité de mémoire.

## Ce qui tourne en ce moment

| Poche | État | Réglages |
|---|---|---|
| **Solana late-bonding** | active | bande **50–98,5 %**, plancher **4 000 $**, trailing **15 %**, stop fixe **−5 %**, aucun palier |
| **Robinhood** | observation pure | source **DexPaprika**, cadence 120 s, liquidité complétée via DexScreener |
| Base | **en pause volontaire** depuis le 17/08 (`paper_pause_state.json`) | rien à réparer |

Service : `aria-shadow-persistent` (vrai fichier unit depuis le 22/08, `enabled`).

## Trois réglages changés en même temps — non attribuables séparément

Bande, plancher et trailing ont bougé dans la même fenêtre. Assumé : bande et
plancher **se masquaient l'un l'autre** (un token à mi-courbe n'a pas encore
5 500 $ de réserve), donc les tester séparément était impossible. Les archives
par époque gardent chaque combinaison comparable à la précédente.

## Crons qui accumulent pendant la nuit

- `runner-frequency-watch` (2 h) — fréquence des coureurs **et** vitesse de
  qualification. Refuse tout verdict sous 2 jours distincts.
- `stale-position-watch` (10 min) — **silencieux** sauf position non vérifiée.
- `trending-shadow` (5 min) — devenir des tokens trending.

## À lire au réveil

```
/opt/aria-data/runner-frequency-watch/report.md
/opt/aria-data/stale-position-watch/report.md
```

## Ce qui est prêt mais bloqué

**LetsBonk** — le décodeur est FAIT et validé à 0,0 % (`services/launchlab_curve.py`).
Ce qui bloque est la DÉCOUVERTE des nouveaux pools : `logsSubscribe` accepté mais
muet (100 s, deux essais), DexScreener trop tardif (44 min), `getProgramAccounts`
refusé (plan payant). Piste suivante : `programSubscribe`.

**À vérifier** : `ARIA_SOLANA_RPC_HTTP` et `ARIA_SOLANA_RPC_HTTP_POLLING` renvoient
le MÊME message d'erreur Chainstack. Soit Helius n'est pas où on le croit, soit les
deux pointent au même endroit — ça change le budget disponible.

## Onze signaux d'entrée testés, aucun ne tient

pression vendeuse, wash, accélération, vitesse SOL/s, créateur sorti, taille du
bundle, acheteurs, vendeurs, pente de pression, retracement, temps de qualification.

Le plafond d'acheteurs semblait donner +1,94 point mais **supprimait un groupe
bénéficiaire** (+1 937 points nets, dont 15 trades à plus de +100 %).

**Constat de fond : au moment de l'achat, un token qui va mourir ressemble à un
token qui va monter.** Le seul filtre validé reste le plancher de liquidité.

## Instrumentation ajoutée cette nuit (rien n'existait la veille)

- parcours de la courbe **avant** l'achat (50 points)
- `progress_retracement_at_entry` — sommet ou repli
- `stop_level_pct` / `fill_level_pct` — le stop et le remplissage en NOMBRES
- `entry_price_source` — quelle source a valorisé l'entrée
- 1 rejet de bande sur 50 suivi

## La leçon de la nuit, gravée dans CLAUDE.md

**Les données d'un système ne peuvent pas valider ce système.** Trois défauts sur
quatre ont été trouvés par l'opérateur avec une capture d'écran, jamais par un
garde-fou : les logs restaient propres, les exceptions étaient avalées par des
gardes bien intentionnés, et la poche avait l'air inactive ou malchanceuse —
jamais cassée.
