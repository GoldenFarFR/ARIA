# Backlog technique — pistes dev promues de la veille Research (#256+)

Extrait de CLAUDE.md le 10/08 (passe de nettoyage, fichier passé de ~90 Ko à
~76 Ko) — contenu déplacé tel quel, aucune perte d'information. CLAUDE.md ne
garde qu'un index compact ; le détail complet (source, action dev précise)
vit ici.

Alimenté par la promotion périodique de `research-log.md` (cf. section
"Automations in place" de CLAUDE.md). Chaque piste simple et vérifiée
externellement reçoit le prochain `#N` disponible (continue la numérotation
déjà utilisée dans CLAUDE.md, ex. #194/#204/#228/#253) — à reprendre dans
n'importe quelle session dev, jamais codée par la passe de promotion
elle-même. Une piste nécessitant une vraie diligence part plutôt dans
`docs/aria-learning-inbox/`. Éditer cette liste EN PLACE (ajouter en fin,
barrer/retirer une fois traitée) — jamais un paragraphe daté empilé ailleurs.

~~**#256**~~ **RÉSOLU (audit 10/08, cf. #273 -- même réponse, question 18/07 enfin tranchée)** — fusion Aerodrome+Velodrome en un DEX unique "Aero". La question posée (l'adresse/ABI du routeur Aerodrome change-t-elle avec la migration ?) devient sans objet pour ARIA : `execute_swap` n'appelle JAMAIS un routeur Aerodrome directement, ni aucune adresse de contrat DEX codée en dur — tout passe par `account.swap()` du SDK CDP, lui-même routé par l'agrégateur 0x ("Argon Router", 373+ sources de liquidité) qui absorbe ce type de migration en interne. Aucune action nécessaire, aucun code ARIA à toucher si/quand la migration Aero a lieu.
- ~~**#257**~~ **RÉSOLU (promotion veille Research 08/08, revérifié WebSearch)** — GoPlus et Blockscout confirmés officiellement déployés sur Robinhood Chain (`robinhoodchain.blockscout.com` vérifié réel ce jour ; couverture GoPlus reprise du journal de veille, pas recontre-vérifiée indépendamment mais faible enjeu). Lève le point de vigilance : plus de bloquant sécurité identifié pour une future extension réelle du sourcing momentum à Robinhood Chain, contrairement à Monad ([[project_monad_chain_candidate]], toujours bloquant).
- **#260 (promotion veille Research 06-07/08)** — x402 V2 (fin juillet/début août 2026) : support multi-chaîne natif via CAIP-2, paiements par SESSION plutôt que par appel (-90% de latence/gas visé sur les appels haute fréquence), architecture modulaire à plugins. **Action dev** : avant tout réarmement mainnet du x402 seller dormant (`ARIA_X402_SELLER_ENABLED`) ou toute extension du budget acheteur x402 (#199), vérifier si le client/SDK x402 utilisé par ARIA a déjà migré vers V2 — les paiements par session pourraient réduire le coût réel des appels fréquents.
- **#261 (promotion veille Research 08/08)** — congestion Base par mauvaise config de propagation de transactions (incident RÉEL vérifié WebSearch : 31/01/2026, pas "cette semaine" comme logué à tort — rollback de config + pipeline retravaillé sur un mois). **Action dev** : vérifier si le circuit breaker momentum déjà en place (#186) couvre une dégradation de propagation réseau (latence/drops de transaction), pas seulement une anomalie de prix/volume.
- ~~**#262**~~ **RÉSOLU (audit 10/08, code vérifié)** — ReasoningBomb (arxiv 2602.00154) : concern confirmed real in principle (short prompt → pathological long reasoning, ~287x cost amplification), but verified NOT a live gap here. `operator_chat_budget.py`'s $0.10/day cap is cumulative-only (checked pre-turn, recorded post-turn) — but every real LLM call site (`llm.py`'s multi-provider client, `momentum_entry.py`'s 3 trading gates, `trade_devils_advocate.py`, `llm_economy.py`'s depth budgets) already passes an explicit, small `max_tokens` (20 for trading gates, 180-1800 for chat depending on depth, 500 for trade review) — a hard per-call output ceiling regardless of prompt content, bounding worst-case single-call cost to a few cents. Extended thinking (`thinking: adaptive`, uncapped by a small `max_tokens`) is used ONLY by the bash-based Devil's Advocate script (`scripts/devils-advocate-lib.sh`), itself already cost-tracked/logged and triggered exclusively by ARIA's own git pushes — never external/adversarial input. No code change needed.
~~**#263**~~ **RÉSOLU (audit 10/08, cf. #273 -- même réponse)** — Odos, smart-order-router multi-entrées/multi-sorties. La question sous-jacente (le swap CDP actuel a-t-il besoin d'un agrégateur tiers en plus ?) est tranchée : `account.swap()` du SDK CDP route déjà via 0x ("Argon Router", 373+ sources de liquidité) — intégrer Odos ou 1inch en plus n'apporterait pas une couverture significativement meilleure à ce stade. Reste une piste de comparaison de PRIX pure (jamais vérifiée empiriquement) si une divergence de coût est un jour suspectée, mais pas une action structurelle nécessaire aujourd'hui.
~~**#264**~~ **RÉSOLU (audit 10/08, 8 workflows `.github/workflows/` inspectés en direct)** — "GitLost" (Noma Security, disclosed 06/07/2026) : une issue publique piégée peut faire lire par un agent GitHub Agentic Workflows le contenu d'un repo PRIVÉ de la même organisation puis le publier en commentaire public — nécessite (1) un agent qui traite du contenu issue/PR non fiable, (2) une portée cross-repo, (3) une sortie publique. Aucune des 3 conditions ne tient ici : `uptime-watch.yml` (seul avec `issues: write`) ne traite jamais de contenu externe (crée/ferme seulement ses propres issues à texte fixe) ; zéro secret/PAT à portée org-wide configuré (`grep secrets\.` ne retourne que des commentaires) — tous les workflows utilisent le `GITHUB_TOKEN` implicite scopé au repo ; aucun `pull_request_target` (déclencheur dangereux) nulle part, uniquement `pull_request`/`push`/`schedule`/`workflow_dispatch`. `aria-ops` est inatteignable depuis la CI de ce repo par construction. Détail : `docs/HANDOFF_SECURITE.md` (entrée 2026.08.10).
~~**#265**~~ **RÉSOLU (audit 10/08, crontab + code réels inspectés en direct)** — rapport VentureBeat Pulse Research juin 2026 (107 entreprises) : 69% des entreprises font tourner des agents avec des identifiants PARTAGÉS quelque part dans leur déploiement. Vérifié cron par cron (`crontab -l` + grep de chaque `run.sh`) : `TELEGRAM_BOT_TOKEN` partagé entre 4 watchdogs et `ARIA_DIAGNOSTIC_TOKEN` entre 2 — risque jugé faible (portée identique et étroite partout, aucune capacité différentielle entre les partageurs). Le vrai credential d'écriture (clé SSH git) est DÉJÀ isolé par dépôt (6 clés distinctes `aria_deploy_key_main/core/democli/profile/showcase/template`). Le token OAuth `gh` (portée plus large, scope `repo`) existe sur la machine mais n'est atteignable par AUCUN cron automatisé : `research-loop` n'a aucun accès Bash, `research-log-promotion.sh` n'autorise que `git *` (jamais `gh`) — la liste blanche d'outils par cron est déjà le vrai garde-fou, plus pertinent qu'une séparation de credentials ici. Aucune action nécessaire.
~~**#266**~~ **RÉSOLU (audit 10/08, mandat autonome v8/8.x — volet Monte-Carlo/Bonferroni/binomial ; volet walk-forward complété le même jour, second passage)** — détection de surapprentissage de backtest par permutation Monte-Carlo + Bonferroni + test binomial, construit dans `backtest_robustness.py` et appliqué au wick-gate v8. Résultat réel : 0/43 trades gagnants live est statistiquement incompatible avec le backtest (60% WR claimé, p=7.7e-18) ET avec son propre groupe de contrôle "sous le seuil" (25.6%, p<0.0001) ; le p=0.026 d'origine ne survit pas à la correction Bonferroni pour ~6 familles de filtres testées sur le même échantillon de 58 trades (alpha corrigé 0.0083 < 0.026). Confirme que la pause de v8 était justifiée. Volet walk-forward (split chronologique train/validation, jamais construit jusqu'ici) ajouté dans le même module : `chronological_train_validation_split` + `evaluate_filter_candidate_out_of_sample`, opérationnalisent bout en bout la règle méthodologique à 3 étapes de CLAUDE.md (split avant de chercher un motif, test sur la validation seule, seuil minimal de données avant tout test). Question ouverte non résolue (pourquoi le live est pire que même le groupe de contrôle — décalage de régime ? définition de "gain" différente ? règles de sortie divergentes du périmètre du backtest ?) : détail complet dans `docs/HANDOFF_PIPELINE_MOMENTUM.md` (entrée 2026.08.10).
~~**#267**~~ **RÉSOLU (audit 10/08, chiffres réels vérifiés sur `/opt/aria-data/llm-usage/2026-08.jsonl`)** — hausse tarifaire API Claude Sonnet 5 confirmée sur plusieurs sources (dont `platform.claude.com/docs`) : le tarif d'introduction 2$/10$ par million de tokens (input/output) prend fin le 31/08/2026, tarif standard 3$/15$ (+50%) dès le 01/09/2026. Volume Anthropic direct réel ce mois : 20 appels / $0.032 total (dont 2 Sonnet, $0.002) sur 4702 appels LLM au total — le volume réel passe encore par Grok/Groq, non tarifés. Impact de la hausse au volume actuel : négligeable (~$0.03 → ~$0.045/mois). Aucun recalibrage de code nécessaire — le plafond chat opérateur (dollars, auto-absorbant) et les scans `/vc` (fréquence manuelle, plafond par appel) restent protégés sans modification. Détail : `docs/HANDOFF_LLM.md` (entrée 2026.08.10).
- **#268 (promotion veille Research 09/08)** — EIP-7702 en délégation CDP Server Wallets (`useCreateEvmEip7702Delegation`) : upgrade IN PLACE de l'EOA existant du pilote agent-wallet vers des capacités smart account (batching, gas sponsoring, spend permissions), transaction de délégation elle-même sponsorisée par Coinbase (pas d'ETH requis pour l'activer), disponible dès maintenant sur Base — répond à la limite déjà documentée le 16/07 sans attendre Base "Cobalt" (septembre 2026, #103). **Action dev** : évaluer avant toute migration réelle de `agent_wallet_pilot.py` — vérifier le patron de sécurité `base/eip-7702-proxy` (Base) associé avant toute délégation, jamais à activer sans cette vérification.
- **#269 (promotion veille Research 09/08)** — érosion mesurée de l'edge sur Polymarket/Kalshi (14 des 20 wallets les plus profitables du leaderboard public sont désormais des bots, 08/2026) — la compétition passe autant par la vitesse que par le jugement. **Action dev** : ajouter à la poche Polymarket paper (#108) une mesure de la disponibilité réelle des fenêtres d'entrée face à des bots plus rapides, question jamais posée à ce jour dans `HANDOFF_POLYMARKET.md` — le seuil `MIN_WIN_PROBABILITY=0.85` reste la bonne doctrine de qualité, mais la vitesse d'exécution n'a jamais été mesurée séparément.
- **#270 (promotion veille Research 09/08)** — architecture multi-agent "Three-Layer" (recherche BlackRock/Columbia University, avril 2026) : agents Bull/Bear qui débattent explicitement, un agent Trader synthétise, un Risk/Portfolio Manager approuve avant toute exécution — surperformance mesurée face à un LLM unique sur des tâches de stratégie crypto (externalise la tension cognitive, réduit le risque d'hallucination directionnelle). **Action dev (mandat autonome v8/8.x)** : patron d'architecture jamais comparé à ce jour face à la confirmation LLM ponctuelle actuelle (#194) — piste de conception à garder en réserve, jamais à adopter sans preuve empirique sur les propres données v8.
- **#271 (promotion veille Research 09/08)** — upgrade Base "Beryl" confirmé : standard B20 exécuté en precompiles Rust avec kit de conformité intégré au niveau protocole (freeze-and-seize, contrôle d'accès par rôle, plafond d'offre, permit EIP-2612), deux variantes (asset token à décimales configurables/rebasing, stablecoin token à 6 décimales fixes). **Action dev** : vérifier si `/api/x402/b20score` évalue déjà ces capacités de conformité intégrées (notamment le droit de gel) comme signal de risque/légitimité distinct d'un ERC-20 classique — un token B20 "stablecoin" avec droit de gel change la nature du risque de contrepartie par rapport à ce que `safety_screen.py` analyse aujourd'hui.
~~**#272**~~ **RÉSOLU (audit 10/08, chaîne de dépendances réelle inspectée)** — faille de génération de nombres aléatoires faible dans la bibliothèque JS CryptoJS (~5,7M$ drainés sur cinq wallets applicatifs distincts, révélé 08/2026). Vérifié directement (`pip show`, lecture du code) : `agent_wallet_pilot.py`/`agent_wallet_cdp_adapter.py` ne génèrent JAMAIS de clé privée localement — tout passe par `cdp.evm.get_or_create_account` (garde-côté-serveur Coinbase, hors de la chaîne de dépendances Python d'ARIA). Dépendances directes du SDK (`cryptography`, `eth-account`, `eth-keys`, `web3`, `solders`, `pyjwt`, `base58`) : aucune n'est liée à CryptoJS ni à un RNG faible connu — `eth-keys` utilise un backend natif basé sur `hashlib`/`hmac` (pas `Math.random()`/PRNG faible), `ecdsa` (bibliothèque pure-Python parfois associée à ce type d'incident) n'est même pas installée. Seul autre appariement trouvé pour `generate_key`/`os.urandom` dans tout `aria_core/` : `admin_totp.py` (2FA opérateur, gate distinct déjà établi, sans rapport avec le wallet). Aucune action nécessaire.
~~**#273**~~ **RÉSOLU (audit 10/08, vérifié WebSearch -- docs Coinbase + docs Base officielles)** — Base MCP (official Coinbase gateway, launched 26/05/2026) routes swaps via dedicated PER-DEX plugins (Aerodrome plugin routes only on Aerodrome via le sugar-sdk Velodrome ; Uniswap plugin routes only on Uniswap), avec approbation humaine OAuth 2.1 obligatoire par transaction. Comparé au chemin réel d'ARIA : `execute_swap` (`agent_wallet_cdp_adapter.py`) appelle `account.swap()` du SDK CDP, routé par l'agrégateur 0x ("Argon Router", 373+ sources de liquidité) — un périmètre d'exécution DÉJÀ plus large que Base MCP, pas un routeur DEX unique. Conclusion : aucun écart d'exécution à corriger. Le seul vrai écart (gouvernance -- Base MCP exige une validation humaine par swap, `agent_wallet_pilot.py` exécute seul sous plafond 10-25$) est un choix opérateur déjà délibéré et documenté (Règles absolues), pas un oubli. Ferme aussi #256 et #263 (même question sous-jacente, même réponse). Détail : `docs/HANDOFF_COINBASE_CDP.md` (entrée 2026.08.10).
- **#274 (promotion veille Research 09/08, verified real via WebSearch — arXiv 2605.27333)** — FinHarness, first published safety harness scoped specifically to finance LLM agents (not a generic agent framework): inline Query Monitor (cross-turn intent drift) + Tool Monitor (per-call permission/parameter/sequence checks) + adaptive Cascade routing between a cheap and an advanced judge, measured cutting attack success rate from 38.3% to 15.0%. **Dev action**: compare its methodology against the momentum pipeline's own LLM security gates (`_llm_confirm`/`_llm_security_gate`) before the next audit of permanent mandate #192 — check specifically whether it catches benign-then-pivot mid-trajectory attacks the current gates were never explicitly tested against.
~~**#275**~~ **RÉSOLU (audit 10/08, doc officielle Claude Code vérifiée, pas supposée)** — CVE-2026-22708 (Cursor): an allowlisted command (e.g. `git`) can be silently hijacked by poisoning `PATH`/`LD_PRELOAD`/`LD_LIBRARY_PATH` through trusted shell builtins (`export`/`typeset`/`declare`). Vérifié contre `code.claude.com/docs/en/permissions.md` (pas supposé) : Claude Code matche la commande COMPLÈTE, chaque sous-commande d'un chaînage (`&&`/`;`/`|`) doit matcher indépendamment `Bash(git *)` — un `git status && export PATH=...` est rejeté, confirmé, pas un simple préfixe naïf. Seul résidu réel trouvé : le scoping n'isole pas l'environnement shell ENTRE appels — un PATH/LD_PRELOAD déjà empoisonné par une compromission du host (jamais par une action propre de l'agent, bloquée par le point précédent) resterait hérité. **Corrigé en défense en profondeur** (pas un exploit vivant corrigé, une réduction de surface résiduelle) : `research-log-promotion.sh` fixe désormais explicitement `PATH` à un chemin système minimal connu et `unset LD_PRELOAD LD_LIBRARY_PATH` en tête de script, plutôt que de faire confiance à l'environnement que cron lui transmet. Détail : `docs/HANDOFF_SECURITE.md` (entrée 2026.08.10).
- **#276 (promotion veille Research 09/08)** — ownership chain never traced before: Clanker (recommended launchpad for a future ARIA tokenization, `docs/base-blockchain-launchpads.md`) was acquired by Farcaster in October 2025, and Farcaster itself was acquired by Neynar in January 2026 — Neynar now indirectly controls Clanker's factory contract/fee structure. **Dev action**: re-verify Clanker's actual governance (fee structure, factory contract control) under Neynar ownership before any future ARIA tokenization decision — the existing recommendation was evaluated under the prior independent Clanker team, not the current owner.
~~**#277**~~ **RÉSOLU (audit 10/08, Explore agent + tracing complet de la chaîne)** — Zscaler ThreatLabz (published 02/07/2026) documents two real indirect prompt-injection campaigns via hidden CSS/JSON-LD text on web pages found through normal search, targeting agents that browse/fetch (WebFetch-class). Audit complet : le dôme anti-injection (`sanitize_untrusted_text` + `<donnees_non_fiables>` + clause système) est déjà appliqué de façon cohérente sur tout le pipeline financier (vc_analysis.py, momentum_entry.py, conviction_research.py, web_verify.py, vc_judge.py, unified_entry.py, operator_conversational.py). Un vrai trou trouvé et corrigé : `gateway/x_engagement.py::compose_mention_reply` + 4 fonctions de `community_feedback.py` (texte visiteur/X tiers injecté brut dans le prompt, sortie postée sur X SANS validation humaine) — dôme appliqué, 8 tests de régression. Résiduel méthodologique non corrigé (hors scope) : le dôme protège contre les INSTRUCTIONS injectées mais seulement partiellement contre de FAUX FAITS présentés comme neutres (ex. faux partenariat annoncé). Détail : `docs/HANDOFF_SECURITE.md` (entrée 2026.08.10).
- **#278 (promotion veille Research 09/08, verified real via WebSearch)** — StartupHub.ai exposes a free, already-built cross-venue prediction market arbitrage scanner (Polymarket/Kalshi/PredictIt) via JSON API and a native MCP tool (`get_arbitrage`), normalizing Yes/No pricing and confirming identical resolution criteria before flagging a spread. **Dev action**: candidate second price feed for the Polymarket paper pocket (#108) without building a direct Kalshi/PredictIt integration — evaluate real reliability/freshness (the authors themselves note windows are thin and short-lived) before any connection, consistent with #269's edge-erosion concern.
~~**#279**~~ **PARTIELLEMENT RÉSOLU (audit 10/08, mandat autonome v8/8.x)** — Look-Ahead-Bench, open benchmark + code (`github.com/benstaf/lookaheadbench`) measuring look-ahead/memorization bias in LLMs used for trading workflows via performance decay across genuinely unseen market regimes (méthodologie réelle vérifiée : split temporel P1/P2 sur des méga-caps notoirement mémorisées, métrique "alpha decay"). Audit du pipeline réel : `scalping_v8` (`scalping_variants.py`) confirmé 100% déterministe, zéro appel LLM, zéro risque de ce type. Les 3 gates LLM partagés (`momentum_entry.py::_llm_confirm`/`_llm_security_gate`/`_llm_confirm_and_gate`) envoient bien le symbole réel du token (jamais l'adresse complète, jamais de date absolue) à chaque décision live — vecteur de mémorisation réel si le symbole est notoire, jamais testé ni neutralisé jusqu'ici (les 3 défenses existantes ciblaient l'injection de prompt, pas la mémorisation). **Corrigé** : clause explicite ajoutée aux 3 system prompts ("si ce symbole te semble familier depuis tes connaissances générales, IGNORE cette impression, seules les données numériques comptent"), 3 tests de régression. **Reste ouvert** : la réplication littérale de Look-Ahead-Bench (split temporel P1/P2, mesure d'alpha decay) suppose un module de backtest historique rejouant de vrais tokens devant un juge LLM — CE module n'existe pas encore dans le repo (confirmé par l'audit). Une mitigation par anonymisation (nom/ticker/date retirés avant transmission au juge) était déjà documentée le 11/07 dans `knowledge/improvement_seeds.yaml` (lignes 64-68) comme discipline à respecter LE JOUR où ce module sera construit — pas construite maintenant pour éviter du code mort sans appelant réel. Détail complet : `docs/HANDOFF_PIPELINE_MOMENTUM.md` (entrée 2026.08.10).
~~**#280**~~ **CODE (10/08, grille construite et testée, pas encore câblée en live)** — LATTICE (arXiv 2604.26235, définitions exactes des 6 dimensions vérifiées via la version HTML du papier, Table 1 -- pas devinées) benchmarks not raw trading P&L but a decision-support's usefulness to a human via 6 LLM-judged dimensions (intent fidelity, mechanism clarity, uncertainty handling, actionability, evidence coverage, response structure). `skills/thesis_quality.py::judge_thesis_quality(thesis_text)` -- même dôme que `vc_judge.py` (sanitize + `<donnees_non_fiables>` + JSON strict + dégradation sûre), mais axe DISTINCT (utilité de décision, pas exactitude factuelle -- les deux juges se complètent). Dégradation volontairement plus stricte que `vc_judge.py` : pas de repli déterministe (aucun proxy fiable non-LLM pour "est-ce bien structuré/actionnable"), un état explicite "non évalué" plutôt qu'un score inventé. 9 tests, suite ciblée verte. **Pas câblé** dans `thesis_journal.py`/`vc_analysis.py` -- ajoute un vrai coût LLM par thèse et change ce que voit l'opérateur, laissé à une décision explicite du moment/de l'endroit où le déclencher plutôt que câblé en silence.
