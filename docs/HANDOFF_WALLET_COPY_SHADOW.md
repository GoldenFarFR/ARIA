# HANDOFF — Wallet-copy shadow (8 wallets suivis, ledgers fictifs indépendants)

> **Repo PUBLIC — jamais d'IP/secret/token/clé/adresse email personnelle en clair ici.** Les 8 adresses de wallet suivies sont publiques (on-chain Base), aucun secret.

> Format : `[STATUT] Sujet` / `Date : AAAA.MM.JJ / Probleme : ...` / `Solution : ... — fichier (hash)`.
> `[STATUT]` : DEPLOYE / CODE (testé, pas déployé) / CONFIG (pas de commit) / ETAT ACTUEL.

[CODE] Sujet  : nouveau shadow — forward-test de copie sur 8 wallets réels (jamais un trigger réel)
Date : 2026.08.08 / Probleme : après l'investigation sur les wallets manipulateurs/insiders de cette session (front-runner confirmé sur "Base is for everyone" + achat AEON 2 jours avant l'annonce Programmable) et la preuve que le leaderboard public de l'app fomo affiche un PnL largement fictif (ex. "change" +2,58M$ affiché vs +14 837$ réel vérifié via fomoscan.sh), l'opérateur veut savoir empiriquement si copier certains wallets réels marcherait — sans jamais copier réellement ni transformer l'observation en trigger (doctrine CLAUDE.md "smart money = confirmation, jamais un trigger").
Solution : `wallet_copy_shadow.py`, même doctrine shadow que `narrative_signal_shadow.py`/`v8_limit_shadow.py` (jamais de capital réel, best-effort, ne lève jamais, ne bloque jamais le heartbeat). 8 wallets suivis, CHACUN avec son propre ledger fictif indépendant (jamais fusionné) : 3 avec un historique all-time vérifié via fomoscan.sh (songz +177 213$/52,1% WR, thokani +119 241$/45,2%, wrld_sol +74 277$/27,7%), 4 labellisés "Smart Money" par GMGN sur Base (PnL 30j positif, preuve plus faible), 1 front-runner récidiviste confirmé (inclus pour TESTER si suivre un front-runner connu est rentable, pas comme "bon trader"). Poll incrémental via `blockscout_client.get_token_transfers` (curseur par wallet, jamais retraité deux fois), ouvre une position fictive à 1 000$ sur un achat détecté (hors tokens de paiement WETH/USDC/USDbC/DAI/cbBTC), ferme au match sur une vente détectée, valorise via `dexscreener.fetch_token_pairs`. `summary()` rapporte le P&L réalisé/latent de CHAQUE wallet séparément — jamais un chiffre agrégé qui noierait un wallet qui marche dans 7 qui ne marchent pas. Câblé au heartbeat (`wallet_copy_shadow_cycle`, 15min), gate dédié `ARIA_WALLET_COPY_SHADOW_ENABLED` (OFF par défaut, pas de double-gate avec le test 1M$ — shadow indépendant).
`wallet_copy_shadow.py`/`heartbeat.py`/`tests/test_wallet_copy_shadow.py` — 10 nouveaux tests (ouverture/fermeture de position, exclusion des tokens de paiement, pas de doublon sur un wallet déjà en position, curseur anti-retraitement, jamais de levée d'exception même DB cassée). Pas encore déployé.

------------------------------------------------------------

[ETAT ACTUEL] Sujet : winrate 0% sur plusieurs poches papier — vérifié réel, pas un bug de calcul
Date : 2026.08.08 / Probleme : l'opérateur a demandé une vérification directe des winrates affichés sur les portefeuilles fictifs ("les wallet fictif ils marche pas je crois"). Vérification faite directement sur `aria.db` (pas de supposition) : swing (18 clôtures), scalping_v8 (43 clôtures), scalping_v1-v7 (poches retirées) et vc (1 clôture) affichent tous 0% de winrate sur trades clôturés.
Solution : le calcul lui-même n'a aucun bug (formule identique `paper_trader.portfolio_summary`/`performance_breakdown.compute_metrics`, aucun `pnl_usd` NULL sur une position clôturée, et scalping_v6 affiche 20% — donc pas un bug systémique). Deux causes réelles distinctes : (1) swing — très majoritairement le bug `BONDING_LIQUIDITY_FLOOR_USD` déjà corrigé le même jour (commit `dec1cf26`, déployé 15h14 UTC) : sorties mécaniques -0,25% à -0,87% quelques minutes après l'entrée ; l'historique gelé reste à 0% jusqu'à accumulation de nouveaux trades post-correctif (3 positions bonding ouvertes juste après le déploiement tenaient toujours 1h30 plus tard, signe que le correctif tient). (2) scalping_v8 — pas nouveau, déjà diagnostiqué le 06/08 comme un problème d'entrée (prix live jusqu'à 1 bougie après le signal), déjà dans le mandat v8 autonome en cours. Winrate 0% de clôture n'implique pas un mauvais résultat global : swing a des positions ouvertes largement gagnantes (ex. +35,7% latent sur une position de 20 000$, vérifié via prix DexScreener en direct) — le winrate ne compte que les trades CLÔTURÉS, jamais le latent.
Pas de fichier modifié — vérification pure, aucun code touché.
