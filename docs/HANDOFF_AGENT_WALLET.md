# HANDOFF — Homemade agent wallet (Safe + AllowanceModule on Robinhood Chain, Squads v4 on Solana)

> **PUBLIC repo — never an IP/secret/token/key/personal email address in clear here.** Variable names OK (e.g. `ARIA_SAFE_ROBINHOOD_TESTNET_RPC_URL`), their values never.

> Format: `[STATUS] Subject` / `Date : YYYY.MM.DD / Probleme : ...` / `Solution : ... — file (hash)`.
> `[STATUS]`: DEPLOYE / CODE (tested, not deployed) / CONFIG (no commit) / ETAT ACTUEL.

> Distinct from `docs/HANDOFF_COINBASE_CDP.md`, which covers the CURRENT 10-25$ real-capital pilot on a commercial provider (Coinbase CDP). This file covers the SELF-BUILT replacement architecture, testnet-only so far.

[CODE] Subject : Robinhood Chain swap router built in ISOLATION, deliberately never wired -- ready for a governance decision that has not been made yet
Date : 2026.08.23 / Probleme : the operator asked for real trading on Robinhood Chain; `CLAUDE.md`'s absolute rules name exactly three real-capital exceptions (fictitious paper-trading, Coinbase CDP pilot on Base, Solana pilot) and Robinhood is not among them -- adding it needs an explicit operator governance decision, never inferred from insistence. Separately, no swap mechanism existed anywhere in this repo for Robinhood Chain at all (`safe_robinhood_signer.py` only ever did bounded AllowanceModule TRANSFERS, never a DEX swap).
Solution : agreed compromise -- build the missing technical piece now, strictly isolated, so it is tested and ready the day the operator formally decides. Diligence first (depth proportional to the stakes, this touches a future real-capital mechanism): confirmed via Uniswap's own announcement (blog.uniswap.org/robinhood-chain-is-live) and its official per-chain deployment page (developers.uniswap.org v3 deployments) that Uniswap v2/v3/v4/UniswapX are genuinely deployed on Robinhood Chain -- but MAINNET ONLY (chain 4663, "Uniswap deployed a dedicated AMM as a primary public liquidity layer on day one"). No testnet (46630) deployment is documented anywhere found. The mainnet addresses this session did retrieve came back through a web-fetch summarization step rather than a byte-for-byte source render -- exactly the failure mode this project's own "never an assumed address in a signed tx, verify via eth_getCode" doctrine exists to catch (`sepolia_wallet.py`, `safe_robinhood_wallet.py`) -- and this session had no live RPC reachability to Robinhood Chain to run that verification (same limitation `sepolia_wallet.py`'s own docstring already documents once). Conclusion, stated rather than papered over: **no router/factory/quoter address is hardcoded anywhere in the new module** -- `RobinhoodSwapConfig` requires every address explicitly, validated at construction (chain_id locked to testnet 46630, slippage bounded to (0, 1000] bps per the project's absolute 10% rule, fee_tier/addresses all required, no default). Built `onchain/robinhood_swap_router.py`: `compute_min_amount_out` (pure slippage-floor arithmetic), `build_swap_transaction` (pure, unsigned `exactInputSingle` construction, reusing the exact same ABI fragment already proven live in `sepolia_wallet.send_test_swap_transaction` -- the stable Uniswap V3 interface, chain-agnostic unlike the address), and `execute_bounded_swap` (wrap-native [optional] -> approve -> swap, mirroring `sepolia_wallet`'s own three-real-signed-transaction sequence, declared `async` purely to match the injectable `send_fn` shape used by `homemade_agent_wallet.attempt_transfer` -- documented as the future integration point, not wired). Isolation enforced by construction, not a runtime flag: **no `ARIA_*_ENABLED` gate exists in this module on purpose** (a gate would imply one flag from production; this isn't meant to be reachable at all yet), no default RPC URL, `w3` is a required parameter with no fallback, and a repo-wide grep confirms the only references to `robinhood_swap_router` outside this file are its own test file. 25 new offline tests (fake-w3/fake-account injection, same doctrine as `test_sepolia_wallet.py`/`test_safe_robinhood_signer.py`, zero live network calls) cover: config validation (chain-id lock, missing addresses, slippage bounds including the exact 10% ceiling), slippage-floor arithmetic (including floor-not-round behavior), pure transaction construction, and the full wrap/approve/swap sequence (happy path with and without wrap, wrong-chain refusal, over-cap/zero-amount rejection, send failure, reverted receipt). 300 tests green covering this new module plus every other Robinhood/Sepolia onchain module and `test_coherence.py` (the mechanical-guardrail suite); a full-repo run was also started but not waited out to completion in this session (heavy CPU contention with a parallel session's own test run) -- CI runs the complete suite on push regardless. OUT OF SCOPE, unchanged and deliberately not started: no real testnet or mainnet address configured anywhere, no production/heartbeat/paper_trader caller, no gate of any kind, no governance decision made -- this ships purely as tested, ready infrastructure. — `onchain/robinhood_swap_router.py`, `tests/test_robinhood_swap_router.py`.

------------------------------------------------------------

[ETAT ACTUEL] Subject : EVM/Robinhood leg's 18/08 "4 real testnet attempts" claim not visible in prod DB -- Solana leg's 2 rows are
Date : 2026.08.23 / Probleme : this file's 18/08 entry claims "all 4 attempts (blocked/failed-during-a-bug-fix/blocked/ok) confirmed present in `agent_wallet_tx_log` with zero silent gaps" for the EVM/Robinhood leg -- a live query against the real `/opt/aria-data/aria.db` finds ZERO rows with `wallet_product='homemade_agent_wallet'`. Same query finds exactly 2 rows for `wallet_product='homemade_agent_wallet_solana'`, matching the 19/08 entry below.
Solution : not corrected in code, root cause identified -- the 18/08 one-off script (never committed, same as every other live-proof script in this file) most likely never called `configure_data_dir("/opt/aria-data")` before writing its log, so it wrote to a dev/session-default DB instead of prod (a known project pitfall, cf. `feedback_scratch_scripts_need_data_dir` memory). Supporting evidence: the 19/08 entry right below explicitly says "prod DB, `configure_data_dir(\"/opt/aria-data\")`" -- a precision absent from the 18/08 entry, consistent with the same mistake being caught and fixed the very next day. The on-chain transactions themselves (`0x7438a685...`, `0x649ba041...`) are real and independently verifiable on Robinhood Chain testnet regardless of this local logging gap -- only the APPLICATION-LEVEL log trail is missing, not the underlying proof. No action needed unless a future promotion decision specifically needs to cite these 4 rows from `agent_wallet_tx_log` -- cite the tx hashes directly instead.
---
[CODE] Subject : Solana leg reaches TRUE wrapper-level parity with the EVM leg -- the real `homemade_agent_wallet.attempt_transfer()` guardrail wrapper (not just the signer module) proven live on devnet, `wallet_product` threading covered by real tests
Date : 2026.08.19 / Probleme : the same-day "Solana leg reaches the SAME promotion" entry right below proved `squads_solana_signer.send_spending_limit_transfer` live on devnet, but that call bypassed the guardrail wrapper entirely (called the signer module's own functions directly) -- unlike the EVM leg's 18/08 milestone, which explicitly re-ran its successful transfer "through the full wrapper stack (gate+kill-switch+cap+real-remaining-check+send+receipt+log)". The two legs' HANDOFF entries both claimed "IDENTICAL... promotion" / "IDENTICAL real-execution parity", but the WRAPPER layer itself (`homemade_agent_wallet.attempt_transfer`) had only ever been proven live for the EVM leg -- the Solana leg's `wallet_product` parameter (added the same day) also had zero test coverage: every test in `test_homemade_agent_wallet.py` exercised only the default (EVM) product, never `WALLET_PRODUCT_SOLANA`, so a caller-vs-default mixup anywhere in `attempt_transfer`'s `_blocked`/`record_transaction` call sites could have gone silently unnoticed.
Solution : two changes. (1) 8 new offline tests in `test_homemade_agent_wallet.py` -- each `_blocked` call site (gate disabled, real-remaining exceeded) and the `ok`/`failed`/`reverted` outcomes now asserted with `wallet_product=WALLET_PRODUCT_SOLANA` explicitly, confirming the logged `agent_wallet_log` row actually carries the Solana product name and not the EVM default -- never exercised before this pass. (2) A one-off devnet script (NOT committed, same doctrine as every other live-proof script in this file) called `homemade_agent_wallet.attempt_transfer()` itself -- the real wrapper, `ARIA_HOMEMADE_AGENT_WALLET_ENABLED=1`, real kill-switch checks (confirmed both OFF before running), `wallet_product=WALLET_PRODUCT_SOLANA` -- against the SAME real multisig/spending-limit/vault from the 18/08-19/08 milestones (spending-limit PDA `pcdcsyywGo2kJaV9exuwHGoAdENcs6L3RaVYPinx1Q5`, vault PDA `2bv2z7K2ZP3vmRYVzWC4fQQmAtKqr5Ww9w4jqi79cJpp`, independently re-derived and matched before use). Two real attempts through the FULL wrapper: (a) 2,400,000 lamports (over the real remaining, 1,900,000 at the time) correctly BLOCKED LOCALLY by `attempt_transfer`'s own `remaining_fn` pre-check -- `tx_hash=""`, `send_fn` never invoked, on-chain state independently re-read and confirmed UNCHANGED (remaining_amount still 1,900,000, vault still 3,900,000 lamports); (b) 25,000 lamports (well under both the app cap and the real remaining) sent through the SAME full wrapper -- `status="ok"`, real tx `67fBmPhNSswzeVgp2w9TYY9iP3RHt9sjEvgNJz5YM1Z84BEt1xp2YVr3Ktx8exRvsCzLpPSQVwFoA2YJuacj8Xuj`, independently re-verified via a FRESH `getTransaction` RPC call (`err: null`, `preBalances`/`postBalances` on the vault account confirming `3,900,000 -> 3,875,000`, program log `"Instruction: SpendingLimitUse"` from `SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf`) and a separate fresh `getBalance` call on the vault PDA. Both attempts confirmed present in the REAL `agent_wallet_tx_log` (prod DB, `configure_data_dir("/opt/aria-data")`), correctly tagged `wallet_product="homemade_agent_wallet_solana"`/`chain="solana"`, zero silent gaps -- the exact guarantee the module's own docstring claims, now proven live rather than assumed from the signer module's own proof. This is the missing half of "both legs at IDENTICAL... parity": the signer-module layer already matched (18/08-19/08 entries below), and now the wrapper layer matches too. Full aria-core suite re-run green after both changes (offline tests + no production code changed by the live script). STILL OUT OF SCOPE, unchanged: no mainnet, no production/heartbeat caller anywhere (gate OFF), the AllowanceModule v0.1.1 audit-gap operator decision remains open, real-capital caps unchanged. — `tests/test_homemade_agent_wallet.py`.

------------------------------------------------------------

[CODE] Subject : Solana leg reaches the SAME promotion as the EVM leg -- real signing module (`squads_solana_signer.py`) committed, tested, and verified with REAL devnet sends; guardrail wrapper extended to both chains
Date : 2026.08.19 / Probleme : the 18/08 "PARITY" milestone proved the full Squads v4 cycle live on devnet, but only through a one-off script, deliberately never committed (same doctrine as the EVM leg had before its own 18/08 promotion) -- no reusable, tested, committed code path existed yet for the Solana leg, and `homemade_agent_wallet.py`'s guardrail wrapper, while already chain-agnostic in its `send_fn`/`remaining_fn` injection, had no distinct `wallet_product` value to tell Solana rows apart from EVM ones in `agent_wallet_log`.
Solution : two changes, both exercised against the REAL devnet before being trusted, never assumed from the 18/08 one-off proof alone. (1) `onchain/squads_solana_signer.py` -- deliberately does NOT use anchorpy at the instruction-building layer (the two anchorpy 0.21 bugs from the 18/08 milestone live inside anchorpy's *generated client* code, never called here). Instead: the Anchor 8-byte instruction discriminator (`sha256("global:spending_limit_use")[:8]`), account order/mut/signer flags, and Borsh arg layout are all taken from Squads v4's real fetched on-chain IDL (`squads_solana_wallet.fetch_program_idl`, re-verified live this session -- `squads_multisig_program` v2.1.0, and its IDL DOES carry `isOptional` metadata contrary to the 18/08 entry's characterization; the real anchorpy bug was in the library's own instruction builder not honouring that metadata, not in the IDL itself) and the real `Squads-Protocol/v4` GitHub source (`state/seeds.rs`, `instructions/spending_limit_use.rs`, `state/spending_limit.rs`, fetched via `gh api` this session, never from memory -- the actual path is `programs/squads_multisig_program/src/...`, not `programs/multisig/src/...` as a naive guess would assume). Raw `solders` primitives build the real transaction; PDA derivation (`derive_multisig_pda`/`derive_spending_limit_pda`/`derive_vault_pda`) was independently re-derived and matched BYTE FOR BYTE against the real already-existing on-chain accounts from the 18/08 milestone before being trusted. `send_spending_limit_transfer()` re-reads the REAL remaining spending limit on-chain right now (never a cached figure, manually Borsh-decoded from `state/spending_limit.rs`'s real layout since this module never constructs an anchorpy account-fetcher), rejects locally if the request exceeds it, builds+signs+sends the real instruction, and waits for a REAL FINALIZED status before reporting success. The delegate key loader (`_load_delegate_key`) reads the exact native `solana-keygen` JSON-array format already on disk at the real, already-funded devnet delegate path (`/opt/aria-data/agent-wallet-solana-devnet/delegate.json`, chmod 600, reused rather than regenerated -- only the address was ever inspected). (2) `homemade_agent_wallet.py` -- added a `wallet_product` parameter to `attempt_transfer()` (defaults to the existing `WALLET_PRODUCT` for the EVM leg, unchanged behavior; pass the new `WALLET_PRODUCT_SOLANA = "homemade_agent_wallet_solana"` for the Solana leg), threaded through every `_blocked`/`record_transaction` call site -- same gate/kill-switch/cap/logging doctrine either way, only the logged product name differs, so both legs share one wrapper with zero duplicated guardrail logic. **One real bug found and fixed live before trusting the module**: `_await_confirmation` originally accepted Solana's `confirmationStatus == "confirmed"` as sufficient for reporting `"ok"` -- a real live send exposed a genuine race: a plain `getAccountInfo` call (`read_spending_limit`, RPC default commitment) made milliseconds after a `"confirmed"` status still returned the PRE-transaction `remaining_amount` (a script run showed `remaining_amount` unchanged right after a transfer whose own `getTransaction` proof showed it had already moved real lamports, `4_000_000 -> 3_950_000`) -- `"confirmed"` and `"finalized"` are genuinely different commitment levels a few slots apart on devnet. Fixed by requiring `confirmationStatus == "finalized"` before ever reporting `"ok"`, trading a few seconds of extra latency for a guarantee that any state a caller reads immediately afterward is already consistent -- re-run live after the fix confirmed the race is gone (`remaining_amount` correctly reflected the spend on the very next read). Real verification, this session, against the same real multisig/spending-limit/vault from the 18/08 milestone (spending-limit PDA `pcdcsyywGo2kJaV9exuwHGoAdENcs6L3RaVYPinx1Q5`, vault PDA `2bv2z7K2ZP3vmRYVzWC4fQQmAtKqr5Ww9w4jqi79cJpp`): a real 50,000-lamport transfer sent through the NEW committed module's full path (`send_spending_limit_transfer`, local guard included) SUCCEEDED, tx `3jWK6S4NEuE5Bvccbwhz213hatPmbRvknZwWpf5LVfWNXuJn9w5JeFpd4KYNvEYpNqsEE2vb28iZ8u1ifQvpshWv`, vault balance and `remaining_amount` both independently re-read and confirmed decremented correctly (`1,950,000 -> 1,900,000`); a request for 5,000,000 lamports (far over the real remaining) through the SAME full committed wrapper was correctly rejected LOCALLY, `tx_hash=None`, no send attempted; then, to reproduce the genuine ON-CHAIN rejection (not just the client-side guard) THROUGH the real committed code -- the same double-sided proof already done once via the 18/08 one-off script, this time through committed code -- a request for 2,000,000 lamports (over the real remaining) was built and sent using the SAME committed low-level building blocks (`build_spending_limit_use_instruction`, `_sign_and_send_instruction`, PDA derivation, delegate key loader) called directly, deliberately bypassing `send_spending_limit_transfer`'s own local pre-check (a guardrail feature, not a bypass hole in production -- that pre-check has no bypass parameter anywhere in the committed module) -- tx `4dLDuNReUMBAkewshV2KrMPB13eqyE1TyvHrckxhS3e638bnxFv8G1ECagu7R7KJzNfiH1yo4CHw3pSFRPbsfNQM` genuinely REVERTED on-chain with the program's own real `6026: Spending limit exceeded` error, vault balance and `remaining_amount` both independently re-read and confirmed UNCHANGED. 26 new offline tests (fake-RPC-client injection for the signer, real offline `solders.Keypair` for the key-loading path, PDA-derivation regression tests pinned against the real devnet values above -- never a live network call in the automated suite), full aria-core suite re-run green. STILL OUT OF SCOPE, unchanged: no mainnet, no production/heartbeat caller anywhere (gate `ARIA_HOMEMADE_AGENT_WALLET_ENABLED` OFF, nothing wired for either chain), the EVM leg's AllowanceModule v0.1.1 audit-gap operator decision remains open, and both legs are still bounded to the same order-of-magnitude caps already proven on-chain (0.003 ETH-equivalent / 0.003 SOL) -- not the operator's eventual 200$ target, a distinct future step. Both legs (EVM Robinhood Chain + Solana) are now at IDENTICAL committed-code promotion, mirroring their prior real-execution parity. — `onchain/squads_solana_signer.py`, `homemade_agent_wallet.py`, `tests/test_squads_solana_signer.py`.

------------------------------------------------------------

[ETAT ACTUEL] Subject : wallet-rotation policy after a drain/compromise incident -- never reuse the address, even if funds are partially returned
Date : 2026.08.19 / Probleme : GoPlus Security tweet flagged TRAITER (whale "TLBL": approval phishing in 2023, then a private-key leak in 2026 on the SAME wallet reused after the first drain, >$50M cumulative loss) -- ARIA runs a real agent wallet (CDP pilot, 10-25$) and is designing this self-built Safe+AllowanceModule/Squads v4 architecture for future real capital, with no documented policy on what happens if a wallet is ever found drained or compromised.
Solution : policy gravee, no code change needed now (nothing to enforce mechanically yet -- this becomes actionable the day an incident actually happens). If the CDP pilot wallet, or any future wallet this architecture manages holding real capital, is ever found drained or compromised -- even if the attacker returns part of the funds -- NEVER reuse that address. Mandatory rotation to a freshly generated address; every hard-coded reference (`ALLOWED_TRANSFER_ADDRESS` and equivalents) updated in the same pass; the incident documented in `docs/HANDOFF_SECURITE.md` BEFORE any activity resumes on the new address. See `docs/runbook-incident-agent.md` for the broader emergency checklist this slots into (stop first, repair from a clean machine, never rotate secrets from the infected machine).

------------------------------------------------------------

[CODE] Subject : EVM leg -- real signing module PROMOTED into the repo (`safe_robinhood_signer.py`) + a guardrail wrapper (`homemade_agent_wallet.py`) mirroring `agent_wallet_pilot.py`'s doctrine, both verified with REAL testnet sends
Date : 2026.08.18 / Probleme : every real transaction proven so far (this file's own "FIRST REAL on-chain cycle" entries) came from one-off scripts, deliberately never committed -- there was no reusable, tested, committed code path that could actually be called from anywhere, and no gate/kill-switch/logging wrapper equivalent to `agent_wallet_pilot.py`'s doctrine existed for this wallet at all.
Solution : two new modules, both exercised against the REAL testnet (chain 46630) before being trusted, never assumed from the simulation module's proof alone. (1) `onchain/safe_robinhood_signer.py` -- reuses `safe_robinhood_simulation.compute_transfer_digest` (already proven byte-for-byte against the contract's own `generateTransferHash`) rather than reimplementing it, adds only the missing piece: `send_allowance_transfer()` re-reads the REAL remaining allowance on-chain right now (never a cached figure), rejects locally if the request exceeds it, signs with `Account.unsafe_sign_hash` (eth_account 0.13.7 -- the explicit "I already have the final digest" API, not a message-construction helper), builds+signs+sends a REAL transaction via `account.sign_transaction`/`w3.eth.send_raw_transaction`, and waits for the real receipt (`status==1` vs `reverted` vs `unknown` -- a mined tx can still revert, a truthy `tx_hash` alone would be a false positive). The delegate private key is loaded from a `{"address","private_key"}` JSON file at a path the caller supplies explicitly (no default -- fail-closed via `DelegateKeyError`); reused the SAME key file already funded during the earlier milestone (`/opt/aria-data/agent-wallet-testnet/robinhood_testnet_delegate.json`, chmod 600, outside the repo, never read into a session transcript -- only its `address` field was ever inspected, via a script that printed key NAMES not values). (2) `homemade_agent_wallet.py` -- the missing guardrail layer: dedicated gate `ARIA_HOMEMADE_AGENT_WALLET_ENABLED` (OFF by default), `outgoing_pause`/`custody_pause` kill-switch checks, an app-level native-unit cap (`MAX_TRANSACTION_NATIVE_UNITS`, deliberately set to the SAME order of magnitude already proven on-chain -- 0.003 ETH-equivalent -- NOT the 200$ target the operator set for AFTER the architecture is proven, see memory `project_agent_wallet_cap_200usd`: raising it is a distinct future operator step), and systematic logging via the SAME `agent_wallet_log.record_transaction` table the CDP pilot uses (`wallet_product="homemade_agent_wallet"` distinguishes the rows). Explicitly documented as DEFENSE IN DEPTH, not the sole guardrail -- unlike the CDP pilot, this wallet's real cap is already enforced ON-CHAIN by the contract itself. REAL verification, this session: a live re-read of the existing Safe's allowance confirmed the exact state left by the earlier milestone (`remaining=2_000_000_000_000_000` wei, matching "Safe balance 0.004 ETH" from that entry); a real 100_000_000_000-wei transfer sent through the NEW committed module alone (tx `0x7438a685...`, `status=1`, `spent` correctly incremented 1.0e15->1.0001e15, module `nonce` incremented 2->3, all independently re-read via a fresh RPC call); an over-remaining-allowance request correctly rejected BEFORE any send attempt; then the SAME transfer re-run through the full wrapper stack (gate+kill-switch+cap+real-remaining-check+send+receipt+log), tx `0x649ba041...`, `status="ok"`, and all 4 attempts (blocked/failed-during-a-bug-fix/blocked/ok) confirmed present in `agent_wallet_tx_log` with zero silent gaps. One real bug found and fixed live before trusting the wrapper: `send_allowance_transfer` was originally a plain sync function, but the wrapper's injectable `send_fn` interface (matching `agent_wallet_pilot.py`'s `swap_fn`/`transfer_fn`) is `async` -- `homemade_agent_wallet.attempt_transfer` crashed with `'dict' object can't be awaited` on the first live run; fixed by declaring `send_allowance_transfer` as `async def` (it never actually yields control mid-call, web3.py's HTTP provider is synchronous -- the `async` is purely an interface-consistency wrapper, documented as such in the function's own docstring). 24 new offline tests (fake-w3 injection for the signer, fake send_fn/remaining_fn for the wrapper, real offline `eth_account` keypairs for the key-loading path -- never a live network call in the automated suite), added to CI. STILL OUT OF SCOPE, unchanged from the parity entry below: no mainnet, no production/heartbeat caller anywhere (gate OFF, nothing wired), the AllowanceModule v0.1.1 audit-gap operator decision remains open, and the Solana leg's own real signer (multisig_create_v2/multisig_add_spending_limit/spending_limit_use instruction builders) is NOT yet promoted into committed code -- still one-off-script-only on that leg, a separate and larger chantier (Squads v4 has no official Python SDK; those instructions were hand-built from the real on-chain Rust source during the earlier milestone and never committed). — `onchain/safe_robinhood_signer.py`, `homemade_agent_wallet.py`, `tests/test_safe_robinhood_signer.py`, `tests/test_homemade_agent_wallet.py`, `.github/workflows/ci.yml`.

------------------------------------------------------------

[DEPLOYE] Subject : Solana leg reaches PARITY with the EVM leg -- FIRST REAL on-chain cycle: multisig created, spending limit added, real delegate-signed transfer accepted AND rejected, all on devnet, zero real value
Date : 2026.08.18 / Probleme : same day, immediately after `fetch_program_idl()` -- operator funded the generated devnet owner address (same manual in-browser faucet pattern as the EVM leg), unblocking real execution on the Solana leg for the first time. Squads v4 has no official Python SDK and this session's first real attempt hit 2 genuine anchorpy 0.21 bugs that had to be found and worked around, not just a happy-path build.
Solution : PDA seeds and account structures NEVER guessed -- pulled from the real `Squads-Protocol/v4` GitHub source (`state/seeds.rs`, `instructions/multisig_create.rs`, `instructions/multisig_add_spending_limit.rs`, `instructions/spending_limit_use.rs`) via `gh api`/raw fetch, the same "verify against official source, never memory" discipline as the EVM leg's AllowanceModule ABI. Two real anchorpy bugs found and worked around: (1) encoding a `solders.Pubkey` NESTED inside a generated Borsh-arg dataclass (e.g. `Member.key`) crashes with `TypeError: cannot pickle 'solders.pubkey.Pubkey' object` (anchorpy's construct-based encoder calls `dataclasses.asdict` -> `copy.deepcopy`, and solders' PyO3 Pubkey isn't picklable) -- fixed by passing raw `bytes(pubkey)` for any Pubkey field going into an ARGS struct (never for instruction ACCOUNTS, which stay real `Pubkey` objects); (2) this IDL predates Anchor's `isOptional` account metadata, so anchorpy has no way to know `mint`/`vault_token_account`/`destination_token_account`/`token_program` are optional (SPL-token-only) accounts -- passing `None` crashes (`cannot be converted to 'Pubkey'`), omitting them crashes too (`not provided`) -- fixed via Anchor's own client convention for an omitted `Option<Account>`: pass the PROGRAM's own id as the sentinel address in that slot (the on-chain runtime deserializes a match against `program_id` as `None`). Real "controlled" multisig created (`config_authority = Some(owner)`, threshold 1/1, matching the EVM leg's single-owner-Safe simplicity) so `multisigAddSpendingLimit` could be called DIRECTLY by the owner, no propose/vote/execute cycle needed -- `Period::OneTime` chosen for the spending limit, same one-shot-never-refills regime the operator already decided for the EVM leg (17/08). Real cycle, 6 real transactions across the pieces (multisig create, spending-limit add, vault funding, delegate gas funding, under-cap spend, over-cap attempt): (1) `multisig_create_v2` -- real tx, `Multisig` account independently re-fetched confirming `threshold=1`, `config_authority=owner`; (2) `multisig_add_spending_limit` -- real tx, `SpendingLimit` account confirms `amount=3_000_000` lamports (0.003 SOL, same order of magnitude as the EVM pilot cap), `members=[delegate]`; (3) vault PDA funded with 0.005 SOL from the owner; (4) delegate funded with 0.002 SOL for ITS OWN transaction fee -- same real lesson already hit once on the EVM leg (the delegate is the fee payer for its own tx even though the SPEND moves the vault's funds, not the delegate's); (5) THE proof -- delegate's OWN signature (never the owner's) used `spending_limit_use`: 0.001 SOL (under the 0.003 cap, 0 already spent) SUCCEEDED, real tx, vault balance 0.005->0.004 SOL, on-chain `remaining_amount` 3_000_000->2_000_000; then 0.0025 SOL (would exceed the 0.002 SOL actually remaining) REJECTED on-chain with the program's OWN real error `6026: Spending limit exceeded` -- NOT a client-side failure this time, a genuine on-chain revert, vault balance and `remaining_amount` both UNCHANGED. Independently re-verified via a RAW RPC call (bypassing anchorpy entirely) -- `getBalance` confirms 4_000_000 lamports, `getSignaturesForAddress` on the vault shows exactly 2 finalized transactions ever touched it (funding + the one successful spend), the rejected over-cap attempt never landed at all. Real cost: ~0.001 SOL total fees across all transactions (zero economic value, devnet). Scripts one-off, NOT committed to the repo -- same doctrine as the EVM leg (`onchain/squads_solana_wallet.py` stays read-only-only, its own guardrail untouched). Keys: owner and delegate each a fresh devnet-only keypair, private keys in `/opt/aria-data/agent-wallet-solana-devnet/` (outside the repo, chmod 600, never displayed -- only public addresses ever shown); `multisig_create_key.json`/`spending_limit_create_key.json` also persisted there so a script re-run reuses the same on-chain multisig/spending-limit instead of spawning duplicates (2 earlier throwaway multisigs exist on devnet from mid-development iteration before this idempotency was added -- harmless, zero value, never cleaned up). STILL OUT OF SCOPE: no mainnet, no `ConfigTransaction` propose/vote/execute path exercised (only the single-owner `config_authority`-direct path), no real signing module added to the repo (same as the EVM leg's own still-open decision). Both legs (EVM Robinhood Chain + Solana) are now at IDENTICAL real-execution parity for the first time. — `onchain/squads_solana_wallet.py` unchanged in this entry (read-only module, no commit needed for this milestone).

------------------------------------------------------------

[CODE] Subject : Solana leg -- anchorpy wired as a real dependency, fetch_program_idl() built and verified live against Squads v4's real devnet IDL
Date : 2026.08.18 / Probleme : the Solana leg was stuck at the read-only "step 1 equivalent" (program-deployment check only), with anchorpy flagged as a real risk (stale PyPI releases, never actually exercised as project code -- only smoke-tested earlier in an isolated throwaway venv, never installed for real).
Solution : anchorpy added as a new `agent_wallet_solana` optional-dependency group (`pyproject.toml`) rather than a hard dependency -- same pattern as `agent_wallet`/`x402`. Installing it surfaced two real, non-obvious problems, both verified and fixed rather than assumed away: (1) it DOWNGRADED the already-present `solana`/`solders` packages (transitive via `cdp-sdk`, which the REAL-CAPITAL pilot depends on) to satisfy its own pin -- checked `pip check` (clean) AND re-ran the full CDP/agent-wallet test suite (480 tests) green on the downgraded versions before trusting it, never assumed compatible from a clean pip resolve alone; (2) anchorpy registers its own pytest plugin via a setuptools entry point requiring `pytest_xprocess` (a package meant for spinning up a local validator for anchorpy's OWN test suite, no use to this project) -- broke pytest COLLECTION project-wide the instant anchorpy was installed, fixed by `addopts = "-p no:anchorpy"` in `pyproject.toml` rather than adding an unused dependency. New `fetch_program_idl()` in `squads_solana_wallet.py` -- read-only, cannot reach a write path (no `Wallet`/signer ever constructed), injectable `fetch_fn` for tests (same DI pattern as `verify_program_deployed`'s `client` param, never a real network call in the automated suite). Verified live TWICE: once via the throwaway venv (pre-dates this commit), and again through the REAL project module post-install -- `squads_multisig_program` v2.1.0, 36 real instructions including `multisigCreateV2`/`spendingLimitUse`, confirmed live right now, not stale. 4 new tests, 7/7 green in this file; full aria-core suite re-run green after both the dependency downgrade and the pytest-plugin fix. NEXT: (2) multisig + SpendingLimit creation on devnet. Checked live rather than assumed: the Solana devnet faucet is ALSO not reliably reachable autonomously -- `api.devnet.solana.com`'s `requestAirdrop` RPC returned a real `429` ("reached your airdrop limit today or the airdrop faucet has run dry"), and the dedicated `faucet.solana.com` web app has no plain REST endpoint at the guessed path (`/api/v1/airdrop` 404s -- it is a JS app, likely gated the same way the Robinhood Chain faucet was). A fresh devnet-only keypair was generated for this leg (`/opt/aria-data/agent-wallet-solana-devnet/owner.json`, chmod 600, address `Bup16ydw8SQDxzzRs4Fy4aT6B7JSpEon1FHYjAesYJ4R` -- public, zero value, safe to share) -- needs the same operator-assisted in-browser funding step that unblocked the Robinhood Chain leg earlier this session before any real transaction can be attempted. — `pyproject.toml`, `onchain/squads_solana_wallet.py`, `tests/test_squads_solana_wallet.py`.

------------------------------------------------------------

[ETAT ACTUEL] Subject : GoPlus Security watch #131 -- wallet-rotation-after-compromise policy documented, no code change
Date : 2026.08.18 / Probleme : whale "TLBL" case (phishing approval drain in 2023, then a private-key leak on the SAME wallet reused after the first drain, cumulative >50M$ lost) flagged TRAITER by the goplus-security-watch judge pass -- ARIA runs a real agent wallet pilot (10-25$, Coinbase CDP) and is actively building the self-built Safe+AllowanceModule/Squads v4 replacement documented in this file, with no written policy on what happens if a wallet is ever found drained or compromised.
Solution : policy only, nothing to build -- if the agent wallet pilot address, or any future ARIA-controlled wallet holding real capital, is ever found drained or otherwise compromised (even if an attacker later returns part of the funds), it is NEVER reused. Mandatory rotation to a freshly generated address; every hard-coded reference updated (`ALLOWED_TRANSFER_ADDRESS` and equivalents); the incident documented in `docs/HANDOFF_SECURITE.md` BEFORE any activity resumes on the new address. Applies equally to the current CDP pilot (`docs/HANDOFF_COINBASE_CDP.md`) and to this file's own testnet architecture once it reaches real capital.

------------------------------------------------------------

[DEPLOYE] Subject : FIRST REAL on-chain cycle -- Safe deployed, module enabled, real cap set, real delegate-signed transfer accepted AND rejected, all on testnet, zero real value
Date : 2026.08.18 / Probleme : every prior milestone (steps 1-4, 17/08) was `eth_call` SIMULATION only -- proven the mechanics were correct, but never a real signed transaction, blocked on testnet gas the official faucet (behind a Vercel bot-check, unreachable from the VPS/session) couldn't deliver autonomously. Operator funded a fresh testnet-only owner EOA manually via the real faucet in-browser this session, unblocking real execution for the first time.
Solution : 6 REAL transactions sent and independently re-verified via a fresh, separate RPC call each time (never trusting script output alone) -- (1) Safe v1.4.1 created via a real `createProxyWithNonce` call (owner-only, threshold 1/1) at `0x0c896addFe4751cE5Bf2276Aac6B0AeB3d09e194`, tx `0xab18553001033709da57cc3de617f432debb47607c236126b9ee4ab85a8568d9`, confirmed via real `eth_getCode` (171 bytes) and the factory's own `ProxyCreation` event; (2) `enableModule(AllowanceModule)` via a real owner-signed `execTransaction` (digest obtained from the Safe's OWN `getTransactionHash()`, never reconstructed locally, to rule out a wrong EIP-712 typehash), tx `0x88cd54479ecd4df72958e12c7d31e61573d7a944a46f62a6716630709fe11745`, `isModuleEnabled` independently re-confirmed `true`; (3) Safe funded with 0.005 real testnet ETH; (4) a SECOND, DISTINCT testnet-only keypair generated to act as the delegate (the future agent's own signing key -- NEVER the owner key, matching the target architecture where the EOA is only ever a signing key and the enforced bounds live in the contract), registered via a real owner-signed `addDelegate`; (5) a real one-shot allowance set via owner-signed `setAllowance(delegate, token=address(0), 0.003 ETH, resetTimeMin=0, resetBaseMin=0)` -- `token=address(0)` as the native-currency convention verified live against the OFFICIAL `safe-global/safe-modules` GitHub source (`if (token == address(0)) { safe.execTransactionFromModule(...) }`), not assumed from memory, since Blockscout's own verified-source lookup was unavailable this session (`robinhood` chain needs a Pro key not configured here) -- real on-chain state read back afterward confirmed `[amount=3e15, spent=0, resetTimeMin=0, ..., nonce=1]`; (6) THE proof -- the delegate's OWN key (never the owner's) signed and sent `executeAllowanceTransfer` twice: 0.001 ETH (under the remaining cap) SUCCEEDED (status 0x1, Safe balance 0.005->0.004 ETH, on-chain `spent` updated to 1e15), then 0.0025 ETH (would have pushed cumulative spend to 0.0035 > the 0.003 cap) REVERTED (status 0x0, Safe balance UNCHANGED at 0.004 ETH) -- the cap rejected a real signed over-limit request from the delegate's own key, exactly the guarantee this whole architecture exists to provide, now proven for real rather than simulated. Total real cost: ~0.00003 ETH testnet gas across all 6 transactions (zero economic value). Scripts are one-off, NOT committed to the repo (`onchain/` stays simulation/read-only only, per its own locked guardrails -- `test_simulation_module_never_sends_a_transaction`/`test_allowance_module_abi_is_read_only` untouched) -- a real signing module is a separate, reviewed, tested future addition if this pattern gets promoted beyond a manual bootstrap. Keys: owner and delegate each a fresh testnet-only keypair, private keys in `/opt/aria-data/agent-wallet-testnet/` (outside the repo, chmod 600, never displayed in any session output -- only public addresses ever shown). STILL OUT OF SCOPE: no mainnet, the v0.1.1 audit-gap operator decision from step 1 remains open, Squads v4/Solana side still only at read-only verification (step 1 equivalent) -- not brought to parity with this real-execution milestone in this pass.

------------------------------------------------------------

[CODE] Subject : First milestone -- read-only contract/program deployment verification (Safe + Squads)
Date : 2026.08.17 / Probleme : the operator validated a self-built dual-key wallet architecture over every commercial provider evaluated (Coinbase CDP / MetaMask Agent Wallet / Turnkey / MoonPay PayBox) for a future modest real-capital pilot on Robinhood Chain + Solana. Nothing existed yet, and the addresses/program IDs came from a research agent -- never trustworthy on the agent's word alone for anything that will later hold capital.
Solution : two new modules scoped strictly to read-only verification against the real TESTNET (Robinhood chain 46630) and DEVNET, NO private key, NO signing logic. Safe v1.4.1 singleton + AllowanceModule v0.1.1 confirmed with real non-empty bytecode via live `eth_getCode`; Squads v4 program confirmed `executable=true` via live `getAccountInfo`. Every address independently re-verified live in-session before commit. Mainnet deliberately out of scope, gated behind its own separate operator validation. — `onchain/safe_robinhood_wallet.py`, `onchain/squads_solana_wallet.py` (f1e37f6d)

------------------------------------------------------------

[CODE] Subject : Step 1 -- AllowanceModule ABI + live allowance reading, with the field order PROVEN experimentally
Date : 2026.08.17 / Probleme : step 1 of the plan (read a live allowance state) needs the module's ABI. Writing an ABI from memory is exactly the kind of shortcut that silently corrupts a financial guardrail -- and a first cross-check attempt was itself found WEAK mid-session and discarded: comparing `getTokenAllowance` against `allowances()` on an unconfigured Safe returns all zeros, which proves nothing about field ORDER (zero == zero in any order). A wrong field order would make `remaining` silently false.
Solution : ABI taken from the contract's VERIFIED source on Blockscout (Robinhood mainnet exposes the same CREATE2 address, `is_verified: true`, compiler v0.7.6), then each 4-byte selector recomputed and confirmed present in the bytecode actually deployed on testnet (a deliberately fake signature confirmed ABSENT in the same pass, proving the check discriminates). Field order then PROVEN experimentally via `eth_call` + `stateDiff` injecting a packed slot with five DISTINCT values (struct packs to exactly one slot, `allowances` is the first non-constant variable -> slot 0): all five returned in order. The same probe proved the contract's read-time reset (inject `spent=250` with a stale `lastResetMin` -> returns `spent=0`), which is what makes `remaining = amount - spent` trustworthy rather than stale. Zero on-chain writes (`stateDiff` is simulation-only). 19 dedicated tests, full suite green (10459). — `onchain/safe_robinhood_wallet.py`, `tests/test_safe_robinhood_wallet.py` (dfc57342)

------------------------------------------------------------

[ETAT ACTUEL] Subject : Real scope today, and the three things that must be settled before any real capital
Date : 2026.08.17 / Probleme : snapshot to avoid a future session assuming this architecture is further along than it is, or missing the open blockers.
Solution : REAL SCOPE TODAY -- read-only, testnet/devnet only, zero private key, zero signing, and the modules are imported NOWHERE in production (verified three ways: no importer outside their own tests, not exported by `onchain/__init__.py`, no gate/env var triggering them). The embedded ABI contains ONLY `view` functions as a STRUCTURAL guardrail (web3 physically raises `ABIFunctionNotFound` on `setAllowance`/`executeAllowanceTransfer`), locked by `test_allowance_module_abi_is_read_only`. THREE OPEN POINTS before real capital: (1) AUDIT GAP -- Safe's official docs list an audit for AllowanceModule v0.1.0 (Oct 2020) ONLY; the deployed v0.1.1 has no audit listed. Mitigating fact, verified in the changelog: 0.1.1 = 0.1.0 + two BUG FIXES (broken EIP-712 typehash, and a `require(resetTimeMin > 0)` guarding a division-by-zero), i.e. strictly safer than the audited version, no new attack surface -- but this remains an operator decision, not a technical one. (2) NETWORK RESIDUAL -- the `read_*` helpers do not re-verify chain id on every call (would double RPC cost); harmless while read-only, but MUST become a hard pre-flight check before step 3 (signing). `read_module_identity()` reports the real chain id for a session that wants certainty. (3) ALLOWANCE REGIME -- `reset_time_min == 0` is a one-shot allowance that never refills (still reachable on v0.1.1: the `require` only fires when `resetBaseMin > 0`); a trading agent almost certainly wants the PERIODIC regime (a cap that refills), an operator design choice at step 2. NEXT STEPS unchanged: (2) Safe creation + module activation on testnet, (3) EIP-712 signing by the agent key, (4) full testnet cycle proving an over-limit spend is REJECTED on-chain, (5) only then a mainnet proposal. — `onchain/safe_robinhood_wallet.py`

------------------------------------------------------------

[CODE] Subject : Step 2 -- full Safe+AllowanceModule cycle PROVEN in simulation, zero gas, zero key, zero on-chain write
Date : 2026.08.17 / Probleme : step 2 (Safe creation + module activation) is the first step that would write on-chain, and it was blocked on a hard operational dependency: the official testnet faucet (faucet.testnet.chain.robinhood.com) sits behind a Vercel challenge and is unreachable from the VPS, so no gas could be obtained autonomously. Waiting on the operator for a faucet would have stalled the whole chantier. Two design questions were also still open from step 1's ETAT ACTUEL: the allowance regime, and the un-enforced chain-id preflight.
Solution : rather than block, the entire cycle was proven by SIMULATION against the real deployed contracts (operator validated "simulate the whole cycle, up to proving the rejection"). New module `onchain/safe_robinhood_simulation.py`, deliberately SEPARATE from `safe_robinhood_wallet.py` so the latter's read-only ABI guardrail (`test_allowance_module_abi_is_read_only`) is not weakened -- the simulation module carries write-function ABIs but can only ever reach them through `eth_call`, locked by `test_simulation_module_never_sends_a_transaction` (AST-based, not a text scan: a text scan flags the module's own docstring, found live while writing it). FOUR things proven live rather than assumed: (1) the FULL Safe v1.4.1 stack is deployed on testnet 46630 -- proxy factory (3054 b), SafeL2 singleton (24421 b), fallback handler (5637 b), multisend -- step 1 had only ever checked the singleton, and without the FACTORY no Safe can be created at all, the real bloquant to rule out first; (2) this RPC accepts injected CODE in `eth_call` state overrides, not just injected storage as step 1 needed (verified with a 10-byte stub returning a sentinel); (3) Safe storage layout -- `threshold` at slot 4, proven by injecting the distinctive sentinel 7 and reading it back through the contract's own `getThreshold()`; (4) THE proof -- with a 100-unit one-shot allowance injected into the REAL AllowanceModule, amounts 50 and 100 clear the cap (stopping later at signature verification) while 101 and 150 revert on the module's own `newSpent > allowance.spent && newSpent <= allowance.amount` require. Testing the PASSING cases is what makes this a proof rather than an illusion: a contract that always reverted would look identical to a working guardrail if only the over-limit case had been checked. Boundary confirmed inclusive (100 passes, 101 rejected) and residual-envelope logic confirmed (with 50 of 100 already spent, 50 passes and 51 is rejected). Predicted Safe address for owner-A1/nonce-0: `0x4b6A95c869B680c5e5B333802B6F30Cf7a592c73` (no code at it -- nothing was deployed). OPEN POINT (2) OF STEP 1 NOW CLOSED: a fail-closed chain-id preflight runs on every public entry point (an RPC repointed at mainnet 4663 raises instead of simulating against real balances), asserted per-entry-point in tests. OPEN POINT (3) DECIDED BY THE OPERATOR (17/08): one-shot regime (`reset_time_min=0`, the cap never refills by itself, re-arming is a human action) for the pilot, chosen over periodic so a compromised agent's maximum loss is the envelope itself rather than the envelope every period; periodic stays reachable by passing a non-zero value, deliberately not the default, locked by `test_one_shot_is_the_default_regime`. Operator also confirmed the same day that the target architecture is smart wallets, not EOAs -- consistent with this Safe-based design (the EOA is only ever a signing key; the capital and the enforced bounds live in the contract). 19 dedicated tests. STILL OUT OF SCOPE: no real Safe exists, no key, no signing, no mainnet -- real execution needs testnet gas from the operator, and open point (1) (v0.1.1 audit gap) remains an operator decision before any real capital. — `onchain/safe_robinhood_simulation.py`, `tests/test_safe_robinhood_simulation.py` (6094410e)

------------------------------------------------------------

[CODE] Subject : Steps 3 AND 4 proven in simulation -- signature path + full cycle, still zero gas and zero key
Date : 2026.08.17 / Probleme : step 3 (EIP-712 signing by the agent key) and step 4 (a full testnet cycle proving an over-limit spend is REJECTED on-chain) were both planned as REAL-transaction milestones, blocked behind testnet gas that cannot be obtained from the VPS (faucet sits behind a Vercel challenge). Step 2's simulation had also left a real hole nobody had flagged: it proved the cap REJECTS too much, but never that the permitted path actually WORKS -- and a guardrail that blocks everything is indistinguishable from a working one if only the blocked case is ever tested.
Solution : both steps proven by simulation against the REAL deployed contracts. THREE storage layouts had to be established EXPERIMENTALLY rather than deduced, and one of them would have been wrong by inspection: `delegates` sits at slot **3**, not the slot 1 the declaration order suggests (proven by injecting a delegate at each candidate slot 0..5 and reading it back through the contract's own `delegates(address,uint48)` getter -- only slot 3 answered); `modules` at slot 1 (same method, via `isModuleEnabled()`); `threshold` at slot 4 (step 2). The EIP-712 digest is recomputed locally from first principles and matches the contract's own `generateTransferHash` BYTE FOR BYTE -- deliberately an independent implementation, since trusting the on-chain getter would prove nothing about whether we understand what is being signed. Step 3 outcomes, live: valid delegate signature ACCEPTED; impostor REJECTED on signature; valid key but UNREGISTERED delegate REJECTED (both conditions required, they are cumulative); valid signature over an over-cap amount still REJECTED BY THE CAP (the cap wins over a perfect signature). Step 4, `simulate_full_cycle()`: a Safe that EXISTS with the module ENABLED, allowance configured, delegate registered, and an ERC20 stub so the Safe's own token call resolves -- amounts 1/50/100 COMPLETE end to end, 101/150 revert on the cap. `cycle_proven` is only true when BOTH halves hold. Module still cannot sign: `simulate_full_cycle` takes a `signature_for` callable supplied by the caller, so the "verifies what a signature does, never creates one" property is preserved (locked by the no-key-material test). 25 dedicated tests. STILL OUT OF SCOPE, unchanged: no real Safe exists, no key, no signing, no mainnet -- real execution needs testnet gas from the operator, and open point (1) (the v0.1.1 audit gap) remains an operator decision before any real capital. — `onchain/safe_robinhood_simulation.py`, `tests/test_safe_robinhood_simulation.py` (2974b601)

------------------------------------------------------------

[ETAT ACTUEL] Subject : 2 real-money-pipeline-audit findings verified as already-correct, no code change made
Date : 2026.08.18 / Problem : the same 20-agent audit that found the 6 real code gaps closed elsewhere today also flagged 2 items on the EOA pilot specifically. (1) **Medium**: `agent_wallet_cdp_policy.py`'s CDP-side Policy Engine (the allowlist counterpart to `ALLOWED_TRANSFER_ADDRESS`) is built but never attached to the real account -- a code path skipping `agent_wallet_pilot.attempt_transfer` and calling `agent_wallet_cdp_adapter.transfer_usdc` directly would be signed by CDP without objection. (2) **Low**: `attempt_transfer()` has zero production caller anywhere in the repo (only `agent_wallet_pilot_cycle.py`'s own `attempt_swap()` is wired into the live heartbeat) -- CLAUDE.md describes "3. Transfert USDC borné" as one of the system's active real-capital mechanisms, worth confirming this isn't documentation drift.
Solution : both re-verified against the current code, no change made. (1) `agent_wallet_cdp_policy.py`'s own docstring already states, verbatim: "Nothing here is applied... No production module imports this file (a dedicated test asserts it)" -- `create_pilot_bounded_policy()` requires an injected `policies_client` PLUS explicit `operator_ack=True`, a real creation on the real account is already gated as a SEPARATE, explicitly-authorized operator step by the module's own design, not something to silently activate as a "fix". Attaching it now (outside this batch's scope of pure code-level gaps) would be a real CDP account mutation, not a code change -- stays a standing operator decision, same doctrine as every other real-capital activation. (2) Confirmed `attempt_transfer` is genuinely unreachable in production (re-grep, matches the audit's own exhaustive trace) -- but CLAUDE.md's own "Transfert USDC borné" description already only describes the function/gates/bounds, never claims it's wired to an autonomous trigger. No drift found -- dormant-by-design, correctly documented as such. — no files changed.

