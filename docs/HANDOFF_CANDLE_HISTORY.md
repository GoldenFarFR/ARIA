# HANDOFF — Persisted candle history (`candle_history.py`, FIFO per token/timeframe)

> Format: `[STATUS] Subject` / `Date: YYYY.MM.DD / Problem: ...` / `Solution: ... — file (short hash)`.
> `[STATUS]` ∈ DEPLOYED / CODE (tested, not deployed) / CONFIG (no commit) / CURRENT STATE.

[CODE] Subject : new module — FIFO candle history per (chain, pool_address, timeframe), passive hook wired into `_fetch_candles`
Date : 2026.08.11 / Problem : before this module, every candle fetch (`_fetch_candles` in `momentum_entry.py`) only lived in a 60-second in-memory cache — no series ever accumulated enough depth for a real trend signal or a reproducible backtest, and ~92% of tokens ARIA ever scans (measured live this session: 1945 distinct tokens/13 days, only ~8% generate any trade) left zero trace once rejected. Operator proposal (11/08, long design negotiation, real numbers verified at every step — DB size, token/day counts, GeckoTerminal throttle history including the documented 6-day live-lock incident): keep a bounded history fed organically by what the pipeline already fetches, plus (not yet built) a dedicated collector cycle over `services/goplus_watchlist.py` ("the watchlist" — the one and only list this bare term refers to on this project, see `feedback_watchlist_means_goplus_watchlist.md`).
Solution : `candle_history.py` — `record_candles()`/`get_history()`, real timeframe INFERRED from the candles' own median spacing (`infer_timeframe()`, never trusted from the caller's `mode` string, since the fetch ladder's chosen rung isn't exposed at that level) so a degraded fetch (e.g. "standard" landing on 4H instead of 1D) never corrupts a series with mixed granularities. Per-timeframe FIFO cap (`FIFO_CAP_BY_TIMEFRAME`: 1D/4H/1H/15M=1000, 30M=500), 5M explicitly excluded from persistence (v9's own trading behavior unaffected — operator decision, "deja le 5 minutes on peut lenlever sa economisera des bougie"). Deliberately NO purge by staleness/age (explicit operator decision: "elle doit tenir le registre des bougies pour tous... quelle y trade ou non") — only the per-key FIFO cap bounds depth, unbounded in the number of distinct keys. Schema deliberately minimal (2-agent Workflow audit finding: no `provider`/`degraded`/`symbol`/`inserted_at` columns, none has a real Phase-1 consumer). Wired as a passive hook right after the existing `candle_staleness_shadow` observation in `_fetch_candles` — independent try/except (a `candle_history` failure must never break a real fetch, same best-effort contract as every other shadow module), zero extra network call.
`candle_history.py` (new)/`momentum_entry.py`/`tests/test_candle_history.py` (new, 14 tests)/`tests/test_momentum_entry.py` (2 new tests: hook invoked with correct args, hook failure never blocks a real fetch). Full suite green (9884 passed, 17 skipped). Not deployed yet.

------------------------------------------------------------

[CODE] Subject : dedicated collector cycle over goplus_watchlist -- keeps candle_history warm independently of real trade evaluations
Date : 2026.08.11 / Problem : the passive hook above only records candles when the momentum pipeline happens to evaluate a token for a real trade -- most watchlist tokens (up to 2000 slots, 608 real rows measured live this session) would never accumulate a series otherwise. Operator asked for a dedicated background cycle to keep the watchlist's own candles warm, explicitly cautious about the shared GeckoTerminal throttle's documented past 6-day live-lock incident ("avant il faut reflechir... on risque pas des rate limit"). Proposed a concrete numbers-backed plan (batch size, cadence, throughput impact) and got explicit operator confirmation ("OUI") before writing any code.
Solution : `run_candle_history_watchlist_cycle()` (`momentum_entry.py`), heartbeat task `candle_history_watchlist_cycle` (15min cadence), gate `ARIA_CANDLE_HISTORY_WATCHLIST_ENABLED` (single dedicated flag, no paper-trading double-gate -- standalone infrastructure, same doctrine as the github/farcaster cascade columns). Each passage: `candle_history.due_for_refresh()` picks 20 tokens from the watchlist (`_CANDLE_HISTORY_WATCHLIST_BATCH_SIZE`, own round-robin cursor `candle_watchlist_cursor` table -- deliberately independent of `goplus_watchlist`'s own honeypot `last_checked_at`, never coupling two unrelated refresh cadences), resolves each token's best DexScreener pair (`_best_pair`, same base/quote-mismatch-safe logic already proven elsewhere in this file), then calls the existing `_fetch_candles(pool_address, chain, contract=contract, pair=best, mode="standard")` -- reusing the passive hook above rather than duplicating the fetch cascade. `mode="standard"` only for this first cut (feeds swing + vc); `scalping` mode deferred to a future iteration once this slice is observed clean in prod. Real throughput: 20 x GeckoTerminal's 4.0s shared-throttle interval = ~80s/passage (~9% of the 15min cadence), 608 tokens / 20 per passage = ~31 passages -> a full watchlist refresh in ~7h45, comfortably under a day. Best-effort per token (one broken fetch/pair-resolution never stops the passage; `mark_watchlist_refreshed` runs in a `finally` so a persistently failing token still cycles to the back of the queue).
`momentum_entry.py`/`heartbeat.py`/`candle_history.py` (new: `due_for_refresh()`/`mark_watchlist_refreshed()`, own cursor table)/`tests/test_candle_history.py` (+6 tests)/`tests/test_momentum_entry.py` (+4 tests)/`tests/test_coherence.py` (gate registered in `_KNOWN_ENABLED_GATES`). Full suite green (9898 passed, 17 skipped). Not deployed yet, gate OFF by default. No pocket reads from `candle_history` yet -- #97 stays explicitly blocked until this collector is observed clean in prod.

------------------------------------------------------------

[CODE] Subject : #97 unblocked -- open swing/vc positions now prioritized in the watchlist collector's refresh queue
Date : 2026.08.13 / Problem : the collector above (`due_for_refresh`) treats every watchlist token identically -- a token ARIA is actually HOLDING (a real swing/vc paper position) waits behind thousands of never-traded watchlist entries on the same unweighted round-robin, same as any speculative candidate. Unblock condition from the entry above ("observed clean in prod") verified live before starting: `candle_history` (mode='standard') shows 5+ consecutive clean days (497-610 distinct tokens/day, 08/09->08/13), 731 distinct tokens total, 204562 candles, zero related `system_issues`/`log-health-watch` entries.
Solution : `due_for_refresh(candidates, limit, *, priority=None)` -- a new optional `priority: set[tuple[contract, chain]]` sorts favorites ahead of the rest (`sorted` key prefixed with `pair not in favorites`), WITHOUT dropping non-favorites from the batch and WITHOUT breaking the existing never-fetched/oldest-first order within each group (a just-refreshed favorite still can't cut ahead of a stale one). `run_candle_history_watchlist_cycle()` computes `priority` from `paper_trader.get_open_positions()` filtered to `pocket in ("swing", "vc")` (this collector's `mode="standard"` only ever fed those two, per the entry above) -- best-effort, a broken read degrades to the unweighted default rather than blocking the collector. Deliberately did NOT wire pockets to READ from `candle_history` for their own entry decision (the other half of #97's original title, "brancher les poches sur l'historique") -- `candle_staleness_shadow.py`'s own docstring is explicit that "no real calibration data exists yet on how often genuine staleness occurs", so serving a swing/vc BUY decision from a DB row that might be hours stale (round-robin collector, `MAX_TOKENS_PER_CYCLE`-bounded) risks exactly the kind of silently-corrupted signal `_candles_price_consistent` (Item #222) already exists to prevent -- same anti-overfitting doctrine as the wick-gate incident (never promote an uncalibrated threshold to a real decision path). That half stays open, gated on a future staleness calibration pass (shadow observations already accumulating since 10/08).
`candle_history.py`/`momentum_entry.py`/`tests/test_candle_history.py` (+4 tests)/`tests/test_momentum_entry.py` (+2 tests, 3 existing fakes updated for the new kwarg). Full suite: see this commit's own test run.

------------------------------------------------------------
