---
id: 17b622c0-b79c-43c4-b9e0-40eb432aa39e
created_at: 2026-07-06T07:53:45.483824+00:00
skill: acp_marketplace
sources: [acp_marketplace]
visitor_id: anonymous
status: pending
---

## Question
Tâche de DIAGNOSTIC RÉSEAU en LECTURE SEULE, à faire exécuter par ton worker 
(sessions_delegate). Objectif : savoir si le WORKER a un accès web sortant. 
Ne fais RIEN d'autre que les 2 requêtes GET ci-dessous. Aucune écriture, aucune 
clé, aucun job, aucune signature.

Fais exécuter par le worker, dans l'ordre :

1. TEST INTERNET GÉNÉRIQUE (cible neutre) :
   GET https://api.github.com
   → Rapporte le code HTTP. (200 = le worker a un accès web.)

2. TEST CIBLE VIRTUALS (uniquement si l'étape 1 renvoie 200) :
   GET https://acpx.virtuals.io/api/agents?sort=grossAgenticAmount:desc
   → Rapporte UNIQUEMENT : le code HTTP, et si ça répond sans authentification.
   NE colle PAS encore les données. Juste : ça passe (200) ou c'est bloqué 
   (403 / timeout).

CONTRAINTES :
- Le worker fait EXACTEMENT ces 2 GET, rien de plus. Pas de scan complet ici.
- Si l'étape 1 échoue (pas de 200), arrête : le worker n'a pas d'accès web, 
  inutile de tenter l'étape 2. Dis-le franchement.
- Aucun secret, aucune clé, aucune action au-delà des 2 requêtes.
- Rapporte les 2 codes HTTP bruts, horodatés. Ne suppose rien.

## Answer
ACP — dis « acp status » pour l'état, « plan revenus acp » pour la stratégie, « traiter jobs acp » pour livrer.

• acp status — agent + offres + fichier events
• traiter jobs acp — poll provider (livrer jobs en attente)
• browse offerings — parcourir le marketplace
• plan revenus acp — stratégie monétisation
• templates offres acp — workflows disponibles
• créer offre acp template <nom> — publier sur marketplace
• lancer produit acp template <nom> et publier sur X
• scan marché acp — étudier offre/demande + gaps workflows
• préparer job acp 0x… — livrable JSON (Hermès)

Lance : vanguard\operator\start-acp-local.ps1

RÈGLES ACP STRICTES :
- Tu te concentres UNIQUEMENT sur tes workflows ACP : gérer/exécuter les jobs, améliorer tes offres, scanner le marché (demande/gaps), proposer de nouveaux workflows utiles.
- « Quels sont tes workflows ? » ou « ce que tu sais faire » → tu ne cites QUE tes offres réelles (celles définies dans acp_offerings.yaml). Aucune longue liste de catégories inventées.
- Tu ne présentes JAMAIS wallet, carte, email, compute ou autres comme des workflows ACP.
- Pour tout deliverable, toute amélioration, toute nouvelle offre : tu demandes TOUJOURS un audit qualité à l'opérateur avant de valider ou promouvoir.
- Ton rôle ACP = exécution + amélioration + veille marché + propositions + audit opérateur systématique.

## Meta
- Grounding: `pending` — only `verified` entries feed future answers
- Sandbox path: `truth-ledger/2026-07-06/075345-17b622c0.md`
