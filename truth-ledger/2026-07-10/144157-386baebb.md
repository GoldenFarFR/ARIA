---
id: 386baebb-5fd5-4e3c-bc38-48cc09564cda
created_at: 2026-07-10T14:41:57.125335+00:00
skill: general
sources: [none]
visitor_id: anonymous
status: pending
---

## Question
Regarde pourquoi

## Answer
Verdict : je lis les signaux, pas le code source d'ici. Voici ce que les logs disent honnêtement.

Deux passes récentes
Passe 1 : skip_incomplete 2, skip_rejected 5 — 0 gardés
Passe 2 : skip_too_old 2, skip_rejected 5, rejected 1 — 0 gardés

Lecture des motifs
skip_rejected (5, récurrent) : le filtre de pertinence recale la majorité. C'est le vrai suspect — soit le seuil est trop strict, soit les sources ne matchent plus les critères crypto/holding.
skip_too_old : items hors fenêtre temporelle — sources qui ne publient pas de neuf.
skip_incomplete : métadonnées manquantes (titre, date, corps) — parsing qui échoue sur certaines pages.

Diagnostic
Ce n'est pas un crash — le pipeline tourne, il rejette tout. Le pattern « 5 rejected à chaque passe » sent le seuil de scoring calé trop haut ou une liste de sources périmée. Pas assez d'entrées entrantes pour qu'un seul survive.

Ce que je ne peux pas faire d'ici : ouvrir le fichier du crawler et lire la logique de rejet. Pour trancher entre « seuil trop strict » et « sources mortes », il faut regarder le code du filtre (`skip_rejected`) dans le repo.

Deux voies
Je te liste les questions précises à poser à Claude Code pour qu'il inspecte le filtre.
Ou tu me colles le bout de code du scoring/rejet et je te dis quoi ajuster.

Ton call.

## Meta
- Grounding: `pending` — only `verified` entries feed future answers
- Sandbox path: `truth-ledger/2026-07-10/144157-386baebb.md`
