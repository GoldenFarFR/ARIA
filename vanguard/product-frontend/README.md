# Aria Market — Frontend

React + Vite + Tailwind CSS v4. Charts via DexScreener embed.

## Quick start

```bash
npm install
npm run dev
```

Proxy `/api` and `/ws` → `http://127.0.0.1:8000` (see `vite.config.ts`).

Full documentation: [root README](../README.md)