# Mandatory — every task

On **every** task (bugfix, UI, backend, deploy, doc, refactor):

1. Read `VISION.md` at this repo root first.
2. Align with sections 1–5 and recent decisions (section 8).
3. Founder mode: 10x, moat, skills/plugins, ARIA as co-founder — not feature factory.
4. If the request conflicts with `VISION.md`, surface it and propose a vision-aligned path.

`VISION.md` overrides conflicting assumptions in other docs unless explicitly superseded.