.DS_Store
Thumbs.db
*.log
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
bot/venv/

# État runtime du hook de démarrage (progression du setup)
.claude/.setup-status
# Compteur de messages (checkpoint auto de session tous les 20)
.claude/.msg-counter
# État runtime du rappel de déploiement (throttle + delta non déployé pour la barre)
.claude/.deploy-reminded-ref
.claude/.undeployed-lines

# IDE / OS
.idea/
.vscode/
*.swp
*~

# Secrets / local vault
vault/
*.pem
*.key
production.env
local.env
vanguard.env
local-sync/.vault-master-secret
local-sync/.vault-totp-secret

# Node / build
node_modules/
vanguard/dist/
vanguard/product-frontend/dist/

# Python / data locale
vanguard/backend/data/
*.sqlite
__pycache__/

# aria-core runtime / tests locaux
packages/aria-core/.aria-test-data/
packages/aria-core/acp_provider_state.json
packages/aria-core/GoldenFar/
/.aria-test-data/
/GoldenFar/
/data/

# Ops privés — vivent dans GoldenFarFR/aria-ops (ne pas re-ajouter)
collegue-memoire/
local-sync/
# Ancré à la racine (/memory/) : non ancré, ce motif matchait aussi
# packages/aria-core/src/aria_core/memory/ (code source légitime, jamais
# voulu) — silencieusement ignoré jusqu'à sa découverte lors de la migration
# LanceDB (12/07) ; consolidation.py (#128) n'a survécu que via un
# git add -f explicite, jamais garanti pour un futur ajout.
/memory/
truth-ledger/
letta-orchestrator/
download/
vanguard/operator/*
!vanguard/operator/start-acp-local.ps1
!vanguard/operator/test-aria-chat.ps1
!vanguard/operator/gen-admin-totp.py
!vanguard/operator/gen-sepolia-wallet.py