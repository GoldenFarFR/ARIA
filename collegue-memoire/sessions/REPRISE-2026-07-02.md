# Reprise session — 2026-07-02 (Phase D aria-core mémoire)

> **SSOT reprise** — l'assistant lit ce fichier **avant toute autre tâche ARIA** si `date >= 2026-07-02`.

---

## Règle assistant (obligatoire)

À la **première session du 2 juillet 2026 ou après** :

1. Lire ce fichier + `COLLEGUE.md` § Journal (ligne reprise mémoire)
2. **Ne pas démarrer Phase D automatiquement**
3. Accueillir Sylvain en 3–5 lignes :
   - Phases **A / B / C** terminées (`67b28c3a` aria-sandbox, ops `9f0ddbf` vanguard)
   - Prod inchangée : `aria_vector_memory=false`, pin ancien sur Render (quota pipeline épuisé)
   - Pause validée jusqu'au **2026-07-02**
4. **Demander explicitement** :
   - « On lance le **plan Phase D** (unification mémoire + injection LLM) ? »
   - ou « Tu préfères **attendre** encore ? »
5. Si **oui** → présenter le plan détaillé ci-dessous (comme Phase C), attendre validation, puis coder **local uniquement**
6. Si **non / pas prêt** → noter dans `COLLEGUE.md` nouvelle date cible, ne rien coder

---

## État au 2026-06-20 (fin session)

| Phase | Statut | Commit clé |
|-------|--------|------------|
| **A** — doc-only | ✅ | `c3a429bb` — `ARCHITECTURE.md`, `WHERE-TO-PUT.md` |
| **B** — package `aria_core.memory` | ✅ | `74a5bea3` — wrappers, stub vector, legacy journal |
| **C** — Chroma opt-in local | ✅ | `67b28c3a` — client, store, ingest, smoke OK |
| **D** — unification + injection LLM | ✅ (anticipé 2026-06-20) | `llm_context.py` — voir commit Phase D |
| **E** — values | ⏳ après D | — |
| **F** — goals | ⏳ après E | — |
| **G** — reflection | ⏳ après F | — |

### Prod / ops

- Render : `pipeline_minutes_exhausted` — pas de deploy groupé avant reset quota ou Starter
- Déployer plus tard : `build-local.ps1` puis **un seul** `deploy-render.ps1 -Reason "..."`
- Flag prod : `aria_vector_memory=false` (défaut safe)
- Tests locaux Chroma : `pip install -e ".[dev,vector]"` + flag true

---

## Phase D — périmètre (plan à valider avant code)

**Objectif** : une façade mémoire unifiée et rappel vectoriel dans le contexte LLM.

### D1 — Façade unifiée `aria_core.memory`

- Point d'entrée unique : journal + cognitive + vector (si flag on)
- API stable pour le reste du cerveau (pas de imports legacy dispersés)
- Rétrocompat exports `memory.py` / `_legacy_journal.py`

### D2 — Injection `build_llm_context`

- Rappel sémantique Chroma **opt-in** (`aria_vector_memory=true` local)
- Budget tokens + filtre sensibilité (pas de secrets / PII brute)
- Fallback gracieux si Chroma absent ou flag off

### D3 — Tests + doc

- Tests unitaires injection (mock store)
- Mise à jour `ARCHITECTURE.md` § flux mémoire → LLM
- Script ops `test-vector-memory.ps1` étendu si besoin

### Hors scope Phase D

- CrewAI / multi-agent frameworks
- Deploy prod automatique (flag reste off jusqu'à validation Sylvain)
- Phases E/F/G

---

## Critères d'acceptation Phase D

- [ ] Un seul module/façade consommée par `build_llm_context`
- [ ] Injection vectorielle derrière flag, testée localement
- [ ] Aucune régression tests aria-core existants (hors `test_truth_ledger` sqlite pré-existant)
- [ ] Plan validé par Sylvain **avant** premier commit D

---

## Actions opérateur optionnelles (non bloquantes)

- Render deploy quand quota OK + bump pin aria-core
- `IMAGE_API_KEY` si bannière xAI souhaitée
- Corriger TOTP vault handoff (`-TotpCode` ou Bitwarden)

---

*Créé : 2026-06-20 — pause validée par Sylvain, reprise cible 2026-07-02*